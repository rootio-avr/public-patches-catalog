--- a/src/werkzeug/security.py
+++ b/src/werkzeug/security.py
@@ -12,6 +12,15 @@
 _os_alt_seps: list[str] = list(
     sep for sep in [os.sep, os.path.altsep] if sep is not None and sep != "/"
 )
+_windows_device_files = {
+    "CON",
+    "PRN",
+    "AUX",
+    "NUL",
+    *(f"COM{i}" for i in range(10)),
+    *(f"LPT{i}" for i in range(10)),
+}
+


 def gen_salt(length: int) -> str:
@@ -134,6 +143,9 @@
 def safe_join(directory: str, *pathnames: str) -> str | None:
     """Safely join zero or more untrusted path components to a base
     directory to avoid escaping the base directory.
+
+    .. versionchanged:: 3.1.4
+        Blocks Windows special device names on Windows.

     :param directory: The trusted base directory.
     :param pathnames: The untrusted path components relative to the
@@ -151,6 +163,10 @@

         if (
             any(sep in filename for sep in _os_alt_seps)
+            or (
+                os.name == "nt"
+                and os.path.splitext(filename)[0].upper() in _windows_device_files
+            )
             or os.path.isabs(filename)
             # ntpath.isabs doesn't catch this on Python < 3.11
             or filename.startswith("/")
--- a/src/werkzeug/utils.py
+++ b/src/werkzeug/utils.py
@@ -21,7 +21,8 @@
 from ._internal import _TAccessorValue
 from .datastructures import Headers
 from .exceptions import NotFound
 from .exceptions import RequestedRangeNotSatisfiable
+from .security import _windows_device_files
 from .security import safe_join
 from .wsgi import wrap_file

@@ -34,14 +35,6 @@
 _T = t.TypeVar("_T")

 _entity_re = re.compile(r"&([^;]+);")
 _filename_ascii_strip_re = re.compile(r"[^A-Za-z0-9_.-]")
-_windows_device_files = {
-    "CON",
-    "PRN",
-    "AUX",
-    "NUL",
-    *(f"COM{i}" for i in range(10)),
-    *(f"LPT{i}" for i in range(10)),
-}


 class cached_property(property, t.Generic[_T]):
--- a/tests/test_security.py
+++ b/tests/test_security.py
@@ -70,3 +70,13 @@

 def test_safe_join_empty_trusted():
     assert safe_join("", "c:test.txt") == "./c:test.txt"
+
+
+def test_safe_join_windows_special(monkeypatch: pytest.MonkeyPatch) -> None:
+    """Windows special device name is not allowed on Windows."""
+    monkeypatch.setattr("os.name", "nt")
+    assert safe_join("a", "CON") is None
+    assert safe_join("a", "NUL.txt") is None
+    assert safe_join("a", "com0") is None
+    monkeypatch.setattr("os.name", "posix")
+    assert safe_join("a", "CON") == "a/CON"

diff --git a/src/werkzeug/security.py b/src/werkzeug/security.py
index e693a9c..666b7c7 100644
--- a/src/werkzeug/security.py
+++ b/src/werkzeug/security.py
@@ -17,8 +17,12 @@ _windows_device_files = {
     "PRN",
     "AUX",
     "NUL",
+    "CONIN$",
+    "CONOUT$",
     *(f"COM{i}" for i in range(10)),
     *(f"LPT{i}" for i in range(10)),
+    *(f"COM{c}" for c in "\xb9\xb2\xb3"),
+    *(f"LPT{c}" for c in "\xb9\xb2\xb3"),
 }
 
 
@@ -167,7 +171,7 @@ def safe_join(directory: str, *pathnames: str) -> str | None:
             any(sep in filename for sep in _os_alt_seps)
             or (
                 os.name == "nt"
-                and os.path.splitext(filename)[0].upper() in _windows_device_files
+                and filename.partition(".")[0].strip().upper() in _windows_device_files
             )
             or os.path.isabs(filename)
             # ntpath.isabs doesn't catch this on Python < 3.11
diff --git a/tests/test_security.py b/tests/test_security.py
index 4cb70aa..4ac387a 100644
--- a/tests/test_security.py
+++ b/tests/test_security.py
@@ -72,11 +72,29 @@ def test_safe_join_empty_trusted():
     assert safe_join("", "c:test.txt") == "./c:test.txt"
 
 
-def test_safe_join_windows_special(monkeypatch: pytest.MonkeyPatch) -> None:
-    """Windows special device name is not allowed on Windows."""
+@pytest.mark.parametrize(
+    "name",
+    [
+        "CON",
+        "CON.txt",
+        "CON.txt.html",
+        "CON  ",
+        "CON . txt",
+    ],
+)
+def test_safe_join_windows_special(
+    monkeypatch: pytest.MonkeyPatch, name: str
+) -> None:
+    """Windows special device names are blocked on Windows regardless of extension or spaces."""
     monkeypatch.setattr("os.name", "nt")
-    assert safe_join("a", "CON") is None
-    assert safe_join("a", "NUL.txt") is None
-    assert safe_join("a", "com0") is None
+    assert safe_join("a", name) is None
+
+
+def test_safe_join_windows_special_allowed_non_windows(
+    monkeypatch: pytest.MonkeyPatch,
+) -> None:
+    """Windows special device names are allowed on non-Windows systems."""
     monkeypatch.setattr("os.name", "posix")
     assert safe_join("a", "CON") == "a/CON"
+    assert safe_join("a", "NUL.txt") == "a/NUL.txt"
+    assert safe_join("a", "com0") == "a/com0"

diff --git a/src/werkzeug/security.py b/src/werkzeug/security.py
index 666b7c7..ad9abfb 100644
--- a/src/werkzeug/security.py
+++ b/src/werkzeug/security.py
@@ -171,7 +171,10 @@ def safe_join(directory: str, *pathnames: str) -> str | None:
             any(sep in filename for sep in _os_alt_seps)
             or (
                 os.name == "nt"
-                and filename.partition(".")[0].strip().upper() in _windows_device_files
+                and any(
+                    p.partition(".")[0].strip().upper() in _windows_device_files
+                    for p in filename.split("/")
+                )
             )
             or os.path.isabs(filename)
             # ntpath.isabs doesn't catch this on Python < 3.11
diff --git a/tests/test_security.py b/tests/test_security.py
index 4ac387a..74804bd 100644
--- a/tests/test_security.py
+++ b/tests/test_security.py
@@ -80,6 +80,7 @@ def test_safe_join_empty_trusted():
         "CON.txt.html",
         "CON  ",
         "CON . txt",
+        "b/CON",
     ],
 )
 def test_safe_join_windows_special(
