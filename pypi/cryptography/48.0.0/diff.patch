--- a/src/cryptography/x509/base.py
+++ b/src/cryptography/x509/base.py
@@ -165,16 +165,86 @@
 CertificateSigningRequest = rust_x509.CertificateSigningRequest
 
 
-load_pem_x509_certificate = rust_x509.load_pem_x509_certificate
-load_der_x509_certificate = rust_x509.load_der_x509_certificate
-
-load_pem_x509_certificates = rust_x509.load_pem_x509_certificates
-
-load_pem_x509_csr = rust_x509.load_pem_x509_csr
-load_der_x509_csr = rust_x509.load_der_x509_csr
-
-load_pem_x509_crl = rust_x509.load_pem_x509_crl
-load_der_x509_crl = rust_x509.load_der_x509_crl
+# GHSA-537c-gmf6-5ccf: cap ASN.1 DER/PEM input at 100 MiB before reaching
+# OpenSSL d2i_* parsers, which have an integer-truncation heap over-read on
+# inputs >= 2 GiB on 64-bit Unix.
+_MAX_ASN1_INPUT_LEN = 100 * 1024 * 1024  # 100 MiB
+
+
+def _check_asn1_length(data: bytes) -> None:
+    try:
+        n = len(data)
+    except TypeError:
+        return
+    if n > _MAX_ASN1_INPUT_LEN:
+        raise ValueError(
+            "ASN.1 input exceeds maximum supported length "
+            f"({_MAX_ASN1_INPUT_LEN} bytes); refusing to parse to "
+            "mitigate GHSA-537c-gmf6-5ccf."
+        )
+
+_rust_load_pem_x509_certificate = rust_x509.load_pem_x509_certificate
+
+
+def load_pem_x509_certificate(*args, **kwargs):
+    if args:
+        _check_asn1_length(args[0])
+    return _rust_load_pem_x509_certificate(*args, **kwargs)
+
+
+_rust_load_der_x509_certificate = rust_x509.load_der_x509_certificate
+
+
+def load_der_x509_certificate(*args, **kwargs):
+    if args:
+        _check_asn1_length(args[0])
+    return _rust_load_der_x509_certificate(*args, **kwargs)
+
+
+_rust_load_pem_x509_certificates = rust_x509.load_pem_x509_certificates
+
+
+def load_pem_x509_certificates(*args, **kwargs):
+    if args:
+        _check_asn1_length(args[0])
+    return _rust_load_pem_x509_certificates(*args, **kwargs)
+
+
+_rust_load_pem_x509_csr = rust_x509.load_pem_x509_csr
+
+
+def load_pem_x509_csr(*args, **kwargs):
+    if args:
+        _check_asn1_length(args[0])
+    return _rust_load_pem_x509_csr(*args, **kwargs)
+
+
+_rust_load_der_x509_csr = rust_x509.load_der_x509_csr
+
+
+def load_der_x509_csr(*args, **kwargs):
+    if args:
+        _check_asn1_length(args[0])
+    return _rust_load_der_x509_csr(*args, **kwargs)
+
+
+_rust_load_pem_x509_crl = rust_x509.load_pem_x509_crl
+
+
+def load_pem_x509_crl(*args, **kwargs):
+    if args:
+        _check_asn1_length(args[0])
+    return _rust_load_pem_x509_crl(*args, **kwargs)
+
+
+_rust_load_der_x509_crl = rust_x509.load_der_x509_crl
+
+
+def load_der_x509_crl(*args, **kwargs):
+    if args:
+        _check_asn1_length(args[0])
+    return _rust_load_der_x509_crl(*args, **kwargs)
+
 
 
 class CertificateSigningRequestBuilder:
--- a/src/cryptography/hazmat/primitives/serialization/base.py
+++ b/src/cryptography/hazmat/primitives/serialization/base.py
@@ -4,11 +4,74 @@
 
 from cryptography.hazmat.bindings._rust import openssl as rust_openssl
 
-load_pem_private_key = rust_openssl.keys.load_pem_private_key
-load_der_private_key = rust_openssl.keys.load_der_private_key
+# GHSA-537c-gmf6-5ccf: cap ASN.1 DER/PEM input at 100 MiB before reaching
+# OpenSSL d2i_* parsers, which have an integer-truncation heap over-read on
+# inputs >= 2 GiB on 64-bit Unix.
+_MAX_ASN1_INPUT_LEN = 100 * 1024 * 1024  # 100 MiB
 
-load_pem_public_key = rust_openssl.keys.load_pem_public_key
-load_der_public_key = rust_openssl.keys.load_der_public_key
 
-load_pem_parameters = rust_openssl.dh.from_pem_parameters
-load_der_parameters = rust_openssl.dh.from_der_parameters
+def _check_asn1_length(data: bytes) -> None:
+    try:
+        n = len(data)
+    except TypeError:
+        return
+    if n > _MAX_ASN1_INPUT_LEN:
+        raise ValueError(
+            "ASN.1 input exceeds maximum supported length "
+            f"({_MAX_ASN1_INPUT_LEN} bytes); refusing to parse to "
+            "mitigate GHSA-537c-gmf6-5ccf."
+        )
+
+_rust_load_pem_private_key = rust_openssl.keys.load_pem_private_key
+
+
+def load_pem_private_key(*args, **kwargs):
+    if args:
+        _check_asn1_length(args[0])
+    return _rust_load_pem_private_key(*args, **kwargs)
+
+
+_rust_load_der_private_key = rust_openssl.keys.load_der_private_key
+
+
+def load_der_private_key(*args, **kwargs):
+    if args:
+        _check_asn1_length(args[0])
+    return _rust_load_der_private_key(*args, **kwargs)
+
+
+_rust_load_pem_public_key = rust_openssl.keys.load_pem_public_key
+
+
+def load_pem_public_key(*args, **kwargs):
+    if args:
+        _check_asn1_length(args[0])
+    return _rust_load_pem_public_key(*args, **kwargs)
+
+
+_rust_load_der_public_key = rust_openssl.keys.load_der_public_key
+
+
+def load_der_public_key(*args, **kwargs):
+    if args:
+        _check_asn1_length(args[0])
+    return _rust_load_der_public_key(*args, **kwargs)
+
+
+_rust_load_pem_parameters = rust_openssl.dh.from_pem_parameters
+
+
+def load_pem_parameters(*args, **kwargs):
+    if args:
+        _check_asn1_length(args[0])
+    return _rust_load_pem_parameters(*args, **kwargs)
+
+
+_rust_load_der_parameters = rust_openssl.dh.from_der_parameters
+
+
+def load_der_parameters(*args, **kwargs):
+    if args:
+        _check_asn1_length(args[0])
+    return _rust_load_der_parameters(*args, **kwargs)
+
--- a/src/cryptography/hazmat/primitives/serialization/pkcs12.py
+++ b/src/cryptography/hazmat/primitives/serialization/pkcs12.py
@@ -110,8 +110,41 @@
         return fmt.format(self.key, self.cert, self.additional_certs)
 
 
-load_key_and_certificates = rust_pkcs12.load_key_and_certificates
-load_pkcs12 = rust_pkcs12.load_pkcs12
+# GHSA-537c-gmf6-5ccf: cap ASN.1 DER/PEM input at 100 MiB before reaching
+# OpenSSL d2i_* parsers, which have an integer-truncation heap over-read on
+# inputs >= 2 GiB on 64-bit Unix.
+_MAX_ASN1_INPUT_LEN = 100 * 1024 * 1024  # 100 MiB
+
+
+def _check_asn1_length(data: bytes) -> None:
+    try:
+        n = len(data)
+    except TypeError:
+        return
+    if n > _MAX_ASN1_INPUT_LEN:
+        raise ValueError(
+            "ASN.1 input exceeds maximum supported length "
+            f"({_MAX_ASN1_INPUT_LEN} bytes); refusing to parse to "
+            "mitigate GHSA-537c-gmf6-5ccf."
+        )
+
+_rust_load_key_and_certificates = rust_pkcs12.load_key_and_certificates
+
+
+def load_key_and_certificates(*args, **kwargs):
+    if args:
+        _check_asn1_length(args[0])
+    return _rust_load_key_and_certificates(*args, **kwargs)
+
+
+_rust_load_pkcs12 = rust_pkcs12.load_pkcs12
+
+
+def load_pkcs12(*args, **kwargs):
+    if args:
+        _check_asn1_length(args[0])
+    return _rust_load_pkcs12(*args, **kwargs)
+
 
 
 _PKCS12CATypes = typing.Union[
--- a/src/cryptography/hazmat/primitives/serialization/pkcs7.py
+++ b/src/cryptography/hazmat/primitives/serialization/pkcs7.py
@@ -22,9 +22,41 @@
 )
 from cryptography.utils import _check_byteslike
 
-load_pem_pkcs7_certificates = rust_pkcs7.load_pem_pkcs7_certificates
-
-load_der_pkcs7_certificates = rust_pkcs7.load_der_pkcs7_certificates
+# GHSA-537c-gmf6-5ccf: cap ASN.1 DER/PEM input at 100 MiB before reaching
+# OpenSSL d2i_* parsers, which have an integer-truncation heap over-read on
+# inputs >= 2 GiB on 64-bit Unix.
+_MAX_ASN1_INPUT_LEN = 100 * 1024 * 1024  # 100 MiB
+
+
+def _check_asn1_length(data: bytes) -> None:
+    try:
+        n = len(data)
+    except TypeError:
+        return
+    if n > _MAX_ASN1_INPUT_LEN:
+        raise ValueError(
+            "ASN.1 input exceeds maximum supported length "
+            f"({_MAX_ASN1_INPUT_LEN} bytes); refusing to parse to "
+            "mitigate GHSA-537c-gmf6-5ccf."
+        )
+
+_rust_load_pem_pkcs7_certificates = rust_pkcs7.load_pem_pkcs7_certificates
+
+
+def load_pem_pkcs7_certificates(*args, **kwargs):
+    if args:
+        _check_asn1_length(args[0])
+    return _rust_load_pem_pkcs7_certificates(*args, **kwargs)
+
+
+_rust_load_der_pkcs7_certificates = rust_pkcs7.load_der_pkcs7_certificates
+
+
+def load_der_pkcs7_certificates(*args, **kwargs):
+    if args:
+        _check_asn1_length(args[0])
+    return _rust_load_der_pkcs7_certificates(*args, **kwargs)
+
 
 serialize_certificates = rust_pkcs7.serialize_certificates
 
