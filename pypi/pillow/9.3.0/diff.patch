Description: Fix CVE-2023-44271 (Pillow DoS via large text input to ImageFont)
 Backport of upstream commit 1fe1bb49c452b0318cad12ea9d97c3bef188e9a7
 (PR #7244, released in Pillow 10.0.0). Adds ImageFont.MAX_STRING_LENGTH
 (default 1,000,000) and a _string_length_check() helper that raises
 ValueError when text exceeds the limit. The check is invoked at the top
 of the ImageFont, FreeTypeFont and TransposedFont measurement/render
 methods that previously allocated unbounded memory for arbitrarily long
 text input (CWE-400 / CWE-770).
Origin: upstream, https://github.com/python-pillow/Pillow/commit/1fe1bb49c452b0318cad12ea9d97c3bef188e9a7
Bug: https://github.com/python-pillow/Pillow/pull/7244
Author: Andrew Murray <radarhere@users.noreply.github.com>
Last-Update: 2026-06-12

--- a/src/PIL/ImageFont.py
+++ b/src/PIL/ImageFont.py
@@ -40,6 +40,9 @@ class Layout(IntEnum):
     RAQM = 1


+MAX_STRING_LENGTH = 1000000
+
+
 def __getattr__(name):
     for enum, prefix in {Layout: "LAYOUT_"}.items():
         if name.startswith(prefix):
@@ -64,6 +67,12 @@ try:
     core = _ImagingFtNotInstalled()


+def _string_length_check(text):
+    if MAX_STRING_LENGTH is not None and len(text) > MAX_STRING_LENGTH:
+        msg = "too many characters in string"
+        raise ValueError(msg)
+
+
 _UNSPECIFIED = object()


@@ -186,6 +195,7 @@ class ImageFont:

         :return: ``(left, top, right, bottom)`` bounding box
         """
+        _string_length_check(text)
         width, height = self.font.getsize(text)
         return 0, 0, width, height

@@ -196,6 +206,7 @@ class ImageFont:

         .. versionadded:: 9.2.0
         """
+        _string_length_check(text)
         width, height = self.font.getsize(text)
         return width

@@ -353,6 +364,7 @@ class FreeTypeFont:

         :return: Width for horizontal, height for vertical text.
         """
+        _string_length_check(text)
         return self.font.getlength(text, mode, direction, features, language) / 64

     def getbbox(
@@ -412,6 +424,7 @@ class FreeTypeFont:

         :return: ``(left, top, right, bottom)`` bounding box
         """
+        _string_length_check(text)
         size, offset = self.font.getsize(
             text, mode, direction, features, language, anchor
         )
@@ -747,6 +760,7 @@ class FreeTypeFont:
             fill = Image.core.fill
         else:
             deprecate("fill", 10)
+        _string_length_check(text)
         size, offset = self.font.getsize(
             text, mode, direction, features, language, anchor
         )
@@ -888,6 +902,7 @@ class TransposedFont:
             raise ValueError(
                 "text length is undefined for text rotated by 90 or 270 degrees"
             )
+        _string_length_check(text)
         return self.font.getlength(text, *args, **kwargs)


--- a/Tests/test_imagefont.py
+++ b/Tests/test_imagefont.py
@@ -1105,6 +1105,25 @@ def test_render_mono_size():
     assert_image_equal_tofile(im, "Tests/images/text_mono.gif")


+def test_too_many_characters(font):
+    with pytest.raises(ValueError):
+        font.getlength("A" * 1000001)
+    with pytest.raises(ValueError):
+        font.getbbox("A" * 1000001)
+    with pytest.raises(ValueError):
+        font.getmask2("A" * 1000001)
+
+    transposed_font = ImageFont.TransposedFont(font)
+    with pytest.raises(ValueError):
+        transposed_font.getlength("A" * 1000001)
+
+    default_font = ImageFont.load_default()
+    with pytest.raises(ValueError):
+        default_font.getlength("A" * 1000001)
+    with pytest.raises(ValueError):
+        default_font.getbbox("A" * 1000001)
+
+
 @pytest.mark.parametrize(
     "test_file",
     [
--- a/docs/reference/ImageFont.rst
+++ b/docs/reference/ImageFont.rst
@@ -18,6 +18,15 @@ OpenType fonts (as well as other font formats supported by the FreeType
 library). For earlier versions, TrueType support is only available as part of
 the imToolkit package.

+.. warning::
+    To protect against potential DOS attacks when using arbitrary strings as
+    text input, Pillow will raise a ``ValueError`` if the number of characters
+    is over a certain limit, :py:data:`MAX_STRING_LENGTH`.
+
+    This threshold can be changed by setting
+    :py:data:`MAX_STRING_LENGTH`. It can be disabled by setting
+    ``ImageFont.MAX_STRING_LENGTH = None``.
+
 Example
 -------

@@ -73,3 +82,12 @@ Constants

     Requires Raqm, you can check support using
     :py:func:`PIL.features.check_feature` with ``feature="raqm"``.
+
+Constants
+---------
+
+.. data:: MAX_STRING_LENGTH
+
+    Set to 1,000,000, to protect against potential DOS attacks. Pillow will
+    raise a ``ValueError`` if the number of characters is over this limit. The
+    check can be disabled by setting ``ImageFont.MAX_STRING_LENGTH = None``.

--- a/depends/install_webp.sh
+++ b/depends/install_webp.sh
@@ -1,7 +1,7 @@
 #!/bin/bash
 # install webp

-archive=libwebp-1.2.4
+archive=libwebp-1.3.2

 ./download-and-extract.sh $archive https://raw.githubusercontent.com/python-pillow/pillow-depends/main/$archive.tar.gz

--- a/winbuild/build_prepare.py
+++ b/winbuild/build_prepare.py
@@ -177,9 +177,9 @@
         "libs": [r"windows\vs2019\Release\{msbuild_arch}\liblzma\liblzma.lib"],
     },
     "libwebp": {
-        "url": "http://downloads.webmproject.org/releases/webp/libwebp-1.2.4.tar.gz",
-        "filename": "libwebp-1.2.4.tar.gz",
-        "dir": "libwebp-1.2.4",
+        "url": "http://downloads.webmproject.org/releases/webp/libwebp-1.3.2.tar.gz",
+        "filename": "libwebp-1.3.2.tar.gz",
+        "dir": "libwebp-1.3.2",
         "license": "COPYING",
         "build": [
             cmd_rmdir(r"output\release-static"),  # clean

--- a/src/PIL/ImageMath.py
+++ b/src/PIL/ImageMath.py
@@ -234,6 +234,11 @@ def eval(expression, _dict={}, **kw):

     # build execution namespace
     args = ops.copy()
+    for k in list(_dict.keys()) + list(kw.keys()):
+        if "__" in k or hasattr(builtins, k):
+            msg = f"'{k}' not allowed"
+            raise ValueError(msg)
+
     args.update(_dict)
     args.update(kw)
     for k, v in list(args.items()):
--- a/Tests/test_imagemath.py
+++ b/Tests/test_imagemath.py
@@ -63,6 +63,16 @@ def test_prevent_exec(expression):
         ImageMath.eval(expression)


+def test_prevent_double_underscores():
+    with pytest.raises(ValueError):
+        ImageMath.eval("1", {"__": None})
+
+
+def test_prevent_builtins():
+    with pytest.raises(ValueError):
+        ImageMath.eval("(lambda: exec('exit()'))()", {"exec": None})
+
+
 def test_logical():
     assert pixel(ImageMath.eval("not A", images)) == 0
     assert pixel(ImageMath.eval("A and B", images)) == "L 2"
