--- a/starlette/datastructures.py
+++ b/starlette/datastructures.py
@@ -1,4 +1,5 @@
 import typing
+import re
 from collections import namedtuple
 from collections.abc import Sequence
 from shlex import shlex
@@ -8,6 +9,9 @@ from starlette.types import Scope

 Address = namedtuple("Address", ["host", "port"])

+# Rejects Host header chars (/, ?, #, @, ...) that would let urlparse produce a path differing from scope["path"].
+_HOST_RE = re.compile(r"^([a-z0-9.-]+|\[[a-f0-9]*:[a-f0-9.:]+\])(?::[0-9]+)?$", re.IGNORECASE)
+

 class URL:
     def __init__(
@@ -27,7 +31,7 @@ class URL:
                     host_header = value.decode("latin-1")
                     break

-            if host_header is not None:
+            if host_header is not None and _HOST_RE.fullmatch(host_header):
                 url = "%s://%s%s" % (scheme, host_header, path)
             elif server is None:
                 url = path
