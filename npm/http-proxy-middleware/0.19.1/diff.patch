--- a/lib/index.js
+++ b/lib/index.js
@@ -83,8 +83,13 @@
    * @return {Boolean}
    */
   function shouldProxy(context, req) {
-    var path = req.originalUrl || req.url
-    return contextMatcher.match(context, path, req)
+    try {
+      var path = req.originalUrl || req.url
+      return contextMatcher.match(context, path, req)
+    } catch (err) {
+      logger.error('[HPM] An error occurred while matching the request URL: %s', err.message)
+      return false
+    }
   }
 
   /**

--- a/lib/router.js
+++ b/lib/router.js
@@ -20,18 +20,30 @@
 
 function getTargetFromProxyTable(req, table) {
   var result
-  var host = req.headers.host
-  var path = req.url
+  var host = req.headers.host || ''
+  var path = req.url || ''
 
-  var hostAndPath = host + path
-
   _.forIn(table, function(value, key) {
     if (containsPath(key)) {
-      if (hostAndPath.indexOf(key) > -1) {
-        // match 'localhost:3000/api'
-        result = table[key]
-        logger.debug('[HPM] Router table match: "%s"', key)
-        return false
+      if (isHostAndPathKey(key)) {
+        var parts = splitHostAndPathKey(key)
+        var keyHost = parts[0]
+        var keyPath = parts[1]
+
+        // SECURITY: host+path keys must match exact host + path prefix.
+        if (host === keyHost && path.indexOf(keyPath) === 0) {
+          // match 'localhost:3000/api'
+          result = table[key]
+          logger.debug('[HPM] Router table match: "%s"', key)
+          return false
+        }
+      } else {
+        if (path.indexOf(key) === 0) {
+          // match '/api'
+          result = table[key]
+          logger.debug('[HPM] Router table match: "%s"', key)
+          return false
+        }
       }
     } else {
       if (key === host) {
@@ -48,4 +60,13 @@
 
 function containsPath(v) {
   return v.indexOf('/') > -1
+}
+
+function isHostAndPathKey(v) {
+  return containsPath(v) && v.charAt(0) !== '/'
+}
+
+function splitHostAndPathKey(v) {
+  var firstSlash = v.indexOf('/')
+  return [v.slice(0, firstSlash), v.slice(firstSlash)]
 }
