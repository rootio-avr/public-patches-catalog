diff --git a/cmd/esbuild/version.go b/cmd/esbuild/version.go
index fef209e7..55cbe6ad 100644
--- a/cmd/esbuild/version.go
+++ b/cmd/esbuild/version.go
@@ -1,3 +1,3 @@
 package main
 
-const esbuildVersion = "0.19.10"
+var esbuildVersion = "0.19.10"
diff --git a/lib/npm/node-platform.ts b/lib/npm/node-platform.ts
index 156d024a..79993769 100644
--- a/lib/npm/node-platform.ts
+++ b/lib/npm/node-platform.ts
@@ -20,35 +20,15 @@ const packageDarwin_arm64 = '@esbuild/darwin-arm64'
 const packageDarwin_x64 = '@esbuild/darwin-x64'
 
 export const knownWindowsPackages: Record<string, string> = {
-  'win32 arm64 LE': '@esbuild/win32-arm64',
-  'win32 ia32 LE': '@esbuild/win32-ia32',
-  'win32 x64 LE': '@esbuild/win32-x64',
 }
 
 export const knownUnixlikePackages: Record<string, string> = {
-  'aix ppc64 BE': '@esbuild/aix-ppc64',
-  'android arm64 LE': '@esbuild/android-arm64',
-  'darwin arm64 LE': '@esbuild/darwin-arm64',
-  'darwin x64 LE': '@esbuild/darwin-x64',
-  'freebsd arm64 LE': '@esbuild/freebsd-arm64',
-  'freebsd x64 LE': '@esbuild/freebsd-x64',
-  'linux arm LE': '@esbuild/linux-arm',
-  'linux arm64 LE': '@esbuild/linux-arm64',
-  'linux ia32 LE': '@esbuild/linux-ia32',
-  'linux mips64el LE': '@esbuild/linux-mips64el',
-  'linux ppc64 LE': '@esbuild/linux-ppc64',
-  'linux riscv64 LE': '@esbuild/linux-riscv64',
-  'linux s390x BE': '@esbuild/linux-s390x',
-  'linux x64 LE': '@esbuild/linux-x64',
-  'linux loong64 LE': '@esbuild/linux-loong64',
-  'netbsd x64 LE': '@esbuild/netbsd-x64',
-  'openbsd x64 LE': '@esbuild/openbsd-x64',
-  'sunos x64 LE': '@esbuild/sunos-x64',
+  'linux x64 LE': '@rootio/esbuild__linux-x64',
+  'linux arm64 LE': '@rootio/esbuild__linux-arm64',
+  'darwin arm64 LE': '@rootio/esbuild__darwin-arm64',
 }
 
 export const knownWebAssemblyFallbackPackages: Record<string, string> = {
-  'android arm LE': '@esbuild/android-arm',
-  'android x64 LE': '@esbuild/android-x64',
 }
 
 export function pkgAndSubpathForCurrentPlatform(): { pkg: string, subpath: string, isWASM: boolean } {

diff --git a/package.json b/package.json
new file mode 100644
index 00000000..e2e319a1
--- /dev/null
+++ b/package.json
@@ -0,0 +1,5 @@
+{
+  "name": "esbuild-source",
+  "private": true,
+  "version": "0.19.10"
+}

diff --git a/lib/deno/mod.ts b/lib/deno/mod.ts
index 7e24f389..e1f7e9c9 100644
--- a/lib/deno/mod.ts
+++ b/lib/deno/mod.ts
@@ -4,6 +4,20 @@ import * as ourselves from "./mod"
 import * as denoflate from "https://deno.land/x/denoflate@1.2.1/mod.ts"
 
 declare const ESBUILD_VERSION: string
+declare const ESBUILD_BINARY_HASHES: Record<string, string>
+
+// SHA-256 of the official v0.19.10 platform binaries on npm. Used when this
+// TypeScript file is imported without the bundled --define substitution.
+// Matches the GHSA-gv7w-rqvm-qjhr / 9ff053e integrity check.
+const DEFAULT_ESBUILD_BINARY_HASHES: Record<string, string> = {
+  "@esbuild/android-arm64/bin/esbuild": "174fd3bb8479d479a0c6ea04936571d885e4ea8b733e934b1f294ba8992af14f",
+  "@esbuild/darwin-arm64/bin/esbuild": "3d2020b3e899adf0b6ddfb484c7a637ffa234b1c69cb42a38964ebf9d6a51724",
+  "@esbuild/darwin-x64/bin/esbuild": "4b0dd7d41b6cf04d98cc79734514da06eef5ddac52a15c993cb0a78f03b881f4",
+  "@esbuild/freebsd-x64/bin/esbuild": "b07165e1989125a48e4d967a5a6f46b8ab9d9709dcf17b408f1a0e57587fa94f",
+  "@esbuild/linux-arm64/bin/esbuild": "d158bc859072690fe6a85419a65ac91704e61226d1da2dedc32675fa6d424921",
+  "@esbuild/linux-x64/bin/esbuild": "b63cd541ef2f09febad477ed0a1e426d09fe04b0a6ac508c2a9f9b99071a4819",
+  "@esbuild/win32-x64/esbuild.exe": "2387e8a454148adaa92eae3217b422f7b6765cedad4bcac0b8c8d7bd67ad07e6",
+}
 
 export let version = ESBUILD_VERSION
 
@@ -59,6 +73,18 @@ export const initialize: typeof types.initialize = async (options) => {
   initializeWasCalled = true
 }
 
+async function binaryIntegrityCheck(pkg: string, subpath: string, bytes: Uint8Array): Promise<void> {
+  const hashBuffer = await crypto.subtle.digest('SHA-256', bytes)
+  const hash = Array.from(new Uint8Array(hashBuffer)).map(b => b.toString(16).padStart(2, '0')).join('')
+  const key = `${pkg}/${subpath}`
+  const table = (typeof ESBUILD_BINARY_HASHES !== 'undefined' && ESBUILD_BINARY_HASHES)
+    ? ESBUILD_BINARY_HASHES
+    : DEFAULT_ESBUILD_BINARY_HASHES
+  const expected = table[key]
+  if (!expected) throw new Error(`Missing hash for "${key}"`)
+  if (hash !== expected) throw new Error(`"${hash.slice(0, 8)}..." doesn't match "${expected.slice(0, 8)}..." for "${pkg}"`)
+}
+
 async function installFromNPM(name: string, subpath: string): Promise<string> {
   const { finalPath, finalDir } = getCachePath(name)
   try {
@@ -71,6 +97,7 @@ async function installFromNPM(name: string, subpath: string): Promise<string> {
   const url = `${npmRegistry}/${name}/-/${name.replace("@esbuild/", "")}-${version}.tgz`
   const buffer = await fetch(url).then(r => r.arrayBuffer())
   const executable = extractFileFromTarGzip(new Uint8Array(buffer), subpath)
+  await binaryIntegrityCheck(name, subpath, executable)
 
   await Deno.mkdir(finalDir, {
     recursive: true,
@@ -130,7 +157,7 @@ function extractFileFromTarGzip(buffer: Uint8Array, file: string): Uint8Array {
     let name = str(offset, 100)
     let size = parseInt(str(offset + 124, 12), 8)
     offset += 512
-    if (!isNaN(size)) {
+    if (!isNaN(size) && size >= 0) {
       if (name === file) return buffer.subarray(offset, offset + size)
       offset += (size + 511) & ~511
     }
diff --git a/scripts/esbuild.js b/scripts/esbuild.js
index ce5faaf5..16f8d3a6 100644
--- a/scripts/esbuild.js
+++ b/scripts/esbuild.js
@@ -229,6 +229,14 @@ const buildDenoLib = async (esbuildPath) => {
     '--outfile=' + path.join(denoDir, 'mod.js'),
     '--target=esnext',
     '--define:ESBUILD_VERSION=' + JSON.stringify(version),
+    '--define:ESBUILD_BINARY_HASHES=' + JSON.stringify((() => {
+      try {
+        const packageJSON = JSON.parse(fs.readFileSync(path.join(npmDir, 'package.json'), 'utf8'))
+        if (packageJSON['esbuild.binaryHashes']) return packageJSON['esbuild.binaryHashes']
+      } catch {
+      }
+      return null
+    })(), null, 2),
     '--platform=neutral',
     '--log-level=warning',
     '--banner:js=/// <reference types="./mod.d.ts" />',
