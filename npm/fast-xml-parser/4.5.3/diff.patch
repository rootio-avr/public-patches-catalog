--- a/src/xmlparser/DocTypeReader.js
+++ b/src/xmlparser/DocTypeReader.js
@@ -21,11 +21,13 @@
                     i += 7; 
                     let entityName, val;
                     [entityName, val,i] = readEntityExp(xmlData,i+1);
-                    if(val.indexOf("&") === -1) //Parameter entities are not supported
+                    if(val.indexOf("&") === -1){ //Parameter entities are not supported
+                        const escaped = entityName.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
                         entities[ validateEntityName(entityName) ] = {
-                            regx : RegExp( `&${entityName};`,"g"),
+                            regx : RegExp( `&${escaped};`,"g"),
                             val: val
                         };
+                    }
                 }
                 else if( hasBody && isElement(xmlData, i))  i += 8;//Not supported
                 else if( hasBody && isAttlist(xmlData, i))  i += 8;//Not supported
--- a/src/xmlparser/OrderedObjParser.js
+++ b/src/xmlparser/OrderedObjParser.js
@@ -63,8 +63,9 @@ function addExternalEntities(externalEntities){
   const entKeys = Object.keys(externalEntities);
   for (let i = 0; i < entKeys.length; i++) {
     const ent = entKeys[i];
+    const escaped = ent.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
     this.lastEntities[ent] = {
-       regex: new RegExp("&"+ent+";","g"),
+       regex: new RegExp("&"+escaped+";","g"),
        val : externalEntities[ent]
     }
   }

--- a/src/xmlparser/OptionsBuilder.js
+++ b/src/xmlparser/OptionsBuilder.js
@@ -40,8 +40,42 @@
     // skipEmptyListItem: false
 };
    
+/**
+ * Normalizes the processEntities option for backward compatibility and to
+ * enforce entity-expansion limits (CVE-2026-26278).
+ * @param {boolean|object} value
+ * @returns {object}
+ */
+function normalizeProcessEntities(value) {
+    if (typeof value === 'boolean') {
+        return {
+            enabled: value,
+            maxEntitySize: 10000,
+            maxExpansionDepth: 10,
+            maxTotalExpansions: 1000,
+            maxExpandedLength: 100000,
+            allowedTags: null,
+            tagFilter: null
+        };
+    }
+    if (typeof value === 'object' && value !== null) {
+        return {
+            enabled: value.enabled !== false,
+            maxEntitySize: value.maxEntitySize ?? 10000,
+            maxExpansionDepth: value.maxExpansionDepth ?? 10,
+            maxTotalExpansions: value.maxTotalExpansions ?? 1000,
+            maxExpandedLength: value.maxExpandedLength ?? 100000,
+            allowedTags: value.allowedTags ?? null,
+            tagFilter: value.tagFilter ?? null
+        };
+    }
+    return normalizeProcessEntities(true);
+}
+
 const buildOptions = function(options) {
-    return Object.assign({}, defaultOptions, options);
+    const built = Object.assign({}, defaultOptions, options);
+    built.processEntities = normalizeProcessEntities(built.processEntities);
+    return built;
 };
 
 exports.buildOptions = buildOptions;
--- a/src/xmlparser/DocTypeReader.js
+++ b/src/xmlparser/DocTypeReader.js
@@ -1,9 +1,11 @@
 const util = require('../util');
 
 //TODO: handle comments
-function readDocType(xmlData, i){
-    
+function readDocType(xmlData, i, entityConfig){
+
     const entities = {};
+    const maxEntitySize = (entityConfig && entityConfig.enabled !== false)
+        ? entityConfig.maxEntitySize : 0;
     if( xmlData[i + 3] === 'O' &&
          xmlData[i + 4] === 'C' &&
          xmlData[i + 5] === 'T' &&
@@ -21,6 +23,11 @@
                     i += 7; 
                     let entityName, val;
                     [entityName, val,i] = readEntityExp(xmlData,i+1);
+                    if(maxEntitySize && val.length > maxEntitySize){
+                        throw new Error(
+                            `Entity "${entityName}" size (${val.length}) exceeds maximum allowed size (${maxEntitySize})`
+                        );
+                    }
                     if(val.indexOf("&") === -1){ //Parameter entities are not supported
                         const escaped = entityName.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
                         entities[ validateEntityName(entityName) ] = {
--- a/src/xmlparser/OrderedObjParser.js
+++ b/src/xmlparser/OrderedObjParser.js
@@ -55,6 +55,8 @@
     this.saveTextToParentTag = saveTextToParentTag;
     this.addChild = addChild;
     this.ignoreAttributesFn = getIgnoreAttributesFn(this.options.ignoreAttributes)
+    this.entityExpansionCount = 0;
+    this.currentExpandedLength = 0;
   }
 
 }
@@ -190,6 +192,10 @@
   let currentNode = xmlObj;
   let textData = "";
   let jPath = "";
+
+  this.entityExpansionCount = 0;
+  this.currentExpandedLength = 0;
+
   for(let i=0; i< xmlData.length; i++){//for each char in XML data
     const ch = xmlData[i];
     if(ch === '<'){
@@ -264,7 +270,7 @@
         }
         i = endIndex;
       } else if( xmlData.substr(i + 1, 2) === '!D') {
-        const result = readDocType(xmlData, i);
+        const result = readDocType(xmlData, i, this.options.processEntities);
         this.docTypeEntities = result.entities;
         i = result.i;
       }else if(xmlData.substr(i + 1, 2) === '![') {
@@ -408,24 +414,47 @@
 }
 
 const replaceEntitiesValue = function(val){
+  if(typeof val !== 'string' || val.indexOf('&') === -1) return val;
+
+  const entityConfig = this.options.processEntities;
+  if(entityConfig && entityConfig.enabled === false) return val;
 
-  if(this.options.processEntities){
-    for(let entityName in this.docTypeEntities){
-      const entity = this.docTypeEntities[entityName];
+  const maxTotalExpansions = entityConfig ? entityConfig.maxTotalExpansions : 0;
+  const maxExpandedLength = entityConfig ? entityConfig.maxExpandedLength : 0;
+
+  for(let entityName in this.docTypeEntities){
+    const entity = this.docTypeEntities[entityName];
+    const matches = val.match(entity.regx);
+    if(matches){
+      this.entityExpansionCount += matches.length;
+      if(maxTotalExpansions && this.entityExpansionCount > maxTotalExpansions){
+        throw new Error(
+          `Entity expansion limit exceeded: ${this.entityExpansionCount} > ${maxTotalExpansions}`
+        );
+      }
+      const lengthBefore = val.length;
       val = val.replace( entity.regx, entity.val);
+      if(maxExpandedLength){
+        this.currentExpandedLength += (val.length - lengthBefore);
+        if(this.currentExpandedLength > maxExpandedLength){
+          throw new Error(
+            `Total expanded content size exceeded: ${this.currentExpandedLength} > ${maxExpandedLength}`
+          );
+        }
+      }
     }
-    for(let entityName in this.lastEntities){
-      const entity = this.lastEntities[entityName];
+  }
+  for(let entityName in this.lastEntities){
+    const entity = this.lastEntities[entityName];
+    val = val.replace( entity.regex, entity.val);
+  }
+  if(this.options.htmlEntities){
+    for(let entityName in this.htmlEntities){
+      const entity = this.htmlEntities[entityName];
       val = val.replace( entity.regex, entity.val);
     }
-    if(this.options.htmlEntities){
-      for(let entityName in this.htmlEntities){
-        const entity = this.htmlEntities[entityName];
-        val = val.replace( entity.regex, entity.val);
-      }
-    }
-    val = val.replace( this.ampEntity.regex, this.ampEntity.val);
   }
+  val = val.replace( this.ampEntity.regex, this.ampEntity.val);
   return val;
 }
 function saveTextToParentTag(textData, currentNode, jPath, isLeafNode) {
--- a/src/fxp.d.ts
+++ b/src/fxp.d.ts
@@ -1,3 +1,13 @@
+export type ProcessEntitiesOptions = {
+  enabled?: boolean;
+  maxEntitySize?: number;
+  maxExpansionDepth?: number;
+  maxTotalExpansions?: number;
+  maxExpandedLength?: number;
+  allowedTags?: string[] | null;
+  tagFilter?: ((tagName: string, jPath: string) => boolean) | null;
+};
+
 type X2jOptions = {
   /**
    * Preserve the order of tags in resulting JS object

--- a/src/xmlparser/OrderedObjParser.js
+++ b/src/xmlparser/OrderedObjParser.js
@@ -422,7 +422,7 @@
   const maxTotalExpansions = entityConfig ? entityConfig.maxTotalExpansions : 0;
   const maxExpandedLength = entityConfig ? entityConfig.maxExpandedLength : 0;
 
-  for(let entityName in this.docTypeEntities){
+  for(const entityName of Object.keys(this.docTypeEntities)){
     const entity = this.docTypeEntities[entityName];
     const matches = val.match(entity.regx);
     if(matches){
@@ -444,13 +444,31 @@
       }
     }
   }
-  for(let entityName in this.lastEntities){
+  for(const entityName of Object.keys(this.lastEntities)){
     const entity = this.lastEntities[entityName];
+    const matches = val.match(entity.regex);
+    if(matches){
+      this.entityExpansionCount += matches.length;
+      if(maxTotalExpansions && this.entityExpansionCount > maxTotalExpansions){
+        throw new Error(
+          `Entity expansion limit exceeded: ${this.entityExpansionCount} > ${maxTotalExpansions}`
+        );
+      }
+    }
     val = val.replace( entity.regex, entity.val);
   }
   if(this.options.htmlEntities){
-    for(let entityName in this.htmlEntities){
+    for(const entityName of Object.keys(this.htmlEntities)){
       const entity = this.htmlEntities[entityName];
+      const matches = val.match(entity.regex);
+      if(matches){
+        this.entityExpansionCount += matches.length;
+        if(maxTotalExpansions && this.entityExpansionCount > maxTotalExpansions){
+          throw new Error(
+            `Entity expansion limit exceeded: ${this.entityExpansionCount} > ${maxTotalExpansions}`
+          );
+        }
+      }
       val = val.replace( entity.regex, entity.val);
     }
   }
