diff --git a/src/v35.js b/src/v35.js
index f3e891c..f3f138a 100644
--- a/src/v35.js
+++ b/src/v35.js
@@ -39,6 +39,10 @@ export default function (name, version, hashfunc) {
     bytes[8] = (bytes[8] & 0x3f) | 0x80;
 
     if (buf) {
+      if (off < 0 || off + 16 > buf.length) {
+        throw new RangeError(`UUID byte range ${off}:${off + 15} is out of buffer bounds`);
+      }
+
       for (var idx = 0; idx < 16; ++idx) {
         buf[off + idx] = bytes[idx];
       }
diff --git a/test/unit/v35.test.js b/test/unit/v35.test.js
index 3bc4a11..479a03e 100644
--- a/test/unit/v35.test.js
+++ b/test/unit/v35.test.js
@@ -114,6 +114,25 @@ describe('v5', () => {
     );
   });
 
+
+  test('v3 throws RangeError for out-of-range indexes', () => {
+    const buf15 = new Array(15);
+    const buf30 = new Array(30);
+
+    assert.throws(() => v3('hello.example.com', v3.DNS, buf15), RangeError);
+    assert.throws(() => v3('hello.example.com', v3.DNS, buf30, -1), RangeError);
+    assert.throws(() => v3('hello.example.com', v3.DNS, buf30, 15), RangeError);
+  });
+
+  test('v5 throws RangeError for out-of-range indexes', () => {
+    const buf15 = new Array(15);
+    const buf30 = new Array(30);
+
+    assert.throws(() => v5('hello.example.com', v5.DNS, buf15), RangeError);
+    assert.throws(() => v5('hello.example.com', v5.DNS, buf30, -1), RangeError);
+    assert.throws(() => v5('hello.example.com', v5.DNS, buf30, 15), RangeError);
+  });
+
   test('v5', () => {
     // Expect to get the same results as http://tools.adjet.org/uuid-v5
     assert.equal(v5('hello.example.com', v5.DNS), 'fdda765f-fc57-5604-a269-52a7df8164ec');


