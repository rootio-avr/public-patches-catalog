

--- a/dist/cjs/v35.js
+++ b/dist/cjs/v35.js
@@ -30,6 +30,9 @@
     bytes[8] = (bytes[8] & 0x3f) | 0x80;
     if (buf) {
         offset = offset || 0;
+        if (offset < 0 || offset + 16 > buf.length) {
+            throw new RangeError(`UUID byte range ${offset}:${offset + 15} is out of buffer bounds`);
+        }
         for (let i = 0; i < 16; ++i) {
             buf[offset + i] = bytes[i];
         }
--- a/dist/cjs/v6.js
+++ b/dist/cjs/v6.js
@@ -10,6 +10,9 @@
     let bytes = (0, v1_js_1.default)({ ...options, _v6: true }, new Uint8Array(16));
     bytes = (0, v1ToV6_js_1.default)(bytes);
     if (buf) {
+        if (offset < 0 || offset + 16 > buf.length) {
+            throw new RangeError(`UUID byte range ${offset}:${offset + 15} is out of buffer bounds`);
+        }
         for (let i = 0; i < 16; i++) {
             buf[offset + i] = bytes[i];
         }
--- a/dist/cjs-browser/v35.js
+++ b/dist/cjs-browser/v35.js
@@ -30,6 +30,9 @@
     bytes[8] = (bytes[8] & 0x3f) | 0x80;
     if (buf) {
         offset = offset || 0;
+        if (offset < 0 || offset + 16 > buf.length) {
+            throw new RangeError(`UUID byte range ${offset}:${offset + 15} is out of buffer bounds`);
+        }
         for (let i = 0; i < 16; ++i) {
             buf[offset + i] = bytes[i];
         }
--- a/dist/cjs-browser/v6.js
+++ b/dist/cjs-browser/v6.js
@@ -10,6 +10,9 @@
     let bytes = (0, v1_js_1.default)({ ...options, _v6: true }, new Uint8Array(16));
     bytes = (0, v1ToV6_js_1.default)(bytes);
     if (buf) {
+        if (offset < 0 || offset + 16 > buf.length) {
+            throw new RangeError(`UUID byte range ${offset}:${offset + 15} is out of buffer bounds`);
+        }
         for (let i = 0; i < 16; i++) {
             buf[offset + i] = bytes[i];
         }
--- a/dist/esm/v35.js
+++ b/dist/esm/v35.js
@@ -25,6 +25,9 @@
     bytes[8] = (bytes[8] & 0x3f) | 0x80;
     if (buf) {
         offset = offset || 0;
+        if (offset < 0 || offset + 16 > buf.length) {
+            throw new RangeError(`UUID byte range ${offset}:${offset + 15} is out of buffer bounds`);
+        }
         for (let i = 0; i < 16; ++i) {
             buf[offset + i] = bytes[i];
         }
--- a/dist/esm/v6.js
+++ b/dist/esm/v6.js
@@ -7,6 +7,9 @@
     let bytes = v1({ ...options, _v6: true }, new Uint8Array(16));
     bytes = v1ToV6(bytes);
     if (buf) {
+        if (offset < 0 || offset + 16 > buf.length) {
+            throw new RangeError(`UUID byte range ${offset}:${offset + 15} is out of buffer bounds`);
+        }
         for (let i = 0; i < 16; i++) {
             buf[offset + i] = bytes[i];
         }
--- a/dist/esm-browser/v35.js
+++ b/dist/esm-browser/v35.js
@@ -25,6 +25,9 @@
     bytes[8] = (bytes[8] & 0x3f) | 0x80;
     if (buf) {
         offset = offset || 0;
+        if (offset < 0 || offset + 16 > buf.length) {
+            throw new RangeError(`UUID byte range ${offset}:${offset + 15} is out of buffer bounds`);
+        }
         for (let i = 0; i < 16; ++i) {
             buf[offset + i] = bytes[i];
         }
--- a/dist/esm-browser/v6.js
+++ b/dist/esm-browser/v6.js
@@ -7,6 +7,9 @@
     let bytes = v1({ ...options, _v6: true }, new Uint8Array(16));
     bytes = v1ToV6(bytes);
     if (buf) {
+        if (offset < 0 || offset + 16 > buf.length) {
+            throw new RangeError(`UUID byte range ${offset}:${offset + 15} is out of buffer bounds`);
+        }
         for (let i = 0; i < 16; i++) {
             buf[offset + i] = bytes[i];
         }
