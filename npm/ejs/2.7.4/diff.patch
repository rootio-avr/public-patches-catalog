--- a/lib/ejs.js
+++ b/lib/ejs.js
@@ -63,6 +63,7 @@
 // so we make an exception for `renderFile`
 var _OPTS_PASSABLE_WITH_DATA_EXPRESS = _OPTS_PASSABLE_WITH_DATA.concat('cache');
 var _BOM = /^\uFEFF/;
+var _JS_IDENTIFIER = /^[a-zA-Z_$][0-9a-zA-Z_$]*$/;

 /**
  * EJS template function cache. This can be a LRU object from lru-cache NPM
@@ -576,12 +577,21 @@
         '  var __output = "";\n' +
         '  function __append(s) { if (s !== undefined && s !== null) __output += s }\n';
       if (opts.outputFunctionName) {
+        if (!_JS_IDENTIFIER.test(opts.outputFunctionName)) {
+          throw new Error('outputFunctionName is not a valid JS identifier.');
+        }
         prepended += '  var ' + opts.outputFunctionName + ' = __append;' + '\n';
       }
+      if (opts.localsName && !_JS_IDENTIFIER.test(opts.localsName)) {
+        throw new Error('localsName is not a valid JS identifier.');
+      }
       if (opts.destructuredLocals && opts.destructuredLocals.length) {
         var destructuring = '  var __locals = (' + opts.localsName + ' || {}),\n';
         for (var i = 0; i < opts.destructuredLocals.length; i++) {
           var name = opts.destructuredLocals[i];
+          if (!_JS_IDENTIFIER.test(name)) {
+            throw new Error(`destructuredLocals[${i}] is not a valid JS identifier.`);
+          }
           if (i > 0) {
             destructuring += ',\n  ';
           }

--- a/lib/ejs.js
+++ b/lib/ejs.js
@@ -501,7 +501,7 @@
 };
 
 function Template(text, opts) {
-  opts = opts || {};
+  opts = utils.hasOwnOnlyObject(opts);
   var options = {};
   this.templateText = text;
   this.mode = null;
--- a/lib/utils.js
+++ b/lib/utils.js
@@ -165,3 +165,24 @@
     this._data = {};
   }
 };
+
+/**
+ * Copy only the own enumerable properties of `obj` into a fresh
+ * null-prototype object, discarding any properties inherited from a
+ * (possibly polluted) prototype chain such as Object.prototype.
+ *
+ * @param  {Object} obj Source object (may be null or undefined)
+ * @return {Object}     New object containing only own properties of `obj`
+ * @static
+ * @private
+ */
+exports.hasOwnOnlyObject = function (obj) {
+  var o = Object.create(null);
+  obj = obj || {};
+  for (var p in obj) {
+    if (Object.prototype.hasOwnProperty.call(obj, p)) {
+      o[p] = obj[p];
+    }
+  }
+  return o;
+};
