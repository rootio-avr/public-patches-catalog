--- a/index.js
+++ b/index.js
@@ -172,6 +172,10 @@ function basePickBy(object, props, predicate) {

   while (++index < length) {
     var key = props[index],
         value = object[key];

+    if (key === '__proto__' || key === 'constructor' || key === 'prototype') {
+      continue;
+    }
+
     if (predicate(value, key)) {
       result[key] = value;
     }
