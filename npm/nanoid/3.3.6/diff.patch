From 0f6e44597dac608705403bd6d8486a545a62ca1b Mon Sep 17 00:00:00 2001
Date: Thu, 8 Jan 2026 13:26:03 +0530
Subject: [PATCH] patch

---
 async/index.browser.cjs | 4 ++--
 async/index.browser.js  | 4 ++--
 async/index.cjs         | 4 ++--
 async/index.js          | 4 ++--
 async/index.native.js   | 4 ++--
 index.browser.cjs       | 2 +-
 index.browser.js        | 2 +-
 index.cjs               | 4 ++--
 index.js                | 4 ++--
 non-secure/index.cjs    | 4 ++--
 non-secure/index.js     | 4 ++--
 11 files changed, 20 insertions(+), 20 deletions(-)

diff --git a/async/index.browser.cjs b/async/index.browser.cjs
index 7e5bba8..a086682 100644
--- a/async/index.browser.cjs
+++ b/async/index.browser.cjs
@@ -6,7 +6,7 @@ let customAlphabet = (alphabet, defaultSize = 21) => {
     let id = ''
     while (true) {
       let bytes = crypto.getRandomValues(new Uint8Array(step))
-      let i = step
+      let i = step | 0
       while (i--) {
         id += alphabet[bytes[i] & mask] || ''
         if (id.length === size) return id
@@ -16,7 +16,7 @@ let customAlphabet = (alphabet, defaultSize = 21) => {
 }
 let nanoid = async (size = 21) => {
   let id = ''
-  let bytes = crypto.getRandomValues(new Uint8Array(size))
+  let bytes = crypto.getRandomValues(new Uint8Array((size |= 0)))
   while (size--) {
     let byte = bytes[size] & 63
     if (byte < 36) {
diff --git a/async/index.browser.js b/async/index.browser.js
index 5ece04d..fbaa230 100644
--- a/async/index.browser.js
+++ b/async/index.browser.js
@@ -6,7 +6,7 @@ let customAlphabet = (alphabet, defaultSize = 21) => {
     let id = ''
     while (true) {
       let bytes = crypto.getRandomValues(new Uint8Array(step))
-      let i = step
+      let i = step | 0
       while (i--) {
         id += alphabet[bytes[i] & mask] || ''
         if (id.length === size) return id
@@ -16,7 +16,7 @@ let customAlphabet = (alphabet, defaultSize = 21) => {
 }
 let nanoid = async (size = 21) => {
   let id = ''
-  let bytes = crypto.getRandomValues(new Uint8Array(size))
+  let bytes = crypto.getRandomValues(new Uint8Array((size |= 0)))
   while (size--) {
     let byte = bytes[size] & 63
     if (byte < 36) {
diff --git a/async/index.cjs b/async/index.cjs
index 50db105..cd2b109 100644
--- a/async/index.cjs
+++ b/async/index.cjs
@@ -18,14 +18,14 @@ let customAlphabet = (alphabet, defaultSize = 21) => {
       let i = step
       while (i--) {
         id += alphabet[bytes[i] & mask] || ''
-        if (id.length === size) return id
+        if (id.length >= size) return id
       }
       return tick(id, size)
     })
   return size => tick('', size)
 }
 let nanoid = (size = 21) =>
-  random(size).then(bytes => {
+  random((size |= 0)).then(bytes => {
     let id = ''
     while (size--) {
       id += urlAlphabet[bytes[size] & 63]
diff --git a/async/index.js b/async/index.js
index 803fad6..cec454a 100644
--- a/async/index.js
+++ b/async/index.js
@@ -18,14 +18,14 @@ let customAlphabet = (alphabet, defaultSize = 21) => {
       let i = step
       while (i--) {
         id += alphabet[bytes[i] & mask] || ''
-        if (id.length === size) return id
+        if (id.length >= size) return id
       }
       return tick(id, size)
     })
   return size => tick('', size)
 }
 let nanoid = (size = 21) =>
-  random(size).then(bytes => {
+  random((size |= 0)).then(bytes => {
     let id = ''
     while (size--) {
       id += urlAlphabet[bytes[size] & 63]
diff --git a/async/index.native.js b/async/index.native.js
index 5cb3d57..7c1d6f3 100644
--- a/async/index.native.js
+++ b/async/index.native.js
@@ -9,14 +9,14 @@ let customAlphabet = (alphabet, defaultSize = 21) => {
       let i = step
       while (i--) {
         id += alphabet[bytes[i] & mask] || ''
-        if (id.length === size) return id
+        if (id.length >= size) return id
       }
       return tick(id, size)
     })
   return size => tick('', size)
 }
 let nanoid = (size = 21) =>
-  random(size).then(bytes => {
+  random((size |= 0)).then(bytes => {
     let id = ''
     while (size--) {
       id += urlAlphabet[bytes[size] & 63]
diff --git a/index.browser.cjs b/index.browser.cjs
index f800d6f..dba7710 100644
--- a/index.browser.cjs
+++ b/index.browser.cjs
@@ -7,7 +7,7 @@ let customRandom = (alphabet, defaultSize, getRandom) => {
     let id = ''
     while (true) {
       let bytes = getRandom(step)
-      let j = step
+      let j = step | 0
       while (j--) {
         id += alphabet[bytes[j] & mask] || ''
         if (id.length === size) return id
diff --git a/index.browser.js b/index.browser.js
index 8b3139b..7d3b876 100644
--- a/index.browser.js
+++ b/index.browser.js
@@ -7,7 +7,7 @@ let customRandom = (alphabet, defaultSize, getRandom) => {
     let id = ''
     while (true) {
       let bytes = getRandom(step)
-      let j = step
+      let j = step | 0
       while (j--) {
         id += alphabet[bytes[j] & mask] || ''
         if (id.length === size) return id
diff --git a/index.cjs b/index.cjs
index 0fa85e9..b7254c4 100644
--- a/index.cjs
+++ b/index.cjs
@@ -14,7 +14,7 @@ let fillPool = bytes => {
   poolOffset += bytes
 }
 let random = bytes => {
-  fillPool((bytes -= 0))
+  fillPool((bytes |= 0))
   return pool.subarray(poolOffset - bytes, poolOffset)
 }
 let customRandom = (alphabet, defaultSize, getRandom) => {
@@ -35,7 +35,7 @@ let customRandom = (alphabet, defaultSize, getRandom) => {
 let customAlphabet = (alphabet, size = 21) =>
   customRandom(alphabet, size, random)
 let nanoid = (size = 21) => {
-  fillPool((size -= 0))
+  fillPool((size |= 0))
   let id = ''
   for (let i = poolOffset - size; i < poolOffset; i++) {
     id += urlAlphabet[pool[i] & 63]
diff --git a/index.js b/index.js
index 21e155f..9bc909d 100644
--- a/index.js
+++ b/index.js
@@ -14,7 +14,7 @@ let fillPool = bytes => {
   poolOffset += bytes
 }
 let random = bytes => {
-  fillPool((bytes -= 0))
+  fillPool((bytes |= 0))
   return pool.subarray(poolOffset - bytes, poolOffset)
 }
 let customRandom = (alphabet, defaultSize, getRandom) => {
@@ -35,7 +35,7 @@ let customRandom = (alphabet, defaultSize, getRandom) => {
 let customAlphabet = (alphabet, size = 21) =>
   customRandom(alphabet, size, random)
 let nanoid = (size = 21) => {
-  fillPool((size -= 0))
+  fillPool((size |= 0))
   let id = ''
   for (let i = poolOffset - size; i < poolOffset; i++) {
     id += urlAlphabet[pool[i] & 63]
diff --git a/non-secure/index.cjs b/non-secure/index.cjs
index 09d57cd..9f6e148 100644
--- a/non-secure/index.cjs
+++ b/non-secure/index.cjs
@@ -3,7 +3,7 @@ let urlAlphabet =
 let customAlphabet = (alphabet, defaultSize = 21) => {
   return (size = defaultSize) => {
     let id = ''
-    let i = size
+    let i = size | 0
     while (i--) {
       id += alphabet[(Math.random() * alphabet.length) | 0]
     }
@@ -12,7 +12,7 @@ let customAlphabet = (alphabet, defaultSize = 21) => {
 }
 let nanoid = (size = 21) => {
   let id = ''
-  let i = size
+  let i = size | 0
   while (i--) {
     id += urlAlphabet[(Math.random() * 64) | 0]
   }
diff --git a/non-secure/index.js b/non-secure/index.js
index e7e19ad..2ea5827 100644
--- a/non-secure/index.js
+++ b/non-secure/index.js
@@ -3,7 +3,7 @@ let urlAlphabet =
 let customAlphabet = (alphabet, defaultSize = 21) => {
   return (size = defaultSize) => {
     let id = ''
-    let i = size
+    let i = size | 0
     while (i--) {
       id += alphabet[(Math.random() * alphabet.length) | 0]
     }
@@ -12,7 +12,7 @@ let customAlphabet = (alphabet, defaultSize = 21) => {
 }
 let nanoid = (size = 21) => {
   let id = ''
-  let i = size
+  let i = size | 0
   while (i--) {
     id += urlAlphabet[(Math.random() * 64) | 0]
   }
-- 
2.43.0

