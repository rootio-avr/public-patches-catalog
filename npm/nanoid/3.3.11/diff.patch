--- a/non-secure/index.cjs
+++ b/non-secure/index.cjs
@@ -12,7 +12,7 @@
     let id = ''
     // A compact alternative for `for (var i = 0; i < step; i++)`.
     let i = size | 0
-    while (i--) {
+    while (i-- > 0) {
       // `| 0` is more compact and faster than `Math.floor()`.
       id += alphabet[(Math.random() * alphabet.length) | 0]
     }
@@ -24,7 +24,7 @@
   let id = ''
   // A compact alternative for `for (var i = 0; i < step; i++)`.
   let i = size | 0
-  while (i--) {
+  while (i-- > 0) {
     // `| 0` is more compact and faster than `Math.floor()`.
     id += urlAlphabet[(Math.random() * 64) | 0]
   }
--- a/non-secure/index.js
+++ b/non-secure/index.js
@@ -4,7 +4,7 @@
   return (size = defaultSize) => {
     let id = ''
     let i = size | 0
-    while (i--) {
+    while (i-- > 0) {
       id += alphabet[(Math.random() * alphabet.length) | 0]
     }
     return id
@@ -13,7 +13,7 @@
 let nanoid = (size = 21) => {
   let id = ''
   let i = size | 0
-  while (i--) {
+  while (i-- > 0) {
     id += urlAlphabet[(Math.random() * 64) | 0]
   }
   return id

Description: CVE-2026-67213 - nanoid customAlphabet/customRandom infinite loop on size <= 0
 When customAlphabet/customRandom (and the async variants) are called with a
 size of 0 or a negative number, the generator never reaches the exit condition
 (id.length === size), causing an unbounded loop that exhausts CPU/memory. This
 backports upstream fix f9d13f150847d117877adee3460a46eceb0cf49b to short-circuit
 and return an empty string for size <= 0, syncing the 0-size behaviour. Test-file
 hunks from upstream are omitted because the published npm tarball ships no test/ dir.
Origin: upstream, https://github.com/ai/nanoid/commit/f9d13f150847d117877adee3460a46eceb0cf49b
--- a/async/index.browser.cjs
+++ b/async/index.browser.cjs
@@ -25,6 +25,7 @@
   let step = -~((1.6 * mask * defaultSize) / alphabet.length)
 
   return async (size = defaultSize) => {
+    if (size <= 0) return ''
     let id = ''
     while (true) {
       let bytes = crypto.getRandomValues(new Uint8Array(step))
--- a/async/index.browser.js
+++ b/async/index.browser.js
@@ -3,6 +3,7 @@
   let mask = (2 << (Math.log(alphabet.length - 1) / Math.LN2)) - 1
   let step = -~((1.6 * mask * defaultSize) / alphabet.length)
   return async (size = defaultSize) => {
+    if (size <= 0) return ''
     let id = ''
     while (true) {
       let bytes = crypto.getRandomValues(new Uint8Array(step))
--- a/async/index.cjs
+++ b/async/index.cjs
@@ -50,7 +50,10 @@
       return tick(id, size)
     })
 
-  return size => tick('', size)
+  return (size = defaultSize) => {
+    if (size <= 0) return Promise.resolve('')
+    return tick('', size)
+  }
 }
 
 let nanoid = (size = 21) =>
--- a/async/index.js
+++ b/async/index.js
@@ -22,7 +22,10 @@
       }
       return tick(id, size)
     })
-  return size => tick('', size)
+  return (size = defaultSize) => {
+    if (size <= 0) return Promise.resolve('')
+    return tick('', size)
+  }
 }
 let nanoid = (size = 21) =>
   random((size |= 0)).then(bytes => {
--- a/index.browser.cjs
+++ b/index.browser.cjs
@@ -30,6 +30,7 @@
   let step = -~((1.6 * mask * defaultSize) / alphabet.length)
 
   return (size = defaultSize) => {
+    if (size <= 0) return ''
     let id = ''
     while (true) {
       let bytes = getRandom(step)
--- a/index.browser.js
+++ b/index.browser.js
@@ -4,6 +4,7 @@
   let mask = (2 << (Math.log(alphabet.length - 1) / Math.LN2)) - 1
   let step = -~((1.6 * mask * defaultSize) / alphabet.length)
   return (size = defaultSize) => {
+    if (size <= 0) return ''
     let id = ''
     while (true) {
       let bytes = getRandom(step)
--- a/index.cjs
+++ b/index.cjs
@@ -49,6 +49,7 @@
   let step = Math.ceil((1.6 * mask * defaultSize) / alphabet.length)
 
   return (size = defaultSize) => {
+    if (size <= 0) return ''
     let id = ''
     while (true) {
       let bytes = getRandom(step)
--- a/index.js
+++ b/index.js
@@ -21,6 +21,7 @@
   let mask = (2 << (31 - Math.clz32((alphabet.length - 1) | 1))) - 1
   let step = Math.ceil((1.6 * mask * defaultSize) / alphabet.length)
   return (size = defaultSize) => {
+    if (size <= 0) return ''
     let id = ''
     while (true) {
       let bytes = getRandom(step)

Backport of nanoid@3.3.12 fix for CVE-2026-73086 (commit 821dfed).

Integer overflow in nanoid(size): size |= 0 wraps values >= 2^31 to negative,
corrupting the process-wide CSPRNG pool in fillPool(). Guard rejects bytes < 0
or > 1024 before pool state is touched. Patches both index.js and index.cjs.

--- a/index.js
+++ b/index.js
@@ -4,6 +4,7 @@ import { urlAlphabet } from './url-alphabet/index.js'
 const POOL_SIZE_MULTIPLIER = 128
 let pool, poolOffset
 let fillPool = bytes => {
+  if (bytes < 0 || bytes > 1024) throw new RangeError('Wrong ID size')
   if (!pool || pool.length < bytes) {
     pool = Buffer.allocUnsafe(bytes * POOL_SIZE_MULTIPLIER)
     crypto.randomFillSync(pool)
--- a/index.cjs
+++ b/index.cjs
@@ -11,6 +11,7 @@ const POOL_SIZE_MULTIPLIER = 128
 let pool, poolOffset
 
 let fillPool = bytes => {
+  if (bytes < 0 || bytes > 1024) throw new RangeError('Wrong ID size')
   if (!pool || pool.length < bytes) {
     pool = Buffer.allocUnsafe(bytes * POOL_SIZE_MULTIPLIER)
     crypto.randomFillSync(pool)
--- /dev/null
+++ b/test/cve-2026-73086.test.cjs
@@ -0,0 +1,39 @@
+let { test } = require('uvu')
+let { throws, not } = require('uvu/assert')
+
+let node = require('../index.js')
+
+test.before(() => {
+  global.crypto = {
+    getRandomValues(array) {
+      for (let i = 0; i < array.length; i++) {
+        array[i] = Math.floor(Math.random() * 256)
+      }
+      return array
+    }
+  }
+})
+
+test.after(() => {
+  delete global.crypto
+})
+
+// Regression test for CVE-2026-73086: nanoid must reject negative or
+// oversized ID lengths instead of allowing a pool-breaking allocation.
+test('nanoid throws on negative or too big ID length', () => {
+  throws(() => {
+    node.nanoid(2147483648)
+  }, /Wrong ID size|Invalid typed array length/)
+  throws(() => {
+    node.nanoid(-10)
+  }, /Wrong ID size|Invalid typed array length/)
+  throws(() => {
+    node.nanoid(1025)
+  }, /Wrong ID size/)
+  // A valid call after a rejected one still produces fresh, distinct IDs.
+  let a = node.nanoid()
+  let b = node.nanoid()
+  not.equal(a, b)
+})
+
+test.run()
