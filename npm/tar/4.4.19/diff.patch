--- a/lib/unpack.js
+++ b/lib/unpack.js
@@ -50,6 +50,8 @@
 const platform = process.env.TESTING_TAR_FAKE_PLATFORM || process.platform
 const isWindows = platform === 'win32'
 
+const DEFAULT_MAX_DEPTH = 1024
+
 // Unlinks on Windows are not atomic.
 //
 // This means that if you have a file entry, followed by another
@@ -174,6 +176,12 @@
     this.processGid = (this.preserveOwner || this.setOwner) && process.getgid ?
       process.getgid() : null
 
+    // prevent excessively deep nesting of subfolders
+    // set to `Infinity` to remove this restriction
+    this.maxDepth = typeof opt.maxDepth === 'number'
+      ? opt.maxDepth
+      : DEFAULT_MAX_DEPTH
+
     // mostly just for testing, but useful in some cases.
     // Forcibly trigger a chown on every entry, no matter what
     this.forceChown = opt.forceChown === true
@@ -219,11 +227,12 @@
   }
 
   [CHECKPATH] (entry) {
+    const p = normPath(entry.path)
+    const parts = p.split('/')
+
     if (this.strip) {
-      const parts = normPath(entry.path).split('/')
       if (parts.length < this.strip)
         return false
-      entry.path = parts.slice(this.strip).join('/')
 
       if (entry.type === 'Link') {
         const linkparts = normPath(entry.linkpath).split('/')
@@ -232,21 +241,34 @@
         else
           return false
       }
+      parts.splice(0, this.strip)
+      entry.path = parts.join('/')
+    }
+
+    // check for excessively deep nesting
+    if (isFinite(this.maxDepth) && parts.length > this.maxDepth) {
+      this.warn('TAR_ENTRY_ERROR', 'path excessively deep', {
+        entry,
+        path: p,
+        depth: parts.length,
+        maxDepth: this.maxDepth,
+      })
+      return false
     }
 
     if (!this.preservePaths) {
-      const p = normPath(entry.path)
-      const parts = p.split('/')
-      if (parts.includes('..') || isWindows && /^[a-z]:\.\.$/i.test(parts[0])) {
-        this.warn(`path contains '..'`, p)
+      const np = normPath(entry.path)
+      const nparts = np.split('/')
+      if (nparts.includes('..') || isWindows && /^[a-z]:\.\.$/i.test(nparts[0])) {
+        this.warn(`path contains '..'`, np)
         return false
       }
 
       // strip off the root
-      const s = stripAbsolutePath(p)
+      const s = stripAbsolutePath(np)
       if (s[0]) {
         entry.path = s[1]
-        this.warn(`stripping ${s[0]} from absolute path`, p)
+        this.warn(`stripping ${s[0]} from absolute path`, np)
       }
     }
 

--- a/lib/unpack.js	2026-06-01 18:29:34.505519888 +0530
+++ b/lib/unpack.js	2026-06-01 18:29:34.507700902 +0530
@@ -34,6 +34,7 @@
 const UNSUPPORTED = Symbol('unsupported')
 const UNKNOWN = Symbol('unknown')
 const CHECKPATH = Symbol('checkPath')
+const STRIPABSOLUTEPATH = Symbol('stripAbsolutePath')
 const MKDIR = Symbol('mkdir')
 const ONERROR = Symbol('onError')
 const PENDING = Symbol('pending')
@@ -226,6 +227,27 @@
     }
   }
 
+  // return false if we need to skip this file
+  // return true if the field was successfully sanitized
+  [STRIPABSOLUTEPATH] (entry, field) {
+    const p = entry[field]
+    if (!p || this.preservePaths) return true
+
+    const parts = normPath(p).split('/')
+    if (parts.includes('..') || isWindows && /^[a-z]:\.\.$/i.test(parts[0])) {
+      this.warn(`${field} contains '..'`, p)
+      return false
+    }
+
+    // strip off the root
+    const s = stripAbsolutePath(p)
+    if (s[0]) {
+      entry[field] = s[1]
+      this.warn(`stripping ${s[0]} from absolute ${field}`, p)
+    }
+    return true
+  }
+
   [CHECKPATH] (entry) {
     const p = normPath(entry.path)
     const parts = p.split('/')
@@ -256,20 +278,11 @@
       return false
     }
 
-    if (!this.preservePaths) {
-      const np = normPath(entry.path)
-      const nparts = np.split('/')
-      if (nparts.includes('..') || isWindows && /^[a-z]:\.\.$/i.test(nparts[0])) {
-        this.warn(`path contains '..'`, np)
-        return false
-      }
-
-      // strip off the root
-      const s = stripAbsolutePath(np)
-      if (s[0]) {
-        entry.path = s[1]
-        this.warn(`stripping ${s[0]} from absolute path`, np)
-      }
+    if (
+      !this[STRIPABSOLUTEPATH](entry, 'path') ||
+      !this[STRIPABSOLUTEPATH](entry, 'linkpath')
+    ) {
+      return false
     }
 
     if (path.isAbsolute(entry.path))

--- a/lib/path-reservations.js	1985-10-26 13:45:00.000000000 +0530
+++ b/lib/path-reservations.js	2026-06-01 18:29:34.513499003 +0530
@@ -118,6 +118,8 @@
     paths = isWindows ? ['win32 parallelization disabled'] : paths.map(p => {
       return stripSlashes(normPath(join(p)))
         .normalize('NFKD')
+        .toLocaleLowerCase('en')
+        .toLocaleUpperCase('en')
         .toLowerCase()
     })
 
--- a/lib/unpack.js	2026-06-01 18:29:34.507700902 +0530
+++ b/lib/unpack.js	2026-06-01 18:29:34.513553648 +0530
@@ -108,6 +108,8 @@
 // to reason about, and collisions are hazards rather than just failures.
 const cacheKeyNormalize = path => stripSlash(normPath(path))
   .normalize('NFKD')
+  .toLocaleLowerCase('en')
+  .toLocaleUpperCase('en')
   .toLowerCase()
 
 const pruneCache = (cache, abs) => {

--- a/lib/unpack.js	2026-06-01 18:29:34.513553648 +0530
+++ b/lib/unpack.js	2026-06-01 18:29:34.519959561 +0530
@@ -235,6 +235,15 @@
     const p = entry[field]
     if (!p || this.preservePaths) return true
 
+    // For hardlinks, also check linkpath for '..' (CVE-2026-24842)
+    if (field === 'linkpath' && entry.type === 'Link' && entry.linkpath) {
+      const linkParts = normPath(entry.linkpath).split('/')
+      if (linkParts.includes('..') || isWindows && /^[a-z]:\.\.$/i.test(linkParts[0])) {
+        this.warn(`linkpath contains '..'`, entry.linkpath)
+        return false
+      }
+    }
+
     const parts = normPath(p).split('/')
     if (parts.includes('..') || isWindows && /^[a-z]:\.\.$/i.test(parts[0])) {
       this.warn(`${field} contains '..'`, p)

--- a/lib/unpack.js	2026-06-01 18:29:34.519959561 +0530
+++ b/lib/unpack.js	2026-06-01 18:29:34.526682147 +0530
@@ -20,6 +20,19 @@
 const normPath = require('./normalize-windows-path.js')
 const stripSlash = require('./strip-trailing-slashes.js')
 
+// SymlinkError for ENSURE_NO_SYMLINK - prevents extraction through symlinks
+class SymlinkError extends Error {
+  constructor (symlink, path) {
+    super('TAR_SYMLINK_ERROR: Cannot extract through symbolic link')
+    this.path = path
+    this.symlink = symlink
+    this.code = 'TAR_SYMLINK_ERROR'
+  }
+  get name () {
+    return 'SymlinkError'
+  }
+}
+
 const ONENTRY = Symbol('onEntry')
 const CHECKFS = Symbol('checkFs')
 const CHECKFS2 = Symbol('checkFs2')
@@ -31,6 +44,7 @@
 const LINK = Symbol('link')
 const SYMLINK = Symbol('symlink')
 const HARDLINK = Symbol('hardlink')
+const ENSURE_NO_SYMLINK = Symbol('ensureNoSymlink')
 const UNSUPPORTED = Symbol('unsupported')
 const UNKNOWN = Symbol('unknown')
 const CHECKPATH = Symbol('checkPath')
@@ -522,12 +536,40 @@
   }
 
   [SYMLINK] (entry, done) {
-    this[LINK](entry, entry.linkpath, 'symlink', done)
+    const parts = normPath(
+      path.relative(
+        this.cwd,
+        path.resolve(
+          path.dirname(entry.absolute),
+          entry.linkpath,
+        ),
+      ),
+    ).split('/')
+    this[ENSURE_NO_SYMLINK](
+      entry,
+      this.cwd,
+      parts,
+      () => this[LINK](entry, entry.linkpath, 'symlink', done),
+      er => {
+        this[ONERROR](er, entry)
+        done()
+      },
+    )
   }
 
   [HARDLINK] (entry, done) {
     const linkpath = normPath(path.resolve(this.cwd, entry.linkpath))
-    this[LINK](entry, linkpath, 'link', done)
+    const parts = normPath(entry.linkpath).split('/')
+    this[ENSURE_NO_SYMLINK](
+      entry,
+      this.cwd,
+      parts,
+      () => this[LINK](entry, linkpath, 'link', done),
+      er => {
+        this[ONERROR](er, entry)
+        done()
+      },
+    )
   }
 
   [PEND] () {
@@ -689,6 +731,21 @@
     }
   }
 
+  [ENSURE_NO_SYMLINK] (entry, cwd, parts, done, onError) {
+    const p = parts.shift()
+    if (this.preservePaths || p === undefined) return done()
+    const t = path.resolve(cwd, p)
+    fs.lstat(t, (er, st) => {
+      if (er) return done()
+      if (st && st.isSymbolicLink()) {
+        return onError(
+          new SymlinkError(t, path.resolve(t, parts.join('/')))
+        )
+      }
+      this[ENSURE_NO_SYMLINK](entry, t, parts, done, onError)
+    })
+  }
+
   [LINK] (entry, linkpath, link, done) {
     // XXX: get the type ('symlink' or 'junction') for windows
     fs[link](linkpath, entry.absolute, er => {
@@ -877,6 +934,22 @@
     }
   }
 
+  [ENSURE_NO_SYMLINK] (_entry, cwd, parts, done, onError) {
+    if (this.preservePaths || !parts.length) return done()
+    let t = cwd
+    for (const p of parts) {
+      t = path.resolve(t, p)
+      const [er, st] = callSync(() => fs.lstatSync(t))
+      if (er) return done()
+      if (st.isSymbolicLink()) {
+        return onError(
+          new SymlinkError(t, path.resolve(cwd, parts.join('/')))
+        )
+      }
+    }
+    done()
+  }
+
   [LINK] (entry, linkpath, link, done) {
     try {
       fs[link + 'Sync'](linkpath, entry.absolute)

--- a/lib/unpack.js	2026-06-01 18:29:34.526682147 +0530
+++ b/lib/unpack.js	2026-06-01 18:29:34.532122349 +0530
@@ -258,17 +258,46 @@
       }
     }
 
-    const parts = normPath(p).split('/')
+    // strip off the root first, before checking for '..'
+    // prevents drive-relative paths like C:../foo from bypassing the check
+    const s = stripAbsolutePath(p)
+    if (s[0]) {
+      entry[field] = s[1]
+      this.warn(`stripping ${s[0]} from absolute ${field}`, p)
+    }
+    const parts = s[1].replace(/\\/g, '/').split('/')
     if (parts.includes('..') || isWindows && /^[a-z]:\.\.$/i.test(parts[0])) {
       this.warn(`${field} contains '..'`, p)
       return false
     }
 
-    // strip off the root
-    const s = stripAbsolutePath(p)
-    if (s[0]) {
-      entry[field] = s[1]
-      this.warn(`stripping ${s[0]} from absolute ${field}`, p)
+    // sanitize linkpath escape for symbolic links
+    if (
+      field === 'linkpath' &&
+      entry.linkpath &&
+      (entry.type === 'Link' || entry.type === 'SymbolicLink')
+    ) {
+      const lp = normPath(entry.linkpath)
+      const ls = stripAbsolutePath(lp)
+      if (ls[0]) {
+        entry.linkpath = ls[1]
+        this.warn(`stripping ${ls[0]} from absolute linkpath`, lp)
+      }
+      const linkparts = ls[1].replace(/\\/g, '/').split('/')
+      if (linkparts.includes('..') || isWindows && /^[a-z]:\.\.$/i.test(linkparts[0])) {
+        if (entry.type === 'Link') {
+          this.warn(`linkpath contains '..'`, lp)
+          return false
+        }
+        const entryDir = path.posix.dirname(entry.path)
+        const resolved = path.posix.normalize(
+          path.posix.join(entryDir, entry.linkpath)
+        )
+        if (resolved.startsWith('../') || resolved === '..') {
+          this.warn(`linkpath escapes extraction directory`, lp)
+          return false
+        }
+      }
     }
     return true
   }

--- a/lib/unpack.js	2026-06-01 18:29:34.532122349 +0530
+++ b/lib/unpack.js	2026-06-01 18:29:34.534878497 +0530
@@ -291,7 +291,7 @@
         }
         const entryDir = path.posix.dirname(entry.path)
         const resolved = path.posix.normalize(
-          path.posix.join(entryDir, entry.linkpath)
+          path.posix.join(entryDir, linkparts.join('/'))
         )
         if (resolved.startsWith('../') || resolved === '..') {
           this.warn(`linkpath escapes extraction directory`, lp)

diff --git a/lib/header.js b/lib/header.js
index d29c3b9..71c7a4d 100644
--- a/lib/header.js
+++ b/lib/header.js
@@ -56,13 +56,28 @@ class Header {
     this.mtime = decDate(buf, off + 136, 12)
     this.cksum = decNumber(buf, off + 148, 12)
 
+    // Decode the typeflag up front (independent of any pending PAX/GNU
+    // extended header) so we can tell whether THIS block is itself an
+    // intermediary extension header (PAX `x`/`g`, GNU long-name `L`, GNU
+    // long-link `K`). Per POSIX pax, a PAX extended header describes the
+    // *next file entry*, not the extension headers that may sit between it
+    // and that file. Applying the pending PAX overrides (notably `size`) to
+    // an intervening L/K/x/g header desynchronizes the stream relative to
+    // other tar implementations and enables tar interpretation-conflict /
+    // file-smuggling attacks (CWE-436, GHSA-vmf3-w455-68vh).
+    this[TYPE] = decString(buf, off + 156, 1)
+    const isNormalFS = types.normalFsTypes.has(this[TYPE])
+
     // if we have extended or global extended headers, apply them now
     // See https://github.com/npm/node-tar/pull/187
-    this[SLURP](ex)
-    this[SLURP](gex, true)
+    // Only slurp ex/gex onto known filesystem entry types; never onto
+    // intermediary extension headers.
+    if (isNormalFS) {
+      this[SLURP](ex)
+      this[SLURP](gex, true)
+    }
 
     // old tar versions marked dirs as a file with a trailing /
-    this[TYPE] = decString(buf, off + 156, 1)
     if (this[TYPE] === '')
       this[TYPE] = '0'
     if (this[TYPE] === '0' && this.path.substr(-1) === '/')
diff --git a/lib/types.js b/lib/types.js
index df42565..96875b9 100644
--- a/lib/types.js
+++ b/lib/types.js
@@ -42,3 +42,11 @@ exports.name = new Map([
 
 // map the other direction
 exports.code = new Map(Array.from(exports.name).map(kv => [kv[1], kv[0]]))
+
+// types that are a normal file system entry, not metadata.
+// These can be the subject of extended/globalExtended headers, long path
+// names, long linkpath names, etc.
+// Any other types are meta, and cannot be targeted by extended PAX headers.
+exports.normalFsTypes = new Set([
+  '0', '', '1', '2', '3', '4', '5', '6', '7', 'D'
+])
