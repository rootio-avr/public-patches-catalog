--- a/dist/immutable.es.js
+++ b/dist/immutable.es.js
@@ -2207,6 +2207,7 @@
     }
     var to = {};
     for (var key in from) {
+        if (key === '__proto__' || key === 'constructor') { continue; }
         if (hasOwnProperty.call(from, key)) {
             to[key] = from[key];
         }
@@ -2271,6 +2272,7 @@
         merged.push(value);
       }
     : function (value, key) {
+        if (key === '__proto__' || key === 'constructor') { return; }
         var hasVal = hasOwnProperty.call(merged, key);
         var nextVal =
           hasVal && merger ? merger(merged[key], value, key) : value;
@@ -3258,6 +3260,7 @@
 }
 
 function set(collection, key, value) {
+    if (typeof key === 'string' && (key === '__proto__' || key === 'constructor')) { return collection; }
     if (!isDataStructure(collection)) {
         throw new TypeError('Cannot update non-data-structure value: ' + collection);
     }
@@ -4974,6 +4977,7 @@
   assertNotInfinite(this.size);
   var object = {};
   this.__iterate(function (v, k) {
+    if (k === '__proto__' || k === 'constructor') { return; }
     object[k] = v;
   });
   return object;
@@ -4994,6 +4998,7 @@
         var result$1 = {};
         // @ts-expect-error `__iterate` exists on all Keyed collections but method is not defined in the type
         value.__iterate(function (v, k) {
+            if (k === '__proto__' || k === 'constructor') { return; }
             result$1[k] = toJS(v);
         });
         return result$1;
--- a/dist/immutable.js
+++ b/dist/immutable.js
@@ -2213,6 +2213,7 @@
         }
         var to = {};
         for (var key in from) {
+            if (key === '__proto__' || key === 'constructor') { continue; }
             if (hasOwnProperty.call(from, key)) {
                 to[key] = from[key];
             }
@@ -2277,6 +2278,7 @@
             merged.push(value);
           }
         : function (value, key) {
+            if (key === '__proto__' || key === 'constructor') { return; }
             var hasVal = hasOwnProperty.call(merged, key);
             var nextVal =
               hasVal && merger ? merger(merged[key], value, key) : value;
@@ -3264,6 +3266,7 @@
     }
 
     function set(collection, key, value) {
+        if (typeof key === 'string' && (key === '__proto__' || key === 'constructor')) { return collection; }
         if (!isDataStructure(collection)) {
             throw new TypeError('Cannot update non-data-structure value: ' + collection);
         }
@@ -4980,6 +4983,7 @@
       assertNotInfinite(this.size);
       var object = {};
       this.__iterate(function (v, k) {
+        if (k === '__proto__' || k === 'constructor') { return; }
         object[k] = v;
       });
       return object;
@@ -5000,6 +5004,7 @@
             var result$1 = {};
             // @ts-expect-error `__iterate` exists on all Keyed collections but method is not defined in the type
             value.__iterate(function (v, k) {
+                if (k === '__proto__' || k === 'constructor') { return; }
                 result$1[k] = toJS(v);
             });
             return result$1;
