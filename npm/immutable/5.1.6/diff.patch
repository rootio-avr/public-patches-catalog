--- a/dist/immutable.js
+++ b/dist/immutable.js
@@ -3894,7 +3894,35 @@
       }
     }
 
+    function validateListBoundsRequest(list, begin, end) {
+      var requestedOrigin = list._origin + (begin === undefined ? 0 : begin);
+      var requestedCapacity =
+        end === undefined
+          ? list._capacity
+          : end < 0
+            ? list._capacity + end
+            : list._origin + end;
+
+      // Keep origin/capacity within the trie's safe signed 32-bit range.
+      if (
+        (Number.isFinite(requestedCapacity) && requestedCapacity > MAX_LIST_SIZE) ||
+        (Number.isFinite(requestedOrigin) && requestedOrigin < -MAX_LIST_SIZE) ||
+        (Number.isFinite(requestedCapacity) &&
+          Number.isFinite(requestedOrigin) &&
+          requestedCapacity - requestedOrigin > MAX_LIST_SIZE)
+      ) {
+        throw new RangeError(
+          'Invalid List size: a List cannot hold more than ' +
+            MAX_LIST_SIZE +
+            ' (2 ** 30) values.'
+        );
+      }
+    }
+
     function setListBounds(list, begin, end) {
+      // Validate full-precision bounds before int32 coercion.
+      validateListBoundsRequest(list, begin, end);
+
       // Sanitize begin & end using this shorthand for ToInt32(argument)
       // http://www.ecma-international.org/ecma-262/6.0/#sec-toint32
       if (begin !== undefined) {
@@ -3933,7 +3961,8 @@
           owner
         );
         newLevel += SHIFT;
-        offsetShift += 1 << newLevel;
+        // Shift origin into non-negative space as trie height grows.
+        offsetShift += levelCapacity(newLevel);
       }
       if (offsetShift) {
         newOrigin += offsetShift;
@@ -3946,7 +3975,7 @@
       var newTailOffset = getTailOffset(newCapacity);
 
       // New size might need creating a higher root.
-      while (newTailOffset >= 1 << (newLevel + SHIFT)) {
+      while (newTailOffset >= levelCapacity(newLevel + SHIFT)) {
         newRoot = new VNode(
           newRoot && newRoot.array.length ? [newRoot] : [],
           owner
@@ -4042,6 +4071,18 @@
 
     function getTailOffset(size) {
       return size < SIZE ? 0 : ((size - 1) >>> SHIFT) << SHIFT;
+    }
+
+    // The largest number of values a List can hold. Above this the 32-bit
+    // trie math in setListBounds() would leave the safe signed 32-bit range.
+    var MAX_LIST_SIZE = 2 ** 30; // 1073741824
+
+    // Computes 2 ** exp for the trie level-raising loops in setListBounds().
+    // Use the cheap bitwise shift when safe, otherwise fall back to
+    // exponentiation, since JS bitwise operators only work on 32-bit signed
+    // integers and would overflow for exp >= 31.
+    function levelCapacity(exp) {
+      return exp < 31 ? 1 << exp : 2 ** exp;
     }
 
     /**
--- a/dist/immutable.es.js
+++ b/dist/immutable.es.js
@@ -3888,7 +3888,35 @@
   }
 }
 
+function validateListBoundsRequest(list, begin, end) {
+  var requestedOrigin = list._origin + (begin === undefined ? 0 : begin);
+  var requestedCapacity =
+    end === undefined
+      ? list._capacity
+      : end < 0
+        ? list._capacity + end
+        : list._origin + end;
+
+  // Keep origin/capacity within the trie's safe signed 32-bit range.
+  if (
+    (Number.isFinite(requestedCapacity) && requestedCapacity > MAX_LIST_SIZE) ||
+    (Number.isFinite(requestedOrigin) && requestedOrigin < -MAX_LIST_SIZE) ||
+    (Number.isFinite(requestedCapacity) &&
+      Number.isFinite(requestedOrigin) &&
+      requestedCapacity - requestedOrigin > MAX_LIST_SIZE)
+  ) {
+    throw new RangeError(
+      'Invalid List size: a List cannot hold more than ' +
+        MAX_LIST_SIZE +
+        ' (2 ** 30) values.'
+    );
+  }
+}
+
 function setListBounds(list, begin, end) {
+  // Validate full-precision bounds before int32 coercion.
+  validateListBoundsRequest(list, begin, end);
+
   // Sanitize begin & end using this shorthand for ToInt32(argument)
   // http://www.ecma-international.org/ecma-262/6.0/#sec-toint32
   if (begin !== undefined) {
@@ -3927,7 +3955,8 @@
       owner
     );
     newLevel += SHIFT;
-    offsetShift += 1 << newLevel;
+    // Shift origin into non-negative space as trie height grows.
+    offsetShift += levelCapacity(newLevel);
   }
   if (offsetShift) {
     newOrigin += offsetShift;
@@ -3940,7 +3969,7 @@
   var newTailOffset = getTailOffset(newCapacity);
 
   // New size might need creating a higher root.
-  while (newTailOffset >= 1 << (newLevel + SHIFT)) {
+  while (newTailOffset >= levelCapacity(newLevel + SHIFT)) {
     newRoot = new VNode(
       newRoot && newRoot.array.length ? [newRoot] : [],
       owner
@@ -4036,6 +4065,18 @@
 
 function getTailOffset(size) {
   return size < SIZE ? 0 : ((size - 1) >>> SHIFT) << SHIFT;
+}
+
+// The largest number of values a List can hold. Above this the 32-bit trie
+// math in setListBounds() would leave the safe signed 32-bit range.
+var MAX_LIST_SIZE = 2 ** 30; // 1073741824
+
+// Computes 2 ** exp for the trie level-raising loops in setListBounds().
+// Use the cheap bitwise shift when safe, otherwise fall back to
+// exponentiation, since JS bitwise operators only work on 32-bit signed
+// integers and would overflow for exp >= 31.
+function levelCapacity(exp) {
+  return exp < 31 ? 1 << exp : 2 ** exp;
 }
 
 /**
--- /dev/null
+++ b/test/cve-2026-59879.test.js
@@ -0,0 +1,49 @@
+/**
+ * Regression test for CVE-2026-59879.
+ *
+ * A List addresses its values through a 32-wide trie using signed 32-bit
+ * bitwise math. Sizes/indices at or beyond 2 ** 30 used to overflow that math:
+ * the level-raising loop in setListBounds() spun forever (hanging an empty
+ * List and OOM-crashing a populated one), and setSize silently wrapped large
+ * values. All of these must now throw a clear, catchable RangeError, while
+ * in-range operations keep working.
+ */
+const assert = require('node:assert');
+const { List, fromJS } = require('../dist/immutable.js');
+
+const tooBig = 2 ** 30;
+
+function arrayOfSize(s) {
+  return new Array(s).fill(0);
+}
+
+// Throws (does not hang) when setting a too-large index on an empty List.
+assert.throws(() => List().set(tooBig, 'x'), RangeError);
+
+// Throws (does not OOM-crash) when setting a too-large index on a populated List.
+assert.throws(() => List(arrayOfSize(64)).set(tooBig, 'x'), RangeError);
+
+// Throws for a numeric-string index coming through a setIn key path.
+assert.throws(
+  () => fromJS({ items: arrayOfSize(64) }).setIn(['items', '1073741824'], 'x'),
+  RangeError
+);
+
+// Throws on setSize beyond the max rather than silently truncating.
+assert.throws(() => List([1, 2, 3]).setSize(2 ** 31), RangeError);
+assert.throws(() => List([1, 2, 3]).setSize(2 ** 32 + 5), RangeError);
+
+// Still allows operations within the addressable range.
+assert.strictEqual(List([1, 2, 3]).setSize(1500).size, 1500);
+assert.strictEqual(List([1, 2, 3]).set(5, 'x').size, 6);
+assert.strictEqual(List([1, 2, 3]).setSize(tooBig).size, tooBig);
+
+// Handles a large in-range negative index without hanging: existing values
+// are shifted to the end of the grown List (exercises the 2 ** exp fallback).
+const result = List([1, 2, 3]).set(-(2 ** 29), 'x');
+assert.strictEqual(result.size, 2 ** 29);
+assert.strictEqual(result.get(result.size - 3), 1);
+assert.strictEqual(result.get(result.size - 2), 2);
+assert.strictEqual(result.get(result.size - 1), 3);
+
+console.log('CVE-2026-59879 regression test passed');
