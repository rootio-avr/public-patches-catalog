From 1fe30fc7b9c7732d3a60323486bad5f698cd11b6 Mon Sep 17 00:00:00 2001
Date: Thu, 26 Feb 2026 15:21:32 +0530
Subject: [PATCH] og

---
 dist/Client.js | 6 ++++++
 1 file changed, 6 insertions(+)

diff --git a/dist/Client.js b/dist/Client.js
index e0644f7..c777ca5 100644
--- a/dist/Client.js
+++ b/dist/Client.js
@@ -614,6 +614,12 @@ class Client {
     async _downloadFromWorkingDir(localDirPath) {
         await ensureLocalDirectory(localDirPath);
         for (const file of await this.list()) {
+            const hasInvalidName = !file.name || (0, path_1.basename)(file.name) !== file.name;
+            if (hasInvalidName) {
+                const safeName = JSON.stringify(file.name);
+                this.ftp.log(`Invalid filename from server listing, will skip file. (${safeName})`);
+                continue;
+            }
             const localPath = (0, path_1.join)(localDirPath, file.name);
             if (file.isDirectory) {
                 await this.cd(file.name);
-- 
2.43.0


--- a/dist/FtpContext.js
+++ b/dist/FtpContext.js
@@ -175,6 +175,10 @@
      * Send an FTP command without waiting for or handling the result.
      */
     send(command) {
+        // Reject control character injection attempts.
+        if (/[\r\n\0]/.test(command)) {
+            throw new Error(`Invalid command: Contains control characters. (${command})`);
+        }
         const containsPassword = command.startsWith("PASS");
         const message = containsPassword ? "> PASS ###" : `> ${command}`;
         this.log(message);

--- a/dist/StringWriter.js	2026-04-23 17:25:59.610151021 +0530
+++ dist/StringWriter.js	2026-04-23 17:22:46.279810454 +0530
@@ -3,14 +3,21 @@
 exports.StringWriter = void 0;
 const stream_1 = require("stream");
 class StringWriter extends stream_1.Writable {
-    constructor() {
-        super(...arguments);
+    constructor(maxByteLength = 40 * 1024 * 1024) {
+        super();
+        this.maxByteLength = maxByteLength;
         this.buf = Buffer.alloc(0);
     }
     _write(chunk, _, callback) {
         if (chunk instanceof Buffer) {
-            this.buf = Buffer.concat([this.buf, chunk]);
-            callback(null);
+            const newSize = this.buf.length + chunk.length;
+            if (newSize > this.maxByteLength) {
+                callback(new Error(`StringWriter exceeded maxByteLength of ${this.maxByteLength}.`));
+            }
+            else {
+                this.buf = Buffer.concat([this.buf, chunk]);
+                callback(null);
+            }
         }
         else {
             callback(new Error("StringWriter expects chunks of type 'Buffer'."));
--- a/dist/StringWriter.d.ts	2026-04-23 17:26:01.482141392 +0530
+++ dist/StringWriter.d.ts	2026-04-23 17:22:50.262761218 +0530
@@ -3,7 +3,9 @@
 import { Writable } from "stream";
 import { StringEncoding } from "./StringEncoding";
 export declare class StringWriter extends Writable {
+    readonly maxByteLength: number;
     protected buf: Buffer;
+    constructor(maxByteLength?: number);
     _write(chunk: Buffer | string | any, _: string, callback: (error: Error | null) => void): void;
     getText(encoding: StringEncoding): string;
 }
--- a/dist/Client.js	2026-04-23 17:26:10.139098264 +0530
+++ dist/Client.js	2026-04-23 17:22:57.932668400 +0530
@@ -33,6 +33,7 @@
      */
     constructor(timeout = 30000) {
         this.availableListCommands = LIST_COMMANDS_DEFAULT;
+        this.maxListingBytes = 40 * 1024 * 1024;
         this.ftp = new FtpContext_1.FTPContext(timeout);
         this.prepareTransfer = this._enterFirstCompatibleMode([transfer_1.enterPassiveModeIPv6, transfer_1.enterPassiveModeIPv4]);
         this.parseList = parseList_1.parseList;
@@ -508,7 +509,7 @@
      * @protected
      */
     async _requestListWithCommand(command) {
-        const buffer = new StringWriter_1.StringWriter();
+        const buffer = new StringWriter_1.StringWriter(this.maxListingBytes);
         await (0, transfer_1.downloadTo)(buffer, {
             ftp: this.ftp,
             tracker: this._progressTracker,
--- a/dist/Client.d.ts	2026-04-23 17:26:16.161069607 +0530
+++ dist/Client.d.ts	2026-04-23 17:23:02.868610050 +0530
@@ -37,6 +37,8 @@
     prepareTransfer: TransferStrategy;
     parseList: RawListParser;
     availableListCommands: readonly string[];
+    /** Maximum byte length of a directory listing. Defaults to 40 MB. */
+    maxListingBytes: number;
     /** Low-level API to interact with FTP server. */
     readonly ftp: FTPContext;
     /** Tracks progress of data transfers. */

--- a/dist/FtpContext.js
+++ b/dist/FtpContext.js
@@ -17,6 +17,8 @@ function doNothing() {
     /** Do nothing */
 }
+// Limit the accepted size of the control response.
+const maxControlResponseLength = 2 ** 16;
 /**
  * FTPContext holds the control and data sockets of an FTP connection and provides a
  * simplified way to interact with an FTP server, handle responses, errors and timeouts.
  *
@@ -271,8 +273,12 @@ class FTPContext {
     _onControlSocketData(chunk) {
         this.log(`< ${chunk}`);
         // This chunk might complete an earlier partial response.
+        if (this._partialResponse.length + chunk.length > maxControlResponseLength) {
+            this.closeWithError(new Error("FTP control response exceeded maximum allowed size"));
+            return;
+        }
         const completeResponse = this._partialResponse + chunk;
         const parsed = (0, parseControlResponse_1.parseControlResponse)(completeResponse);
         // Remember any incomplete remainder.
         this._partialResponse = parsed.rest;
         // Each response group is passed along individually.
