diff --git a/dist/server/lib/router-utils/resolve-routes.js b/dist/server/lib/router-utils/resolve-routes.js
index 992cf00d1..66a76ef31 100644
--- a/dist/server/lib/router-utils/resolve-routes.js
+++ b/dist/server/lib/router-utils/resolve-routes.js
@@ -169,6 +169,10 @@ function getResolveRoutes(fsChecker, config, opts, renderServer, renderServerOpt
                 }
                 curPathname = (curPathname == null ? void 0 : curPathname.substring(config.basePath.length)) || '/';
             }
+            const setIsNextDataRequest = ()=>{
+                (0, _requestmeta.addRequestMeta)(req, 'isNextDataReq', true);
+                req.headers['x-nextjs-data'] = '1';
+            };
             const localeResult = fsChecker.handleLocale(curPathname || '');
             for (const route of dynamicRoutes){
                 // when resolving fallback: false the
@@ -187,7 +191,7 @@ function getResolveRoutes(fsChecker, config, opts, renderServer, renderServerOpt
                         continue;
                     }
                     if (pageOutput && (curPathname == null ? void 0 : curPathname.startsWith('/_next/data'))) {
-                        (0, _requestmeta.addRequestMeta)(req, 'isNextDataReq', true);
+                        setIsNextDataRequest();
                     }
                     if (config.useFileSystemPublicRoutes || didRewrite) {
                         return pageOutput;
@@ -246,12 +250,16 @@ function getResolveRoutes(fsChecker, config, opts, renderServer, renderServerOpt
                         if (hadBasePath && normalizers.basePath) {
                             normalized = normalizers.basePath.normalize(normalized, true);
                         }
+                        const isNextDataPath = (0, _pathhasprefix.pathHasPrefix)(normalized, '/_next/data') && normalized.endsWith('.json');
+                        const hasCurrentBuildIdDataPath = normalizers.data.match(normalized);
                         let updated = false;
-                        if (normalizers.data.match(normalized)) {
+                        if (hasCurrentBuildIdDataPath) {
                             updated = true;
-                            (0, _requestmeta.addRequestMeta)(req, 'isNextDataReq', true);
                             normalized = normalizers.data.normalize(normalized, true);
                         }
+                        if (isNextDataPath) {
+                            setIsNextDataRequest();
+                        }
                         if (config.i18n) {
                             const curLocaleResult = (0, _normalizelocalepath.normalizeLocalePath)(normalized, config.i18n.locales);
                             if (curLocaleResult.detectedLocale) {
diff --git a/dist/esm/server/lib/router-utils/resolve-routes.js b/dist/esm/server/lib/router-utils/resolve-routes.js
index 5c06fb6ec..33544fdcb 100644
--- a/dist/esm/server/lib/router-utils/resolve-routes.js
+++ b/dist/esm/server/lib/router-utils/resolve-routes.js
@@ -154,6 +154,10 @@ export function getResolveRoutes(fsChecker, config, opts, renderServer, renderSe
                 }
                 curPathname = (curPathname == null ? void 0 : curPathname.substring(config.basePath.length)) || '/';
             }
+            const setIsNextDataRequest = ()=>{
+                addRequestMeta(req, 'isNextDataReq', true);
+                req.headers['x-nextjs-data'] = '1';
+            };
             const localeResult = fsChecker.handleLocale(curPathname || '');
             for (const route of dynamicRoutes){
                 // when resolving fallback: false the
@@ -172,7 +176,7 @@ export function getResolveRoutes(fsChecker, config, opts, renderServer, renderSe
                         continue;
                     }
                     if (pageOutput && (curPathname == null ? void 0 : curPathname.startsWith('/_next/data'))) {
-                        addRequestMeta(req, 'isNextDataReq', true);
+                        setIsNextDataRequest();
                     }
                     if (config.useFileSystemPublicRoutes || didRewrite) {
                         return pageOutput;
@@ -231,12 +235,16 @@ export function getResolveRoutes(fsChecker, config, opts, renderServer, renderSe
                         if (hadBasePath && normalizers.basePath) {
                             normalized = normalizers.basePath.normalize(normalized, true);
                         }
+                        const isNextDataPath = pathHasPrefix(normalized, '/_next/data') && normalized.endsWith('.json');
+                        const hasCurrentBuildIdDataPath = normalizers.data.match(normalized);
                         let updated = false;
-                        if (normalizers.data.match(normalized)) {
+                        if (hasCurrentBuildIdDataPath) {
                             updated = true;
-                            addRequestMeta(req, 'isNextDataReq', true);
                             normalized = normalizers.data.normalize(normalized, true);
                         }
+                        if (isNextDataPath) {
+                            setIsNextDataRequest();
+                        }
                         if (config.i18n) {
                             const curLocaleResult = normalizeLocalePath(normalized, config.i18n.locales);
                             if (curLocaleResult.detectedLocale) {
diff --git a/dist/server/lib/server-ipc/utils.js b/dist/server/lib/server-ipc/utils.js
index dc813d574..3e6048115 100644
--- a/dist/server/lib/server-ipc/utils.js
+++ b/dist/server/lib/server-ipc/utils.js
@@ -67,7 +67,9 @@ const INTERNAL_HEADERS = [
     'x-middleware-override-headers',
     'x-middleware-next',
     'x-now-route-matches',
-    'x-matched-path'
+    'x-matched-path',
+    'x-nextjs-data',
+    'x-next-resume-state-length'
 ];
 const filterInternalHeaders = (headers)=>{
     for(const header in headers){
diff --git a/dist/esm/server/lib/server-ipc/utils.js b/dist/esm/server/lib/server-ipc/utils.js
index 5e94a46dc..cd91098c6 100644
--- a/dist/esm/server/lib/server-ipc/utils.js
+++ b/dist/esm/server/lib/server-ipc/utils.js
@@ -37,7 +37,9 @@ const INTERNAL_HEADERS = [
     'x-middleware-override-headers',
     'x-middleware-next',
     'x-now-route-matches',
-    'x-matched-path'
+    'x-matched-path',
+    'x-nextjs-data',
+    'x-next-resume-state-length'
 ];
 export const filterInternalHeaders = (headers)=>{
     for(const header in headers){

diff --git a/dist/server/lib/router-utils/resolve-routes.js b/dist/server/lib/router-utils/resolve-routes.js
index 992cf00..15af0f1 100644
--- a/dist/server/lib/router-utils/resolve-routes.js
+++ b/dist/server/lib/router-utils/resolve-routes.js
@@ -430,6 +430,8 @@ function getResolveRoutes(fsChecker, config, opts, renderServer, renderServerOpt
                                 const curLocaleResult = (0, _normalizelocalepath.normalizeLocalePath)(parsedUrl.pathname || '', config.i18n.locales);
                                 if (curLocaleResult.detectedLocale) {
                                     (0, _requestmeta.addRequestMeta)(req, 'locale', curLocaleResult.detectedLocale);
+                                } else if (defaultLocale && !curLocaleResult.pathname.startsWith('/_next/')) {
+                                    normalized = (0, _apppathprefix.addPathPrefix)(curLocaleResult.pathname === '/' ? `/${defaultLocale}` : (0, _apppathprefix.addPathPrefix)(curLocaleResult.pathname || '', `/${defaultLocale}`));
                                 }
                             }
                         }

diff --git a/dist/server/next-server.js b/dist/server/next-server.js
index 3830e5a..04bbe90 100644
--- a/dist/server/next-server.js
+++ b/dist/server/next-server.js
@@ -329,6 +329,7 @@ class NextNodeServer extends _baseserver.default {
                 };
             }
             _routerservercontext.routerServerGlobal[_routerservercontext.RouterServerContextSymbol][relativeProjectDir].nextConfig = this.nextConfig;
+            _routerservercontext.routerServerGlobal[_routerservercontext.RouterServerContextSymbol][relativeProjectDir].isWrappedByNextServer = true;
             try {
                 var _this_i18nProvider;
                 // next.js core assumes page path without trailing slash
diff --git a/dist/server/route-modules/route-module.js b/dist/server/route-modules/route-module.js
index 37892f1..cb015b3 100644
--- a/dist/server/route-modules/route-module.js
+++ b/dist/server/route-modules/route-module.js
@@ -384,7 +384,11 @@ class RouteModule {
             }
         }
         serverUtils.normalizeCdnUrl(req, combinedParamKeys);
-        serverUtils.normalizeQueryParams(query, routeParamKeys);
+        if (!_routerservercontext.routerServerGlobal[_routerservercontext.RouterServerContextSymbol]?.[(0, _requestmeta.getRequestMeta)(req, 'relativeProjectDir') || this.relativeProjectDir]?.isWrappedByNextServer) {
+            serverUtils.normalizeQueryParams(query, routeParamKeys);
+        } else {
+            serverUtils.filterInternalQuery(query, []);
+        }
         serverUtils.filterInternalQuery(originalQuery, combinedParamKeys);
         if (pageIsDynamic) {
             const queryResult = serverUtils.normalizeDynamicRouteParams(query, true);

diff --git a/dist/build/analysis/get-page-static-info.js b/dist/build/analysis/get-page-static-info.js
index ab35696..386f2df 100644
--- a/dist/build/analysis/get-page-static-info.js
+++ b/dist/build/analysis/get-page-static-info.js
@@ -256,7 +256,13 @@ function getMiddlewareMatchers(matcherOrMatchers, nextConfig) {
         if ((i18n == null ? void 0 : i18n.locales) && matcher.locale !== false) {
             source = `/:nextInternalLocale((?!_next/)[^/.]{1,})${isRoot ? '' : source}`;
         }
-        source = `/:nextData(_next/data/[^/]{1,})?${source}${isRoot ? `(${nextConfig.i18n ? '|\\.json|' : ''}/?index|/?index\\.json)?` : '{(\\.json)}?'}`;
+        const APP_ROUTE_RSC_SUFFIX_MATCHER = '\\.rsc';
+        const APP_ROUTE_SEGMENT_PREFETCH_SUFFIX_MATCHER = '\\.segments\\/.+\\.segment\\.rsc';
+        const APP_ROUTE_TRANSPORT_SUFFIX_MATCHER = `${APP_ROUTE_RSC_SUFFIX_MATCHER}|${APP_ROUTE_SEGMENT_PREFETCH_SUFFIX_MATCHER}`;
+        const ROOT_APP_ROUTE_TRANSPORT_MATCHER = `/?index(?:${APP_ROUTE_TRANSPORT_SUFFIX_MATCHER})`;
+        const MIDDLEWARE_DATA_SUFFIX_MATCHER = `\\.json|${APP_ROUTE_TRANSPORT_SUFFIX_MATCHER}`;
+        const sourceSuffix = isRoot ? `(${nextConfig.i18n ? '|\\.json|' : ''}/?index|/?index\\.json|${ROOT_APP_ROUTE_TRANSPORT_MATCHER})?` : `{(${MIDDLEWARE_DATA_SUFFIX_MATCHER})}?`;
+        source = `/:nextData(_next/data/[^/]{1,})?${source}${sourceSuffix}`;
         if (nextConfig.basePath) {
             source = `${nextConfig.basePath}${source}`;
         }
diff --git a/dist/esm/build/analysis/get-page-static-info.js b/dist/esm/build/analysis/get-page-static-info.js
index 0cafe25..f40881e 100644
--- a/dist/esm/build/analysis/get-page-static-info.js
+++ b/dist/esm/build/analysis/get-page-static-info.js
@@ -183,7 +183,13 @@ async function tryToReadFile(filePath, shouldThrow) {
         if ((i18n == null ? void 0 : i18n.locales) && matcher.locale !== false) {
             source = `/:nextInternalLocale((?!_next/)[^/.]{1,})${isRoot ? '' : source}`;
         }
-        source = `/:nextData(_next/data/[^/]{1,})?${source}${isRoot ? `(${nextConfig.i18n ? '|\\.json|' : ''}/?index|/?index\\.json)?` : '{(\\.json)}?'}`;
+        const APP_ROUTE_RSC_SUFFIX_MATCHER = '\\.rsc';
+        const APP_ROUTE_SEGMENT_PREFETCH_SUFFIX_MATCHER = '\\.segments\\/.+\\.segment\\.rsc';
+        const APP_ROUTE_TRANSPORT_SUFFIX_MATCHER = `${APP_ROUTE_RSC_SUFFIX_MATCHER}|${APP_ROUTE_SEGMENT_PREFETCH_SUFFIX_MATCHER}`;
+        const ROOT_APP_ROUTE_TRANSPORT_MATCHER = `/?index(?:${APP_ROUTE_TRANSPORT_SUFFIX_MATCHER})`;
+        const MIDDLEWARE_DATA_SUFFIX_MATCHER = `\\.json|${APP_ROUTE_TRANSPORT_SUFFIX_MATCHER}`;
+        const sourceSuffix = isRoot ? `(${nextConfig.i18n ? '|\\.json|' : ''}/?index|/?index\\.json|${ROOT_APP_ROUTE_TRANSPORT_MATCHER})?` : `{(${MIDDLEWARE_DATA_SUFFIX_MATCHER})}?`;
+        source = `/:nextData(_next/data/[^/]{1,})?${source}${sourceSuffix}`;
         if (nextConfig.basePath) {
             source = `${nextConfig.basePath}${source}`;
         }

diff --git a/dist/client/components/router-reducer/fetch-server-response.js b/dist/client/components/router-reducer/fetch-server-response.js
index 653b8ce13..1b5e66b4c 100644
--- a/dist/client/components/router-reducer/fetch-server-response.js
+++ b/dist/client/components/router-reducer/fetch-server-response.js
@@ -186,7 +186,7 @@ async function createFetch(url, headers, fetchPriority, signal) {
     // search param to it. This should not leak outside of this function, so we
     // track them separately.
     let fetchUrl = new URL(url);
-    (0, _setcachebustingsearchparam.setCacheBustingSearchParam)(fetchUrl, headers);
+    await (0, _setcachebustingsearchparam.setCacheBustingSearchParam)(fetchUrl, headers);
     let browserResponse = await fetch(fetchUrl, fetchOptions);
     // If the server responds with a redirect (e.g. 307), and the redirected
     // location does not contain the cache busting search param set in the
@@ -229,7 +229,7 @@ async function createFetch(url, headers, fetchPriority, signal) {
             // Append the cache busting search param to the redirected URL and
             // fetch again.
             fetchUrl = new URL(responseUrl);
-            (0, _setcachebustingsearchparam.setCacheBustingSearchParam)(fetchUrl, headers);
+            await (0, _setcachebustingsearchparam.setCacheBustingSearchParam)(fetchUrl, headers);
             browserResponse = await fetch(fetchUrl, fetchOptions);
             // We just performed a manual redirect, so this is now true.
             redirected = true;
diff --git a/dist/esm/client/components/router-reducer/fetch-server-response.js b/dist/esm/client/components/router-reducer/fetch-server-response.js
index 366362f21..06c097629 100644
--- a/dist/esm/client/components/router-reducer/fetch-server-response.js
+++ b/dist/esm/client/components/router-reducer/fetch-server-response.js
@@ -165,7 +165,7 @@ export async function createFetch(url, headers, fetchPriority, signal) {
     // search param to it. This should not leak outside of this function, so we
     // track them separately.
     let fetchUrl = new URL(url);
-    setCacheBustingSearchParam(fetchUrl, headers);
+    await setCacheBustingSearchParam(fetchUrl, headers);
     let browserResponse = await fetch(fetchUrl, fetchOptions);
     // If the server responds with a redirect (e.g. 307), and the redirected
     // location does not contain the cache busting search param set in the
@@ -208,7 +208,7 @@ export async function createFetch(url, headers, fetchPriority, signal) {
             // Append the cache busting search param to the redirected URL and
             // fetch again.
             fetchUrl = new URL(responseUrl);
-            setCacheBustingSearchParam(fetchUrl, headers);
+            await setCacheBustingSearchParam(fetchUrl, headers);
             browserResponse = await fetch(fetchUrl, fetchOptions);
             // We just performed a manual redirect, so this is now true.
             redirected = true;
diff --git a/dist/client/components/router-reducer/set-cache-busting-search-param.js b/dist/client/components/router-reducer/set-cache-busting-search-param.js
index ba5151828..10f125387 100644
--- a/dist/client/components/router-reducer/set-cache-busting-search-param.js
+++ b/dist/client/components/router-reducer/set-cache-busting-search-param.js
@@ -23,8 +23,15 @@ _export(exports, {
 });
 const _cachebustingsearchparam = require("../../../shared/lib/router/utils/cache-busting-search-param");
 const _approuterheaders = require("../app-router-headers");
-const setCacheBustingSearchParam = (url, headers)=>{
-    const uniqueCacheKey = (0, _cachebustingsearchparam.computeCacheBustingSearchParam)(headers[_approuterheaders.NEXT_ROUTER_PREFETCH_HEADER], headers[_approuterheaders.NEXT_ROUTER_SEGMENT_PREFETCH_HEADER], headers[_approuterheaders.NEXT_ROUTER_STATE_TREE_HEADER], headers[_approuterheaders.NEXT_URL]);
+async function computeClientCacheBustingSearchParam(headers) {
+    var _globalThis_crypto_subtle, _globalThis_crypto;
+    if (typeof ((_globalThis_crypto = globalThis.crypto) == null ? void 0 : (_globalThis_crypto_subtle = _globalThis_crypto.subtle) == null ? void 0 : _globalThis_crypto_subtle.digest) === 'function') {
+        return (0, _cachebustingsearchparam.computeCacheBustingSearchParam)(headers[_approuterheaders.NEXT_ROUTER_PREFETCH_HEADER], headers[_approuterheaders.NEXT_ROUTER_SEGMENT_PREFETCH_HEADER], headers[_approuterheaders.NEXT_ROUTER_STATE_TREE_HEADER], headers[_approuterheaders.NEXT_URL]);
+    }
+    return (0, _cachebustingsearchparam.computeLegacyCacheBustingSearchParam)(headers[_approuterheaders.NEXT_ROUTER_PREFETCH_HEADER], headers[_approuterheaders.NEXT_ROUTER_SEGMENT_PREFETCH_HEADER], headers[_approuterheaders.NEXT_ROUTER_STATE_TREE_HEADER], headers[_approuterheaders.NEXT_URL]);
+}
+const setCacheBustingSearchParam = async (url, headers)=>{
+    const uniqueCacheKey = await computeClientCacheBustingSearchParam(headers);
     setCacheBustingSearchParamWithHash(url, uniqueCacheKey);
 };
 const setCacheBustingSearchParamWithHash = (url, hash)=>{
diff --git a/dist/esm/client/components/router-reducer/set-cache-busting-search-param.js b/dist/esm/client/components/router-reducer/set-cache-busting-search-param.js
index f66195574..1aae0f331 100644
--- a/dist/esm/client/components/router-reducer/set-cache-busting-search-param.js
+++ b/dist/esm/client/components/router-reducer/set-cache-busting-search-param.js
@@ -1,6 +1,13 @@
 'use client';
-import { computeCacheBustingSearchParam } from '../../../shared/lib/router/utils/cache-busting-search-param';
+import { computeCacheBustingSearchParam, computeLegacyCacheBustingSearchParam } from '../../../shared/lib/router/utils/cache-busting-search-param';
 import { NEXT_ROUTER_PREFETCH_HEADER, NEXT_ROUTER_SEGMENT_PREFETCH_HEADER, NEXT_ROUTER_STATE_TREE_HEADER, NEXT_URL, NEXT_RSC_UNION_QUERY } from '../app-router-headers';
+async function computeClientCacheBustingSearchParam(headers) {
+    var _globalThis_crypto_subtle, _globalThis_crypto;
+    if (typeof ((_globalThis_crypto = globalThis.crypto) == null ? void 0 : (_globalThis_crypto_subtle = _globalThis_crypto.subtle) == null ? void 0 : _globalThis_crypto_subtle.digest) === 'function') {
+        return computeCacheBustingSearchParam(headers[NEXT_ROUTER_PREFETCH_HEADER], headers[NEXT_ROUTER_SEGMENT_PREFETCH_HEADER], headers[NEXT_ROUTER_STATE_TREE_HEADER], headers[NEXT_URL]);
+    }
+    return computeLegacyCacheBustingSearchParam(headers[NEXT_ROUTER_PREFETCH_HEADER], headers[NEXT_ROUTER_SEGMENT_PREFETCH_HEADER], headers[NEXT_ROUTER_STATE_TREE_HEADER], headers[NEXT_URL]);
+}
 /**
  * Mutates the provided URL by adding a cache-busting search parameter for CDNs that don't
  * support custom headers. This helps avoid caching conflicts by making each request unique.
@@ -16,8 +23,8 @@ import { NEXT_ROUTER_PREFETCH_HEADER, NEXT_ROUTER_SEGMENT_PREFETCH_HEADER, NEXT_
  *
  * TODO: Since we need to use a search param anyway, we could simplify by removing the custom
  * headers approach entirely and just use search params.
- */ export const setCacheBustingSearchParam = (url, headers)=>{
-    const uniqueCacheKey = computeCacheBustingSearchParam(headers[NEXT_ROUTER_PREFETCH_HEADER], headers[NEXT_ROUTER_SEGMENT_PREFETCH_HEADER], headers[NEXT_ROUTER_STATE_TREE_HEADER], headers[NEXT_URL]);
+ */ export const setCacheBustingSearchParam = async (url, headers)=>{
+    const uniqueCacheKey = await computeClientCacheBustingSearchParam(headers);
     setCacheBustingSearchParamWithHash(url, uniqueCacheKey);
 };
 /**
diff --git a/dist/client/components/router-reducer/set-cache-busting-search-param.d.ts b/dist/client/components/router-reducer/set-cache-busting-search-param.d.ts
index 044aca9e0..c38a091a4 100644
--- a/dist/client/components/router-reducer/set-cache-busting-search-param.d.ts
+++ b/dist/client/components/router-reducer/set-cache-busting-search-param.d.ts
@@ -10,12 +10,13 @@ import type { RequestHeaders } from './fetch-server-response';
  * URL before: https://example.com/path?query=1
  * URL after: https://example.com/path?query=1&_rsc=abc123
  *
- * Note: This function mutates the input URL directly and does not return anything.
+ * Note: This function mutates the input URL directly and resolves once the
+ * cache-busting value has been computed and applied.
  *
  * TODO: Since we need to use a search param anyway, we could simplify by removing the custom
  * headers approach entirely and just use search params.
  */
-export declare const setCacheBustingSearchParam: (url: URL, headers: RequestHeaders) => void;
+export declare const setCacheBustingSearchParam: (url: URL, headers: RequestHeaders) => Promise<void>;
 /**
  * Sets a cache-busting search parameter on a URL using a provided hash value.
  *
@@ -27,7 +28,7 @@ export declare const setCacheBustingSearchParam: (url: URL, headers: RequestHead
  * hash: "abc123"
  * URL after: https://example.com/path?query=1&_rsc=abc123
  *
- * If the hash is null, we will set `_rsc` search param without a value.
+ * If the hash is empty, we will set `_rsc` search param without a value.
  * Like this: https://example.com/path?query=1&_rsc
  *
  * Note: This function mutates the input URL directly and does not return anything.
diff --git a/dist/server/app-render/action-handler.js b/dist/server/app-render/action-handler.js
index 2215e6384..883b4eb19 100644
--- a/dist/server/app-render/action-handler.js
+++ b/dist/server/app-render/action-handler.js
@@ -44,6 +44,7 @@ const _invarianterror = require("../../shared/lib/invariant-error");
 const _revalidationutils = require("../revalidation-utils");
 const _requestmeta = require("../request-meta");
 const _setcachebustingsearchparam = require("../../client/components/router-reducer/set-cache-busting-search-param");
+const _cachebustingsearchparam = require("../../shared/lib/router/utils/cache-busting-search-param");
 function _interop_require_default(obj) {
     return obj && obj.__esModule ? obj : {
         default: obj
@@ -242,12 +243,8 @@ async function createRedirectRenderResult(req, res, originalHost, redirectUrl, r
         forwardedHeaders.delete(_approuterheaders.ACTION_HEADER);
         try {
             var _response_headers_get;
-            (0, _setcachebustingsearchparam.setCacheBustingSearchParam)(fetchUrl, {
-                [_approuterheaders.NEXT_ROUTER_PREFETCH_HEADER]: forwardedHeaders.get(_approuterheaders.NEXT_ROUTER_PREFETCH_HEADER) ? '1' : undefined,
-                [_approuterheaders.NEXT_ROUTER_SEGMENT_PREFETCH_HEADER]: forwardedHeaders.get(_approuterheaders.NEXT_ROUTER_SEGMENT_PREFETCH_HEADER) ?? undefined,
-                [_approuterheaders.NEXT_ROUTER_STATE_TREE_HEADER]: forwardedHeaders.get(_approuterheaders.NEXT_ROUTER_STATE_TREE_HEADER) ?? undefined,
-                [_approuterheaders.NEXT_URL]: forwardedHeaders.get(_approuterheaders.NEXT_URL) ?? undefined
-            });
+            const cacheBustingSearchParam = await (0, _cachebustingsearchparam.computeCacheBustingSearchParam)(forwardedHeaders.get(_approuterheaders.NEXT_ROUTER_PREFETCH_HEADER) ? '1' : undefined, forwardedHeaders.get(_approuterheaders.NEXT_ROUTER_SEGMENT_PREFETCH_HEADER) ?? undefined, forwardedHeaders.get(_approuterheaders.NEXT_ROUTER_STATE_TREE_HEADER) ?? undefined, forwardedHeaders.get(_approuterheaders.NEXT_URL) ?? undefined);
+            (0, _setcachebustingsearchparam.setCacheBustingSearchParamWithHash)(fetchUrl, cacheBustingSearchParam);
             const response = await fetch(fetchUrl, {
                 method: 'GET',
                 headers: forwardedHeaders,
diff --git a/dist/esm/server/app-render/action-handler.js b/dist/esm/server/app-render/action-handler.js
index d6e2e647f..0c9122ea4 100644
--- a/dist/esm/server/app-render/action-handler.js
+++ b/dist/esm/server/app-render/action-handler.js
@@ -21,7 +21,8 @@ import { workUnitAsyncStorage } from '../app-render/work-unit-async-storage.exte
 import { InvariantError } from '../../shared/lib/invariant-error';
 import { executeRevalidates } from '../revalidation-utils';
 import { getRequestMeta } from '../request-meta';
-import { setCacheBustingSearchParam } from '../../client/components/router-reducer/set-cache-busting-search-param';
+import { setCacheBustingSearchParamWithHash } from '../../client/components/router-reducer/set-cache-busting-search-param';
+import { computeCacheBustingSearchParam } from '../../shared/lib/router/utils/cache-busting-search-param';
 /**
  * Checks if the app has any server actions defined in any runtime.
  */ function hasServerActions(manifest) {
@@ -215,12 +216,8 @@ async function createRedirectRenderResult(req, res, originalHost, redirectUrl, r
         forwardedHeaders.delete(ACTION_HEADER);
         try {
             var _response_headers_get;
-            setCacheBustingSearchParam(fetchUrl, {
-                [NEXT_ROUTER_PREFETCH_HEADER]: forwardedHeaders.get(NEXT_ROUTER_PREFETCH_HEADER) ? '1' : undefined,
-                [NEXT_ROUTER_SEGMENT_PREFETCH_HEADER]: forwardedHeaders.get(NEXT_ROUTER_SEGMENT_PREFETCH_HEADER) ?? undefined,
-                [NEXT_ROUTER_STATE_TREE_HEADER]: forwardedHeaders.get(NEXT_ROUTER_STATE_TREE_HEADER) ?? undefined,
-                [NEXT_URL]: forwardedHeaders.get(NEXT_URL) ?? undefined
-            });
+            const cacheBustingSearchParam = await computeCacheBustingSearchParam(forwardedHeaders.get(NEXT_ROUTER_PREFETCH_HEADER) ? '1' : undefined, forwardedHeaders.get(NEXT_ROUTER_SEGMENT_PREFETCH_HEADER) ?? undefined, forwardedHeaders.get(NEXT_ROUTER_STATE_TREE_HEADER) ?? undefined, forwardedHeaders.get(NEXT_URL) ?? undefined);
+            setCacheBustingSearchParamWithHash(fetchUrl, cacheBustingSearchParam);
             const response = await fetch(fetchUrl, {
                 method: 'GET',
                 headers: forwardedHeaders,
diff --git a/dist/server/base-server.js b/dist/server/base-server.js
index 74eef89a3..c2c95ab4a 100644
--- a/dist/server/base-server.js
+++ b/dist/server/base-server.js
@@ -1187,12 +1187,19 @@ class Server {
             // This should only happen for static prefetches, so we only handle those here.
             (0, _requestmeta.getRequestMeta)(req, 'isPrefetchRSCRequest') ? '1' : undefined;
             const segmentPrefetchRSCRequest = headers[_approuterheaders.NEXT_ROUTER_SEGMENT_PREFETCH_HEADER] || (0, _requestmeta.getRequestMeta)(req, 'segmentPrefetchRSCRequest');
-            const expectedHash = (0, _cachebustingsearchparam.computeCacheBustingSearchParam)(routerPrefetch, segmentPrefetchRSCRequest, headers[_approuterheaders.NEXT_ROUTER_STATE_TREE_HEADER], headers[_approuterheaders.NEXT_URL]);
+            const expectedHash = await (0, _cachebustingsearchparam.computeCacheBustingSearchParam)(routerPrefetch, segmentPrefetchRSCRequest, headers[_approuterheaders.NEXT_ROUTER_STATE_TREE_HEADER], headers[_approuterheaders.NEXT_URL]);
             const actualHash = (0, _requestmeta.getRequestMeta)(req, 'cacheBustingSearchParam') ?? new URL(req.url || '', 'http://localhost').searchParams.get(_approuterheaders.NEXT_RSC_UNION_QUERY);
-            if (expectedHash !== actualHash) {
+            let matchesHash = expectedHash === actualHash;
+            if (!matchesHash && actualHash !== null) {
+                // We'll fallback to checking the legacy hash format to support clients that do not have a secure context
+                matchesHash = (0, _cachebustingsearchparam.computeLegacyCacheBustingSearchParam)(routerPrefetch, segmentPrefetchRSCRequest, headers[_approuterheaders.NEXT_ROUTER_STATE_TREE_HEADER], headers[_approuterheaders.NEXT_URL]) === actualHash;
+            }
+            if (!matchesHash) {
                 // The hash sent by the client does not match the expected value.
                 // Redirect to the URL with the correct cache-busting search param.
                 // This prevents cache poisoning attacks on CDNs that don't respect Vary headers.
+                // We continue to accept the legacy short hash for clients that still
+                // generate the 5-character `_rsc` form.
                 // Note: When no headers are present, expectedHash is empty string and client
                 // must send `_rsc` param, otherwise actualHash is null and hash check fails.
                 const url = new URL(req.url || '', 'http://localhost');
diff --git a/dist/esm/server/base-server.js b/dist/esm/server/base-server.js
index e956ba836..a3150d19c 100644
--- a/dist/esm/server/base-server.js
+++ b/dist/esm/server/base-server.js
@@ -56,7 +56,7 @@ import { decodeQueryPathParameter } from './lib/decode-query-path-parameter';
 import { NoFallbackError } from '../shared/lib/no-fallback-error.external';
 import { getCacheHandlers } from './use-cache/handlers';
 import { fixMojibake } from './lib/fix-mojibake';
-import { computeCacheBustingSearchParam } from '../shared/lib/router/utils/cache-busting-search-param';
+import { computeCacheBustingSearchParam, computeLegacyCacheBustingSearchParam } from '../shared/lib/router/utils/cache-busting-search-param';
 import { setCacheBustingSearchParamWithHash } from '../client/components/router-reducer/set-cache-busting-search-param';
 // Internal wrapper around build errors at development
 // time, to prevent us from propagating or logging them
@@ -1122,12 +1122,19 @@ export default class Server {
             // This should only happen for static prefetches, so we only handle those here.
             getRequestMeta(req, 'isPrefetchRSCRequest') ? '1' : undefined;
             const segmentPrefetchRSCRequest = headers[NEXT_ROUTER_SEGMENT_PREFETCH_HEADER] || getRequestMeta(req, 'segmentPrefetchRSCRequest');
-            const expectedHash = computeCacheBustingSearchParam(routerPrefetch, segmentPrefetchRSCRequest, headers[NEXT_ROUTER_STATE_TREE_HEADER], headers[NEXT_URL]);
+            const expectedHash = await computeCacheBustingSearchParam(routerPrefetch, segmentPrefetchRSCRequest, headers[NEXT_ROUTER_STATE_TREE_HEADER], headers[NEXT_URL]);
             const actualHash = getRequestMeta(req, 'cacheBustingSearchParam') ?? new URL(req.url || '', 'http://localhost').searchParams.get(NEXT_RSC_UNION_QUERY);
-            if (expectedHash !== actualHash) {
+            let matchesHash = expectedHash === actualHash;
+            if (!matchesHash && actualHash !== null) {
+                // We'll fallback to checking the legacy hash format to support clients that do not have a secure context
+                matchesHash = computeLegacyCacheBustingSearchParam(routerPrefetch, segmentPrefetchRSCRequest, headers[NEXT_ROUTER_STATE_TREE_HEADER], headers[NEXT_URL]) === actualHash;
+            }
+            if (!matchesHash) {
                 // The hash sent by the client does not match the expected value.
                 // Redirect to the URL with the correct cache-busting search param.
                 // This prevents cache poisoning attacks on CDNs that don't respect Vary headers.
+                // We continue to accept the legacy short hash for clients that still
+                // generate the 5-character `_rsc` form.
                 // Note: When no headers are present, expectedHash is empty string and client
                 // must send `_rsc` param, otherwise actualHash is null and hash check fails.
                 const url = new URL(req.url || '', 'http://localhost');
diff --git a/dist/shared/lib/router/utils/cache-busting-search-param.js b/dist/shared/lib/router/utils/cache-busting-search-param.js
index 6ea90cd6c..384182d81 100644
--- a/dist/shared/lib/router/utils/cache-busting-search-param.js
+++ b/dist/shared/lib/router/utils/cache-busting-search-param.js
@@ -2,23 +2,68 @@
 Object.defineProperty(exports, "__esModule", {
     value: true
 });
-Object.defineProperty(exports, "computeCacheBustingSearchParam", {
-    enumerable: true,
-    get: function() {
+0 && (module.exports = {
+    computeCacheBustingSearchParam: null,
+    computeLegacyCacheBustingSearchParam: null
+});
+function _export(target, all) {
+    for(var name in all)Object.defineProperty(target, name, {
+        enumerable: true,
+        get: all[name]
+    });
+}
+_export(exports, {
+    computeCacheBustingSearchParam: function() {
         return computeCacheBustingSearchParam;
+    },
+    computeLegacyCacheBustingSearchParam: function() {
+        return computeLegacyCacheBustingSearchParam;
     }
 });
 const _hash = require("../../hash");
-function computeCacheBustingSearchParam(prefetchHeader, segmentPrefetchHeader, stateTreeHeader, nextUrlHeader) {
+const CACHE_BUSTING_SEARCH_PARAM_DIGEST_BYTES = 12;
+const textEncoder = new TextEncoder();
+function encodeCacheBustingSearchParam(bytes) {
+    let binary = '';
+    for(let i = 0; i < bytes.length; i++){
+        binary += String.fromCharCode(bytes[i]);
+    }
+    return btoa(binary).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
+}
+function normalizeCacheBustingInput(value) {
+    if (value === undefined) {
+        return '0';
+    }
+    return Array.isArray(value) ? value.join(',') : value;
+}
+function createCacheBustingSearchParamInput(prefetchHeader, segmentPrefetchHeader, stateTreeHeader, nextUrlHeader) {
     if ((prefetchHeader === undefined || prefetchHeader === '0') && segmentPrefetchHeader === undefined && stateTreeHeader === undefined && nextUrlHeader === undefined) {
+        return null;
+    }
+    return [
+        prefetchHeader ?? '0',
+        normalizeCacheBustingInput(segmentPrefetchHeader),
+        normalizeCacheBustingInput(stateTreeHeader),
+        normalizeCacheBustingInput(nextUrlHeader)
+    ].join(',');
+}
+async function computeCacheBustingSearchParamFromInput(input) {
+    const digest = await globalThis.crypto.subtle.digest('SHA-256', textEncoder.encode(input));
+    return encodeCacheBustingSearchParam(new Uint8Array(digest).subarray(0, CACHE_BUSTING_SEARCH_PARAM_DIGEST_BYTES));
+}
+async function computeCacheBustingSearchParam(prefetchHeader, segmentPrefetchHeader, stateTreeHeader, nextUrlHeader) {
+    const input = createCacheBustingSearchParamInput(prefetchHeader, segmentPrefetchHeader, stateTreeHeader, nextUrlHeader);
+    if (input === null) {
+        return '';
+    }
+    return computeCacheBustingSearchParamFromInput(input);
+}
+function computeLegacyCacheBustingSearchParam(prefetchHeader, segmentPrefetchHeader, stateTreeHeader, nextUrlHeader) {
+    const input = createCacheBustingSearchParamInput(prefetchHeader, segmentPrefetchHeader, stateTreeHeader, nextUrlHeader);
+    if (input === null) {
         return '';
     }
-    return (0, _hash.hexHash)([
-        prefetchHeader || '0',
-        segmentPrefetchHeader || '0',
-        stateTreeHeader || '0',
-        nextUrlHeader || '0'
-    ].join(','));
+    return (0, _hash.hexHash)(input);
 }
 
 //# sourceMappingURL=cache-busting-search-param.js.map
\ No newline at end of file
diff --git a/dist/esm/shared/lib/router/utils/cache-busting-search-param.js b/dist/esm/shared/lib/router/utils/cache-busting-search-param.js
index 36206721d..1bdea0220 100644
--- a/dist/esm/shared/lib/router/utils/cache-busting-search-param.js
+++ b/dist/esm/shared/lib/router/utils/cache-busting-search-param.js
@@ -1,14 +1,47 @@
 import { hexHash } from '../../hash';
-export function computeCacheBustingSearchParam(prefetchHeader, segmentPrefetchHeader, stateTreeHeader, nextUrlHeader) {
+const CACHE_BUSTING_SEARCH_PARAM_DIGEST_BYTES = 12;
+const textEncoder = new TextEncoder();
+function encodeCacheBustingSearchParam(bytes) {
+    let binary = '';
+    for(let i = 0; i < bytes.length; i++){
+        binary += String.fromCharCode(bytes[i]);
+    }
+    return btoa(binary).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
+}
+function normalizeCacheBustingInput(value) {
+    if (value === undefined) {
+        return '0';
+    }
+    return Array.isArray(value) ? value.join(',') : value;
+}
+function createCacheBustingSearchParamInput(prefetchHeader, segmentPrefetchHeader, stateTreeHeader, nextUrlHeader) {
     if ((prefetchHeader === undefined || prefetchHeader === '0') && segmentPrefetchHeader === undefined && stateTreeHeader === undefined && nextUrlHeader === undefined) {
+        return null;
+    }
+    return [
+        prefetchHeader ?? '0',
+        normalizeCacheBustingInput(segmentPrefetchHeader),
+        normalizeCacheBustingInput(stateTreeHeader),
+        normalizeCacheBustingInput(nextUrlHeader)
+    ].join(',');
+}
+async function computeCacheBustingSearchParamFromInput(input) {
+    const digest = await globalThis.crypto.subtle.digest('SHA-256', textEncoder.encode(input));
+    return encodeCacheBustingSearchParam(new Uint8Array(digest).subarray(0, CACHE_BUSTING_SEARCH_PARAM_DIGEST_BYTES));
+}
+export async function computeCacheBustingSearchParam(prefetchHeader, segmentPrefetchHeader, stateTreeHeader, nextUrlHeader) {
+    const input = createCacheBustingSearchParamInput(prefetchHeader, segmentPrefetchHeader, stateTreeHeader, nextUrlHeader);
+    if (input === null) {
+        return '';
+    }
+    return computeCacheBustingSearchParamFromInput(input);
+}
+export function computeLegacyCacheBustingSearchParam(prefetchHeader, segmentPrefetchHeader, stateTreeHeader, nextUrlHeader) {
+    const input = createCacheBustingSearchParamInput(prefetchHeader, segmentPrefetchHeader, stateTreeHeader, nextUrlHeader);
+    if (input === null) {
         return '';
     }
-    return hexHash([
-        prefetchHeader || '0',
-        segmentPrefetchHeader || '0',
-        stateTreeHeader || '0',
-        nextUrlHeader || '0'
-    ].join(','));
+    return hexHash(input);
 }
 
 //# sourceMappingURL=cache-busting-search-param.js.map
\ No newline at end of file
diff --git a/dist/shared/lib/router/utils/cache-busting-search-param.d.ts b/dist/shared/lib/router/utils/cache-busting-search-param.d.ts
index a3953a2d0..1417882a9 100644
--- a/dist/shared/lib/router/utils/cache-busting-search-param.d.ts
+++ b/dist/shared/lib/router/utils/cache-busting-search-param.d.ts
@@ -1 +1,2 @@
-export declare function computeCacheBustingSearchParam(prefetchHeader: '1' | '2' | '0' | undefined, segmentPrefetchHeader: string | string[] | undefined, stateTreeHeader: string | string[] | undefined, nextUrlHeader: string | string[] | undefined): string;
+export declare function computeCacheBustingSearchParam(prefetchHeader: '1' | '2' | '0' | undefined, segmentPrefetchHeader: string | string[] | undefined, stateTreeHeader: string | string[] | undefined, nextUrlHeader: string | string[] | undefined): Promise<string>;
+export declare function computeLegacyCacheBustingSearchParam(prefetchHeader: '1' | '2' | '0' | undefined, segmentPrefetchHeader: string | string[] | undefined, stateTreeHeader: string | string[] | undefined, nextUrlHeader: string | string[] | undefined): string;

diff --git a/dist/server/image-optimizer.js b/dist/server/image-optimizer.js
index eaf442d33..b631800be 100644
--- a/dist/server/image-optimizer.js
+++ b/dist/server/image-optimizer.js
@@ -906,12 +906,13 @@ async function fetchExternalImage(href, maximumResponseBody) {
         etag
     };
 }
-async function fetchInternalImage(href, _req, _res, handleRequest) {
+async function fetchInternalImage(href, _req, _res, maximumResponseBody, handleRequest) {
     try {
         const mocked = (0, _mockrequest.createRequestResponseMocks)({
             url: href,
             method: _req.method || 'GET',
-            socket: _req.socket
+            socket: _req.socket,
+            maximumResponseBody
         });
         await handleRequest(mocked.req, mocked.res, _url.default.parse(href, true));
         await mocked.res.hasStreamed;
@@ -923,6 +924,14 @@ async function fetchInternalImage(href, _req, _res, handleRequest) {
                 configurable: true
             });
         }
+        if (mocked.res.buffers.length === 0) {
+            _log.error('internal image response is empty for', href);
+            throw Object.defineProperty(new ImageError(400, '"url" parameter is valid but internal response is invalid'), "__NEXT_ERROR_CODE", {
+                value: "E394",
+                enumerable: false,
+                configurable: true
+            });
+        }
         const buffer = Buffer.concat(mocked.res.buffers);
         const contentType = mocked.res.getHeader('Content-Type');
         const cacheControl = mocked.res.getHeader('Cache-Control');
@@ -934,6 +943,17 @@ async function fetchInternalImage(href, _req, _res, handleRequest) {
             etag
         };
     } catch (err) {
+        if (err instanceof ImageError) {
+            throw err;
+        }
+        if (err && typeof err === 'object' && 'code' in err && err.code === 'ERR_MAX_BODY_SIZE_EXCEEDED') {
+            _log.error('internal image response exceeded maximum size for', href);
+            throw Object.defineProperty(new ImageError(413, '"url" parameter is valid but internal response is invalid'), "__NEXT_ERROR_CODE", {
+                value: "E394",
+                enumerable: false,
+                configurable: true
+            });
+        }
         _log.error('upstream image response failed for', href, err);
         throw Object.defineProperty(new ImageError(500, '"url" parameter is valid but upstream response is invalid'), "__NEXT_ERROR_CODE", {
             value: "E394",
diff --git a/dist/esm/server/image-optimizer.js b/dist/esm/server/image-optimizer.js
index 9b99032be..83d1a180e 100644
--- a/dist/esm/server/image-optimizer.js
+++ b/dist/esm/server/image-optimizer.js
@@ -790,12 +790,13 @@ export async function fetchExternalImage(href, maximumResponseBody) {
         etag
     };
 }
-export async function fetchInternalImage(href, _req, _res, handleRequest) {
+export async function fetchInternalImage(href, _req, _res, maximumResponseBody, handleRequest) {
     try {
         const mocked = createRequestResponseMocks({
             url: href,
             method: _req.method || 'GET',
-            socket: _req.socket
+            socket: _req.socket,
+            maximumResponseBody
         });
         await handleRequest(mocked.req, mocked.res, nodeUrl.parse(href, true));
         await mocked.res.hasStreamed;
@@ -807,6 +808,14 @@ export async function fetchInternalImage(href, _req, _res, handleRequest) {
                 configurable: true
             });
         }
+        if (mocked.res.buffers.length === 0) {
+            Log.error('internal image response is empty for', href);
+            throw Object.defineProperty(new ImageError(400, '"url" parameter is valid but internal response is invalid'), "__NEXT_ERROR_CODE", {
+                value: "E394",
+                enumerable: false,
+                configurable: true
+            });
+        }
         const buffer = Buffer.concat(mocked.res.buffers);
         const contentType = mocked.res.getHeader('Content-Type');
         const cacheControl = mocked.res.getHeader('Cache-Control');
@@ -818,6 +827,17 @@ export async function fetchInternalImage(href, _req, _res, handleRequest) {
             etag
         };
     } catch (err) {
+        if (err instanceof ImageError) {
+            throw err;
+        }
+        if (err && typeof err === 'object' && 'code' in err && err.code === 'ERR_MAX_BODY_SIZE_EXCEEDED') {
+            Log.error('internal image response exceeded maximum size for', href);
+            throw Object.defineProperty(new ImageError(413, '"url" parameter is valid but internal response is invalid'), "__NEXT_ERROR_CODE", {
+                value: "E394",
+                enumerable: false,
+                configurable: true
+            });
+        }
         Log.error('upstream image response failed for', href, err);
         throw Object.defineProperty(new ImageError(500, '"url" parameter is valid but upstream response is invalid'), "__NEXT_ERROR_CODE", {
             value: "E394",
diff --git a/dist/server/lib/mock-request.js b/dist/server/lib/mock-request.js
index 4bee92ae8..0fb3f7164 100644
--- a/dist/server/lib/mock-request.js
+++ b/dist/server/lib/mock-request.js
@@ -147,10 +147,11 @@ class MockedResponse extends _stream.default.Writable {
    * A list of buffers that have been written to the response.
    *
    * @internal - used internally by Next.js
-   */ this.buffers = [];
+   */ this.buffers = [], this.totalSize = 0;
         this.statusCode = res.statusCode ?? 200;
         this.socket = res.socket ?? null;
         this.headers = res.headers ? (0, _utils.fromNodeOutgoingHttpHeaders)(res.headers) : new Headers();
+        this.maximumResponseBody = res.maximumResponseBody;
         this.headPromise = new Promise((resolve)=>{
             this.headPromiseResolve = resolve;
         });
@@ -195,10 +196,27 @@ class MockedResponse extends _stream.default.Writable {
         if (this.resWriter) {
             return this.resWriter(chunk);
         }
-        this.buffers.push(Buffer.isBuffer(chunk) ? chunk : Buffer.from(chunk));
+        const buffer = Buffer.isBuffer(chunk) ? chunk : Buffer.from(chunk);
+        if (this.maximumResponseBody !== undefined) {
+            this.totalSize += buffer.byteLength;
+            if (this.totalSize > this.maximumResponseBody) {
+                const error = Object.defineProperty(new Error(`Response body exceeded maximum size of ${this.maximumResponseBody} bytes`), "__NEXT_ERROR_CODE", {
+                    value: "E792",
+                    enumerable: false,
+                    configurable: true
+                });
+                error.code = 'ERR_MAX_BODY_SIZE_EXCEEDED';
+                this.destroy(error);
+                return true;
+            }
+        }
+        this.buffers.push(buffer);
         return true;
     }
     end() {
+        if (this.destroyed) {
+            return this;
+        }
         this.finished = true;
         return super.end(...arguments);
     }
@@ -216,7 +234,11 @@ class MockedResponse extends _stream.default.Writable {
         // invoked, the 'finish' event will not be emitted.
         //
         // https://nodejs.org/docs/latest-v16.x/api/stream.html#writable_writechunk-encoding-callback
-        callback();
+        if (this.destroyed && this.errored) {
+            callback(this.errored);
+        } else {
+            callback();
+        }
     }
     writeHead(statusCode, statusMessage, headers) {
         if (!headers && typeof statusMessage !== 'string') {
@@ -397,7 +419,7 @@ class MockedResponse extends _stream.default.Writable {
         });
     }
 }
-function createRequestResponseMocks({ url, headers = {}, method = 'GET', bodyReadable, resWriter, socket = null }) {
+function createRequestResponseMocks({ url, headers = {}, method = 'GET', bodyReadable, resWriter, socket = null, maximumResponseBody }) {
     return {
         req: new MockedRequest({
             url,
@@ -408,7 +430,8 @@ function createRequestResponseMocks({ url, headers = {}, method = 'GET', bodyRea
         }),
         res: new MockedResponse({
             socket,
-            resWriter
+            resWriter,
+            maximumResponseBody
         })
     };
 }
diff --git a/dist/esm/server/lib/mock-request.js b/dist/esm/server/lib/mock-request.js
index f3e728ecf..d340f68a8 100644
--- a/dist/esm/server/lib/mock-request.js
+++ b/dist/esm/server/lib/mock-request.js
@@ -116,10 +116,11 @@ export class MockedResponse extends Stream.Writable {
    * A list of buffers that have been written to the response.
    *
    * @internal - used internally by Next.js
-   */ this.buffers = [];
+   */ this.buffers = [], this.totalSize = 0;
         this.statusCode = res.statusCode ?? 200;
         this.socket = res.socket ?? null;
         this.headers = res.headers ? fromNodeOutgoingHttpHeaders(res.headers) : new Headers();
+        this.maximumResponseBody = res.maximumResponseBody;
         this.headPromise = new Promise((resolve)=>{
             this.headPromiseResolve = resolve;
         });
@@ -164,10 +165,27 @@ export class MockedResponse extends Stream.Writable {
         if (this.resWriter) {
             return this.resWriter(chunk);
         }
-        this.buffers.push(Buffer.isBuffer(chunk) ? chunk : Buffer.from(chunk));
+        const buffer = Buffer.isBuffer(chunk) ? chunk : Buffer.from(chunk);
+        if (this.maximumResponseBody !== undefined) {
+            this.totalSize += buffer.byteLength;
+            if (this.totalSize > this.maximumResponseBody) {
+                const error = Object.defineProperty(new Error(`Response body exceeded maximum size of ${this.maximumResponseBody} bytes`), "__NEXT_ERROR_CODE", {
+                    value: "E792",
+                    enumerable: false,
+                    configurable: true
+                });
+                error.code = 'ERR_MAX_BODY_SIZE_EXCEEDED';
+                this.destroy(error);
+                return true;
+            }
+        }
+        this.buffers.push(buffer);
         return true;
     }
     end() {
+        if (this.destroyed) {
+            return this;
+        }
         this.finished = true;
         return super.end(...arguments);
     }
@@ -185,7 +203,11 @@ export class MockedResponse extends Stream.Writable {
         // invoked, the 'finish' event will not be emitted.
         //
         // https://nodejs.org/docs/latest-v16.x/api/stream.html#writable_writechunk-encoding-callback
-        callback();
+        if (this.destroyed && this.errored) {
+            callback(this.errored);
+        } else {
+            callback();
+        }
     }
     writeHead(statusCode, statusMessage, headers) {
         if (!headers && typeof statusMessage !== 'string') {
@@ -366,7 +388,7 @@ export class MockedResponse extends Stream.Writable {
         });
     }
 }
-export function createRequestResponseMocks({ url, headers = {}, method = 'GET', bodyReadable, resWriter, socket = null }) {
+export function createRequestResponseMocks({ url, headers = {}, method = 'GET', bodyReadable, resWriter, socket = null, maximumResponseBody }) {
     return {
         req: new MockedRequest({
             url,
@@ -377,7 +399,8 @@ export function createRequestResponseMocks({ url, headers = {}, method = 'GET',
         }),
         res: new MockedResponse({
             socket,
-            resWriter
+            resWriter,
+            maximumResponseBody
         })
     };
 }
diff --git a/dist/server/next-server.js b/dist/server/next-server.js
index 3830e5a12..6c00d4c1d 100644
--- a/dist/server/next-server.js
+++ b/dist/server/next-server.js
@@ -832,7 +832,7 @@ class NextNodeServer extends _baseserver.default {
                 return;
             };
             const { isAbsolute, href } = paramsResult;
-            const imageUpstream = isAbsolute ? await fetchExternalImage(href, this.nextConfig.images.maximumResponseBody) : await fetchInternalImage(href, req.originalRequest, res.originalResponse, handleInternalReq);
+            const imageUpstream = isAbsolute ? await fetchExternalImage(href, this.nextConfig.images.maximumResponseBody) : await fetchInternalImage(href, req.originalRequest, res.originalResponse, this.nextConfig.images.maximumResponseBody, handleInternalReq);
             return imageOptimizer(imageUpstream, paramsResult, this.nextConfig, {
                 isDev: this.renderOpts.dev,
                 previousCacheEntry
diff --git a/dist/esm/server/next-server.js b/dist/esm/server/next-server.js
index a6228acf1..f6b08f848 100644
--- a/dist/esm/server/next-server.js
+++ b/dist/esm/server/next-server.js
@@ -763,7 +763,7 @@ export default class NextNodeServer extends BaseServer {
                 return;
             };
             const { isAbsolute, href } = paramsResult;
-            const imageUpstream = isAbsolute ? await fetchExternalImage(href, this.nextConfig.images.maximumResponseBody) : await fetchInternalImage(href, req.originalRequest, res.originalResponse, handleInternalReq);
+            const imageUpstream = isAbsolute ? await fetchExternalImage(href, this.nextConfig.images.maximumResponseBody) : await fetchInternalImage(href, req.originalRequest, res.originalResponse, this.nextConfig.images.maximumResponseBody, handleInternalReq);
             return imageOptimizer(imageUpstream, paramsResult, this.nextConfig, {
                 isDev: this.renderOpts.dev,
                 previousCacheEntry
diff --git a/dist/server/image-optimizer.d.ts b/dist/server/image-optimizer.d.ts
index 619b05ae3..24ec69939 100644
--- a/dist/server/image-optimizer.d.ts
+++ b/dist/server/image-optimizer.d.ts
@@ -73,7 +73,7 @@ export declare function optimizeImage({ buffer, contentType, quality, width, hei
     timeoutInSeconds?: number;
 }): Promise<Buffer>;
 export declare function fetchExternalImage(href: string, maximumResponseBody: number): Promise<ImageUpstream>;
-export declare function fetchInternalImage(href: string, _req: IncomingMessage, _res: ServerResponse, handleRequest: (newReq: IncomingMessage, newRes: ServerResponse, newParsedUrl?: NextUrlWithParsedQuery) => Promise<void>): Promise<ImageUpstream>;
+export declare function fetchInternalImage(href: string, _req: IncomingMessage, _res: ServerResponse, maximumResponseBody: number, handleRequest: (newReq: IncomingMessage, newRes: ServerResponse, newParsedUrl?: NextUrlWithParsedQuery) => Promise<void>): Promise<ImageUpstream>;
 export declare function imageOptimizer(imageUpstream: ImageUpstream, paramsResult: Pick<ImageParamsResult, 'href' | 'width' | 'quality' | 'mimeType'>, nextConfig: {
     experimental: Pick<NextConfigComplete['experimental'], 'imgOptConcurrency' | 'imgOptMaxInputPixels' | 'imgOptSequentialRead' | 'imgOptSkipMetadata' | 'imgOptTimeoutInSeconds'>;
     images: Pick<NextConfigComplete['images'], 'dangerouslyAllowSVG' | 'minimumCacheTTL'>;
diff --git a/dist/server/lib/mock-request.d.ts b/dist/server/lib/mock-request.d.ts
index 29b09c343..153da5561 100644
--- a/dist/server/lib/mock-request.d.ts
+++ b/dist/server/lib/mock-request.d.ts
@@ -41,6 +41,7 @@ export interface MockedResponseOptions {
     socket?: Socket | null;
     headers?: OutgoingHttpHeaders;
     resWriter?: (chunk: Uint8Array | Buffer | string) => boolean;
+    maximumResponseBody?: number;
 }
 export declare class MockedResponse extends Stream.Writable implements ServerResponse {
     statusCode: number;
@@ -69,7 +70,7 @@ export declare class MockedResponse extends Stream.Writable implements ServerRes
      * @see https://github.com/nodejs/node/pull/7949
      */
     _implicitHeader(): void;
-    _write(chunk: Buffer | string, _encoding: string, callback: () => void): void;
+    _write(chunk: Buffer | string, _encoding: string, callback: (error?: Error | null) => void): void;
     writeHead(statusCode: number, statusMessage?: string | undefined, headers?: OutgoingHttpHeaders | OutgoingHttpHeader[] | undefined): this;
     writeHead(statusCode: number, headers?: OutgoingHttpHeaders | OutgoingHttpHeader[] | undefined): this;
     hasHeader(name: string): boolean;
@@ -102,8 +103,9 @@ interface RequestResponseMockerOptions {
     bodyReadable?: Stream.Readable;
     resWriter?: (chunk: Uint8Array | Buffer | string) => boolean;
     socket?: Socket | null;
+    maximumResponseBody?: number;
 }
-export declare function createRequestResponseMocks({ url, headers, method, bodyReadable, resWriter, socket, }: RequestResponseMockerOptions): {
+export declare function createRequestResponseMocks({ url, headers, method, bodyReadable, resWriter, socket, maximumResponseBody, }: RequestResponseMockerOptions): {
     req: MockedRequest;
     res: MockedResponse;
 };

diff --git a/dist/server/lib/router-server.js b/dist/server/lib/router-server.js
index 35ea234..a71e5a5 100644
--- a/dist/server/lib/router-server.js
+++ b/dist/server/lib/router-server.js
@@ -613,7 +613,7 @@ async function initialize(opts) {
                     });
                 }
             });
-            const { matchedOutput, parsedUrl } = await resolveRoutes({
+            const { finished, matchedOutput, parsedUrl, statusCode } = await resolveRoutes({
                 req,
                 res,
                 isUpgradeReq: true,
@@ -624,8 +624,11 @@ async function initialize(opts) {
             if (matchedOutput) {
                 return socket.end();
             }
-            if (parsedUrl.protocol) {
-                return await (0, _proxyrequest.proxyRequest)(req, socket, parsedUrl, head);
+            if (finished && parsedUrl.protocol) {
+                if (!statusCode) {
+                    return await (0, _proxyrequest.proxyRequest)(req, socket, parsedUrl, head);
+                }
+                return socket.end();
             }
         // If there's no matched output, we don't handle the request as user's
         // custom WS server may be listening on the same path.

diff --git a/dist/server/lib/server-ipc/utils.js b/dist/server/lib/server-ipc/utils.js
index 3e60481..bae5d92 100644
--- a/dist/server/lib/server-ipc/utils.js
+++ b/dist/server/lib/server-ipc/utils.js
@@ -69,7 +69,8 @@ const INTERNAL_HEADERS = [
     'x-now-route-matches',
     'x-matched-path',
     'x-nextjs-data',
-    'x-next-resume-state-length'
+    'x-next-resume-state-length',
+    'next-resume'
 ];
 const filterInternalHeaders = (headers)=>{
     for(const header in headers){

diff --git a/dist/client/script.js b/dist/client/script.js
index d54a4629d..0d9356a91 100644
--- a/dist/client/script.js
+++ b/dist/client/script.js
@@ -33,6 +33,7 @@ const _react = /*#__PURE__*/ _interop_require_wildcard._(require("react"));
 const _headmanagercontextsharedruntime = require("../shared/lib/head-manager-context.shared-runtime");
 const _setattributesfromprops = require("./set-attributes-from-props");
 const _requestidlecallback = require("./request-idle-callback");
+const _htmlescape = require("../server/htmlescape");
 const ScriptCache = new Map();
 const LoadCache = new Set();
 const insertStylesheets = (stylesheets)=>{
@@ -281,13 +282,13 @@ function initScriptLoader(scriptLoaderItems) {
                 return /*#__PURE__*/ (0, _jsxruntime.jsx)("script", {
                     nonce: nonce,
                     dangerouslySetInnerHTML: {
-                        __html: "(self.__next_s=self.__next_s||[]).push(" + JSON.stringify([
+                        __html: "(self.__next_s=self.__next_s||[]).push(" + (0, _htmlescape.htmlEscapeJsonString)(JSON.stringify([
                             0,
                             {
                                 ...restProps,
                                 id
                             }
-                        ]) + ")"
+                        ])) + ")"
                     }
                 });
             } else {
@@ -305,13 +306,13 @@ function initScriptLoader(scriptLoaderItems) {
                 return /*#__PURE__*/ (0, _jsxruntime.jsx)("script", {
                     nonce: nonce,
                     dangerouslySetInnerHTML: {
-                        __html: "(self.__next_s=self.__next_s||[]).push(" + JSON.stringify([
+                        __html: "(self.__next_s=self.__next_s||[]).push(" + (0, _htmlescape.htmlEscapeJsonString)(JSON.stringify([
                             src,
                             {
                                 ...restProps,
                                 id
                             }
-                        ]) + ")"
+                        ])) + ")"
                     }
                 });
             }
diff --git a/dist/esm/client/script.js b/dist/esm/client/script.js
index 1c67cc581..7ef649e72 100644
--- a/dist/esm/client/script.js
+++ b/dist/esm/client/script.js
@@ -5,6 +5,7 @@ import React, { useEffect, useContext, useRef } from 'react';
 import { HeadManagerContext } from '../shared/lib/head-manager-context.shared-runtime';
 import { setAttributesFromProps } from './set-attributes-from-props';
 import { requestIdleCallback } from './request-idle-callback';
+import { htmlEscapeJsonString } from '../server/htmlescape';
 const ScriptCache = new Map();
 const LoadCache = new Set();
 const insertStylesheets = (stylesheets)=>{
@@ -253,13 +254,13 @@ export function initScriptLoader(scriptLoaderItems) {
                 return /*#__PURE__*/ _jsx("script", {
                     nonce: nonce,
                     dangerouslySetInnerHTML: {
-                        __html: "(self.__next_s=self.__next_s||[]).push(" + JSON.stringify([
+                        __html: "(self.__next_s=self.__next_s||[]).push(" + htmlEscapeJsonString(JSON.stringify([
                             0,
                             {
                                 ...restProps,
                                 id
                             }
-                        ]) + ")"
+                        ])) + ")"
                     }
                 });
             } else {
@@ -277,13 +278,13 @@ export function initScriptLoader(scriptLoaderItems) {
                 return /*#__PURE__*/ _jsx("script", {
                     nonce: nonce,
                     dangerouslySetInnerHTML: {
-                        __html: "(self.__next_s=self.__next_s||[]).push(" + JSON.stringify([
+                        __html: "(self.__next_s=self.__next_s||[]).push(" + htmlEscapeJsonString(JSON.stringify([
                             src,
                             {
                                 ...restProps,
                                 id
                             }
-                        ]) + ")"
+                        ])) + ")"
                     }
                 });
             }

diff --git a/dist/server/htmlescape.js b/dist/server/htmlescape.js
index 95d8920ed..8fb3d5862 100644
--- a/dist/server/htmlescape.js
+++ b/dist/server/htmlescape.js
@@ -6,6 +6,7 @@ Object.defineProperty(exports, "__esModule", {
 });
 0 && (module.exports = {
     ESCAPE_REGEX: null,
+    htmlEscapeAttributeString: null,
     htmlEscapeJsonString: null
 });
 function _export(target, all) {
@@ -18,6 +19,9 @@ _export(exports, {
     ESCAPE_REGEX: function() {
         return ESCAPE_REGEX;
     },
+    htmlEscapeAttributeString: function() {
+        return htmlEscapeAttributeString;
+    },
     htmlEscapeJsonString: function() {
         return htmlEscapeJsonString;
     }
@@ -33,5 +37,16 @@ const ESCAPE_REGEX = /[&><\u2028\u2029]/g;
 function htmlEscapeJsonString(str) {
     return str.replace(ESCAPE_REGEX, (match)=>ESCAPE_LOOKUP[match]);
 }
+const ATTRIBUTE_ESCAPE_LOOKUP = {
+    '&': '&amp;',
+    '"': '&quot;',
+    "'": '&#39;',
+    '<': '&lt;',
+    '>': '&gt;'
+};
+const ATTRIBUTE_ESCAPE_REGEX = /[&"'<>]/g;
+function htmlEscapeAttributeString(str) {
+    return str.replace(ATTRIBUTE_ESCAPE_REGEX, (match)=>ATTRIBUTE_ESCAPE_LOOKUP[match]);
+}
 
 //# sourceMappingURL=htmlescape.js.map
\ No newline at end of file
diff --git a/dist/esm/server/htmlescape.js b/dist/esm/server/htmlescape.js
index 5fdb62560..ea20ec01b 100644
--- a/dist/esm/server/htmlescape.js
+++ b/dist/esm/server/htmlescape.js
@@ -11,5 +11,16 @@ export const ESCAPE_REGEX = /[&><\u2028\u2029]/g;
 export function htmlEscapeJsonString(str) {
     return str.replace(ESCAPE_REGEX, (match)=>ESCAPE_LOOKUP[match]);
 }
+const ATTRIBUTE_ESCAPE_LOOKUP = {
+    '&': '&amp;',
+    '"': '&quot;',
+    "'": '&#39;',
+    '<': '&lt;',
+    '>': '&gt;'
+};
+const ATTRIBUTE_ESCAPE_REGEX = /[&"'<>]/g;
+export function htmlEscapeAttributeString(str) {
+    return str.replace(ATTRIBUTE_ESCAPE_REGEX, (match)=>ATTRIBUTE_ESCAPE_LOOKUP[match]);
+}
 
 //# sourceMappingURL=htmlescape.js.map
\ No newline at end of file
diff --git a/dist/server/htmlescape.d.ts b/dist/server/htmlescape.d.ts
index b1c62d244..32122bd2f 100644
--- a/dist/server/htmlescape.d.ts
+++ b/dist/server/htmlescape.d.ts
@@ -1,2 +1,3 @@
 export declare const ESCAPE_REGEX: RegExp;
 export declare function htmlEscapeJsonString(str: string): string;
+export declare function htmlEscapeAttributeString(str: string): string;
diff --git a/dist/server/app-render/get-script-nonce-from-header.js b/dist/server/app-render/get-script-nonce-from-header.js
index a3113c790..36b35ea6d 100644
--- a/dist/server/app-render/get-script-nonce-from-header.js
+++ b/dist/server/app-render/get-script-nonce-from-header.js
@@ -8,9 +8,8 @@ Object.defineProperty(exports, "getScriptNonceFromHeader", {
         return getScriptNonceFromHeader;
     }
 });
-const _htmlescape = require("../htmlescape");
+const CSP_NONCE_SOURCE_REGEX = /^'nonce-([A-Za-z0-9+/_-]+={0,2})'$/;
 function getScriptNonceFromHeader(cspHeaderValue) {
-    var _directive_split_slice_map_find;
     const directives = cspHeaderValue// Directives are split by ';'.
     .split(';').map((directive)=>directive.trim());
     // First try to find the directive for the 'script-src', otherwise try to
@@ -20,25 +19,14 @@ function getScriptNonceFromHeader(cspHeaderValue) {
     if (!directive) {
         return;
     }
-    // Extract the nonce from the directive
-    const nonce = (_directive_split_slice_map_find = directive.split(' ')// Remove the 'strict-src'/'default-src' string, this can't be the nonce.
-    .slice(1).map((source)=>source.trim())// Find the first source with the 'nonce-' prefix.
-    .find((source)=>source.startsWith("'nonce-") && source.length > 8 && source.endsWith("'"))) == null ? void 0 : _directive_split_slice_map_find.slice(7, -1);
-    // If we could't find the nonce, then we're done.
-    if (!nonce) {
-        return;
-    }
-    // Don't accept the nonce value if it contains HTML escape characters.
-    // Technically, the spec requires a base64'd value, but this is just an
-    // extra layer.
-    if (_htmlescape.ESCAPE_REGEX.test(nonce)) {
-        throw Object.defineProperty(new Error('Nonce value from Content-Security-Policy contained HTML escape characters.\nLearn more: https://nextjs.org/docs/messages/nonce-contained-invalid-characters'), "__NEXT_ERROR_CODE", {
-            value: "E440",
-            enumerable: false,
-            configurable: true
-        });
+    // Extract the first valid nonce from the directive. Malformed nonces are
+    // ignored so the request can continue without a nonce instead of failing.
+    for (const source of directive.split(/\s+/).slice(1)){
+        const match = source.trim().match(CSP_NONCE_SOURCE_REGEX);
+        if (match) {
+            return match[1];
+        }
     }
-    return nonce;
 }
 
 //# sourceMappingURL=get-script-nonce-from-header.js.map
\ No newline at end of file
diff --git a/dist/esm/server/app-render/get-script-nonce-from-header.js b/dist/esm/server/app-render/get-script-nonce-from-header.js
index 6d6916271..e57d3025d 100644
--- a/dist/esm/server/app-render/get-script-nonce-from-header.js
+++ b/dist/esm/server/app-render/get-script-nonce-from-header.js
@@ -1,6 +1,5 @@
-import { ESCAPE_REGEX } from '../htmlescape';
+const CSP_NONCE_SOURCE_REGEX = /^'nonce-([A-Za-z0-9+/_-]+={0,2})'$/;
 export function getScriptNonceFromHeader(cspHeaderValue) {
-    var _directive_split_slice_map_find;
     const directives = cspHeaderValue// Directives are split by ';'.
     .split(';').map((directive)=>directive.trim());
     // First try to find the directive for the 'script-src', otherwise try to
@@ -10,25 +9,14 @@ export function getScriptNonceFromHeader(cspHeaderValue) {
     if (!directive) {
         return;
     }
-    // Extract the nonce from the directive
-    const nonce = (_directive_split_slice_map_find = directive.split(' ')// Remove the 'strict-src'/'default-src' string, this can't be the nonce.
-    .slice(1).map((source)=>source.trim())// Find the first source with the 'nonce-' prefix.
-    .find((source)=>source.startsWith("'nonce-") && source.length > 8 && source.endsWith("'"))) == null ? void 0 : _directive_split_slice_map_find.slice(7, -1);
-    // If we could't find the nonce, then we're done.
-    if (!nonce) {
-        return;
-    }
-    // Don't accept the nonce value if it contains HTML escape characters.
-    // Technically, the spec requires a base64'd value, but this is just an
-    // extra layer.
-    if (ESCAPE_REGEX.test(nonce)) {
-        throw Object.defineProperty(new Error('Nonce value from Content-Security-Policy contained HTML escape characters.\nLearn more: https://nextjs.org/docs/messages/nonce-contained-invalid-characters'), "__NEXT_ERROR_CODE", {
-            value: "E440",
-            enumerable: false,
-            configurable: true
-        });
+    // Extract the first valid nonce from the directive. Malformed nonces are
+    // ignored so the request can continue without a nonce instead of failing.
+    for (const source of directive.split(/\s+/).slice(1)){
+        const match = source.trim().match(CSP_NONCE_SOURCE_REGEX);
+        if (match) {
+            return match[1];
+        }
     }
-    return nonce;
 }
 
 //# sourceMappingURL=get-script-nonce-from-header.js.map
\ No newline at end of file
diff --git a/dist/server/app-render/metadata-insertion/create-server-inserted-metadata.js b/dist/server/app-render/metadata-insertion/create-server-inserted-metadata.js
index 737f30e30..383c32b88 100644
--- a/dist/server/app-render/metadata-insertion/create-server-inserted-metadata.js
+++ b/dist/server/app-render/metadata-insertion/create-server-inserted-metadata.js
@@ -12,6 +12,7 @@ Object.defineProperty(exports, "createServerInsertedMetadata", {
         return createServerInsertedMetadata;
     }
 });
+const _htmlescape = require("../../htmlescape");
 const REINSERT_ICON_SCRIPT = `\
 document.querySelectorAll('body link[rel="icon"], body link[rel="apple-touch-icon"]').forEach(el => document.head.appendChild(el))`;
 function createServerInsertedMetadata(nonce) {
@@ -21,7 +22,7 @@ function createServerInsertedMetadata(nonce) {
             return '';
         }
         inserted = true;
-        return `<script ${nonce ? `nonce="${nonce}"` : ''}>${REINSERT_ICON_SCRIPT}</script>`;
+        return `<script${nonce ? ` nonce="${(0, _htmlescape.htmlEscapeAttributeString)(nonce)}"` : ''}>${REINSERT_ICON_SCRIPT}</script>`;
     };
 }
 
diff --git a/dist/esm/server/app-render/metadata-insertion/create-server-inserted-metadata.js b/dist/esm/server/app-render/metadata-insertion/create-server-inserted-metadata.js
index 4e70580de..912b64275 100644
--- a/dist/esm/server/app-render/metadata-insertion/create-server-inserted-metadata.js
+++ b/dist/esm/server/app-render/metadata-insertion/create-server-inserted-metadata.js
@@ -2,7 +2,8 @@
  * For chromium based browsers (Chrome, Edge, etc.) and Safari,
  * icons need to stay under <head> to be picked up by the browser.
  *
- */ const REINSERT_ICON_SCRIPT = `\
+ */ import { htmlEscapeAttributeString } from '../../htmlescape';
+const REINSERT_ICON_SCRIPT = `\
 document.querySelectorAll('body link[rel="icon"], body link[rel="apple-touch-icon"]').forEach(el => document.head.appendChild(el))`;
 export function createServerInsertedMetadata(nonce) {
     let inserted = false;
@@ -11,7 +12,7 @@ export function createServerInsertedMetadata(nonce) {
             return '';
         }
         inserted = true;
-        return `<script ${nonce ? `nonce="${nonce}"` : ''}>${REINSERT_ICON_SCRIPT}</script>`;
+        return `<script${nonce ? ` nonce="${htmlEscapeAttributeString(nonce)}"` : ''}>${REINSERT_ICON_SCRIPT}</script>`;
     };
 }
 
diff --git a/dist/server/app-render/use-flight-response.js b/dist/server/app-render/use-flight-response.js
index 12dee2299..c90b68fce 100644
--- a/dist/server/app-render/use-flight-response.js
+++ b/dist/server/app-render/use-flight-response.js
@@ -83,7 +83,7 @@ function useFlightStream(flightStream, clientReferenceManifest, nonce) {
     return newResponse;
 }
 function createInlinedDataReadableStream(flightStream, nonce, formState) {
-    const startScriptTag = nonce ? `<script nonce=${JSON.stringify(nonce)}>` : '<script>';
+    const startScriptTag = nonce ? `<script nonce="${(0, _htmlescape.htmlEscapeAttributeString)(nonce)}">` : '<script>';
     const flightReader = flightStream.getReader();
     const decoder = new TextDecoder('utf-8', {
         fatal: true
diff --git a/dist/esm/server/app-render/use-flight-response.js b/dist/esm/server/app-render/use-flight-response.js
index 7e259cfa2..b00028739 100644
--- a/dist/esm/server/app-render/use-flight-response.js
+++ b/dist/esm/server/app-render/use-flight-response.js
@@ -1,4 +1,4 @@
-import { htmlEscapeJsonString } from '../htmlescape';
+import { htmlEscapeAttributeString, htmlEscapeJsonString } from '../htmlescape';
 import { workUnitAsyncStorage } from './work-unit-async-storage.external';
 import { InvariantError } from '../../shared/lib/invariant-error';
 const isEdgeRuntime = process.env.NEXT_RUNTIME === 'edge';
@@ -72,7 +72,7 @@ const findSourceMapURL = process.env.NODE_ENV !== 'production' ? require('../lib
  * @param formState optionally the formState used with this particular render
  * @returns a ReadableStream without the complete property. This signifies a lazy ReadableStream
  */ export function createInlinedDataReadableStream(flightStream, nonce, formState) {
-    const startScriptTag = nonce ? `<script nonce=${JSON.stringify(nonce)}>` : '<script>';
+    const startScriptTag = nonce ? `<script nonce="${htmlEscapeAttributeString(nonce)}">` : '<script>';
     const flightReader = flightStream.getReader();
     const decoder = new TextDecoder('utf-8', {
         fatal: true

diff --git a/dist/server/lib/is-rsc-request.js b/dist/server/lib/is-rsc-request.js
new file mode 100644
index 000000000..ea3235169
--- /dev/null
+++ b/dist/server/lib/is-rsc-request.js
@@ -0,0 +1,15 @@
+"use strict";
+Object.defineProperty(exports, "__esModule", {
+    value: true
+});
+Object.defineProperty(exports, "isRSCRequestHeader", {
+    enumerable: true,
+    get: function() {
+        return isRSCRequestHeader;
+    }
+});
+function isRSCRequestHeader(value) {
+    return value === '1';
+}
+
+//# sourceMappingURL=is-rsc-request.js.map
diff --git a/dist/build/templates/app-page.js b/dist/build/templates/app-page.js
index 269cddbeb..e69cf2b2c 100644
--- a/dist/build/templates/app-page.js
+++ b/dist/build/templates/app-page.js
@@ -47,6 +47,7 @@ const _interopdefault = require("../../server/app-render/interop-default");
 const _stripflightheaders = require("../../server/app-render/strip-flight-headers");
 const _node = require("../../server/base-http/node");
 const _ppr = require("../../server/lib/experimental/ppr");
+const _isrscrequest = require("../../server/lib/is-rsc-request");
 const _fallbackparams = require("../../server/request/fallback-params");
 const _encryptionutils = require("../../server/app-render/encryption-utils");
 const _streamingmetadata = require("../../server/lib/streaming-metadata");
@@ -186,7 +187,7 @@ async function handler(req, res, ctx) {
    */ const isPrefetchRSCRequest = (0, _requestmeta.getRequestMeta)(req, 'isPrefetchRSCRequest') ?? req.headers[_approuterheaders.NEXT_ROUTER_PREFETCH_HEADER] === '1' // exclude runtime prefetches, which use '2'
     ;
     // NOTE: Don't delete headers[RSC] yet, it still needs to be used in renderToHTML later
-    const isRSCRequest = (0, _requestmeta.getRequestMeta)(req, 'isRSCRequest') ?? Boolean(req.headers[_approuterheaders.RSC_HEADER]);
+    const isRSCRequest = (0, _requestmeta.getRequestMeta)(req, 'isRSCRequest') ?? (0, _isrscrequest.isRSCRequestHeader)(req.headers[_approuterheaders.RSC_HEADER]);
     const isPossibleServerAction = (0, _serveractionrequestmeta.getIsPossibleServerAction)(req);
     /**
    * If the route being rendered is an app page, and the ppr feature has been
diff --git a/dist/server/app-render/app-render.js b/dist/server/app-render/app-render.js
index 947676f10..815a6209e 100644
--- a/dist/server/app-render/app-render.js
+++ b/dist/server/app-render/app-render.js
@@ -17,6 +17,7 @@ const _internalutils = require("../internal-utils");
 const _approuterheaders = require("../../client/components/app-router-headers");
 const _metadatacontext = require("../../lib/metadata/metadata-context");
 const _requeststore = require("../async-storage/request-store");
+const _isrscrequest = require("../lib/is-rsc-request");
 const _workstore = require("../async-storage/work-store");
 const _httpaccessfallback = require("../../client/components/http-access-fallback/http-access-fallback");
 const _redirect = require("../../client/components/redirect");
@@ -99,7 +100,7 @@ function parseRequestHeaders(headers, options) {
     const isRuntimePrefetchRequest = headers[_approuterheaders.NEXT_ROUTER_PREFETCH_HEADER] === '2';
     const isHmrRefresh = headers[_approuterheaders.NEXT_HMR_REFRESH_HEADER] !== undefined;
     // dev warmup requests are treated as prefetch RSC requests
-    const isRSCRequest = isDevWarmupRequest || headers[_approuterheaders.RSC_HEADER] !== undefined;
+    const isRSCRequest = isDevWarmupRequest || (0, _isrscrequest.isRSCRequestHeader)(headers[_approuterheaders.RSC_HEADER]);
     const shouldProvideFlightRouterState = isRSCRequest && (!isPrefetchRequest || !options.isRoutePPREnabled);
     const flightRouterState = shouldProvideFlightRouterState ? (0, _parseandvalidateflightrouterstate.parseAndValidateFlightRouterState)(headers[_approuterheaders.NEXT_ROUTER_STATE_TREE_HEADER]) : undefined;
     // Checks if this is a prefetch of the Route Tree by the Segment Cache
diff --git a/dist/server/base-server.js b/dist/server/base-server.js
index c2c95ab4a..296b3baef 100644
--- a/dist/server/base-server.js
+++ b/dist/server/base-server.js
@@ -23,6 +23,7 @@ _export(exports, {
 const _utils = require("../shared/lib/utils");
 const _url = require("url");
 const _formathostname = require("./lib/format-hostname");
+const _isrscrequest = require("./lib/is-rsc-request");
 const _constants = require("../shared/lib/constants");
 const _utils1 = require("../shared/lib/router/utils");
 const _runtimeconfigexternal = require("../shared/lib/runtime-config.external");
@@ -176,7 +177,7 @@ class Server {
                 // request path for flight information.
                 (0, _stripflightheaders.stripFlightHeaders)(req.headers);
                 return false;
-            } else if (req.headers[_approuterheaders.RSC_HEADER] === '1') {
+            } else if ((0, _isrscrequest.isRSCRequestHeader)(req.headers[_approuterheaders.RSC_HEADER])) {
                 (0, _requestmeta.addRequestMeta)(req, 'isRSCRequest', true);
                 if (req.headers[_approuterheaders.NEXT_ROUTER_PREFETCH_HEADER] === '1') {
                     (0, _requestmeta.addRequestMeta)(req, 'isPrefetchRSCRequest', true);
diff --git a/dist/server/lib/router-utils/resolve-routes.js b/dist/server/lib/router-utils/resolve-routes.js
index a700f9107..84dd4b61a 100644
--- a/dist/server/lib/router-utils/resolve-routes.js
+++ b/dist/server/lib/router-utils/resolve-routes.js
@@ -29,6 +29,7 @@ const _removepathprefix = require("../../../shared/lib/router/utils/remove-path-
 const _nextdata = require("../../normalizers/request/next-data");
 const _basepath = require("../../normalizers/request/base-path");
 const _requestmeta = require("../../request-meta");
+const _isrscrequest = require("../is-rsc-request");
 const _preparedestination = require("../../../shared/lib/router/utils/prepare-destination");
 const _approuterheaders = require("../../../client/components/app-router-headers");
 const _computechangedpath = require("../../../client/components/router-reducer/compute-changed-path");
@@ -558,7 +559,7 @@ function getResolveRoutes(fsChecker, config, opts, renderServer, renderServerOpt
                         };
                     }
                     // Set the rewrite headers only if this is a RSC request.
-                    if (req.headers[_approuterheaders.RSC_HEADER] === '1') {
+                    if ((0, _isrscrequest.isRSCRequestHeader)(req.headers[_approuterheaders.RSC_HEADER])) {
                         // We set the rewritten path and query headers on the response now
                         // that we know that the it's not an external rewrite.
                         if (parsedUrl.pathname !== parsedDestination.pathname) {
diff --git a/dist/server/web/adapter.js b/dist/server/web/adapter.js
index 31925a0c2..877f9dc3f 100644
--- a/dist/server/web/adapter.js
+++ b/dist/server/web/adapter.js
@@ -41,6 +41,7 @@ const _webonclose = require("./web-on-close");
 const _getedgepreviewprops = require("./get-edge-preview-props");
 const _builtinrequestcontext = require("../after/builtin-request-context");
 const _implicittags = require("../lib/implicit-tags");
+const _isrscrequest = require("../lib/is-rsc-request");
 class NextRequestHint extends _request.NextRequest {
     constructor(params){
         super(params.input, params.init);
@@ -129,7 +130,7 @@ async function adapter(params) {
     }
     const requestHeaders = (0, _utils.fromNodeOutgoingHttpHeaders)(params.request.headers);
     const isNextDataRequest = requestHeaders.has('x-nextjs-data');
-    const isRSCRequest = requestHeaders.get(_approuterheaders.RSC_HEADER) === '1';
+    const isRSCRequest = (0, _isrscrequest.isRSCRequestHeader)(requestHeaders.get(_approuterheaders.RSC_HEADER));
     if (isNextDataRequest && requestURL.pathname === '/index') {
         requestURL.pathname = '/';
     }

diff --git a/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-client.browser.development.js b/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-client.browser.development.js
index 9bbc58c..44bc85a 100644
--- a/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-client.browser.development.js
+++ b/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-client.browser.development.js
@@ -534,7 +534,7 @@
             null === formData && (formData = new FormData());
             var _data3 = formData;
             key = nextPartId++;
-            var prefix = formFieldPrefix + key + "_";
+            var prefix = formFieldPrefix + "_" + key + "_";
             value.forEach(function (originalValue, originalKey) {
               _data3.append(prefix + originalKey, originalValue);
             });
diff --git a/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-client.browser.production.js b/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-client.browser.production.js
index 8bbb80a..dd5b7b4 100644
--- a/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-client.browser.production.js
+++ b/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-client.browser.production.js
@@ -334,7 +334,7 @@ function processReply(
         null === formData && (formData = new FormData());
         var data$31 = formData;
         key = nextPartId++;
-        var prefix = formFieldPrefix + key + "_";
+        var prefix = formFieldPrefix + "_" + key + "_";
         value.forEach(function (originalValue, originalKey) {
           data$31.append(prefix + originalKey, originalValue);
         });
diff --git a/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-client.edge.development.js b/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-client.edge.development.js
index c3a1d2b..f3e581f 100644
--- a/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-client.edge.development.js
+++ b/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-client.edge.development.js
@@ -559,7 +559,7 @@
             null === formData && (formData = new FormData());
             var _data3 = formData;
             key = nextPartId++;
-            var prefix = formFieldPrefix + key + "_";
+            var prefix = formFieldPrefix + "_" + key + "_";
             value.forEach(function (originalValue, originalKey) {
               _data3.append(prefix + originalKey, originalValue);
             });
diff --git a/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-client.edge.production.js b/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-client.edge.production.js
index 49b2a6a..0d8ed22 100644
--- a/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-client.edge.production.js
+++ b/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-client.edge.production.js
@@ -355,7 +355,7 @@ function processReply(
         null === formData && (formData = new FormData());
         var data$31 = formData;
         key = nextPartId++;
-        var prefix = formFieldPrefix + key + "_";
+        var prefix = formFieldPrefix + "_" + key + "_";
         value.forEach(function (originalValue, originalKey) {
           data$31.append(prefix + originalKey, originalValue);
         });
diff --git a/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-client.node.development.js b/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-client.node.development.js
index c243ceb..d5a8d68 100644
--- a/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-client.node.development.js
+++ b/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-client.node.development.js
@@ -559,7 +559,7 @@
             null === formData && (formData = new FormData());
             var _data3 = formData;
             key = nextPartId++;
-            var prefix = formFieldPrefix + key + "_";
+            var prefix = formFieldPrefix + "_" + key + "_";
             value.forEach(function (originalValue, originalKey) {
               _data3.append(prefix + originalKey, originalValue);
             });
diff --git a/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-client.node.production.js b/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-client.node.production.js
index c666445..bc8d37b 100644
--- a/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-client.node.production.js
+++ b/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-client.node.production.js
@@ -356,7 +356,7 @@ function processReply(
         null === formData && (formData = new FormData());
         var data$31 = formData;
         key = nextPartId++;
-        var prefix = formFieldPrefix + key + "_";
+        var prefix = formFieldPrefix + "_" + key + "_";
         value.forEach(function (originalValue, originalKey) {
           data$31.append(prefix + originalKey, originalValue);
         });
diff --git a/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-server.browser.development.js b/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-server.browser.development.js
index e8b58f7..987978d 100644
--- a/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-server.browser.development.js
+++ b/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-server.browser.development.js
@@ -4063,7 +4063,7 @@
       var chunks = response._chunks,
         chunk = chunks.get(id);
       chunk ||
-        ((chunk = response._formData.get(response._prefix + id)),
+        ((chunk = response._formData.data.get(response._prefix + id)),
         (chunk =
           "string" === typeof chunk
             ? new ReactPromise(
@@ -4175,6 +4175,10 @@
         case "fulfilled":
           id = chunk.value;
           chunk = chunk.reason;
+          if (null !== chunk && "error" in chunk)
+            throw Error(
+              "Expected an initialized chunk but got an initialized stream chunk instead. This payload may have been submitted by an older version of React."
+            );
           for (
             var localLength = 0,
               rootArrayContexts = response._rootArrayContexts,
@@ -4267,22 +4271,19 @@
       if (!isArrayImpl(model)) throw Error("Invalid Map initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Map.");
       model.$$consumed = !0;
-      response = new Map(model);
-      return response;
+      return new Map(model);
     }
     function createSet(response, model) {
       if (!isArrayImpl(model)) throw Error("Invalid Set initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Set.");
       model.$$consumed = !0;
-      response = new Set(model);
-      return response;
+      return new Set(model);
     }
     function extractIterator(response, model) {
       if (!isArrayImpl(model)) throw Error("Invalid Iterator initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Iterator.");
       model.$$consumed = !0;
-      response = model[Symbol.iterator]();
-      return response;
+      return model[Symbol.iterator]();
     }
     function createModel(response, model, parentObject, key) {
       return "then" === key && "function" === typeof model ? null : model;
@@ -4320,7 +4321,7 @@
           Error("Already initialized typed array.")
         )
       );
-      reference = response._formData.get(key).arrayBuffer();
+      reference = response._formData.data.get(key).arrayBuffer();
       if (initializingHandler) {
         var handler = initializingHandler;
         handler.deps++;
@@ -4364,7 +4365,7 @@
       var chunks = response._chunks;
       stream = new ReactPromise("fulfilled", stream, controller);
       chunks.set(id, stream);
-      response = response._formData.getAll(response._prefix + id);
+      response = response._formData.data.getAll(response._prefix + id);
       for (id = 0; id < response.length; id++)
         (chunks = response[id]),
           "string" === typeof chunks &&
@@ -4584,24 +4585,33 @@
               getOutlinedModel(response, arrayRoot, obj, key, null, createSet)
             );
           case "K":
-            obj = value.slice(2);
-            obj = response._prefix + obj + "_";
-            key = new FormData();
-            response = response._formData;
-            arrayRoot = Array.from(response.keys());
-            for (value = 0; value < arrayRoot.length; value++)
-              if (((reference = arrayRoot[value]), reference.startsWith(obj))) {
+            key = value.slice(2);
+            obj = response._prefix + "_";
+            key = obj + key + "_";
+            arrayRoot = new FormData();
+            for (response = response._formData; ; ) {
+              value = response;
+              reference = value.keys;
+              null === reference &&
+                ((reference = value.keys = Array.from(value.data.keys())),
+                (value.keyPointer = 0));
+              value = reference[value.keyPointer];
+              if (void 0 === value) break;
+              if (value.startsWith(key)) {
+                reference = response.data.getAll(value);
                 for (
-                  var entries = response.getAll(reference),
-                    newKey = reference.slice(obj.length),
-                    j = 0;
-                  j < entries.length;
-                  j++
+                  var referencedFormDataKey = value.slice(key.length), i = 0;
+                  i < reference.length;
+                  i++
                 )
-                  key.append(newKey, entries[j]);
-                response.delete(reference);
-              }
-            return key;
+                  arrayRoot.append(referencedFormDataKey, reference[i]);
+                reference = response;
+                reference.data.delete(value);
+                reference.keyPointer++;
+              } else if (value.startsWith(obj)) break;
+              else response.keyPointer++;
+            }
+            return arrayRoot;
           case "i":
             return (
               (arrayRoot = value.slice(2)),
@@ -4770,7 +4780,7 @@
           case "B":
             return (
               (obj = parseInt(value.slice(2), 16)),
-              response._formData.get(response._prefix + obj)
+              response._formData.data.get(response._prefix + obj)
             );
           case "R":
             return parseReadableStream(response, value, void 0);
@@ -4809,7 +4819,7 @@
       return {
         _bundlerConfig: bundlerConfig,
         _prefix: formFieldPrefix,
-        _formData: backingFormData,
+        _formData: { data: backingFormData, keyPointer: -1, keys: null },
         _chunks: chunks,
         _closed: !1,
         _closedReason: null,
diff --git a/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-server.browser.production.js b/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-server.browser.production.js
index c02b4f9..b7d6275 100644
--- a/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-server.browser.production.js
+++ b/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-server.browser.production.js
@@ -2575,7 +2575,7 @@ function getChunk(response, id) {
   var chunks = response._chunks,
     chunk = chunks.get(id);
   chunk ||
-    ((chunk = response._formData.get(response._prefix + id)),
+    ((chunk = response._formData.data.get(response._prefix + id)),
     (chunk =
       "string" === typeof chunk
         ? createResolvedModelChunk(response, chunk, id)
@@ -2676,6 +2676,10 @@ function getOutlinedModel(
     case "fulfilled":
       id = chunk.value;
       chunk = chunk.reason;
+      if (null !== chunk && "error" in chunk)
+        throw Error(
+          "Expected an initialized chunk but got an initialized stream chunk instead. This payload may have been submitted by an older version of React."
+        );
       for (
         var localLength = 0,
           rootArrayContexts = response._rootArrayContexts,
@@ -2759,22 +2763,19 @@ function createMap(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Map initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Map.");
   model.$$consumed = !0;
-  response = new Map(model);
-  return response;
+  return new Map(model);
 }
 function createSet(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Set initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Set.");
   model.$$consumed = !0;
-  response = new Set(model);
-  return response;
+  return new Set(model);
 }
 function extractIterator(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Iterator initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Iterator.");
   model.$$consumed = !0;
-  response = model[Symbol.iterator]();
-  return response;
+  return model[Symbol.iterator]();
 }
 function createModel(response, model, parentObject, key) {
   return "then" === key && "function" === typeof model ? null : model;
@@ -2812,7 +2813,7 @@ function parseTypedArray(
       Error("Already initialized typed array.")
     )
   );
-  reference = response._formData.get(key).arrayBuffer();
+  reference = response._formData.data.get(key).arrayBuffer();
   if (initializingHandler) {
     var handler = initializingHandler;
     handler.deps++;
@@ -2856,7 +2857,7 @@ function resolveStream(response, id, stream, controller) {
   var chunks = response._chunks;
   stream = new ReactPromise("fulfilled", stream, controller);
   chunks.set(id, stream);
-  response = response._formData.getAll(response._prefix + id);
+  response = response._formData.data.getAll(response._prefix + id);
   for (id = 0; id < response.length; id++)
     (chunks = response[id]),
       "string" === typeof chunks &&
@@ -3071,24 +3072,31 @@ function parseModelString(response, obj, key, value, reference, arrayRoot) {
           getOutlinedModel(response, arrayRoot, obj, key, null, createSet)
         );
       case "K":
-        obj = value.slice(2);
-        obj = response._prefix + obj + "_";
-        key = new FormData();
-        response = response._formData;
-        arrayRoot = Array.from(response.keys());
-        for (value = 0; value < arrayRoot.length; value++)
-          if (((reference = arrayRoot[value]), reference.startsWith(obj))) {
+        key = value.slice(2);
+        obj = response._prefix + "_";
+        key = obj + key + "_";
+        arrayRoot = new FormData();
+        for (response = response._formData; ; ) {
+          value = response.keys;
+          null === value &&
+            ((value = response.keys = Array.from(response.data.keys())),
+            (response.keyPointer = 0));
+          value = value[response.keyPointer];
+          if (void 0 === value) break;
+          if (value.startsWith(key)) {
+            reference = response.data.getAll(value);
             for (
-              var entries = response.getAll(reference),
-                newKey = reference.slice(obj.length),
-                j = 0;
-              j < entries.length;
-              j++
+              var referencedFormDataKey = value.slice(key.length), i = 0;
+              i < reference.length;
+              i++
             )
-              key.append(newKey, entries[j]);
-            response.delete(reference);
-          }
-        return key;
+              arrayRoot.append(referencedFormDataKey, reference[i]);
+            response.data.delete(value);
+            response.keyPointer++;
+          } else if (value.startsWith(obj)) break;
+          else response.keyPointer++;
+        }
+        return arrayRoot;
       case "i":
         return (
           (arrayRoot = value.slice(2)),
@@ -3247,7 +3255,7 @@ function parseModelString(response, obj, key, value, reference, arrayRoot) {
       case "B":
         return (
           (obj = parseInt(value.slice(2), 16)),
-          response._formData.get(response._prefix + obj)
+          response._formData.data.get(response._prefix + obj)
         );
       case "R":
         return parseReadableStream(response, value, void 0);
@@ -3275,7 +3283,7 @@ function createResponse(bundlerConfig, formFieldPrefix, temporaryReferences) {
   return {
     _bundlerConfig: bundlerConfig,
     _prefix: formFieldPrefix,
-    _formData: backingFormData,
+    _formData: { data: backingFormData, keyPointer: -1, keys: null },
     _chunks: chunks,
     _closed: !1,
     _closedReason: null,
diff --git a/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-server.edge.development.js b/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-server.edge.development.js
index 2307ac7..be8e22a 100644
--- a/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-server.edge.development.js
+++ b/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-server.edge.development.js
@@ -3837,6 +3837,14 @@
       if (hasOwnProperty.call(moduleExports, metadata[2]))
         return moduleExports[metadata[2]];
     }
+    function appendBackingEntry(backingStore, key, value) {
+      backingStore.data.append(key, value);
+      value = backingStore.keys;
+      null === value
+        ? ((backingStore.keys = Array.from(backingStore.data.keys())),
+          (backingStore.keyPointer = 0))
+        : value.push(key);
+    }
     function ReactPromise(status, value, reason) {
       this.status = status;
       this.value = value;
@@ -4149,7 +4157,7 @@
       var chunks = response._chunks,
         chunk = chunks.get(id);
       chunk ||
-        ((chunk = response._formData.get(response._prefix + id)),
+        ((chunk = response._formData.data.get(response._prefix + id)),
         (chunk =
           "string" === typeof chunk
             ? new ReactPromise(
@@ -4261,6 +4269,10 @@
         case "fulfilled":
           id = chunk.value;
           chunk = chunk.reason;
+          if (null !== chunk && "error" in chunk)
+            throw Error(
+              "Expected an initialized chunk but got an initialized stream chunk instead. This payload may have been submitted by an older version of React."
+            );
           for (
             var localLength = 0,
               rootArrayContexts = response._rootArrayContexts,
@@ -4353,22 +4365,19 @@
       if (!isArrayImpl(model)) throw Error("Invalid Map initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Map.");
       model.$$consumed = !0;
-      response = new Map(model);
-      return response;
+      return new Map(model);
     }
     function createSet(response, model) {
       if (!isArrayImpl(model)) throw Error("Invalid Set initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Set.");
       model.$$consumed = !0;
-      response = new Set(model);
-      return response;
+      return new Set(model);
     }
     function extractIterator(response, model) {
       if (!isArrayImpl(model)) throw Error("Invalid Iterator initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Iterator.");
       model.$$consumed = !0;
-      response = model[Symbol.iterator]();
-      return response;
+      return model[Symbol.iterator]();
     }
     function createModel(response, model, parentObject, key) {
       return "then" === key && "function" === typeof model ? null : model;
@@ -4406,7 +4415,7 @@
           Error("Already initialized typed array.")
         )
       );
-      reference = response._formData.get(key).arrayBuffer();
+      reference = response._formData.data.get(key).arrayBuffer();
       if (initializingHandler) {
         var handler = initializingHandler;
         handler.deps++;
@@ -4450,7 +4459,7 @@
       var chunks = response._chunks;
       stream = new ReactPromise("fulfilled", stream, controller);
       chunks.set(id, stream);
-      response = response._formData.getAll(response._prefix + id);
+      response = response._formData.data.getAll(response._prefix + id);
       for (id = 0; id < response.length; id++)
         (chunks = response[id]),
           "string" === typeof chunks &&
@@ -4670,24 +4679,33 @@
               getOutlinedModel(response, arrayRoot, obj, key, null, createSet)
             );
           case "K":
-            obj = value.slice(2);
-            obj = response._prefix + obj + "_";
-            key = new FormData();
-            response = response._formData;
-            arrayRoot = Array.from(response.keys());
-            for (value = 0; value < arrayRoot.length; value++)
-              if (((reference = arrayRoot[value]), reference.startsWith(obj))) {
+            key = value.slice(2);
+            obj = response._prefix + "_";
+            key = obj + key + "_";
+            arrayRoot = new FormData();
+            for (response = response._formData; ; ) {
+              value = response;
+              reference = value.keys;
+              null === reference &&
+                ((reference = value.keys = Array.from(value.data.keys())),
+                (value.keyPointer = 0));
+              value = reference[value.keyPointer];
+              if (void 0 === value) break;
+              if (value.startsWith(key)) {
+                reference = response.data.getAll(value);
                 for (
-                  var entries = response.getAll(reference),
-                    newKey = reference.slice(obj.length),
-                    j = 0;
-                  j < entries.length;
-                  j++
+                  var referencedFormDataKey = value.slice(key.length), i = 0;
+                  i < reference.length;
+                  i++
                 )
-                  key.append(newKey, entries[j]);
-                response.delete(reference);
-              }
-            return key;
+                  arrayRoot.append(referencedFormDataKey, reference[i]);
+                reference = response;
+                reference.data.delete(value);
+                reference.keyPointer++;
+              } else if (value.startsWith(obj)) break;
+              else response.keyPointer++;
+            }
+            return arrayRoot;
           case "i":
             return (
               (arrayRoot = value.slice(2)),
@@ -4856,7 +4874,7 @@
           case "B":
             return (
               (obj = parseInt(value.slice(2), 16)),
-              response._formData.get(response._prefix + obj)
+              response._formData.data.get(response._prefix + obj)
             );
           case "R":
             return parseReadableStream(response, value, void 0);
@@ -4895,7 +4913,7 @@
       return {
         _bundlerConfig: bundlerConfig,
         _prefix: formFieldPrefix,
-        _formData: backingFormData,
+        _formData: { data: backingFormData, keyPointer: -1, keys: null },
         _chunks: chunks,
         _closed: !1,
         _closedReason: null,
@@ -5656,7 +5674,7 @@
           entry = entry[1];
           if ("string" === typeof entry) {
             var response = response$jscomp$0;
-            response._formData.append(name, entry);
+            appendBackingEntry(response._formData, name, entry);
             var prefix = response._prefix;
             if (name.startsWith(prefix)) {
               var chunks = response._chunks;
@@ -5664,7 +5682,7 @@
               (chunks = chunks.get(name)) &&
                 resolveModelChunk(response, chunks, entry, name);
             }
-          } else response$jscomp$0._formData.append(name, entry);
+          } else appendBackingEntry(response$jscomp$0._formData, name, entry);
           iterator.next().then(progress, error);
         }
       }
diff --git a/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-server.edge.production.js b/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-server.edge.production.js
index 748f62d..9c9fbbb 100644
--- a/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-server.edge.production.js
+++ b/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-server.edge.production.js
@@ -2210,6 +2210,14 @@ function requireModule(metadata) {
   if (hasOwnProperty.call(moduleExports, metadata[2]))
     return moduleExports[metadata[2]];
 }
+function appendBackingEntry(backingStore, key, value) {
+  backingStore.data.append(key, value);
+  value = backingStore.keys;
+  null === value
+    ? ((backingStore.keys = Array.from(backingStore.data.keys())),
+      (backingStore.keyPointer = 0))
+    : value.push(key);
+}
 var RESPONSE_SYMBOL = Symbol();
 function ReactPromise(status, value, reason) {
   this.status = status;
@@ -2582,7 +2590,7 @@ function getChunk(response, id) {
   var chunks = response._chunks,
     chunk = chunks.get(id);
   chunk ||
-    ((chunk = response._formData.get(response._prefix + id)),
+    ((chunk = response._formData.data.get(response._prefix + id)),
     (chunk =
       "string" === typeof chunk
         ? createResolvedModelChunk(response, chunk, id)
@@ -2683,6 +2691,10 @@ function getOutlinedModel(
     case "fulfilled":
       id = chunk.value;
       chunk = chunk.reason;
+      if (null !== chunk && "error" in chunk)
+        throw Error(
+          "Expected an initialized chunk but got an initialized stream chunk instead. This payload may have been submitted by an older version of React."
+        );
       for (
         var localLength = 0,
           rootArrayContexts = response._rootArrayContexts,
@@ -2766,22 +2778,19 @@ function createMap(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Map initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Map.");
   model.$$consumed = !0;
-  response = new Map(model);
-  return response;
+  return new Map(model);
 }
 function createSet(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Set initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Set.");
   model.$$consumed = !0;
-  response = new Set(model);
-  return response;
+  return new Set(model);
 }
 function extractIterator(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Iterator initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Iterator.");
   model.$$consumed = !0;
-  response = model[Symbol.iterator]();
-  return response;
+  return model[Symbol.iterator]();
 }
 function createModel(response, model, parentObject, key) {
   return "then" === key && "function" === typeof model ? null : model;
@@ -2819,7 +2828,7 @@ function parseTypedArray(
       Error("Already initialized typed array.")
     )
   );
-  reference = response._formData.get(key).arrayBuffer();
+  reference = response._formData.data.get(key).arrayBuffer();
   if (initializingHandler) {
     var handler = initializingHandler;
     handler.deps++;
@@ -2863,7 +2872,7 @@ function resolveStream(response, id, stream, controller) {
   var chunks = response._chunks;
   stream = new ReactPromise("fulfilled", stream, controller);
   chunks.set(id, stream);
-  response = response._formData.getAll(response._prefix + id);
+  response = response._formData.data.getAll(response._prefix + id);
   for (id = 0; id < response.length; id++)
     (chunks = response[id]),
       "string" === typeof chunks &&
@@ -3078,24 +3087,31 @@ function parseModelString(response, obj, key, value, reference, arrayRoot) {
           getOutlinedModel(response, arrayRoot, obj, key, null, createSet)
         );
       case "K":
-        obj = value.slice(2);
-        obj = response._prefix + obj + "_";
-        key = new FormData();
-        response = response._formData;
-        arrayRoot = Array.from(response.keys());
-        for (value = 0; value < arrayRoot.length; value++)
-          if (((reference = arrayRoot[value]), reference.startsWith(obj))) {
+        key = value.slice(2);
+        obj = response._prefix + "_";
+        key = obj + key + "_";
+        arrayRoot = new FormData();
+        for (response = response._formData; ; ) {
+          value = response.keys;
+          null === value &&
+            ((value = response.keys = Array.from(response.data.keys())),
+            (response.keyPointer = 0));
+          value = value[response.keyPointer];
+          if (void 0 === value) break;
+          if (value.startsWith(key)) {
+            reference = response.data.getAll(value);
             for (
-              var entries = response.getAll(reference),
-                newKey = reference.slice(obj.length),
-                j = 0;
-              j < entries.length;
-              j++
+              var referencedFormDataKey = value.slice(key.length), i = 0;
+              i < reference.length;
+              i++
             )
-              key.append(newKey, entries[j]);
-            response.delete(reference);
-          }
-        return key;
+              arrayRoot.append(referencedFormDataKey, reference[i]);
+            response.data.delete(value);
+            response.keyPointer++;
+          } else if (value.startsWith(obj)) break;
+          else response.keyPointer++;
+        }
+        return arrayRoot;
       case "i":
         return (
           (arrayRoot = value.slice(2)),
@@ -3254,7 +3270,7 @@ function parseModelString(response, obj, key, value, reference, arrayRoot) {
       case "B":
         return (
           (obj = parseInt(value.slice(2), 16)),
-          response._formData.get(response._prefix + obj)
+          response._formData.data.get(response._prefix + obj)
         );
       case "R":
         return parseReadableStream(response, value, void 0);
@@ -3282,7 +3298,7 @@ function createResponse(bundlerConfig, formFieldPrefix, temporaryReferences) {
   return {
     _bundlerConfig: bundlerConfig,
     _prefix: formFieldPrefix,
-    _formData: backingFormData,
+    _formData: { data: backingFormData, keyPointer: -1, keys: null },
     _chunks: chunks,
     _closed: !1,
     _closedReason: null,
@@ -3418,7 +3434,7 @@ exports.decodeReplyFromAsyncIterable = function (
       var name = entry[0];
       entry = entry[1];
       if ("string" === typeof entry) {
-        response._formData.append(name, entry);
+        appendBackingEntry(response._formData, name, entry);
         var prefix = response._prefix;
         if (name.startsWith(prefix)) {
           var chunks = response._chunks;
@@ -3426,7 +3442,7 @@ exports.decodeReplyFromAsyncIterable = function (
           (chunks = chunks.get(name)) &&
             resolveModelChunk(response, chunks, entry, name);
         }
-      } else response._formData.append(name, entry);
+      } else appendBackingEntry(response._formData, name, entry);
       iterator.next().then(progress, error);
     }
   }
diff --git a/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-server.node.development.js b/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-server.node.development.js
index 0f56c05..47adf81 100644
--- a/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-server.node.development.js
+++ b/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-server.node.development.js
@@ -4233,6 +4233,14 @@
       if (hasOwnProperty.call(moduleExports, metadata[2]))
         return moduleExports[metadata[2]];
     }
+    function appendBackingEntry(backingStore, key, value) {
+      backingStore.data.append(key, value);
+      value = backingStore.keys;
+      null === value
+        ? ((backingStore.keys = Array.from(backingStore.data.keys())),
+          (backingStore.keyPointer = 0))
+        : value.push(key);
+    }
     function ReactPromise(status, value, reason) {
       this.status = status;
       this.value = value;
@@ -4545,7 +4553,7 @@
       var chunks = response._chunks,
         chunk = chunks.get(id);
       chunk ||
-        ((chunk = response._formData.get(response._prefix + id)),
+        ((chunk = response._formData.data.get(response._prefix + id)),
         (chunk =
           "string" === typeof chunk
             ? new ReactPromise(
@@ -4657,6 +4665,10 @@
         case "fulfilled":
           id = chunk.value;
           chunk = chunk.reason;
+          if (null !== chunk && "error" in chunk)
+            throw Error(
+              "Expected an initialized chunk but got an initialized stream chunk instead. This payload may have been submitted by an older version of React."
+            );
           for (
             var localLength = 0,
               rootArrayContexts = response._rootArrayContexts,
@@ -4749,22 +4761,19 @@
       if (!isArrayImpl(model)) throw Error("Invalid Map initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Map.");
       model.$$consumed = !0;
-      response = new Map(model);
-      return response;
+      return new Map(model);
     }
     function createSet(response, model) {
       if (!isArrayImpl(model)) throw Error("Invalid Set initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Set.");
       model.$$consumed = !0;
-      response = new Set(model);
-      return response;
+      return new Set(model);
     }
     function extractIterator(response, model) {
       if (!isArrayImpl(model)) throw Error("Invalid Iterator initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Iterator.");
       model.$$consumed = !0;
-      response = model[Symbol.iterator]();
-      return response;
+      return model[Symbol.iterator]();
     }
     function createModel(response, model, parentObject, key) {
       return "then" === key && "function" === typeof model ? null : model;
@@ -4802,7 +4811,7 @@
           Error("Already initialized typed array.")
         )
       );
-      reference = response._formData.get(key).arrayBuffer();
+      reference = response._formData.data.get(key).arrayBuffer();
       if (initializingHandler) {
         var handler = initializingHandler;
         handler.deps++;
@@ -4846,7 +4855,7 @@
       var chunks = response._chunks;
       stream = new ReactPromise("fulfilled", stream, controller);
       chunks.set(id, stream);
-      response = response._formData.getAll(response._prefix + id);
+      response = response._formData.data.getAll(response._prefix + id);
       for (id = 0; id < response.length; id++)
         (chunks = response[id]),
           "string" === typeof chunks &&
@@ -5066,24 +5075,33 @@
               getOutlinedModel(response, arrayRoot, obj, key, null, createSet)
             );
           case "K":
-            obj = value.slice(2);
-            obj = response._prefix + obj + "_";
-            key = new FormData();
-            response = response._formData;
-            arrayRoot = Array.from(response.keys());
-            for (value = 0; value < arrayRoot.length; value++)
-              if (((reference = arrayRoot[value]), reference.startsWith(obj))) {
+            key = value.slice(2);
+            obj = response._prefix + "_";
+            key = obj + key + "_";
+            arrayRoot = new FormData();
+            for (response = response._formData; ; ) {
+              value = response;
+              reference = value.keys;
+              null === reference &&
+                ((reference = value.keys = Array.from(value.data.keys())),
+                (value.keyPointer = 0));
+              value = reference[value.keyPointer];
+              if (void 0 === value) break;
+              if (value.startsWith(key)) {
+                reference = response.data.getAll(value);
                 for (
-                  var entries = response.getAll(reference),
-                    newKey = reference.slice(obj.length),
-                    j = 0;
-                  j < entries.length;
-                  j++
+                  var referencedFormDataKey = value.slice(key.length), i = 0;
+                  i < reference.length;
+                  i++
                 )
-                  key.append(newKey, entries[j]);
-                response.delete(reference);
-              }
-            return key;
+                  arrayRoot.append(referencedFormDataKey, reference[i]);
+                reference = response;
+                reference.data.delete(value);
+                reference.keyPointer++;
+              } else if (value.startsWith(obj)) break;
+              else response.keyPointer++;
+            }
+            return arrayRoot;
           case "i":
             return (
               (arrayRoot = value.slice(2)),
@@ -5252,7 +5270,7 @@
           case "B":
             return (
               (obj = parseInt(value.slice(2), 16)),
-              response._formData.get(response._prefix + obj)
+              response._formData.data.get(response._prefix + obj)
             );
           case "R":
             return parseReadableStream(response, value, void 0);
@@ -5291,7 +5309,7 @@
       return {
         _bundlerConfig: bundlerConfig,
         _prefix: formFieldPrefix,
-        _formData: backingFormData,
+        _formData: { data: backingFormData, keyPointer: -1, keys: null },
         _chunks: chunks,
         _closed: !1,
         _closedReason: null,
@@ -5301,7 +5319,7 @@
       };
     }
     function resolveField(response, key, value) {
-      response._formData.append(key, value);
+      appendBackingEntry(response._formData, key, value);
       var prefix = response._prefix;
       if (key.startsWith(prefix)) {
         var chunks = response._chunks;
@@ -6275,7 +6293,7 @@
           _entry$value = _entry$value[1];
           "string" === typeof _entry$value
             ? resolveField(response, entry, _entry$value)
-            : response._formData.append(entry, _entry$value);
+            : appendBackingEntry(response._formData, entry, _entry$value);
           iterator.next().then(progress, error);
         }
       }
@@ -6329,16 +6347,22 @@
           );
         else {
           pendingFiles++;
-          var JSCompiler_object_inline_chunks_252 = [];
+          var JSCompiler_object_inline_chunks_260 = [];
           value.on("data", function (chunk) {
-            JSCompiler_object_inline_chunks_252.push(chunk);
+            JSCompiler_object_inline_chunks_260.push(chunk);
           });
           value.on("end", function () {
             try {
-              var blob = new Blob(JSCompiler_object_inline_chunks_252, {
-                type: mimeType
-              });
-              response._formData.append(name, blob, filename);
+              var blob = new Blob(JSCompiler_object_inline_chunks_260, {
+                  type: mimeType
+                }),
+                backingStore = response._formData;
+              backingStore.data.append(name, blob, filename);
+              var keys = backingStore.keys;
+              null === keys
+                ? ((backingStore.keys = Array.from(backingStore.data.keys())),
+                  (backingStore.keyPointer = 0))
+                : keys.push(name);
               pendingFiles--;
               if (0 === pendingFiles) {
                 for (blob = 0; blob < queuedFields.length; blob += 2)
diff --git a/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-server.node.production.js b/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-server.node.production.js
index c3f28c7..4728385 100644
--- a/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-server.node.production.js
+++ b/dist/compiled/react-server-dom-turbopack-experimental/cjs/react-server-dom-turbopack-server.node.production.js
@@ -2235,6 +2235,14 @@ function requireModule(metadata) {
   if (hasOwnProperty.call(moduleExports, metadata[2]))
     return moduleExports[metadata[2]];
 }
+function appendBackingEntry(backingStore, key, value) {
+  backingStore.data.append(key, value);
+  value = backingStore.keys;
+  null === value
+    ? ((backingStore.keys = Array.from(backingStore.data.keys())),
+      (backingStore.keyPointer = 0))
+    : value.push(key);
+}
 var RESPONSE_SYMBOL = Symbol();
 function ReactPromise(status, value, reason) {
   this.status = status;
@@ -2607,7 +2615,7 @@ function getChunk(response, id) {
   var chunks = response._chunks,
     chunk = chunks.get(id);
   chunk ||
-    ((chunk = response._formData.get(response._prefix + id)),
+    ((chunk = response._formData.data.get(response._prefix + id)),
     (chunk =
       "string" === typeof chunk
         ? createResolvedModelChunk(response, chunk, id)
@@ -2708,6 +2716,10 @@ function getOutlinedModel(
     case "fulfilled":
       id = chunk.value;
       chunk = chunk.reason;
+      if (null !== chunk && "error" in chunk)
+        throw Error(
+          "Expected an initialized chunk but got an initialized stream chunk instead. This payload may have been submitted by an older version of React."
+        );
       for (
         var localLength = 0,
           rootArrayContexts = response._rootArrayContexts,
@@ -2791,22 +2803,19 @@ function createMap(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Map initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Map.");
   model.$$consumed = !0;
-  response = new Map(model);
-  return response;
+  return new Map(model);
 }
 function createSet(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Set initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Set.");
   model.$$consumed = !0;
-  response = new Set(model);
-  return response;
+  return new Set(model);
 }
 function extractIterator(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Iterator initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Iterator.");
   model.$$consumed = !0;
-  response = model[Symbol.iterator]();
-  return response;
+  return model[Symbol.iterator]();
 }
 function createModel(response, model, parentObject, key) {
   return "then" === key && "function" === typeof model ? null : model;
@@ -2844,7 +2853,7 @@ function parseTypedArray(
       Error("Already initialized typed array.")
     )
   );
-  reference = response._formData.get(key).arrayBuffer();
+  reference = response._formData.data.get(key).arrayBuffer();
   if (initializingHandler) {
     var handler = initializingHandler;
     handler.deps++;
@@ -2888,7 +2897,7 @@ function resolveStream(response, id, stream, controller) {
   var chunks = response._chunks;
   stream = new ReactPromise("fulfilled", stream, controller);
   chunks.set(id, stream);
-  response = response._formData.getAll(response._prefix + id);
+  response = response._formData.data.getAll(response._prefix + id);
   for (id = 0; id < response.length; id++)
     (chunks = response[id]),
       "string" === typeof chunks &&
@@ -3103,24 +3112,31 @@ function parseModelString(response, obj, key, value, reference, arrayRoot) {
           getOutlinedModel(response, arrayRoot, obj, key, null, createSet)
         );
       case "K":
-        obj = value.slice(2);
-        obj = response._prefix + obj + "_";
-        key = new FormData();
-        response = response._formData;
-        arrayRoot = Array.from(response.keys());
-        for (value = 0; value < arrayRoot.length; value++)
-          if (((reference = arrayRoot[value]), reference.startsWith(obj))) {
+        key = value.slice(2);
+        obj = response._prefix + "_";
+        key = obj + key + "_";
+        arrayRoot = new FormData();
+        for (response = response._formData; ; ) {
+          value = response.keys;
+          null === value &&
+            ((value = response.keys = Array.from(response.data.keys())),
+            (response.keyPointer = 0));
+          value = value[response.keyPointer];
+          if (void 0 === value) break;
+          if (value.startsWith(key)) {
+            reference = response.data.getAll(value);
             for (
-              var entries = response.getAll(reference),
-                newKey = reference.slice(obj.length),
-                j = 0;
-              j < entries.length;
-              j++
+              var referencedFormDataKey = value.slice(key.length), i = 0;
+              i < reference.length;
+              i++
             )
-              key.append(newKey, entries[j]);
-            response.delete(reference);
-          }
-        return key;
+              arrayRoot.append(referencedFormDataKey, reference[i]);
+            response.data.delete(value);
+            response.keyPointer++;
+          } else if (value.startsWith(obj)) break;
+          else response.keyPointer++;
+        }
+        return arrayRoot;
       case "i":
         return (
           (arrayRoot = value.slice(2)),
@@ -3279,7 +3295,7 @@ function parseModelString(response, obj, key, value, reference, arrayRoot) {
       case "B":
         return (
           (obj = parseInt(value.slice(2), 16)),
-          response._formData.get(response._prefix + obj)
+          response._formData.data.get(response._prefix + obj)
         );
       case "R":
         return parseReadableStream(response, value, void 0);
@@ -3307,7 +3323,7 @@ function createResponse(bundlerConfig, formFieldPrefix, temporaryReferences) {
   return {
     _bundlerConfig: bundlerConfig,
     _prefix: formFieldPrefix,
-    _formData: backingFormData,
+    _formData: { data: backingFormData, keyPointer: -1, keys: null },
     _chunks: chunks,
     _closed: !1,
     _closedReason: null,
@@ -3317,7 +3333,7 @@ function createResponse(bundlerConfig, formFieldPrefix, temporaryReferences) {
   };
 }
 function resolveField(response, key, value) {
-  response._formData.append(key, value);
+  appendBackingEntry(response._formData, key, value);
   var prefix = response._prefix;
   if (key.startsWith(prefix)) {
     var chunks = response._chunks;
@@ -3495,7 +3511,7 @@ exports.decodeReplyFromAsyncIterable = function (
       _entry$value = _entry$value[1];
       "string" === typeof _entry$value
         ? resolveField(response, entry, _entry$value)
-        : response._formData.append(entry, _entry$value);
+        : appendBackingEntry(response._formData, entry, _entry$value);
       iterator.next().then(progress, error);
     }
   }
@@ -3545,16 +3561,22 @@ exports.decodeReplyFromBusboy = function (busboyStream, turbopackMap, options) {
       );
     else {
       pendingFiles++;
-      var JSCompiler_object_inline_chunks_285 = [];
+      var JSCompiler_object_inline_chunks_294 = [];
       value.on("data", function (chunk) {
-        JSCompiler_object_inline_chunks_285.push(chunk);
+        JSCompiler_object_inline_chunks_294.push(chunk);
       });
       value.on("end", function () {
         try {
-          var blob = new Blob(JSCompiler_object_inline_chunks_285, {
-            type: mimeType
-          });
-          response._formData.append(name, blob, filename);
+          var blob = new Blob(JSCompiler_object_inline_chunks_294, {
+              type: mimeType
+            }),
+            backingStore = response._formData;
+          backingStore.data.append(name, blob, filename);
+          var keys = backingStore.keys;
+          null === keys
+            ? ((backingStore.keys = Array.from(backingStore.data.keys())),
+              (backingStore.keyPointer = 0))
+            : keys.push(name);
           pendingFiles--;
           if (0 === pendingFiles) {
             for (blob = 0; blob < queuedFields.length; blob += 2)
diff --git a/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-client.browser.development.js b/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-client.browser.development.js
index a9df077..43f7b9d 100644
--- a/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-client.browser.development.js
+++ b/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-client.browser.development.js
@@ -532,7 +532,7 @@
             null === formData && (formData = new FormData());
             var _data3 = formData;
             key = nextPartId++;
-            var prefix = formFieldPrefix + key + "_";
+            var prefix = formFieldPrefix + "_" + key + "_";
             value.forEach(function (originalValue, originalKey) {
               _data3.append(prefix + originalKey, originalValue);
             });
@@ -3492,10 +3492,10 @@
       return hook.checkDCE ? !0 : !1;
     })({
       bundleType: 1,
-      version: "19.2.0-canary-9aa4f723-20260123",
+      version: "19.2.0-canary-b8e10c8a-20260420",
       rendererPackageName: "react-server-dom-turbopack",
       currentDispatcherRef: ReactSharedInternals,
-      reconcilerVersion: "19.2.0-canary-9aa4f723-20260123",
+      reconcilerVersion: "19.2.0-canary-b8e10c8a-20260420",
       getCurrentComponentInfo: function () {
         return currentOwnerInDEV;
       }
diff --git a/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-client.browser.production.js b/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-client.browser.production.js
index 0c278bf..f313172 100644
--- a/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-client.browser.production.js
+++ b/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-client.browser.production.js
@@ -333,7 +333,7 @@ function processReply(
         null === formData && (formData = new FormData());
         var data$31 = formData;
         key = nextPartId++;
-        var prefix = formFieldPrefix + key + "_";
+        var prefix = formFieldPrefix + "_" + key + "_";
         value.forEach(function (originalValue, originalKey) {
           data$31.append(prefix + originalKey, originalValue);
         });
diff --git a/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-client.edge.development.js b/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-client.edge.development.js
index 11ab2b6..73d97aa 100644
--- a/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-client.edge.development.js
+++ b/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-client.edge.development.js
@@ -557,7 +557,7 @@
             null === formData && (formData = new FormData());
             var _data3 = formData;
             key = nextPartId++;
-            var prefix = formFieldPrefix + key + "_";
+            var prefix = formFieldPrefix + "_" + key + "_";
             value.forEach(function (originalValue, originalKey) {
               _data3.append(prefix + originalKey, originalValue);
             });
diff --git a/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-client.edge.production.js b/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-client.edge.production.js
index 5ede063..b285b1d 100644
--- a/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-client.edge.production.js
+++ b/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-client.edge.production.js
@@ -354,7 +354,7 @@ function processReply(
         null === formData && (formData = new FormData());
         var data$31 = formData;
         key = nextPartId++;
-        var prefix = formFieldPrefix + key + "_";
+        var prefix = formFieldPrefix + "_" + key + "_";
         value.forEach(function (originalValue, originalKey) {
           data$31.append(prefix + originalKey, originalValue);
         });
diff --git a/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-client.node.development.js b/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-client.node.development.js
index dafe036..478eff1 100644
--- a/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-client.node.development.js
+++ b/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-client.node.development.js
@@ -557,7 +557,7 @@
             null === formData && (formData = new FormData());
             var _data3 = formData;
             key = nextPartId++;
-            var prefix = formFieldPrefix + key + "_";
+            var prefix = formFieldPrefix + "_" + key + "_";
             value.forEach(function (originalValue, originalKey) {
               _data3.append(prefix + originalKey, originalValue);
             });
diff --git a/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-client.node.production.js b/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-client.node.production.js
index ed490fa..70f1791 100644
--- a/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-client.node.production.js
+++ b/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-client.node.production.js
@@ -355,7 +355,7 @@ function processReply(
         null === formData && (formData = new FormData());
         var data$31 = formData;
         key = nextPartId++;
-        var prefix = formFieldPrefix + key + "_";
+        var prefix = formFieldPrefix + "_" + key + "_";
         value.forEach(function (originalValue, originalKey) {
           data$31.append(prefix + originalKey, originalValue);
         });
diff --git a/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-server.browser.development.js b/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-server.browser.development.js
index 9091dbc..562d170 100644
--- a/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-server.browser.development.js
+++ b/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-server.browser.development.js
@@ -3879,7 +3879,7 @@
       var chunks = response._chunks,
         chunk = chunks.get(id);
       chunk ||
-        ((chunk = response._formData.get(response._prefix + id)),
+        ((chunk = response._formData.data.get(response._prefix + id)),
         (chunk =
           "string" === typeof chunk
             ? new ReactPromise(
@@ -3991,6 +3991,10 @@
         case "fulfilled":
           id = chunk.value;
           chunk = chunk.reason;
+          if (null !== chunk && "error" in chunk)
+            throw Error(
+              "Expected an initialized chunk but got an initialized stream chunk instead. This payload may have been submitted by an older version of React."
+            );
           for (
             var localLength = 0,
               rootArrayContexts = response._rootArrayContexts,
@@ -4083,22 +4087,19 @@
       if (!isArrayImpl(model)) throw Error("Invalid Map initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Map.");
       model.$$consumed = !0;
-      response = new Map(model);
-      return response;
+      return new Map(model);
     }
     function createSet(response, model) {
       if (!isArrayImpl(model)) throw Error("Invalid Set initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Set.");
       model.$$consumed = !0;
-      response = new Set(model);
-      return response;
+      return new Set(model);
     }
     function extractIterator(response, model) {
       if (!isArrayImpl(model)) throw Error("Invalid Iterator initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Iterator.");
       model.$$consumed = !0;
-      response = model[Symbol.iterator]();
-      return response;
+      return model[Symbol.iterator]();
     }
     function createModel(response, model, parentObject, key) {
       return "then" === key && "function" === typeof model ? null : model;
@@ -4136,7 +4137,7 @@
           Error("Already initialized typed array.")
         )
       );
-      reference = response._formData.get(key).arrayBuffer();
+      reference = response._formData.data.get(key).arrayBuffer();
       if (initializingHandler) {
         var handler = initializingHandler;
         handler.deps++;
@@ -4180,7 +4181,7 @@
       var chunks = response._chunks;
       stream = new ReactPromise("fulfilled", stream, controller);
       chunks.set(id, stream);
-      response = response._formData.getAll(response._prefix + id);
+      response = response._formData.data.getAll(response._prefix + id);
       for (id = 0; id < response.length; id++)
         (chunks = response[id]),
           "string" === typeof chunks &&
@@ -4400,24 +4401,33 @@
               getOutlinedModel(response, arrayRoot, obj, key, null, createSet)
             );
           case "K":
-            obj = value.slice(2);
-            obj = response._prefix + obj + "_";
-            key = new FormData();
-            response = response._formData;
-            arrayRoot = Array.from(response.keys());
-            for (value = 0; value < arrayRoot.length; value++)
-              if (((reference = arrayRoot[value]), reference.startsWith(obj))) {
+            key = value.slice(2);
+            obj = response._prefix + "_";
+            key = obj + key + "_";
+            arrayRoot = new FormData();
+            for (response = response._formData; ; ) {
+              value = response;
+              reference = value.keys;
+              null === reference &&
+                ((reference = value.keys = Array.from(value.data.keys())),
+                (value.keyPointer = 0));
+              value = reference[value.keyPointer];
+              if (void 0 === value) break;
+              if (value.startsWith(key)) {
+                reference = response.data.getAll(value);
                 for (
-                  var entries = response.getAll(reference),
-                    newKey = reference.slice(obj.length),
-                    j = 0;
-                  j < entries.length;
-                  j++
+                  var referencedFormDataKey = value.slice(key.length), i = 0;
+                  i < reference.length;
+                  i++
                 )
-                  key.append(newKey, entries[j]);
-                response.delete(reference);
-              }
-            return key;
+                  arrayRoot.append(referencedFormDataKey, reference[i]);
+                reference = response;
+                reference.data.delete(value);
+                reference.keyPointer++;
+              } else if (value.startsWith(obj)) break;
+              else response.keyPointer++;
+            }
+            return arrayRoot;
           case "i":
             return (
               (arrayRoot = value.slice(2)),
@@ -4586,7 +4596,7 @@
           case "B":
             return (
               (obj = parseInt(value.slice(2), 16)),
-              response._formData.get(response._prefix + obj)
+              response._formData.data.get(response._prefix + obj)
             );
           case "R":
             return parseReadableStream(response, value, void 0);
@@ -4625,7 +4635,7 @@
       return {
         _bundlerConfig: bundlerConfig,
         _prefix: formFieldPrefix,
-        _formData: backingFormData,
+        _formData: { data: backingFormData, keyPointer: -1, keys: null },
         _chunks: chunks,
         _closed: !1,
         _closedReason: null,
diff --git a/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-server.browser.production.js b/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-server.browser.production.js
index 07bec67..50ba2c2 100644
--- a/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-server.browser.production.js
+++ b/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-server.browser.production.js
@@ -2420,7 +2420,7 @@ function getChunk(response, id) {
   var chunks = response._chunks,
     chunk = chunks.get(id);
   chunk ||
-    ((chunk = response._formData.get(response._prefix + id)),
+    ((chunk = response._formData.data.get(response._prefix + id)),
     (chunk =
       "string" === typeof chunk
         ? createResolvedModelChunk(response, chunk, id)
@@ -2521,6 +2521,10 @@ function getOutlinedModel(
     case "fulfilled":
       id = chunk.value;
       chunk = chunk.reason;
+      if (null !== chunk && "error" in chunk)
+        throw Error(
+          "Expected an initialized chunk but got an initialized stream chunk instead. This payload may have been submitted by an older version of React."
+        );
       for (
         var localLength = 0,
           rootArrayContexts = response._rootArrayContexts,
@@ -2604,22 +2608,19 @@ function createMap(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Map initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Map.");
   model.$$consumed = !0;
-  response = new Map(model);
-  return response;
+  return new Map(model);
 }
 function createSet(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Set initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Set.");
   model.$$consumed = !0;
-  response = new Set(model);
-  return response;
+  return new Set(model);
 }
 function extractIterator(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Iterator initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Iterator.");
   model.$$consumed = !0;
-  response = model[Symbol.iterator]();
-  return response;
+  return model[Symbol.iterator]();
 }
 function createModel(response, model, parentObject, key) {
   return "then" === key && "function" === typeof model ? null : model;
@@ -2657,7 +2658,7 @@ function parseTypedArray(
       Error("Already initialized typed array.")
     )
   );
-  reference = response._formData.get(key).arrayBuffer();
+  reference = response._formData.data.get(key).arrayBuffer();
   if (initializingHandler) {
     var handler = initializingHandler;
     handler.deps++;
@@ -2701,7 +2702,7 @@ function resolveStream(response, id, stream, controller) {
   var chunks = response._chunks;
   stream = new ReactPromise("fulfilled", stream, controller);
   chunks.set(id, stream);
-  response = response._formData.getAll(response._prefix + id);
+  response = response._formData.data.getAll(response._prefix + id);
   for (id = 0; id < response.length; id++)
     (chunks = response[id]),
       "string" === typeof chunks &&
@@ -2916,24 +2917,31 @@ function parseModelString(response, obj, key, value, reference, arrayRoot) {
           getOutlinedModel(response, arrayRoot, obj, key, null, createSet)
         );
       case "K":
-        obj = value.slice(2);
-        obj = response._prefix + obj + "_";
-        key = new FormData();
-        response = response._formData;
-        arrayRoot = Array.from(response.keys());
-        for (value = 0; value < arrayRoot.length; value++)
-          if (((reference = arrayRoot[value]), reference.startsWith(obj))) {
+        key = value.slice(2);
+        obj = response._prefix + "_";
+        key = obj + key + "_";
+        arrayRoot = new FormData();
+        for (response = response._formData; ; ) {
+          value = response.keys;
+          null === value &&
+            ((value = response.keys = Array.from(response.data.keys())),
+            (response.keyPointer = 0));
+          value = value[response.keyPointer];
+          if (void 0 === value) break;
+          if (value.startsWith(key)) {
+            reference = response.data.getAll(value);
             for (
-              var entries = response.getAll(reference),
-                newKey = reference.slice(obj.length),
-                j = 0;
-              j < entries.length;
-              j++
+              var referencedFormDataKey = value.slice(key.length), i = 0;
+              i < reference.length;
+              i++
             )
-              key.append(newKey, entries[j]);
-            response.delete(reference);
-          }
-        return key;
+              arrayRoot.append(referencedFormDataKey, reference[i]);
+            response.data.delete(value);
+            response.keyPointer++;
+          } else if (value.startsWith(obj)) break;
+          else response.keyPointer++;
+        }
+        return arrayRoot;
       case "i":
         return (
           (arrayRoot = value.slice(2)),
@@ -3092,7 +3100,7 @@ function parseModelString(response, obj, key, value, reference, arrayRoot) {
       case "B":
         return (
           (obj = parseInt(value.slice(2), 16)),
-          response._formData.get(response._prefix + obj)
+          response._formData.data.get(response._prefix + obj)
         );
       case "R":
         return parseReadableStream(response, value, void 0);
@@ -3120,7 +3128,7 @@ function createResponse(bundlerConfig, formFieldPrefix, temporaryReferences) {
   return {
     _bundlerConfig: bundlerConfig,
     _prefix: formFieldPrefix,
-    _formData: backingFormData,
+    _formData: { data: backingFormData, keyPointer: -1, keys: null },
     _chunks: chunks,
     _closed: !1,
     _closedReason: null,
diff --git a/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-server.edge.development.js b/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-server.edge.development.js
index 43e84ed..49ba251 100644
--- a/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-server.edge.development.js
+++ b/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-server.edge.development.js
@@ -3642,6 +3642,14 @@
       if (hasOwnProperty.call(moduleExports, metadata[2]))
         return moduleExports[metadata[2]];
     }
+    function appendBackingEntry(backingStore, key, value) {
+      backingStore.data.append(key, value);
+      value = backingStore.keys;
+      null === value
+        ? ((backingStore.keys = Array.from(backingStore.data.keys())),
+          (backingStore.keyPointer = 0))
+        : value.push(key);
+    }
     function ReactPromise(status, value, reason) {
       this.status = status;
       this.value = value;
@@ -3954,7 +3962,7 @@
       var chunks = response._chunks,
         chunk = chunks.get(id);
       chunk ||
-        ((chunk = response._formData.get(response._prefix + id)),
+        ((chunk = response._formData.data.get(response._prefix + id)),
         (chunk =
           "string" === typeof chunk
             ? new ReactPromise(
@@ -4066,6 +4074,10 @@
         case "fulfilled":
           id = chunk.value;
           chunk = chunk.reason;
+          if (null !== chunk && "error" in chunk)
+            throw Error(
+              "Expected an initialized chunk but got an initialized stream chunk instead. This payload may have been submitted by an older version of React."
+            );
           for (
             var localLength = 0,
               rootArrayContexts = response._rootArrayContexts,
@@ -4158,22 +4170,19 @@
       if (!isArrayImpl(model)) throw Error("Invalid Map initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Map.");
       model.$$consumed = !0;
-      response = new Map(model);
-      return response;
+      return new Map(model);
     }
     function createSet(response, model) {
       if (!isArrayImpl(model)) throw Error("Invalid Set initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Set.");
       model.$$consumed = !0;
-      response = new Set(model);
-      return response;
+      return new Set(model);
     }
     function extractIterator(response, model) {
       if (!isArrayImpl(model)) throw Error("Invalid Iterator initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Iterator.");
       model.$$consumed = !0;
-      response = model[Symbol.iterator]();
-      return response;
+      return model[Symbol.iterator]();
     }
     function createModel(response, model, parentObject, key) {
       return "then" === key && "function" === typeof model ? null : model;
@@ -4211,7 +4220,7 @@
           Error("Already initialized typed array.")
         )
       );
-      reference = response._formData.get(key).arrayBuffer();
+      reference = response._formData.data.get(key).arrayBuffer();
       if (initializingHandler) {
         var handler = initializingHandler;
         handler.deps++;
@@ -4255,7 +4264,7 @@
       var chunks = response._chunks;
       stream = new ReactPromise("fulfilled", stream, controller);
       chunks.set(id, stream);
-      response = response._formData.getAll(response._prefix + id);
+      response = response._formData.data.getAll(response._prefix + id);
       for (id = 0; id < response.length; id++)
         (chunks = response[id]),
           "string" === typeof chunks &&
@@ -4475,24 +4484,33 @@
               getOutlinedModel(response, arrayRoot, obj, key, null, createSet)
             );
           case "K":
-            obj = value.slice(2);
-            obj = response._prefix + obj + "_";
-            key = new FormData();
-            response = response._formData;
-            arrayRoot = Array.from(response.keys());
-            for (value = 0; value < arrayRoot.length; value++)
-              if (((reference = arrayRoot[value]), reference.startsWith(obj))) {
+            key = value.slice(2);
+            obj = response._prefix + "_";
+            key = obj + key + "_";
+            arrayRoot = new FormData();
+            for (response = response._formData; ; ) {
+              value = response;
+              reference = value.keys;
+              null === reference &&
+                ((reference = value.keys = Array.from(value.data.keys())),
+                (value.keyPointer = 0));
+              value = reference[value.keyPointer];
+              if (void 0 === value) break;
+              if (value.startsWith(key)) {
+                reference = response.data.getAll(value);
                 for (
-                  var entries = response.getAll(reference),
-                    newKey = reference.slice(obj.length),
-                    j = 0;
-                  j < entries.length;
-                  j++
+                  var referencedFormDataKey = value.slice(key.length), i = 0;
+                  i < reference.length;
+                  i++
                 )
-                  key.append(newKey, entries[j]);
-                response.delete(reference);
-              }
-            return key;
+                  arrayRoot.append(referencedFormDataKey, reference[i]);
+                reference = response;
+                reference.data.delete(value);
+                reference.keyPointer++;
+              } else if (value.startsWith(obj)) break;
+              else response.keyPointer++;
+            }
+            return arrayRoot;
           case "i":
             return (
               (arrayRoot = value.slice(2)),
@@ -4661,7 +4679,7 @@
           case "B":
             return (
               (obj = parseInt(value.slice(2), 16)),
-              response._formData.get(response._prefix + obj)
+              response._formData.data.get(response._prefix + obj)
             );
           case "R":
             return parseReadableStream(response, value, void 0);
@@ -4700,7 +4718,7 @@
       return {
         _bundlerConfig: bundlerConfig,
         _prefix: formFieldPrefix,
-        _formData: backingFormData,
+        _formData: { data: backingFormData, keyPointer: -1, keys: null },
         _chunks: chunks,
         _closed: !1,
         _closedReason: null,
@@ -5453,7 +5471,7 @@
           entry = entry[1];
           if ("string" === typeof entry) {
             var response = response$jscomp$0;
-            response._formData.append(name, entry);
+            appendBackingEntry(response._formData, name, entry);
             var prefix = response._prefix;
             if (name.startsWith(prefix)) {
               var chunks = response._chunks;
@@ -5461,7 +5479,7 @@
               (chunks = chunks.get(name)) &&
                 resolveModelChunk(response, chunks, entry, name);
             }
-          } else response$jscomp$0._formData.append(name, entry);
+          } else appendBackingEntry(response$jscomp$0._formData, name, entry);
           iterator.next().then(progress, error);
         }
       }
diff --git a/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-server.edge.production.js b/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-server.edge.production.js
index d3448cc..10287fc 100644
--- a/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-server.edge.production.js
+++ b/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-server.edge.production.js
@@ -2053,6 +2053,14 @@ function requireModule(metadata) {
   if (hasOwnProperty.call(moduleExports, metadata[2]))
     return moduleExports[metadata[2]];
 }
+function appendBackingEntry(backingStore, key, value) {
+  backingStore.data.append(key, value);
+  value = backingStore.keys;
+  null === value
+    ? ((backingStore.keys = Array.from(backingStore.data.keys())),
+      (backingStore.keyPointer = 0))
+    : value.push(key);
+}
 var RESPONSE_SYMBOL = Symbol();
 function ReactPromise(status, value, reason) {
   this.status = status;
@@ -2425,7 +2433,7 @@ function getChunk(response, id) {
   var chunks = response._chunks,
     chunk = chunks.get(id);
   chunk ||
-    ((chunk = response._formData.get(response._prefix + id)),
+    ((chunk = response._formData.data.get(response._prefix + id)),
     (chunk =
       "string" === typeof chunk
         ? createResolvedModelChunk(response, chunk, id)
@@ -2526,6 +2534,10 @@ function getOutlinedModel(
     case "fulfilled":
       id = chunk.value;
       chunk = chunk.reason;
+      if (null !== chunk && "error" in chunk)
+        throw Error(
+          "Expected an initialized chunk but got an initialized stream chunk instead. This payload may have been submitted by an older version of React."
+        );
       for (
         var localLength = 0,
           rootArrayContexts = response._rootArrayContexts,
@@ -2609,22 +2621,19 @@ function createMap(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Map initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Map.");
   model.$$consumed = !0;
-  response = new Map(model);
-  return response;
+  return new Map(model);
 }
 function createSet(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Set initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Set.");
   model.$$consumed = !0;
-  response = new Set(model);
-  return response;
+  return new Set(model);
 }
 function extractIterator(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Iterator initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Iterator.");
   model.$$consumed = !0;
-  response = model[Symbol.iterator]();
-  return response;
+  return model[Symbol.iterator]();
 }
 function createModel(response, model, parentObject, key) {
   return "then" === key && "function" === typeof model ? null : model;
@@ -2662,7 +2671,7 @@ function parseTypedArray(
       Error("Already initialized typed array.")
     )
   );
-  reference = response._formData.get(key).arrayBuffer();
+  reference = response._formData.data.get(key).arrayBuffer();
   if (initializingHandler) {
     var handler = initializingHandler;
     handler.deps++;
@@ -2706,7 +2715,7 @@ function resolveStream(response, id, stream, controller) {
   var chunks = response._chunks;
   stream = new ReactPromise("fulfilled", stream, controller);
   chunks.set(id, stream);
-  response = response._formData.getAll(response._prefix + id);
+  response = response._formData.data.getAll(response._prefix + id);
   for (id = 0; id < response.length; id++)
     (chunks = response[id]),
       "string" === typeof chunks &&
@@ -2921,24 +2930,31 @@ function parseModelString(response, obj, key, value, reference, arrayRoot) {
           getOutlinedModel(response, arrayRoot, obj, key, null, createSet)
         );
       case "K":
-        obj = value.slice(2);
-        obj = response._prefix + obj + "_";
-        key = new FormData();
-        response = response._formData;
-        arrayRoot = Array.from(response.keys());
-        for (value = 0; value < arrayRoot.length; value++)
-          if (((reference = arrayRoot[value]), reference.startsWith(obj))) {
+        key = value.slice(2);
+        obj = response._prefix + "_";
+        key = obj + key + "_";
+        arrayRoot = new FormData();
+        for (response = response._formData; ; ) {
+          value = response.keys;
+          null === value &&
+            ((value = response.keys = Array.from(response.data.keys())),
+            (response.keyPointer = 0));
+          value = value[response.keyPointer];
+          if (void 0 === value) break;
+          if (value.startsWith(key)) {
+            reference = response.data.getAll(value);
             for (
-              var entries = response.getAll(reference),
-                newKey = reference.slice(obj.length),
-                j = 0;
-              j < entries.length;
-              j++
+              var referencedFormDataKey = value.slice(key.length), i = 0;
+              i < reference.length;
+              i++
             )
-              key.append(newKey, entries[j]);
-            response.delete(reference);
-          }
-        return key;
+              arrayRoot.append(referencedFormDataKey, reference[i]);
+            response.data.delete(value);
+            response.keyPointer++;
+          } else if (value.startsWith(obj)) break;
+          else response.keyPointer++;
+        }
+        return arrayRoot;
       case "i":
         return (
           (arrayRoot = value.slice(2)),
@@ -3097,7 +3113,7 @@ function parseModelString(response, obj, key, value, reference, arrayRoot) {
       case "B":
         return (
           (obj = parseInt(value.slice(2), 16)),
-          response._formData.get(response._prefix + obj)
+          response._formData.data.get(response._prefix + obj)
         );
       case "R":
         return parseReadableStream(response, value, void 0);
@@ -3125,7 +3141,7 @@ function createResponse(bundlerConfig, formFieldPrefix, temporaryReferences) {
   return {
     _bundlerConfig: bundlerConfig,
     _prefix: formFieldPrefix,
-    _formData: backingFormData,
+    _formData: { data: backingFormData, keyPointer: -1, keys: null },
     _chunks: chunks,
     _closed: !1,
     _closedReason: null,
@@ -3261,7 +3277,7 @@ exports.decodeReplyFromAsyncIterable = function (
       var name = entry[0];
       entry = entry[1];
       if ("string" === typeof entry) {
-        response._formData.append(name, entry);
+        appendBackingEntry(response._formData, name, entry);
         var prefix = response._prefix;
         if (name.startsWith(prefix)) {
           var chunks = response._chunks;
@@ -3269,7 +3285,7 @@ exports.decodeReplyFromAsyncIterable = function (
           (chunks = chunks.get(name)) &&
             resolveModelChunk(response, chunks, entry, name);
         }
-      } else response._formData.append(name, entry);
+      } else appendBackingEntry(response._formData, name, entry);
       iterator.next().then(progress, error);
     }
   }
diff --git a/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-server.node.development.js b/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-server.node.development.js
index 2bf2b59..d22e000 100644
--- a/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-server.node.development.js
+++ b/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-server.node.development.js
@@ -4043,6 +4043,14 @@
       if (hasOwnProperty.call(moduleExports, metadata[2]))
         return moduleExports[metadata[2]];
     }
+    function appendBackingEntry(backingStore, key, value) {
+      backingStore.data.append(key, value);
+      value = backingStore.keys;
+      null === value
+        ? ((backingStore.keys = Array.from(backingStore.data.keys())),
+          (backingStore.keyPointer = 0))
+        : value.push(key);
+    }
     function ReactPromise(status, value, reason) {
       this.status = status;
       this.value = value;
@@ -4355,7 +4363,7 @@
       var chunks = response._chunks,
         chunk = chunks.get(id);
       chunk ||
-        ((chunk = response._formData.get(response._prefix + id)),
+        ((chunk = response._formData.data.get(response._prefix + id)),
         (chunk =
           "string" === typeof chunk
             ? new ReactPromise(
@@ -4467,6 +4475,10 @@
         case "fulfilled":
           id = chunk.value;
           chunk = chunk.reason;
+          if (null !== chunk && "error" in chunk)
+            throw Error(
+              "Expected an initialized chunk but got an initialized stream chunk instead. This payload may have been submitted by an older version of React."
+            );
           for (
             var localLength = 0,
               rootArrayContexts = response._rootArrayContexts,
@@ -4559,22 +4571,19 @@
       if (!isArrayImpl(model)) throw Error("Invalid Map initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Map.");
       model.$$consumed = !0;
-      response = new Map(model);
-      return response;
+      return new Map(model);
     }
     function createSet(response, model) {
       if (!isArrayImpl(model)) throw Error("Invalid Set initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Set.");
       model.$$consumed = !0;
-      response = new Set(model);
-      return response;
+      return new Set(model);
     }
     function extractIterator(response, model) {
       if (!isArrayImpl(model)) throw Error("Invalid Iterator initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Iterator.");
       model.$$consumed = !0;
-      response = model[Symbol.iterator]();
-      return response;
+      return model[Symbol.iterator]();
     }
     function createModel(response, model, parentObject, key) {
       return "then" === key && "function" === typeof model ? null : model;
@@ -4612,7 +4621,7 @@
           Error("Already initialized typed array.")
         )
       );
-      reference = response._formData.get(key).arrayBuffer();
+      reference = response._formData.data.get(key).arrayBuffer();
       if (initializingHandler) {
         var handler = initializingHandler;
         handler.deps++;
@@ -4656,7 +4665,7 @@
       var chunks = response._chunks;
       stream = new ReactPromise("fulfilled", stream, controller);
       chunks.set(id, stream);
-      response = response._formData.getAll(response._prefix + id);
+      response = response._formData.data.getAll(response._prefix + id);
       for (id = 0; id < response.length; id++)
         (chunks = response[id]),
           "string" === typeof chunks &&
@@ -4876,24 +4885,33 @@
               getOutlinedModel(response, arrayRoot, obj, key, null, createSet)
             );
           case "K":
-            obj = value.slice(2);
-            obj = response._prefix + obj + "_";
-            key = new FormData();
-            response = response._formData;
-            arrayRoot = Array.from(response.keys());
-            for (value = 0; value < arrayRoot.length; value++)
-              if (((reference = arrayRoot[value]), reference.startsWith(obj))) {
+            key = value.slice(2);
+            obj = response._prefix + "_";
+            key = obj + key + "_";
+            arrayRoot = new FormData();
+            for (response = response._formData; ; ) {
+              value = response;
+              reference = value.keys;
+              null === reference &&
+                ((reference = value.keys = Array.from(value.data.keys())),
+                (value.keyPointer = 0));
+              value = reference[value.keyPointer];
+              if (void 0 === value) break;
+              if (value.startsWith(key)) {
+                reference = response.data.getAll(value);
                 for (
-                  var entries = response.getAll(reference),
-                    newKey = reference.slice(obj.length),
-                    j = 0;
-                  j < entries.length;
-                  j++
+                  var referencedFormDataKey = value.slice(key.length), i = 0;
+                  i < reference.length;
+                  i++
                 )
-                  key.append(newKey, entries[j]);
-                response.delete(reference);
-              }
-            return key;
+                  arrayRoot.append(referencedFormDataKey, reference[i]);
+                reference = response;
+                reference.data.delete(value);
+                reference.keyPointer++;
+              } else if (value.startsWith(obj)) break;
+              else response.keyPointer++;
+            }
+            return arrayRoot;
           case "i":
             return (
               (arrayRoot = value.slice(2)),
@@ -5062,7 +5080,7 @@
           case "B":
             return (
               (obj = parseInt(value.slice(2), 16)),
-              response._formData.get(response._prefix + obj)
+              response._formData.data.get(response._prefix + obj)
             );
           case "R":
             return parseReadableStream(response, value, void 0);
@@ -5101,7 +5119,7 @@
       return {
         _bundlerConfig: bundlerConfig,
         _prefix: formFieldPrefix,
-        _formData: backingFormData,
+        _formData: { data: backingFormData, keyPointer: -1, keys: null },
         _chunks: chunks,
         _closed: !1,
         _closedReason: null,
@@ -5111,7 +5129,7 @@
       };
     }
     function resolveField(response, key, value) {
-      response._formData.append(key, value);
+      appendBackingEntry(response._formData, key, value);
       var prefix = response._prefix;
       if (key.startsWith(prefix)) {
         var chunks = response._chunks;
@@ -6077,7 +6095,7 @@
           _entry$value = _entry$value[1];
           "string" === typeof _entry$value
             ? resolveField(response, entry, _entry$value)
-            : response._formData.append(entry, _entry$value);
+            : appendBackingEntry(response._formData, entry, _entry$value);
           iterator.next().then(progress, error);
         }
       }
@@ -6131,16 +6149,22 @@
           );
         else {
           pendingFiles++;
-          var JSCompiler_object_inline_chunks_246 = [];
+          var JSCompiler_object_inline_chunks_254 = [];
           value.on("data", function (chunk) {
-            JSCompiler_object_inline_chunks_246.push(chunk);
+            JSCompiler_object_inline_chunks_254.push(chunk);
           });
           value.on("end", function () {
             try {
-              var blob = new Blob(JSCompiler_object_inline_chunks_246, {
-                type: mimeType
-              });
-              response._formData.append(name, blob, filename);
+              var blob = new Blob(JSCompiler_object_inline_chunks_254, {
+                  type: mimeType
+                }),
+                backingStore = response._formData;
+              backingStore.data.append(name, blob, filename);
+              var keys = backingStore.keys;
+              null === keys
+                ? ((backingStore.keys = Array.from(backingStore.data.keys())),
+                  (backingStore.keyPointer = 0))
+                : keys.push(name);
               pendingFiles--;
               if (0 === pendingFiles) {
                 for (blob = 0; blob < queuedFields.length; blob += 2)
diff --git a/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-server.node.production.js b/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-server.node.production.js
index f6423ea..03070a8 100644
--- a/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-server.node.production.js
+++ b/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-server.node.production.js
@@ -2082,6 +2082,14 @@ function requireModule(metadata) {
   if (hasOwnProperty.call(moduleExports, metadata[2]))
     return moduleExports[metadata[2]];
 }
+function appendBackingEntry(backingStore, key, value) {
+  backingStore.data.append(key, value);
+  value = backingStore.keys;
+  null === value
+    ? ((backingStore.keys = Array.from(backingStore.data.keys())),
+      (backingStore.keyPointer = 0))
+    : value.push(key);
+}
 var RESPONSE_SYMBOL = Symbol();
 function ReactPromise(status, value, reason) {
   this.status = status;
@@ -2454,7 +2462,7 @@ function getChunk(response, id) {
   var chunks = response._chunks,
     chunk = chunks.get(id);
   chunk ||
-    ((chunk = response._formData.get(response._prefix + id)),
+    ((chunk = response._formData.data.get(response._prefix + id)),
     (chunk =
       "string" === typeof chunk
         ? createResolvedModelChunk(response, chunk, id)
@@ -2555,6 +2563,10 @@ function getOutlinedModel(
     case "fulfilled":
       id = chunk.value;
       chunk = chunk.reason;
+      if (null !== chunk && "error" in chunk)
+        throw Error(
+          "Expected an initialized chunk but got an initialized stream chunk instead. This payload may have been submitted by an older version of React."
+        );
       for (
         var localLength = 0,
           rootArrayContexts = response._rootArrayContexts,
@@ -2638,22 +2650,19 @@ function createMap(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Map initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Map.");
   model.$$consumed = !0;
-  response = new Map(model);
-  return response;
+  return new Map(model);
 }
 function createSet(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Set initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Set.");
   model.$$consumed = !0;
-  response = new Set(model);
-  return response;
+  return new Set(model);
 }
 function extractIterator(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Iterator initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Iterator.");
   model.$$consumed = !0;
-  response = model[Symbol.iterator]();
-  return response;
+  return model[Symbol.iterator]();
 }
 function createModel(response, model, parentObject, key) {
   return "then" === key && "function" === typeof model ? null : model;
@@ -2691,7 +2700,7 @@ function parseTypedArray(
       Error("Already initialized typed array.")
     )
   );
-  reference = response._formData.get(key).arrayBuffer();
+  reference = response._formData.data.get(key).arrayBuffer();
   if (initializingHandler) {
     var handler = initializingHandler;
     handler.deps++;
@@ -2735,7 +2744,7 @@ function resolveStream(response, id, stream, controller) {
   var chunks = response._chunks;
   stream = new ReactPromise("fulfilled", stream, controller);
   chunks.set(id, stream);
-  response = response._formData.getAll(response._prefix + id);
+  response = response._formData.data.getAll(response._prefix + id);
   for (id = 0; id < response.length; id++)
     (chunks = response[id]),
       "string" === typeof chunks &&
@@ -2950,24 +2959,31 @@ function parseModelString(response, obj, key, value, reference, arrayRoot) {
           getOutlinedModel(response, arrayRoot, obj, key, null, createSet)
         );
       case "K":
-        obj = value.slice(2);
-        obj = response._prefix + obj + "_";
-        key = new FormData();
-        response = response._formData;
-        arrayRoot = Array.from(response.keys());
-        for (value = 0; value < arrayRoot.length; value++)
-          if (((reference = arrayRoot[value]), reference.startsWith(obj))) {
+        key = value.slice(2);
+        obj = response._prefix + "_";
+        key = obj + key + "_";
+        arrayRoot = new FormData();
+        for (response = response._formData; ; ) {
+          value = response.keys;
+          null === value &&
+            ((value = response.keys = Array.from(response.data.keys())),
+            (response.keyPointer = 0));
+          value = value[response.keyPointer];
+          if (void 0 === value) break;
+          if (value.startsWith(key)) {
+            reference = response.data.getAll(value);
             for (
-              var entries = response.getAll(reference),
-                newKey = reference.slice(obj.length),
-                j = 0;
-              j < entries.length;
-              j++
+              var referencedFormDataKey = value.slice(key.length), i = 0;
+              i < reference.length;
+              i++
             )
-              key.append(newKey, entries[j]);
-            response.delete(reference);
-          }
-        return key;
+              arrayRoot.append(referencedFormDataKey, reference[i]);
+            response.data.delete(value);
+            response.keyPointer++;
+          } else if (value.startsWith(obj)) break;
+          else response.keyPointer++;
+        }
+        return arrayRoot;
       case "i":
         return (
           (arrayRoot = value.slice(2)),
@@ -3126,7 +3142,7 @@ function parseModelString(response, obj, key, value, reference, arrayRoot) {
       case "B":
         return (
           (obj = parseInt(value.slice(2), 16)),
-          response._formData.get(response._prefix + obj)
+          response._formData.data.get(response._prefix + obj)
         );
       case "R":
         return parseReadableStream(response, value, void 0);
@@ -3154,7 +3170,7 @@ function createResponse(bundlerConfig, formFieldPrefix, temporaryReferences) {
   return {
     _bundlerConfig: bundlerConfig,
     _prefix: formFieldPrefix,
-    _formData: backingFormData,
+    _formData: { data: backingFormData, keyPointer: -1, keys: null },
     _chunks: chunks,
     _closed: !1,
     _closedReason: null,
@@ -3164,7 +3180,7 @@ function createResponse(bundlerConfig, formFieldPrefix, temporaryReferences) {
   };
 }
 function resolveField(response, key, value) {
-  response._formData.append(key, value);
+  appendBackingEntry(response._formData, key, value);
   var prefix = response._prefix;
   if (key.startsWith(prefix)) {
     var chunks = response._chunks;
@@ -3342,7 +3358,7 @@ exports.decodeReplyFromAsyncIterable = function (
       _entry$value = _entry$value[1];
       "string" === typeof _entry$value
         ? resolveField(response, entry, _entry$value)
-        : response._formData.append(entry, _entry$value);
+        : appendBackingEntry(response._formData, entry, _entry$value);
       iterator.next().then(progress, error);
     }
   }
@@ -3392,16 +3408,22 @@ exports.decodeReplyFromBusboy = function (busboyStream, turbopackMap, options) {
       );
     else {
       pendingFiles++;
-      var JSCompiler_object_inline_chunks_275 = [];
+      var JSCompiler_object_inline_chunks_284 = [];
       value.on("data", function (chunk) {
-        JSCompiler_object_inline_chunks_275.push(chunk);
+        JSCompiler_object_inline_chunks_284.push(chunk);
       });
       value.on("end", function () {
         try {
-          var blob = new Blob(JSCompiler_object_inline_chunks_275, {
-            type: mimeType
-          });
-          response._formData.append(name, blob, filename);
+          var blob = new Blob(JSCompiler_object_inline_chunks_284, {
+              type: mimeType
+            }),
+            backingStore = response._formData;
+          backingStore.data.append(name, blob, filename);
+          var keys = backingStore.keys;
+          null === keys
+            ? ((backingStore.keys = Array.from(backingStore.data.keys())),
+              (backingStore.keyPointer = 0))
+            : keys.push(name);
           pendingFiles--;
           if (0 === pendingFiles) {
             for (blob = 0; blob < queuedFields.length; blob += 2)
diff --git a/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-client.browser.development.js b/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-client.browser.development.js
index 806d815..a28026d 100644
--- a/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-client.browser.development.js
+++ b/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-client.browser.development.js
@@ -543,7 +543,7 @@
             null === formData && (formData = new FormData());
             var _data3 = formData;
             key = nextPartId++;
-            var prefix = formFieldPrefix + key + "_";
+            var prefix = formFieldPrefix + "_" + key + "_";
             value.forEach(function (originalValue, originalKey) {
               _data3.append(prefix + originalKey, originalValue);
             });
diff --git a/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-client.browser.production.js b/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-client.browser.production.js
index e3f9e2e..1650598 100644
--- a/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-client.browser.production.js
+++ b/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-client.browser.production.js
@@ -347,7 +347,7 @@ function processReply(
         null === formData && (formData = new FormData());
         var data$31 = formData;
         key = nextPartId++;
-        var prefix = formFieldPrefix + key + "_";
+        var prefix = formFieldPrefix + "_" + key + "_";
         value.forEach(function (originalValue, originalKey) {
           data$31.append(prefix + originalKey, originalValue);
         });
diff --git a/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-client.edge.development.js b/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-client.edge.development.js
index 395cd14..45f0d64 100644
--- a/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-client.edge.development.js
+++ b/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-client.edge.development.js
@@ -564,7 +564,7 @@
             null === formData && (formData = new FormData());
             var _data3 = formData;
             key = nextPartId++;
-            var prefix = formFieldPrefix + key + "_";
+            var prefix = formFieldPrefix + "_" + key + "_";
             value.forEach(function (originalValue, originalKey) {
               _data3.append(prefix + originalKey, originalValue);
             });
diff --git a/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-client.edge.production.js b/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-client.edge.production.js
index d3edae7..5d03152 100644
--- a/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-client.edge.production.js
+++ b/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-client.edge.production.js
@@ -359,7 +359,7 @@ function processReply(
         null === formData && (formData = new FormData());
         var data$31 = formData;
         key = nextPartId++;
-        var prefix = formFieldPrefix + key + "_";
+        var prefix = formFieldPrefix + "_" + key + "_";
         value.forEach(function (originalValue, originalKey) {
           data$31.append(prefix + originalKey, originalValue);
         });
diff --git a/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-client.node.development.js b/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-client.node.development.js
index ab432bd..868f28e 100644
--- a/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-client.node.development.js
+++ b/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-client.node.development.js
@@ -564,7 +564,7 @@
             null === formData && (formData = new FormData());
             var _data3 = formData;
             key = nextPartId++;
-            var prefix = formFieldPrefix + key + "_";
+            var prefix = formFieldPrefix + "_" + key + "_";
             value.forEach(function (originalValue, originalKey) {
               _data3.append(prefix + originalKey, originalValue);
             });
diff --git a/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-client.node.production.js b/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-client.node.production.js
index ac35378..e953488 100644
--- a/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-client.node.production.js
+++ b/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-client.node.production.js
@@ -360,7 +360,7 @@ function processReply(
         null === formData && (formData = new FormData());
         var data$31 = formData;
         key = nextPartId++;
-        var prefix = formFieldPrefix + key + "_";
+        var prefix = formFieldPrefix + "_" + key + "_";
         value.forEach(function (originalValue, originalKey) {
           data$31.append(prefix + originalKey, originalValue);
         });
diff --git a/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-server.browser.development.js b/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-server.browser.development.js
index d08aa9f..8ed1061 100644
--- a/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-server.browser.development.js
+++ b/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-server.browser.development.js
@@ -4072,7 +4072,7 @@
       var chunks = response._chunks,
         chunk = chunks.get(id);
       chunk ||
-        ((chunk = response._formData.get(response._prefix + id)),
+        ((chunk = response._formData.data.get(response._prefix + id)),
         (chunk =
           "string" === typeof chunk
             ? new ReactPromise(
@@ -4184,6 +4184,10 @@
         case "fulfilled":
           id = chunk.value;
           chunk = chunk.reason;
+          if (null !== chunk && "error" in chunk)
+            throw Error(
+              "Expected an initialized chunk but got an initialized stream chunk instead. This payload may have been submitted by an older version of React."
+            );
           for (
             var localLength = 0,
               rootArrayContexts = response._rootArrayContexts,
@@ -4276,22 +4280,19 @@
       if (!isArrayImpl(model)) throw Error("Invalid Map initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Map.");
       model.$$consumed = !0;
-      response = new Map(model);
-      return response;
+      return new Map(model);
     }
     function createSet(response, model) {
       if (!isArrayImpl(model)) throw Error("Invalid Set initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Set.");
       model.$$consumed = !0;
-      response = new Set(model);
-      return response;
+      return new Set(model);
     }
     function extractIterator(response, model) {
       if (!isArrayImpl(model)) throw Error("Invalid Iterator initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Iterator.");
       model.$$consumed = !0;
-      response = model[Symbol.iterator]();
-      return response;
+      return model[Symbol.iterator]();
     }
     function createModel(response, model, parentObject, key) {
       return "then" === key && "function" === typeof model ? null : model;
@@ -4329,7 +4330,7 @@
           Error("Already initialized typed array.")
         )
       );
-      reference = response._formData.get(key).arrayBuffer();
+      reference = response._formData.data.get(key).arrayBuffer();
       if (initializingHandler) {
         var handler = initializingHandler;
         handler.deps++;
@@ -4373,7 +4374,7 @@
       var chunks = response._chunks;
       stream = new ReactPromise("fulfilled", stream, controller);
       chunks.set(id, stream);
-      response = response._formData.getAll(response._prefix + id);
+      response = response._formData.data.getAll(response._prefix + id);
       for (id = 0; id < response.length; id++)
         (chunks = response[id]),
           "string" === typeof chunks &&
@@ -4593,24 +4594,33 @@
               getOutlinedModel(response, arrayRoot, obj, key, null, createSet)
             );
           case "K":
-            obj = value.slice(2);
-            obj = response._prefix + obj + "_";
-            key = new FormData();
-            response = response._formData;
-            arrayRoot = Array.from(response.keys());
-            for (value = 0; value < arrayRoot.length; value++)
-              if (((reference = arrayRoot[value]), reference.startsWith(obj))) {
+            key = value.slice(2);
+            obj = response._prefix + "_";
+            key = obj + key + "_";
+            arrayRoot = new FormData();
+            for (response = response._formData; ; ) {
+              value = response;
+              reference = value.keys;
+              null === reference &&
+                ((reference = value.keys = Array.from(value.data.keys())),
+                (value.keyPointer = 0));
+              value = reference[value.keyPointer];
+              if (void 0 === value) break;
+              if (value.startsWith(key)) {
+                reference = response.data.getAll(value);
                 for (
-                  var entries = response.getAll(reference),
-                    newKey = reference.slice(obj.length),
-                    j = 0;
-                  j < entries.length;
-                  j++
+                  var referencedFormDataKey = value.slice(key.length), i = 0;
+                  i < reference.length;
+                  i++
                 )
-                  key.append(newKey, entries[j]);
-                response.delete(reference);
-              }
-            return key;
+                  arrayRoot.append(referencedFormDataKey, reference[i]);
+                reference = response;
+                reference.data.delete(value);
+                reference.keyPointer++;
+              } else if (value.startsWith(obj)) break;
+              else response.keyPointer++;
+            }
+            return arrayRoot;
           case "i":
             return (
               (arrayRoot = value.slice(2)),
@@ -4779,7 +4789,7 @@
           case "B":
             return (
               (obj = parseInt(value.slice(2), 16)),
-              response._formData.get(response._prefix + obj)
+              response._formData.data.get(response._prefix + obj)
             );
           case "R":
             return parseReadableStream(response, value, void 0);
@@ -4818,7 +4828,7 @@
       return {
         _bundlerConfig: bundlerConfig,
         _prefix: formFieldPrefix,
-        _formData: backingFormData,
+        _formData: { data: backingFormData, keyPointer: -1, keys: null },
         _chunks: chunks,
         _closed: !1,
         _closedReason: null,
diff --git a/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-server.browser.production.js b/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-server.browser.production.js
index 8703906..f7303b6 100644
--- a/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-server.browser.production.js
+++ b/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-server.browser.production.js
@@ -2588,7 +2588,7 @@ function getChunk(response, id) {
   var chunks = response._chunks,
     chunk = chunks.get(id);
   chunk ||
-    ((chunk = response._formData.get(response._prefix + id)),
+    ((chunk = response._formData.data.get(response._prefix + id)),
     (chunk =
       "string" === typeof chunk
         ? createResolvedModelChunk(response, chunk, id)
@@ -2689,6 +2689,10 @@ function getOutlinedModel(
     case "fulfilled":
       id = chunk.value;
       chunk = chunk.reason;
+      if (null !== chunk && "error" in chunk)
+        throw Error(
+          "Expected an initialized chunk but got an initialized stream chunk instead. This payload may have been submitted by an older version of React."
+        );
       for (
         var localLength = 0,
           rootArrayContexts = response._rootArrayContexts,
@@ -2772,22 +2776,19 @@ function createMap(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Map initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Map.");
   model.$$consumed = !0;
-  response = new Map(model);
-  return response;
+  return new Map(model);
 }
 function createSet(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Set initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Set.");
   model.$$consumed = !0;
-  response = new Set(model);
-  return response;
+  return new Set(model);
 }
 function extractIterator(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Iterator initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Iterator.");
   model.$$consumed = !0;
-  response = model[Symbol.iterator]();
-  return response;
+  return model[Symbol.iterator]();
 }
 function createModel(response, model, parentObject, key) {
   return "then" === key && "function" === typeof model ? null : model;
@@ -2825,7 +2826,7 @@ function parseTypedArray(
       Error("Already initialized typed array.")
     )
   );
-  reference = response._formData.get(key).arrayBuffer();
+  reference = response._formData.data.get(key).arrayBuffer();
   if (initializingHandler) {
     var handler = initializingHandler;
     handler.deps++;
@@ -2869,7 +2870,7 @@ function resolveStream(response, id, stream, controller) {
   var chunks = response._chunks;
   stream = new ReactPromise("fulfilled", stream, controller);
   chunks.set(id, stream);
-  response = response._formData.getAll(response._prefix + id);
+  response = response._formData.data.getAll(response._prefix + id);
   for (id = 0; id < response.length; id++)
     (chunks = response[id]),
       "string" === typeof chunks &&
@@ -3084,24 +3085,31 @@ function parseModelString(response, obj, key, value, reference, arrayRoot) {
           getOutlinedModel(response, arrayRoot, obj, key, null, createSet)
         );
       case "K":
-        obj = value.slice(2);
-        obj = response._prefix + obj + "_";
-        key = new FormData();
-        response = response._formData;
-        arrayRoot = Array.from(response.keys());
-        for (value = 0; value < arrayRoot.length; value++)
-          if (((reference = arrayRoot[value]), reference.startsWith(obj))) {
+        key = value.slice(2);
+        obj = response._prefix + "_";
+        key = obj + key + "_";
+        arrayRoot = new FormData();
+        for (response = response._formData; ; ) {
+          value = response.keys;
+          null === value &&
+            ((value = response.keys = Array.from(response.data.keys())),
+            (response.keyPointer = 0));
+          value = value[response.keyPointer];
+          if (void 0 === value) break;
+          if (value.startsWith(key)) {
+            reference = response.data.getAll(value);
             for (
-              var entries = response.getAll(reference),
-                newKey = reference.slice(obj.length),
-                j = 0;
-              j < entries.length;
-              j++
+              var referencedFormDataKey = value.slice(key.length), i = 0;
+              i < reference.length;
+              i++
             )
-              key.append(newKey, entries[j]);
-            response.delete(reference);
-          }
-        return key;
+              arrayRoot.append(referencedFormDataKey, reference[i]);
+            response.data.delete(value);
+            response.keyPointer++;
+          } else if (value.startsWith(obj)) break;
+          else response.keyPointer++;
+        }
+        return arrayRoot;
       case "i":
         return (
           (arrayRoot = value.slice(2)),
@@ -3260,7 +3268,7 @@ function parseModelString(response, obj, key, value, reference, arrayRoot) {
       case "B":
         return (
           (obj = parseInt(value.slice(2), 16)),
-          response._formData.get(response._prefix + obj)
+          response._formData.data.get(response._prefix + obj)
         );
       case "R":
         return parseReadableStream(response, value, void 0);
@@ -3288,7 +3296,7 @@ function createResponse(bundlerConfig, formFieldPrefix, temporaryReferences) {
   return {
     _bundlerConfig: bundlerConfig,
     _prefix: formFieldPrefix,
-    _formData: backingFormData,
+    _formData: { data: backingFormData, keyPointer: -1, keys: null },
     _chunks: chunks,
     _closed: !1,
     _closedReason: null,
diff --git a/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-server.edge.development.js b/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-server.edge.development.js
index a9c5396..5dd5302 100644
--- a/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-server.edge.development.js
+++ b/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-server.edge.development.js
@@ -3842,6 +3842,14 @@
       if (hasOwnProperty.call(moduleExports, metadata[2]))
         return moduleExports[metadata[2]];
     }
+    function appendBackingEntry(backingStore, key, value) {
+      backingStore.data.append(key, value);
+      value = backingStore.keys;
+      null === value
+        ? ((backingStore.keys = Array.from(backingStore.data.keys())),
+          (backingStore.keyPointer = 0))
+        : value.push(key);
+    }
     function ReactPromise(status, value, reason) {
       this.status = status;
       this.value = value;
@@ -4154,7 +4162,7 @@
       var chunks = response._chunks,
         chunk = chunks.get(id);
       chunk ||
-        ((chunk = response._formData.get(response._prefix + id)),
+        ((chunk = response._formData.data.get(response._prefix + id)),
         (chunk =
           "string" === typeof chunk
             ? new ReactPromise(
@@ -4266,6 +4274,10 @@
         case "fulfilled":
           id = chunk.value;
           chunk = chunk.reason;
+          if (null !== chunk && "error" in chunk)
+            throw Error(
+              "Expected an initialized chunk but got an initialized stream chunk instead. This payload may have been submitted by an older version of React."
+            );
           for (
             var localLength = 0,
               rootArrayContexts = response._rootArrayContexts,
@@ -4358,22 +4370,19 @@
       if (!isArrayImpl(model)) throw Error("Invalid Map initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Map.");
       model.$$consumed = !0;
-      response = new Map(model);
-      return response;
+      return new Map(model);
     }
     function createSet(response, model) {
       if (!isArrayImpl(model)) throw Error("Invalid Set initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Set.");
       model.$$consumed = !0;
-      response = new Set(model);
-      return response;
+      return new Set(model);
     }
     function extractIterator(response, model) {
       if (!isArrayImpl(model)) throw Error("Invalid Iterator initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Iterator.");
       model.$$consumed = !0;
-      response = model[Symbol.iterator]();
-      return response;
+      return model[Symbol.iterator]();
     }
     function createModel(response, model, parentObject, key) {
       return "then" === key && "function" === typeof model ? null : model;
@@ -4411,7 +4420,7 @@
           Error("Already initialized typed array.")
         )
       );
-      reference = response._formData.get(key).arrayBuffer();
+      reference = response._formData.data.get(key).arrayBuffer();
       if (initializingHandler) {
         var handler = initializingHandler;
         handler.deps++;
@@ -4455,7 +4464,7 @@
       var chunks = response._chunks;
       stream = new ReactPromise("fulfilled", stream, controller);
       chunks.set(id, stream);
-      response = response._formData.getAll(response._prefix + id);
+      response = response._formData.data.getAll(response._prefix + id);
       for (id = 0; id < response.length; id++)
         (chunks = response[id]),
           "string" === typeof chunks &&
@@ -4675,24 +4684,33 @@
               getOutlinedModel(response, arrayRoot, obj, key, null, createSet)
             );
           case "K":
-            obj = value.slice(2);
-            obj = response._prefix + obj + "_";
-            key = new FormData();
-            response = response._formData;
-            arrayRoot = Array.from(response.keys());
-            for (value = 0; value < arrayRoot.length; value++)
-              if (((reference = arrayRoot[value]), reference.startsWith(obj))) {
+            key = value.slice(2);
+            obj = response._prefix + "_";
+            key = obj + key + "_";
+            arrayRoot = new FormData();
+            for (response = response._formData; ; ) {
+              value = response;
+              reference = value.keys;
+              null === reference &&
+                ((reference = value.keys = Array.from(value.data.keys())),
+                (value.keyPointer = 0));
+              value = reference[value.keyPointer];
+              if (void 0 === value) break;
+              if (value.startsWith(key)) {
+                reference = response.data.getAll(value);
                 for (
-                  var entries = response.getAll(reference),
-                    newKey = reference.slice(obj.length),
-                    j = 0;
-                  j < entries.length;
-                  j++
+                  var referencedFormDataKey = value.slice(key.length), i = 0;
+                  i < reference.length;
+                  i++
                 )
-                  key.append(newKey, entries[j]);
-                response.delete(reference);
-              }
-            return key;
+                  arrayRoot.append(referencedFormDataKey, reference[i]);
+                reference = response;
+                reference.data.delete(value);
+                reference.keyPointer++;
+              } else if (value.startsWith(obj)) break;
+              else response.keyPointer++;
+            }
+            return arrayRoot;
           case "i":
             return (
               (arrayRoot = value.slice(2)),
@@ -4861,7 +4879,7 @@
           case "B":
             return (
               (obj = parseInt(value.slice(2), 16)),
-              response._formData.get(response._prefix + obj)
+              response._formData.data.get(response._prefix + obj)
             );
           case "R":
             return parseReadableStream(response, value, void 0);
@@ -4900,7 +4918,7 @@
       return {
         _bundlerConfig: bundlerConfig,
         _prefix: formFieldPrefix,
-        _formData: backingFormData,
+        _formData: { data: backingFormData, keyPointer: -1, keys: null },
         _chunks: chunks,
         _closed: !1,
         _closedReason: null,
@@ -5660,7 +5678,7 @@
           entry = entry[1];
           if ("string" === typeof entry) {
             var response = response$jscomp$0;
-            response._formData.append(name, entry);
+            appendBackingEntry(response._formData, name, entry);
             var prefix = response._prefix;
             if (name.startsWith(prefix)) {
               var chunks = response._chunks;
@@ -5668,7 +5686,7 @@
               (chunks = chunks.get(name)) &&
                 resolveModelChunk(response, chunks, entry, name);
             }
-          } else response$jscomp$0._formData.append(name, entry);
+          } else appendBackingEntry(response$jscomp$0._formData, name, entry);
           iterator.next().then(progress, error);
         }
       }
diff --git a/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-server.edge.production.js b/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-server.edge.production.js
index a245f63..f06a7c8 100644
--- a/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-server.edge.production.js
+++ b/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-server.edge.production.js
@@ -2214,6 +2214,14 @@ function requireModule(metadata) {
   if (hasOwnProperty.call(moduleExports, metadata[2]))
     return moduleExports[metadata[2]];
 }
+function appendBackingEntry(backingStore, key, value) {
+  backingStore.data.append(key, value);
+  value = backingStore.keys;
+  null === value
+    ? ((backingStore.keys = Array.from(backingStore.data.keys())),
+      (backingStore.keyPointer = 0))
+    : value.push(key);
+}
 var RESPONSE_SYMBOL = Symbol();
 function ReactPromise(status, value, reason) {
   this.status = status;
@@ -2586,7 +2594,7 @@ function getChunk(response, id) {
   var chunks = response._chunks,
     chunk = chunks.get(id);
   chunk ||
-    ((chunk = response._formData.get(response._prefix + id)),
+    ((chunk = response._formData.data.get(response._prefix + id)),
     (chunk =
       "string" === typeof chunk
         ? createResolvedModelChunk(response, chunk, id)
@@ -2687,6 +2695,10 @@ function getOutlinedModel(
     case "fulfilled":
       id = chunk.value;
       chunk = chunk.reason;
+      if (null !== chunk && "error" in chunk)
+        throw Error(
+          "Expected an initialized chunk but got an initialized stream chunk instead. This payload may have been submitted by an older version of React."
+        );
       for (
         var localLength = 0,
           rootArrayContexts = response._rootArrayContexts,
@@ -2770,22 +2782,19 @@ function createMap(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Map initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Map.");
   model.$$consumed = !0;
-  response = new Map(model);
-  return response;
+  return new Map(model);
 }
 function createSet(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Set initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Set.");
   model.$$consumed = !0;
-  response = new Set(model);
-  return response;
+  return new Set(model);
 }
 function extractIterator(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Iterator initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Iterator.");
   model.$$consumed = !0;
-  response = model[Symbol.iterator]();
-  return response;
+  return model[Symbol.iterator]();
 }
 function createModel(response, model, parentObject, key) {
   return "then" === key && "function" === typeof model ? null : model;
@@ -2823,7 +2832,7 @@ function parseTypedArray(
       Error("Already initialized typed array.")
     )
   );
-  reference = response._formData.get(key).arrayBuffer();
+  reference = response._formData.data.get(key).arrayBuffer();
   if (initializingHandler) {
     var handler = initializingHandler;
     handler.deps++;
@@ -2867,7 +2876,7 @@ function resolveStream(response, id, stream, controller) {
   var chunks = response._chunks;
   stream = new ReactPromise("fulfilled", stream, controller);
   chunks.set(id, stream);
-  response = response._formData.getAll(response._prefix + id);
+  response = response._formData.data.getAll(response._prefix + id);
   for (id = 0; id < response.length; id++)
     (chunks = response[id]),
       "string" === typeof chunks &&
@@ -3082,24 +3091,31 @@ function parseModelString(response, obj, key, value, reference, arrayRoot) {
           getOutlinedModel(response, arrayRoot, obj, key, null, createSet)
         );
       case "K":
-        obj = value.slice(2);
-        obj = response._prefix + obj + "_";
-        key = new FormData();
-        response = response._formData;
-        arrayRoot = Array.from(response.keys());
-        for (value = 0; value < arrayRoot.length; value++)
-          if (((reference = arrayRoot[value]), reference.startsWith(obj))) {
+        key = value.slice(2);
+        obj = response._prefix + "_";
+        key = obj + key + "_";
+        arrayRoot = new FormData();
+        for (response = response._formData; ; ) {
+          value = response.keys;
+          null === value &&
+            ((value = response.keys = Array.from(response.data.keys())),
+            (response.keyPointer = 0));
+          value = value[response.keyPointer];
+          if (void 0 === value) break;
+          if (value.startsWith(key)) {
+            reference = response.data.getAll(value);
             for (
-              var entries = response.getAll(reference),
-                newKey = reference.slice(obj.length),
-                j = 0;
-              j < entries.length;
-              j++
+              var referencedFormDataKey = value.slice(key.length), i = 0;
+              i < reference.length;
+              i++
             )
-              key.append(newKey, entries[j]);
-            response.delete(reference);
-          }
-        return key;
+              arrayRoot.append(referencedFormDataKey, reference[i]);
+            response.data.delete(value);
+            response.keyPointer++;
+          } else if (value.startsWith(obj)) break;
+          else response.keyPointer++;
+        }
+        return arrayRoot;
       case "i":
         return (
           (arrayRoot = value.slice(2)),
@@ -3258,7 +3274,7 @@ function parseModelString(response, obj, key, value, reference, arrayRoot) {
       case "B":
         return (
           (obj = parseInt(value.slice(2), 16)),
-          response._formData.get(response._prefix + obj)
+          response._formData.data.get(response._prefix + obj)
         );
       case "R":
         return parseReadableStream(response, value, void 0);
@@ -3286,7 +3302,7 @@ function createResponse(bundlerConfig, formFieldPrefix, temporaryReferences) {
   return {
     _bundlerConfig: bundlerConfig,
     _prefix: formFieldPrefix,
-    _formData: backingFormData,
+    _formData: { data: backingFormData, keyPointer: -1, keys: null },
     _chunks: chunks,
     _closed: !1,
     _closedReason: null,
@@ -3422,7 +3438,7 @@ exports.decodeReplyFromAsyncIterable = function (
       var name = entry[0];
       entry = entry[1];
       if ("string" === typeof entry) {
-        response._formData.append(name, entry);
+        appendBackingEntry(response._formData, name, entry);
         var prefix = response._prefix;
         if (name.startsWith(prefix)) {
           var chunks = response._chunks;
@@ -3430,7 +3446,7 @@ exports.decodeReplyFromAsyncIterable = function (
           (chunks = chunks.get(name)) &&
             resolveModelChunk(response, chunks, entry, name);
         }
-      } else response._formData.append(name, entry);
+      } else appendBackingEntry(response._formData, name, entry);
       iterator.next().then(progress, error);
     }
   }
diff --git a/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-server.node.development.js b/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-server.node.development.js
index ed7ec01..8327ea8 100644
--- a/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-server.node.development.js
+++ b/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-server.node.development.js
@@ -4238,6 +4238,14 @@
       if (hasOwnProperty.call(moduleExports, metadata[2]))
         return moduleExports[metadata[2]];
     }
+    function appendBackingEntry(backingStore, key, value) {
+      backingStore.data.append(key, value);
+      value = backingStore.keys;
+      null === value
+        ? ((backingStore.keys = Array.from(backingStore.data.keys())),
+          (backingStore.keyPointer = 0))
+        : value.push(key);
+    }
     function ReactPromise(status, value, reason) {
       this.status = status;
       this.value = value;
@@ -4550,7 +4558,7 @@
       var chunks = response._chunks,
         chunk = chunks.get(id);
       chunk ||
-        ((chunk = response._formData.get(response._prefix + id)),
+        ((chunk = response._formData.data.get(response._prefix + id)),
         (chunk =
           "string" === typeof chunk
             ? new ReactPromise(
@@ -4662,6 +4670,10 @@
         case "fulfilled":
           id = chunk.value;
           chunk = chunk.reason;
+          if (null !== chunk && "error" in chunk)
+            throw Error(
+              "Expected an initialized chunk but got an initialized stream chunk instead. This payload may have been submitted by an older version of React."
+            );
           for (
             var localLength = 0,
               rootArrayContexts = response._rootArrayContexts,
@@ -4754,22 +4766,19 @@
       if (!isArrayImpl(model)) throw Error("Invalid Map initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Map.");
       model.$$consumed = !0;
-      response = new Map(model);
-      return response;
+      return new Map(model);
     }
     function createSet(response, model) {
       if (!isArrayImpl(model)) throw Error("Invalid Set initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Set.");
       model.$$consumed = !0;
-      response = new Set(model);
-      return response;
+      return new Set(model);
     }
     function extractIterator(response, model) {
       if (!isArrayImpl(model)) throw Error("Invalid Iterator initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Iterator.");
       model.$$consumed = !0;
-      response = model[Symbol.iterator]();
-      return response;
+      return model[Symbol.iterator]();
     }
     function createModel(response, model, parentObject, key) {
       return "then" === key && "function" === typeof model ? null : model;
@@ -4807,7 +4816,7 @@
           Error("Already initialized typed array.")
         )
       );
-      reference = response._formData.get(key).arrayBuffer();
+      reference = response._formData.data.get(key).arrayBuffer();
       if (initializingHandler) {
         var handler = initializingHandler;
         handler.deps++;
@@ -4851,7 +4860,7 @@
       var chunks = response._chunks;
       stream = new ReactPromise("fulfilled", stream, controller);
       chunks.set(id, stream);
-      response = response._formData.getAll(response._prefix + id);
+      response = response._formData.data.getAll(response._prefix + id);
       for (id = 0; id < response.length; id++)
         (chunks = response[id]),
           "string" === typeof chunks &&
@@ -5071,24 +5080,33 @@
               getOutlinedModel(response, arrayRoot, obj, key, null, createSet)
             );
           case "K":
-            obj = value.slice(2);
-            obj = response._prefix + obj + "_";
-            key = new FormData();
-            response = response._formData;
-            arrayRoot = Array.from(response.keys());
-            for (value = 0; value < arrayRoot.length; value++)
-              if (((reference = arrayRoot[value]), reference.startsWith(obj))) {
+            key = value.slice(2);
+            obj = response._prefix + "_";
+            key = obj + key + "_";
+            arrayRoot = new FormData();
+            for (response = response._formData; ; ) {
+              value = response;
+              reference = value.keys;
+              null === reference &&
+                ((reference = value.keys = Array.from(value.data.keys())),
+                (value.keyPointer = 0));
+              value = reference[value.keyPointer];
+              if (void 0 === value) break;
+              if (value.startsWith(key)) {
+                reference = response.data.getAll(value);
                 for (
-                  var entries = response.getAll(reference),
-                    newKey = reference.slice(obj.length),
-                    j = 0;
-                  j < entries.length;
-                  j++
+                  var referencedFormDataKey = value.slice(key.length), i = 0;
+                  i < reference.length;
+                  i++
                 )
-                  key.append(newKey, entries[j]);
-                response.delete(reference);
-              }
-            return key;
+                  arrayRoot.append(referencedFormDataKey, reference[i]);
+                reference = response;
+                reference.data.delete(value);
+                reference.keyPointer++;
+              } else if (value.startsWith(obj)) break;
+              else response.keyPointer++;
+            }
+            return arrayRoot;
           case "i":
             return (
               (arrayRoot = value.slice(2)),
@@ -5257,7 +5275,7 @@
           case "B":
             return (
               (obj = parseInt(value.slice(2), 16)),
-              response._formData.get(response._prefix + obj)
+              response._formData.data.get(response._prefix + obj)
             );
           case "R":
             return parseReadableStream(response, value, void 0);
@@ -5296,7 +5314,7 @@
       return {
         _bundlerConfig: bundlerConfig,
         _prefix: formFieldPrefix,
-        _formData: backingFormData,
+        _formData: { data: backingFormData, keyPointer: -1, keys: null },
         _chunks: chunks,
         _closed: !1,
         _closedReason: null,
@@ -5306,7 +5324,7 @@
       };
     }
     function resolveField(response, key, value) {
-      response._formData.append(key, value);
+      appendBackingEntry(response._formData, key, value);
       var prefix = response._prefix;
       if (key.startsWith(prefix)) {
         var chunks = response._chunks;
@@ -6279,7 +6297,7 @@
           _entry$value = _entry$value[1];
           "string" === typeof _entry$value
             ? resolveField(response, entry, _entry$value)
-            : response._formData.append(entry, _entry$value);
+            : appendBackingEntry(response._formData, entry, _entry$value);
           iterator.next().then(progress, error);
         }
       }
@@ -6333,16 +6351,22 @@
           );
         else {
           pendingFiles++;
-          var JSCompiler_object_inline_chunks_252 = [];
+          var JSCompiler_object_inline_chunks_260 = [];
           value.on("data", function (chunk) {
-            JSCompiler_object_inline_chunks_252.push(chunk);
+            JSCompiler_object_inline_chunks_260.push(chunk);
           });
           value.on("end", function () {
             try {
-              var blob = new Blob(JSCompiler_object_inline_chunks_252, {
-                type: mimeType
-              });
-              response._formData.append(name, blob, filename);
+              var blob = new Blob(JSCompiler_object_inline_chunks_260, {
+                  type: mimeType
+                }),
+                backingStore = response._formData;
+              backingStore.data.append(name, blob, filename);
+              var keys = backingStore.keys;
+              null === keys
+                ? ((backingStore.keys = Array.from(backingStore.data.keys())),
+                  (backingStore.keyPointer = 0))
+                : keys.push(name);
               pendingFiles--;
               if (0 === pendingFiles) {
                 for (blob = 0; blob < queuedFields.length; blob += 2)
diff --git a/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-server.node.production.js b/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-server.node.production.js
index d15d355..31c8a02 100644
--- a/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-server.node.production.js
+++ b/dist/compiled/react-server-dom-webpack-experimental/cjs/react-server-dom-webpack-server.node.production.js
@@ -2239,6 +2239,14 @@ function requireModule(metadata) {
   if (hasOwnProperty.call(moduleExports, metadata[2]))
     return moduleExports[metadata[2]];
 }
+function appendBackingEntry(backingStore, key, value) {
+  backingStore.data.append(key, value);
+  value = backingStore.keys;
+  null === value
+    ? ((backingStore.keys = Array.from(backingStore.data.keys())),
+      (backingStore.keyPointer = 0))
+    : value.push(key);
+}
 var RESPONSE_SYMBOL = Symbol();
 function ReactPromise(status, value, reason) {
   this.status = status;
@@ -2611,7 +2619,7 @@ function getChunk(response, id) {
   var chunks = response._chunks,
     chunk = chunks.get(id);
   chunk ||
-    ((chunk = response._formData.get(response._prefix + id)),
+    ((chunk = response._formData.data.get(response._prefix + id)),
     (chunk =
       "string" === typeof chunk
         ? createResolvedModelChunk(response, chunk, id)
@@ -2712,6 +2720,10 @@ function getOutlinedModel(
     case "fulfilled":
       id = chunk.value;
       chunk = chunk.reason;
+      if (null !== chunk && "error" in chunk)
+        throw Error(
+          "Expected an initialized chunk but got an initialized stream chunk instead. This payload may have been submitted by an older version of React."
+        );
       for (
         var localLength = 0,
           rootArrayContexts = response._rootArrayContexts,
@@ -2795,22 +2807,19 @@ function createMap(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Map initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Map.");
   model.$$consumed = !0;
-  response = new Map(model);
-  return response;
+  return new Map(model);
 }
 function createSet(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Set initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Set.");
   model.$$consumed = !0;
-  response = new Set(model);
-  return response;
+  return new Set(model);
 }
 function extractIterator(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Iterator initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Iterator.");
   model.$$consumed = !0;
-  response = model[Symbol.iterator]();
-  return response;
+  return model[Symbol.iterator]();
 }
 function createModel(response, model, parentObject, key) {
   return "then" === key && "function" === typeof model ? null : model;
@@ -2848,7 +2857,7 @@ function parseTypedArray(
       Error("Already initialized typed array.")
     )
   );
-  reference = response._formData.get(key).arrayBuffer();
+  reference = response._formData.data.get(key).arrayBuffer();
   if (initializingHandler) {
     var handler = initializingHandler;
     handler.deps++;
@@ -2892,7 +2901,7 @@ function resolveStream(response, id, stream, controller) {
   var chunks = response._chunks;
   stream = new ReactPromise("fulfilled", stream, controller);
   chunks.set(id, stream);
-  response = response._formData.getAll(response._prefix + id);
+  response = response._formData.data.getAll(response._prefix + id);
   for (id = 0; id < response.length; id++)
     (chunks = response[id]),
       "string" === typeof chunks &&
@@ -3107,24 +3116,31 @@ function parseModelString(response, obj, key, value, reference, arrayRoot) {
           getOutlinedModel(response, arrayRoot, obj, key, null, createSet)
         );
       case "K":
-        obj = value.slice(2);
-        obj = response._prefix + obj + "_";
-        key = new FormData();
-        response = response._formData;
-        arrayRoot = Array.from(response.keys());
-        for (value = 0; value < arrayRoot.length; value++)
-          if (((reference = arrayRoot[value]), reference.startsWith(obj))) {
+        key = value.slice(2);
+        obj = response._prefix + "_";
+        key = obj + key + "_";
+        arrayRoot = new FormData();
+        for (response = response._formData; ; ) {
+          value = response.keys;
+          null === value &&
+            ((value = response.keys = Array.from(response.data.keys())),
+            (response.keyPointer = 0));
+          value = value[response.keyPointer];
+          if (void 0 === value) break;
+          if (value.startsWith(key)) {
+            reference = response.data.getAll(value);
             for (
-              var entries = response.getAll(reference),
-                newKey = reference.slice(obj.length),
-                j = 0;
-              j < entries.length;
-              j++
+              var referencedFormDataKey = value.slice(key.length), i = 0;
+              i < reference.length;
+              i++
             )
-              key.append(newKey, entries[j]);
-            response.delete(reference);
-          }
-        return key;
+              arrayRoot.append(referencedFormDataKey, reference[i]);
+            response.data.delete(value);
+            response.keyPointer++;
+          } else if (value.startsWith(obj)) break;
+          else response.keyPointer++;
+        }
+        return arrayRoot;
       case "i":
         return (
           (arrayRoot = value.slice(2)),
@@ -3283,7 +3299,7 @@ function parseModelString(response, obj, key, value, reference, arrayRoot) {
       case "B":
         return (
           (obj = parseInt(value.slice(2), 16)),
-          response._formData.get(response._prefix + obj)
+          response._formData.data.get(response._prefix + obj)
         );
       case "R":
         return parseReadableStream(response, value, void 0);
@@ -3311,7 +3327,7 @@ function createResponse(bundlerConfig, formFieldPrefix, temporaryReferences) {
   return {
     _bundlerConfig: bundlerConfig,
     _prefix: formFieldPrefix,
-    _formData: backingFormData,
+    _formData: { data: backingFormData, keyPointer: -1, keys: null },
     _chunks: chunks,
     _closed: !1,
     _closedReason: null,
@@ -3321,7 +3337,7 @@ function createResponse(bundlerConfig, formFieldPrefix, temporaryReferences) {
   };
 }
 function resolveField(response, key, value) {
-  response._formData.append(key, value);
+  appendBackingEntry(response._formData, key, value);
   var prefix = response._prefix;
   if (key.startsWith(prefix)) {
     var chunks = response._chunks;
@@ -3499,7 +3515,7 @@ exports.decodeReplyFromAsyncIterable = function (
       _entry$value = _entry$value[1];
       "string" === typeof _entry$value
         ? resolveField(response, entry, _entry$value)
-        : response._formData.append(entry, _entry$value);
+        : appendBackingEntry(response._formData, entry, _entry$value);
       iterator.next().then(progress, error);
     }
   }
@@ -3549,16 +3565,22 @@ exports.decodeReplyFromBusboy = function (busboyStream, webpackMap, options) {
       );
     else {
       pendingFiles++;
-      var JSCompiler_object_inline_chunks_285 = [];
+      var JSCompiler_object_inline_chunks_294 = [];
       value.on("data", function (chunk) {
-        JSCompiler_object_inline_chunks_285.push(chunk);
+        JSCompiler_object_inline_chunks_294.push(chunk);
       });
       value.on("end", function () {
         try {
-          var blob = new Blob(JSCompiler_object_inline_chunks_285, {
-            type: mimeType
-          });
-          response._formData.append(name, blob, filename);
+          var blob = new Blob(JSCompiler_object_inline_chunks_294, {
+              type: mimeType
+            }),
+            backingStore = response._formData;
+          backingStore.data.append(name, blob, filename);
+          var keys = backingStore.keys;
+          null === keys
+            ? ((backingStore.keys = Array.from(backingStore.data.keys())),
+              (backingStore.keyPointer = 0))
+            : keys.push(name);
           pendingFiles--;
           if (0 === pendingFiles) {
             for (blob = 0; blob < queuedFields.length; blob += 2)
diff --git a/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-client.browser.development.js b/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-client.browser.development.js
index 30dd7c9..193c686 100644
--- a/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-client.browser.development.js
+++ b/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-client.browser.development.js
@@ -541,7 +541,7 @@
             null === formData && (formData = new FormData());
             var _data3 = formData;
             key = nextPartId++;
-            var prefix = formFieldPrefix + key + "_";
+            var prefix = formFieldPrefix + "_" + key + "_";
             value.forEach(function (originalValue, originalKey) {
               _data3.append(prefix + originalKey, originalValue);
             });
@@ -3510,10 +3510,10 @@
       return hook.checkDCE ? !0 : !1;
     })({
       bundleType: 1,
-      version: "19.2.0-canary-9aa4f723-20260123",
+      version: "19.2.0-canary-b8e10c8a-20260420",
       rendererPackageName: "react-server-dom-webpack",
       currentDispatcherRef: ReactSharedInternals,
-      reconcilerVersion: "19.2.0-canary-9aa4f723-20260123",
+      reconcilerVersion: "19.2.0-canary-b8e10c8a-20260420",
       getCurrentComponentInfo: function () {
         return currentOwnerInDEV;
       }
diff --git a/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-client.browser.production.js b/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-client.browser.production.js
index 09412b2..31033d8 100644
--- a/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-client.browser.production.js
+++ b/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-client.browser.production.js
@@ -346,7 +346,7 @@ function processReply(
         null === formData && (formData = new FormData());
         var data$31 = formData;
         key = nextPartId++;
-        var prefix = formFieldPrefix + key + "_";
+        var prefix = formFieldPrefix + "_" + key + "_";
         value.forEach(function (originalValue, originalKey) {
           data$31.append(prefix + originalKey, originalValue);
         });
diff --git a/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-client.edge.development.js b/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-client.edge.development.js
index fd696ee..3bf052a 100644
--- a/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-client.edge.development.js
+++ b/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-client.edge.development.js
@@ -562,7 +562,7 @@
             null === formData && (formData = new FormData());
             var _data3 = formData;
             key = nextPartId++;
-            var prefix = formFieldPrefix + key + "_";
+            var prefix = formFieldPrefix + "_" + key + "_";
             value.forEach(function (originalValue, originalKey) {
               _data3.append(prefix + originalKey, originalValue);
             });
diff --git a/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-client.edge.production.js b/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-client.edge.production.js
index d83816b..0d8f1a3 100644
--- a/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-client.edge.production.js
+++ b/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-client.edge.production.js
@@ -358,7 +358,7 @@ function processReply(
         null === formData && (formData = new FormData());
         var data$31 = formData;
         key = nextPartId++;
-        var prefix = formFieldPrefix + key + "_";
+        var prefix = formFieldPrefix + "_" + key + "_";
         value.forEach(function (originalValue, originalKey) {
           data$31.append(prefix + originalKey, originalValue);
         });
diff --git a/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-client.node.development.js b/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-client.node.development.js
index 1985828..b2922de 100644
--- a/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-client.node.development.js
+++ b/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-client.node.development.js
@@ -562,7 +562,7 @@
             null === formData && (formData = new FormData());
             var _data3 = formData;
             key = nextPartId++;
-            var prefix = formFieldPrefix + key + "_";
+            var prefix = formFieldPrefix + "_" + key + "_";
             value.forEach(function (originalValue, originalKey) {
               _data3.append(prefix + originalKey, originalValue);
             });
diff --git a/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-client.node.production.js b/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-client.node.production.js
index 8a3fb2c..2ebca50 100644
--- a/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-client.node.production.js
+++ b/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-client.node.production.js
@@ -359,7 +359,7 @@ function processReply(
         null === formData && (formData = new FormData());
         var data$31 = formData;
         key = nextPartId++;
-        var prefix = formFieldPrefix + key + "_";
+        var prefix = formFieldPrefix + "_" + key + "_";
         value.forEach(function (originalValue, originalKey) {
           data$31.append(prefix + originalKey, originalValue);
         });
diff --git a/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-server.browser.development.js b/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-server.browser.development.js
index ad1394b..471e94b 100644
--- a/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-server.browser.development.js
+++ b/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-server.browser.development.js
@@ -3888,7 +3888,7 @@
       var chunks = response._chunks,
         chunk = chunks.get(id);
       chunk ||
-        ((chunk = response._formData.get(response._prefix + id)),
+        ((chunk = response._formData.data.get(response._prefix + id)),
         (chunk =
           "string" === typeof chunk
             ? new ReactPromise(
@@ -4000,6 +4000,10 @@
         case "fulfilled":
           id = chunk.value;
           chunk = chunk.reason;
+          if (null !== chunk && "error" in chunk)
+            throw Error(
+              "Expected an initialized chunk but got an initialized stream chunk instead. This payload may have been submitted by an older version of React."
+            );
           for (
             var localLength = 0,
               rootArrayContexts = response._rootArrayContexts,
@@ -4092,22 +4096,19 @@
       if (!isArrayImpl(model)) throw Error("Invalid Map initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Map.");
       model.$$consumed = !0;
-      response = new Map(model);
-      return response;
+      return new Map(model);
     }
     function createSet(response, model) {
       if (!isArrayImpl(model)) throw Error("Invalid Set initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Set.");
       model.$$consumed = !0;
-      response = new Set(model);
-      return response;
+      return new Set(model);
     }
     function extractIterator(response, model) {
       if (!isArrayImpl(model)) throw Error("Invalid Iterator initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Iterator.");
       model.$$consumed = !0;
-      response = model[Symbol.iterator]();
-      return response;
+      return model[Symbol.iterator]();
     }
     function createModel(response, model, parentObject, key) {
       return "then" === key && "function" === typeof model ? null : model;
@@ -4145,7 +4146,7 @@
           Error("Already initialized typed array.")
         )
       );
-      reference = response._formData.get(key).arrayBuffer();
+      reference = response._formData.data.get(key).arrayBuffer();
       if (initializingHandler) {
         var handler = initializingHandler;
         handler.deps++;
@@ -4189,7 +4190,7 @@
       var chunks = response._chunks;
       stream = new ReactPromise("fulfilled", stream, controller);
       chunks.set(id, stream);
-      response = response._formData.getAll(response._prefix + id);
+      response = response._formData.data.getAll(response._prefix + id);
       for (id = 0; id < response.length; id++)
         (chunks = response[id]),
           "string" === typeof chunks &&
@@ -4409,24 +4410,33 @@
               getOutlinedModel(response, arrayRoot, obj, key, null, createSet)
             );
           case "K":
-            obj = value.slice(2);
-            obj = response._prefix + obj + "_";
-            key = new FormData();
-            response = response._formData;
-            arrayRoot = Array.from(response.keys());
-            for (value = 0; value < arrayRoot.length; value++)
-              if (((reference = arrayRoot[value]), reference.startsWith(obj))) {
+            key = value.slice(2);
+            obj = response._prefix + "_";
+            key = obj + key + "_";
+            arrayRoot = new FormData();
+            for (response = response._formData; ; ) {
+              value = response;
+              reference = value.keys;
+              null === reference &&
+                ((reference = value.keys = Array.from(value.data.keys())),
+                (value.keyPointer = 0));
+              value = reference[value.keyPointer];
+              if (void 0 === value) break;
+              if (value.startsWith(key)) {
+                reference = response.data.getAll(value);
                 for (
-                  var entries = response.getAll(reference),
-                    newKey = reference.slice(obj.length),
-                    j = 0;
-                  j < entries.length;
-                  j++
+                  var referencedFormDataKey = value.slice(key.length), i = 0;
+                  i < reference.length;
+                  i++
                 )
-                  key.append(newKey, entries[j]);
-                response.delete(reference);
-              }
-            return key;
+                  arrayRoot.append(referencedFormDataKey, reference[i]);
+                reference = response;
+                reference.data.delete(value);
+                reference.keyPointer++;
+              } else if (value.startsWith(obj)) break;
+              else response.keyPointer++;
+            }
+            return arrayRoot;
           case "i":
             return (
               (arrayRoot = value.slice(2)),
@@ -4595,7 +4605,7 @@
           case "B":
             return (
               (obj = parseInt(value.slice(2), 16)),
-              response._formData.get(response._prefix + obj)
+              response._formData.data.get(response._prefix + obj)
             );
           case "R":
             return parseReadableStream(response, value, void 0);
@@ -4634,7 +4644,7 @@
       return {
         _bundlerConfig: bundlerConfig,
         _prefix: formFieldPrefix,
-        _formData: backingFormData,
+        _formData: { data: backingFormData, keyPointer: -1, keys: null },
         _chunks: chunks,
         _closed: !1,
         _closedReason: null,
diff --git a/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-server.browser.production.js b/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-server.browser.production.js
index 10d64a2..c87fc1d 100644
--- a/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-server.browser.production.js
+++ b/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-server.browser.production.js
@@ -2433,7 +2433,7 @@ function getChunk(response, id) {
   var chunks = response._chunks,
     chunk = chunks.get(id);
   chunk ||
-    ((chunk = response._formData.get(response._prefix + id)),
+    ((chunk = response._formData.data.get(response._prefix + id)),
     (chunk =
       "string" === typeof chunk
         ? createResolvedModelChunk(response, chunk, id)
@@ -2534,6 +2534,10 @@ function getOutlinedModel(
     case "fulfilled":
       id = chunk.value;
       chunk = chunk.reason;
+      if (null !== chunk && "error" in chunk)
+        throw Error(
+          "Expected an initialized chunk but got an initialized stream chunk instead. This payload may have been submitted by an older version of React."
+        );
       for (
         var localLength = 0,
           rootArrayContexts = response._rootArrayContexts,
@@ -2617,22 +2621,19 @@ function createMap(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Map initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Map.");
   model.$$consumed = !0;
-  response = new Map(model);
-  return response;
+  return new Map(model);
 }
 function createSet(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Set initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Set.");
   model.$$consumed = !0;
-  response = new Set(model);
-  return response;
+  return new Set(model);
 }
 function extractIterator(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Iterator initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Iterator.");
   model.$$consumed = !0;
-  response = model[Symbol.iterator]();
-  return response;
+  return model[Symbol.iterator]();
 }
 function createModel(response, model, parentObject, key) {
   return "then" === key && "function" === typeof model ? null : model;
@@ -2670,7 +2671,7 @@ function parseTypedArray(
       Error("Already initialized typed array.")
     )
   );
-  reference = response._formData.get(key).arrayBuffer();
+  reference = response._formData.data.get(key).arrayBuffer();
   if (initializingHandler) {
     var handler = initializingHandler;
     handler.deps++;
@@ -2714,7 +2715,7 @@ function resolveStream(response, id, stream, controller) {
   var chunks = response._chunks;
   stream = new ReactPromise("fulfilled", stream, controller);
   chunks.set(id, stream);
-  response = response._formData.getAll(response._prefix + id);
+  response = response._formData.data.getAll(response._prefix + id);
   for (id = 0; id < response.length; id++)
     (chunks = response[id]),
       "string" === typeof chunks &&
@@ -2929,24 +2930,31 @@ function parseModelString(response, obj, key, value, reference, arrayRoot) {
           getOutlinedModel(response, arrayRoot, obj, key, null, createSet)
         );
       case "K":
-        obj = value.slice(2);
-        obj = response._prefix + obj + "_";
-        key = new FormData();
-        response = response._formData;
-        arrayRoot = Array.from(response.keys());
-        for (value = 0; value < arrayRoot.length; value++)
-          if (((reference = arrayRoot[value]), reference.startsWith(obj))) {
+        key = value.slice(2);
+        obj = response._prefix + "_";
+        key = obj + key + "_";
+        arrayRoot = new FormData();
+        for (response = response._formData; ; ) {
+          value = response.keys;
+          null === value &&
+            ((value = response.keys = Array.from(response.data.keys())),
+            (response.keyPointer = 0));
+          value = value[response.keyPointer];
+          if (void 0 === value) break;
+          if (value.startsWith(key)) {
+            reference = response.data.getAll(value);
             for (
-              var entries = response.getAll(reference),
-                newKey = reference.slice(obj.length),
-                j = 0;
-              j < entries.length;
-              j++
+              var referencedFormDataKey = value.slice(key.length), i = 0;
+              i < reference.length;
+              i++
             )
-              key.append(newKey, entries[j]);
-            response.delete(reference);
-          }
-        return key;
+              arrayRoot.append(referencedFormDataKey, reference[i]);
+            response.data.delete(value);
+            response.keyPointer++;
+          } else if (value.startsWith(obj)) break;
+          else response.keyPointer++;
+        }
+        return arrayRoot;
       case "i":
         return (
           (arrayRoot = value.slice(2)),
@@ -3105,7 +3113,7 @@ function parseModelString(response, obj, key, value, reference, arrayRoot) {
       case "B":
         return (
           (obj = parseInt(value.slice(2), 16)),
-          response._formData.get(response._prefix + obj)
+          response._formData.data.get(response._prefix + obj)
         );
       case "R":
         return parseReadableStream(response, value, void 0);
@@ -3133,7 +3141,7 @@ function createResponse(bundlerConfig, formFieldPrefix, temporaryReferences) {
   return {
     _bundlerConfig: bundlerConfig,
     _prefix: formFieldPrefix,
-    _formData: backingFormData,
+    _formData: { data: backingFormData, keyPointer: -1, keys: null },
     _chunks: chunks,
     _closed: !1,
     _closedReason: null,
diff --git a/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-server.edge.development.js b/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-server.edge.development.js
index 5af3867..cbe50ec 100644
--- a/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-server.edge.development.js
+++ b/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-server.edge.development.js
@@ -3647,6 +3647,14 @@
       if (hasOwnProperty.call(moduleExports, metadata[2]))
         return moduleExports[metadata[2]];
     }
+    function appendBackingEntry(backingStore, key, value) {
+      backingStore.data.append(key, value);
+      value = backingStore.keys;
+      null === value
+        ? ((backingStore.keys = Array.from(backingStore.data.keys())),
+          (backingStore.keyPointer = 0))
+        : value.push(key);
+    }
     function ReactPromise(status, value, reason) {
       this.status = status;
       this.value = value;
@@ -3959,7 +3967,7 @@
       var chunks = response._chunks,
         chunk = chunks.get(id);
       chunk ||
-        ((chunk = response._formData.get(response._prefix + id)),
+        ((chunk = response._formData.data.get(response._prefix + id)),
         (chunk =
           "string" === typeof chunk
             ? new ReactPromise(
@@ -4071,6 +4079,10 @@
         case "fulfilled":
           id = chunk.value;
           chunk = chunk.reason;
+          if (null !== chunk && "error" in chunk)
+            throw Error(
+              "Expected an initialized chunk but got an initialized stream chunk instead. This payload may have been submitted by an older version of React."
+            );
           for (
             var localLength = 0,
               rootArrayContexts = response._rootArrayContexts,
@@ -4163,22 +4175,19 @@
       if (!isArrayImpl(model)) throw Error("Invalid Map initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Map.");
       model.$$consumed = !0;
-      response = new Map(model);
-      return response;
+      return new Map(model);
     }
     function createSet(response, model) {
       if (!isArrayImpl(model)) throw Error("Invalid Set initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Set.");
       model.$$consumed = !0;
-      response = new Set(model);
-      return response;
+      return new Set(model);
     }
     function extractIterator(response, model) {
       if (!isArrayImpl(model)) throw Error("Invalid Iterator initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Iterator.");
       model.$$consumed = !0;
-      response = model[Symbol.iterator]();
-      return response;
+      return model[Symbol.iterator]();
     }
     function createModel(response, model, parentObject, key) {
       return "then" === key && "function" === typeof model ? null : model;
@@ -4216,7 +4225,7 @@
           Error("Already initialized typed array.")
         )
       );
-      reference = response._formData.get(key).arrayBuffer();
+      reference = response._formData.data.get(key).arrayBuffer();
       if (initializingHandler) {
         var handler = initializingHandler;
         handler.deps++;
@@ -4260,7 +4269,7 @@
       var chunks = response._chunks;
       stream = new ReactPromise("fulfilled", stream, controller);
       chunks.set(id, stream);
-      response = response._formData.getAll(response._prefix + id);
+      response = response._formData.data.getAll(response._prefix + id);
       for (id = 0; id < response.length; id++)
         (chunks = response[id]),
           "string" === typeof chunks &&
@@ -4480,24 +4489,33 @@
               getOutlinedModel(response, arrayRoot, obj, key, null, createSet)
             );
           case "K":
-            obj = value.slice(2);
-            obj = response._prefix + obj + "_";
-            key = new FormData();
-            response = response._formData;
-            arrayRoot = Array.from(response.keys());
-            for (value = 0; value < arrayRoot.length; value++)
-              if (((reference = arrayRoot[value]), reference.startsWith(obj))) {
+            key = value.slice(2);
+            obj = response._prefix + "_";
+            key = obj + key + "_";
+            arrayRoot = new FormData();
+            for (response = response._formData; ; ) {
+              value = response;
+              reference = value.keys;
+              null === reference &&
+                ((reference = value.keys = Array.from(value.data.keys())),
+                (value.keyPointer = 0));
+              value = reference[value.keyPointer];
+              if (void 0 === value) break;
+              if (value.startsWith(key)) {
+                reference = response.data.getAll(value);
                 for (
-                  var entries = response.getAll(reference),
-                    newKey = reference.slice(obj.length),
-                    j = 0;
-                  j < entries.length;
-                  j++
+                  var referencedFormDataKey = value.slice(key.length), i = 0;
+                  i < reference.length;
+                  i++
                 )
-                  key.append(newKey, entries[j]);
-                response.delete(reference);
-              }
-            return key;
+                  arrayRoot.append(referencedFormDataKey, reference[i]);
+                reference = response;
+                reference.data.delete(value);
+                reference.keyPointer++;
+              } else if (value.startsWith(obj)) break;
+              else response.keyPointer++;
+            }
+            return arrayRoot;
           case "i":
             return (
               (arrayRoot = value.slice(2)),
@@ -4666,7 +4684,7 @@
           case "B":
             return (
               (obj = parseInt(value.slice(2), 16)),
-              response._formData.get(response._prefix + obj)
+              response._formData.data.get(response._prefix + obj)
             );
           case "R":
             return parseReadableStream(response, value, void 0);
@@ -4705,7 +4723,7 @@
       return {
         _bundlerConfig: bundlerConfig,
         _prefix: formFieldPrefix,
-        _formData: backingFormData,
+        _formData: { data: backingFormData, keyPointer: -1, keys: null },
         _chunks: chunks,
         _closed: !1,
         _closedReason: null,
@@ -5457,7 +5475,7 @@
           entry = entry[1];
           if ("string" === typeof entry) {
             var response = response$jscomp$0;
-            response._formData.append(name, entry);
+            appendBackingEntry(response._formData, name, entry);
             var prefix = response._prefix;
             if (name.startsWith(prefix)) {
               var chunks = response._chunks;
@@ -5465,7 +5483,7 @@
               (chunks = chunks.get(name)) &&
                 resolveModelChunk(response, chunks, entry, name);
             }
-          } else response$jscomp$0._formData.append(name, entry);
+          } else appendBackingEntry(response$jscomp$0._formData, name, entry);
           iterator.next().then(progress, error);
         }
       }
diff --git a/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-server.edge.production.js b/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-server.edge.production.js
index 0c51123..f972c0a 100644
--- a/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-server.edge.production.js
+++ b/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-server.edge.production.js
@@ -2057,6 +2057,14 @@ function requireModule(metadata) {
   if (hasOwnProperty.call(moduleExports, metadata[2]))
     return moduleExports[metadata[2]];
 }
+function appendBackingEntry(backingStore, key, value) {
+  backingStore.data.append(key, value);
+  value = backingStore.keys;
+  null === value
+    ? ((backingStore.keys = Array.from(backingStore.data.keys())),
+      (backingStore.keyPointer = 0))
+    : value.push(key);
+}
 var RESPONSE_SYMBOL = Symbol();
 function ReactPromise(status, value, reason) {
   this.status = status;
@@ -2429,7 +2437,7 @@ function getChunk(response, id) {
   var chunks = response._chunks,
     chunk = chunks.get(id);
   chunk ||
-    ((chunk = response._formData.get(response._prefix + id)),
+    ((chunk = response._formData.data.get(response._prefix + id)),
     (chunk =
       "string" === typeof chunk
         ? createResolvedModelChunk(response, chunk, id)
@@ -2530,6 +2538,10 @@ function getOutlinedModel(
     case "fulfilled":
       id = chunk.value;
       chunk = chunk.reason;
+      if (null !== chunk && "error" in chunk)
+        throw Error(
+          "Expected an initialized chunk but got an initialized stream chunk instead. This payload may have been submitted by an older version of React."
+        );
       for (
         var localLength = 0,
           rootArrayContexts = response._rootArrayContexts,
@@ -2613,22 +2625,19 @@ function createMap(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Map initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Map.");
   model.$$consumed = !0;
-  response = new Map(model);
-  return response;
+  return new Map(model);
 }
 function createSet(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Set initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Set.");
   model.$$consumed = !0;
-  response = new Set(model);
-  return response;
+  return new Set(model);
 }
 function extractIterator(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Iterator initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Iterator.");
   model.$$consumed = !0;
-  response = model[Symbol.iterator]();
-  return response;
+  return model[Symbol.iterator]();
 }
 function createModel(response, model, parentObject, key) {
   return "then" === key && "function" === typeof model ? null : model;
@@ -2666,7 +2675,7 @@ function parseTypedArray(
       Error("Already initialized typed array.")
     )
   );
-  reference = response._formData.get(key).arrayBuffer();
+  reference = response._formData.data.get(key).arrayBuffer();
   if (initializingHandler) {
     var handler = initializingHandler;
     handler.deps++;
@@ -2710,7 +2719,7 @@ function resolveStream(response, id, stream, controller) {
   var chunks = response._chunks;
   stream = new ReactPromise("fulfilled", stream, controller);
   chunks.set(id, stream);
-  response = response._formData.getAll(response._prefix + id);
+  response = response._formData.data.getAll(response._prefix + id);
   for (id = 0; id < response.length; id++)
     (chunks = response[id]),
       "string" === typeof chunks &&
@@ -2925,24 +2934,31 @@ function parseModelString(response, obj, key, value, reference, arrayRoot) {
           getOutlinedModel(response, arrayRoot, obj, key, null, createSet)
         );
       case "K":
-        obj = value.slice(2);
-        obj = response._prefix + obj + "_";
-        key = new FormData();
-        response = response._formData;
-        arrayRoot = Array.from(response.keys());
-        for (value = 0; value < arrayRoot.length; value++)
-          if (((reference = arrayRoot[value]), reference.startsWith(obj))) {
+        key = value.slice(2);
+        obj = response._prefix + "_";
+        key = obj + key + "_";
+        arrayRoot = new FormData();
+        for (response = response._formData; ; ) {
+          value = response.keys;
+          null === value &&
+            ((value = response.keys = Array.from(response.data.keys())),
+            (response.keyPointer = 0));
+          value = value[response.keyPointer];
+          if (void 0 === value) break;
+          if (value.startsWith(key)) {
+            reference = response.data.getAll(value);
             for (
-              var entries = response.getAll(reference),
-                newKey = reference.slice(obj.length),
-                j = 0;
-              j < entries.length;
-              j++
+              var referencedFormDataKey = value.slice(key.length), i = 0;
+              i < reference.length;
+              i++
             )
-              key.append(newKey, entries[j]);
-            response.delete(reference);
-          }
-        return key;
+              arrayRoot.append(referencedFormDataKey, reference[i]);
+            response.data.delete(value);
+            response.keyPointer++;
+          } else if (value.startsWith(obj)) break;
+          else response.keyPointer++;
+        }
+        return arrayRoot;
       case "i":
         return (
           (arrayRoot = value.slice(2)),
@@ -3101,7 +3117,7 @@ function parseModelString(response, obj, key, value, reference, arrayRoot) {
       case "B":
         return (
           (obj = parseInt(value.slice(2), 16)),
-          response._formData.get(response._prefix + obj)
+          response._formData.data.get(response._prefix + obj)
         );
       case "R":
         return parseReadableStream(response, value, void 0);
@@ -3129,7 +3145,7 @@ function createResponse(bundlerConfig, formFieldPrefix, temporaryReferences) {
   return {
     _bundlerConfig: bundlerConfig,
     _prefix: formFieldPrefix,
-    _formData: backingFormData,
+    _formData: { data: backingFormData, keyPointer: -1, keys: null },
     _chunks: chunks,
     _closed: !1,
     _closedReason: null,
@@ -3265,7 +3281,7 @@ exports.decodeReplyFromAsyncIterable = function (
       var name = entry[0];
       entry = entry[1];
       if ("string" === typeof entry) {
-        response._formData.append(name, entry);
+        appendBackingEntry(response._formData, name, entry);
         var prefix = response._prefix;
         if (name.startsWith(prefix)) {
           var chunks = response._chunks;
@@ -3273,7 +3289,7 @@ exports.decodeReplyFromAsyncIterable = function (
           (chunks = chunks.get(name)) &&
             resolveModelChunk(response, chunks, entry, name);
         }
-      } else response._formData.append(name, entry);
+      } else appendBackingEntry(response._formData, name, entry);
       iterator.next().then(progress, error);
     }
   }
diff --git a/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-server.node.development.js b/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-server.node.development.js
index 0527a21..b1c2847 100644
--- a/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-server.node.development.js
+++ b/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-server.node.development.js
@@ -4048,6 +4048,14 @@
       if (hasOwnProperty.call(moduleExports, metadata[2]))
         return moduleExports[metadata[2]];
     }
+    function appendBackingEntry(backingStore, key, value) {
+      backingStore.data.append(key, value);
+      value = backingStore.keys;
+      null === value
+        ? ((backingStore.keys = Array.from(backingStore.data.keys())),
+          (backingStore.keyPointer = 0))
+        : value.push(key);
+    }
     function ReactPromise(status, value, reason) {
       this.status = status;
       this.value = value;
@@ -4360,7 +4368,7 @@
       var chunks = response._chunks,
         chunk = chunks.get(id);
       chunk ||
-        ((chunk = response._formData.get(response._prefix + id)),
+        ((chunk = response._formData.data.get(response._prefix + id)),
         (chunk =
           "string" === typeof chunk
             ? new ReactPromise(
@@ -4472,6 +4480,10 @@
         case "fulfilled":
           id = chunk.value;
           chunk = chunk.reason;
+          if (null !== chunk && "error" in chunk)
+            throw Error(
+              "Expected an initialized chunk but got an initialized stream chunk instead. This payload may have been submitted by an older version of React."
+            );
           for (
             var localLength = 0,
               rootArrayContexts = response._rootArrayContexts,
@@ -4564,22 +4576,19 @@
       if (!isArrayImpl(model)) throw Error("Invalid Map initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Map.");
       model.$$consumed = !0;
-      response = new Map(model);
-      return response;
+      return new Map(model);
     }
     function createSet(response, model) {
       if (!isArrayImpl(model)) throw Error("Invalid Set initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Set.");
       model.$$consumed = !0;
-      response = new Set(model);
-      return response;
+      return new Set(model);
     }
     function extractIterator(response, model) {
       if (!isArrayImpl(model)) throw Error("Invalid Iterator initializer.");
       if (!0 === model.$$consumed) throw Error("Already initialized Iterator.");
       model.$$consumed = !0;
-      response = model[Symbol.iterator]();
-      return response;
+      return model[Symbol.iterator]();
     }
     function createModel(response, model, parentObject, key) {
       return "then" === key && "function" === typeof model ? null : model;
@@ -4617,7 +4626,7 @@
           Error("Already initialized typed array.")
         )
       );
-      reference = response._formData.get(key).arrayBuffer();
+      reference = response._formData.data.get(key).arrayBuffer();
       if (initializingHandler) {
         var handler = initializingHandler;
         handler.deps++;
@@ -4661,7 +4670,7 @@
       var chunks = response._chunks;
       stream = new ReactPromise("fulfilled", stream, controller);
       chunks.set(id, stream);
-      response = response._formData.getAll(response._prefix + id);
+      response = response._formData.data.getAll(response._prefix + id);
       for (id = 0; id < response.length; id++)
         (chunks = response[id]),
           "string" === typeof chunks &&
@@ -4881,24 +4890,33 @@
               getOutlinedModel(response, arrayRoot, obj, key, null, createSet)
             );
           case "K":
-            obj = value.slice(2);
-            obj = response._prefix + obj + "_";
-            key = new FormData();
-            response = response._formData;
-            arrayRoot = Array.from(response.keys());
-            for (value = 0; value < arrayRoot.length; value++)
-              if (((reference = arrayRoot[value]), reference.startsWith(obj))) {
+            key = value.slice(2);
+            obj = response._prefix + "_";
+            key = obj + key + "_";
+            arrayRoot = new FormData();
+            for (response = response._formData; ; ) {
+              value = response;
+              reference = value.keys;
+              null === reference &&
+                ((reference = value.keys = Array.from(value.data.keys())),
+                (value.keyPointer = 0));
+              value = reference[value.keyPointer];
+              if (void 0 === value) break;
+              if (value.startsWith(key)) {
+                reference = response.data.getAll(value);
                 for (
-                  var entries = response.getAll(reference),
-                    newKey = reference.slice(obj.length),
-                    j = 0;
-                  j < entries.length;
-                  j++
+                  var referencedFormDataKey = value.slice(key.length), i = 0;
+                  i < reference.length;
+                  i++
                 )
-                  key.append(newKey, entries[j]);
-                response.delete(reference);
-              }
-            return key;
+                  arrayRoot.append(referencedFormDataKey, reference[i]);
+                reference = response;
+                reference.data.delete(value);
+                reference.keyPointer++;
+              } else if (value.startsWith(obj)) break;
+              else response.keyPointer++;
+            }
+            return arrayRoot;
           case "i":
             return (
               (arrayRoot = value.slice(2)),
@@ -5067,7 +5085,7 @@
           case "B":
             return (
               (obj = parseInt(value.slice(2), 16)),
-              response._formData.get(response._prefix + obj)
+              response._formData.data.get(response._prefix + obj)
             );
           case "R":
             return parseReadableStream(response, value, void 0);
@@ -5106,7 +5124,7 @@
       return {
         _bundlerConfig: bundlerConfig,
         _prefix: formFieldPrefix,
-        _formData: backingFormData,
+        _formData: { data: backingFormData, keyPointer: -1, keys: null },
         _chunks: chunks,
         _closed: !1,
         _closedReason: null,
@@ -5116,7 +5134,7 @@
       };
     }
     function resolveField(response, key, value) {
-      response._formData.append(key, value);
+      appendBackingEntry(response._formData, key, value);
       var prefix = response._prefix;
       if (key.startsWith(prefix)) {
         var chunks = response._chunks;
@@ -6081,7 +6099,7 @@
           _entry$value = _entry$value[1];
           "string" === typeof _entry$value
             ? resolveField(response, entry, _entry$value)
-            : response._formData.append(entry, _entry$value);
+            : appendBackingEntry(response._formData, entry, _entry$value);
           iterator.next().then(progress, error);
         }
       }
@@ -6135,16 +6153,22 @@
           );
         else {
           pendingFiles++;
-          var JSCompiler_object_inline_chunks_246 = [];
+          var JSCompiler_object_inline_chunks_254 = [];
           value.on("data", function (chunk) {
-            JSCompiler_object_inline_chunks_246.push(chunk);
+            JSCompiler_object_inline_chunks_254.push(chunk);
           });
           value.on("end", function () {
             try {
-              var blob = new Blob(JSCompiler_object_inline_chunks_246, {
-                type: mimeType
-              });
-              response._formData.append(name, blob, filename);
+              var blob = new Blob(JSCompiler_object_inline_chunks_254, {
+                  type: mimeType
+                }),
+                backingStore = response._formData;
+              backingStore.data.append(name, blob, filename);
+              var keys = backingStore.keys;
+              null === keys
+                ? ((backingStore.keys = Array.from(backingStore.data.keys())),
+                  (backingStore.keyPointer = 0))
+                : keys.push(name);
               pendingFiles--;
               if (0 === pendingFiles) {
                 for (blob = 0; blob < queuedFields.length; blob += 2)
diff --git a/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-server.node.production.js b/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-server.node.production.js
index 572eac5..af6684e 100644
--- a/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-server.node.production.js
+++ b/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-server.node.production.js
@@ -2086,6 +2086,14 @@ function requireModule(metadata) {
   if (hasOwnProperty.call(moduleExports, metadata[2]))
     return moduleExports[metadata[2]];
 }
+function appendBackingEntry(backingStore, key, value) {
+  backingStore.data.append(key, value);
+  value = backingStore.keys;
+  null === value
+    ? ((backingStore.keys = Array.from(backingStore.data.keys())),
+      (backingStore.keyPointer = 0))
+    : value.push(key);
+}
 var RESPONSE_SYMBOL = Symbol();
 function ReactPromise(status, value, reason) {
   this.status = status;
@@ -2458,7 +2466,7 @@ function getChunk(response, id) {
   var chunks = response._chunks,
     chunk = chunks.get(id);
   chunk ||
-    ((chunk = response._formData.get(response._prefix + id)),
+    ((chunk = response._formData.data.get(response._prefix + id)),
     (chunk =
       "string" === typeof chunk
         ? createResolvedModelChunk(response, chunk, id)
@@ -2559,6 +2567,10 @@ function getOutlinedModel(
     case "fulfilled":
       id = chunk.value;
       chunk = chunk.reason;
+      if (null !== chunk && "error" in chunk)
+        throw Error(
+          "Expected an initialized chunk but got an initialized stream chunk instead. This payload may have been submitted by an older version of React."
+        );
       for (
         var localLength = 0,
           rootArrayContexts = response._rootArrayContexts,
@@ -2642,22 +2654,19 @@ function createMap(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Map initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Map.");
   model.$$consumed = !0;
-  response = new Map(model);
-  return response;
+  return new Map(model);
 }
 function createSet(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Set initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Set.");
   model.$$consumed = !0;
-  response = new Set(model);
-  return response;
+  return new Set(model);
 }
 function extractIterator(response, model) {
   if (!isArrayImpl(model)) throw Error("Invalid Iterator initializer.");
   if (!0 === model.$$consumed) throw Error("Already initialized Iterator.");
   model.$$consumed = !0;
-  response = model[Symbol.iterator]();
-  return response;
+  return model[Symbol.iterator]();
 }
 function createModel(response, model, parentObject, key) {
   return "then" === key && "function" === typeof model ? null : model;
@@ -2695,7 +2704,7 @@ function parseTypedArray(
       Error("Already initialized typed array.")
     )
   );
-  reference = response._formData.get(key).arrayBuffer();
+  reference = response._formData.data.get(key).arrayBuffer();
   if (initializingHandler) {
     var handler = initializingHandler;
     handler.deps++;
@@ -2739,7 +2748,7 @@ function resolveStream(response, id, stream, controller) {
   var chunks = response._chunks;
   stream = new ReactPromise("fulfilled", stream, controller);
   chunks.set(id, stream);
-  response = response._formData.getAll(response._prefix + id);
+  response = response._formData.data.getAll(response._prefix + id);
   for (id = 0; id < response.length; id++)
     (chunks = response[id]),
       "string" === typeof chunks &&
@@ -2954,24 +2963,31 @@ function parseModelString(response, obj, key, value, reference, arrayRoot) {
           getOutlinedModel(response, arrayRoot, obj, key, null, createSet)
         );
       case "K":
-        obj = value.slice(2);
-        obj = response._prefix + obj + "_";
-        key = new FormData();
-        response = response._formData;
-        arrayRoot = Array.from(response.keys());
-        for (value = 0; value < arrayRoot.length; value++)
-          if (((reference = arrayRoot[value]), reference.startsWith(obj))) {
+        key = value.slice(2);
+        obj = response._prefix + "_";
+        key = obj + key + "_";
+        arrayRoot = new FormData();
+        for (response = response._formData; ; ) {
+          value = response.keys;
+          null === value &&
+            ((value = response.keys = Array.from(response.data.keys())),
+            (response.keyPointer = 0));
+          value = value[response.keyPointer];
+          if (void 0 === value) break;
+          if (value.startsWith(key)) {
+            reference = response.data.getAll(value);
             for (
-              var entries = response.getAll(reference),
-                newKey = reference.slice(obj.length),
-                j = 0;
-              j < entries.length;
-              j++
+              var referencedFormDataKey = value.slice(key.length), i = 0;
+              i < reference.length;
+              i++
             )
-              key.append(newKey, entries[j]);
-            response.delete(reference);
-          }
-        return key;
+              arrayRoot.append(referencedFormDataKey, reference[i]);
+            response.data.delete(value);
+            response.keyPointer++;
+          } else if (value.startsWith(obj)) break;
+          else response.keyPointer++;
+        }
+        return arrayRoot;
       case "i":
         return (
           (arrayRoot = value.slice(2)),
@@ -3130,7 +3146,7 @@ function parseModelString(response, obj, key, value, reference, arrayRoot) {
       case "B":
         return (
           (obj = parseInt(value.slice(2), 16)),
-          response._formData.get(response._prefix + obj)
+          response._formData.data.get(response._prefix + obj)
         );
       case "R":
         return parseReadableStream(response, value, void 0);
@@ -3158,7 +3174,7 @@ function createResponse(bundlerConfig, formFieldPrefix, temporaryReferences) {
   return {
     _bundlerConfig: bundlerConfig,
     _prefix: formFieldPrefix,
-    _formData: backingFormData,
+    _formData: { data: backingFormData, keyPointer: -1, keys: null },
     _chunks: chunks,
     _closed: !1,
     _closedReason: null,
@@ -3168,7 +3184,7 @@ function createResponse(bundlerConfig, formFieldPrefix, temporaryReferences) {
   };
 }
 function resolveField(response, key, value) {
-  response._formData.append(key, value);
+  appendBackingEntry(response._formData, key, value);
   var prefix = response._prefix;
   if (key.startsWith(prefix)) {
     var chunks = response._chunks;
@@ -3346,7 +3362,7 @@ exports.decodeReplyFromAsyncIterable = function (
       _entry$value = _entry$value[1];
       "string" === typeof _entry$value
         ? resolveField(response, entry, _entry$value)
-        : response._formData.append(entry, _entry$value);
+        : appendBackingEntry(response._formData, entry, _entry$value);
       iterator.next().then(progress, error);
     }
   }
@@ -3396,16 +3412,22 @@ exports.decodeReplyFromBusboy = function (busboyStream, webpackMap, options) {
       );
     else {
       pendingFiles++;
-      var JSCompiler_object_inline_chunks_275 = [];
+      var JSCompiler_object_inline_chunks_284 = [];
       value.on("data", function (chunk) {
-        JSCompiler_object_inline_chunks_275.push(chunk);
+        JSCompiler_object_inline_chunks_284.push(chunk);
       });
       value.on("end", function () {
         try {
-          var blob = new Blob(JSCompiler_object_inline_chunks_275, {
-            type: mimeType
-          });
-          response._formData.append(name, blob, filename);
+          var blob = new Blob(JSCompiler_object_inline_chunks_284, {
+              type: mimeType
+            }),
+            backingStore = response._formData;
+          backingStore.data.append(name, blob, filename);
+          var keys = backingStore.keys;
+          null === keys
+            ? ((backingStore.keys = Array.from(backingStore.data.keys())),
+              (backingStore.keyPointer = 0))
+            : keys.push(name);
           pendingFiles--;
           if (0 === pendingFiles) {
             for (blob = 0; blob < queuedFields.length; blob += 2)
