--- a/dist/server/config-schema.js
+++ b/dist/server/config-schema.js
@@ -263,6 +263,9 @@
                 gzipSize: {
                     type: "boolean"
                 },
+                imgOptDangerouslyAllowAVIF: {
+                    type: "boolean"
+                },
                 images: {
                     additionalProperties: false,
                     properties: {
--- a/dist/server/config-shared.js
+++ b/dist/server/config-shared.js
@@ -94,6 +94,7 @@
         externalDir: false,
         disableOptimizedLoading: false,
         gzipSize: true,
+        imgOptDangerouslyAllowAVIF: false,
         swcFileReading: true,
         craCompat: false,
         esmExternals: true,
--- a/dist/server/image-optimizer.js
+++ b/dist/server/image-optimizer.js
@@ -259,6 +259,20 @@
         console.error(`The requested resource "${href}" has type "${upstreamType}" but dangerouslyAllowSVG is disabled`);
         throw new ImageError(400, '"url" parameter is valid but image type is not allowed');
     }
+    if (upstreamType === AVIF && !nextConfig.experimental.imgOptDangerouslyAllowAVIF) {
+        // Optimizing an AVIF *input* requires decoding it with a native image
+        // decoder (sharp/libheif, or the Squoosh WASM fallback). A crafted AVIF
+        // image can trigger a heap buffer overflow leading to remote code
+        // execution (GHSA-2xp9-vwfh-vxw4 / GHSA-g89c-p67h-r497). Skip
+        // optimization and pass the original bytes through untouched unless the
+        // application has explicitly opted in via
+        // experimental.imgOptDangerouslyAllowAVIF.
+        return {
+            buffer: upstreamBuffer,
+            contentType: upstreamType,
+            maxAge
+        };
+    }
     if (upstreamType) {
         const vector = VECTOR_TYPES.includes(upstreamType);
         const animate = ANIMATABLE_TYPES.includes(upstreamType) && (0, _isAnimated).default(upstreamBuffer);

--- a/dist/server/base-server.js
+++ b/dist/server/base-server.js
@@ -156,6 +156,34 @@
             if (typeof parsedUrl.query === "string") {
                 parsedUrl.query = (0, _querystring).parse(parsedUrl.query);
             }
+            // Security (CVE-2024-51479): the internal i18n routing query
+            // parameters (`__nextLocale`, `__nextDefaultLocale`,
+            // `__nextInferredLocaleFromDefault`) are derived internally from the
+            // request URL and must never be trusted when supplied by the client.
+            // A crafted `__nextLocale` was carried into the locale-aware pathname
+            // reconstruction used for route/middleware matching, which allowed
+            // pathname-based authorization checks in middleware to be bypassed
+            // for pages directly under the application root. Strip these params
+            // unless i18n is configured and the value is a valid configured locale.
+            {
+                const i18n = this.nextConfig.i18n;
+                const validLocale = (value)=>{
+                    if (!i18n || typeof value !== "string") {
+                        return false;
+                    }
+                    const lowerCased = value.toLowerCase();
+                    return i18n.locales.some((locale)=>locale.toLowerCase() === lowerCased);
+                };
+                if ("__nextLocale" in parsedUrl.query && !validLocale(parsedUrl.query.__nextLocale)) {
+                    delete parsedUrl.query.__nextLocale;
+                }
+                if ("__nextDefaultLocale" in parsedUrl.query && !validLocale(parsedUrl.query.__nextDefaultLocale)) {
+                    delete parsedUrl.query.__nextDefaultLocale;
+                }
+                if ("__nextInferredLocaleFromDefault" in parsedUrl.query) {
+                    delete parsedUrl.query.__nextInferredLocaleFromDefault;
+                }
+            }
             this.attachRequestMeta(req, parsedUrl);
             const domainLocale = (0, _detectDomainLocale).detectDomainLocale((ref = this.nextConfig.i18n) == null ? void 0 : ref.domains, (0, _getHostname).getHostname(parsedUrl, req.headers));
             const defaultLocale = (domainLocale == null ? void 0 : domainLocale.defaultLocale) || ((ref3 = this.nextConfig.i18n) == null ? void 0 : ref3.defaultLocale);

--- a/dist/server/web/sandbox/context.js
+++ b/dist/server/web/sandbox/context.js
@@ -4,6 +4,7 @@
 });
 exports.clearModuleContext = clearModuleContext;
 exports.getModuleContext = getModuleContext;
+exports.getMiddlewareSubrequestId = getMiddlewareSubrequestId;
 var _middleware = require("next/dist/compiled/@next/react-dev-overlay/dist/middleware");
 var _constants = require("../../../shared/lib/constants");
 var _edgeRuntime = require("next/dist/compiled/edge-runtime");
@@ -11,6 +12,22 @@
 var _utils = require("../utils");
 var _pick = require("../../../lib/pick");
 var _fetchInlineAssets = require("./fetch-inline-assets");
+var _crypto = require("crypto");
+const MIDDLEWARE_SUBREQUEST_ID_SYMBOL = Symbol.for("@next/middleware-subrequest-id");
+/**
+ * Returns a per-process, unguessable identifier used to authenticate the
+ * `x-middleware-subrequest` header. Only subrequests originated by this
+ * server session carry the matching `x-middleware-subrequest-id` header, so
+ * an externally supplied `x-middleware-subrequest` header can no longer be
+ * used to skip middleware execution (CVE-2025-29927).
+ */ function getMiddlewareSubrequestId() {
+    let id = globalThis[MIDDLEWARE_SUBREQUEST_ID_SYMBOL];
+    if (!id) {
+        id = _crypto.randomBytes(8).toString("hex");
+        globalThis[MIDDLEWARE_SUBREQUEST_ID_SYMBOL] = id;
+    }
+    return id;
+}
 const WEBPACK_HASH_REGEX = /__webpack_require__\.h = function\(\) \{ return "[0-9a-f]+"; \}/g;
 /**
  * A Map of cached module contexts indexed by the module name. It allows
@@ -135,6 +152,9 @@
                 const prevs = ((ref = init.headers.get(`x-middleware-subrequest`)) == null ? void 0 : ref.split(":")) || [];
                 const value = prevs.concat(options.moduleName).join(":");
                 init.headers.set("x-middleware-subrequest", value);
+                // Authenticate this internal subrequest so the receiving middleware
+                // runner can distinguish it from an attacker-supplied header (CVE-2025-29927).
+                init.headers.set("x-middleware-subrequest-id", getMiddlewareSubrequestId());
                 if (!init.headers.has("user-agent")) {
                     init.headers.set(`user-agent`, `Next.js Middleware`);
                 }
--- a/dist/server/web/sandbox/sandbox.js
+++ b/dist/server/web/sandbox/sandbox.js
@@ -24,7 +24,13 @@
     }
     const subreq = params.request.headers[`x-middleware-subrequest`];
     const subrequests = typeof subreq === "string" ? subreq.split(":") : [];
-    if (subrequests.includes(params.name)) {
+    // Only honor the `x-middleware-subrequest` skip mechanism when the request
+    // also carries the matching, session-private subrequest id. This prevents
+    // an attacker-controlled `x-middleware-subrequest` header from bypassing
+    // middleware (CVE-2025-29927).
+    const subreqId = params.request.headers[`x-middleware-subrequest-id`];
+    const hasValidSubrequestId = typeof subreqId === "string" && subreqId === (0, _context).getMiddlewareSubrequestId();
+    if (hasValidSubrequestId && subrequests.includes(params.name)) {
         return {
             waitUntil: Promise.resolve(),
             response: new runtime.context.Response(null, {

--- a/dist/server/next-server.js
+++ b/dist/server/next-server.js
@@ -328,6 +328,9 @@
         delete parsedUrl.query;
         parsedUrl.search = (0, _baseServer).stringifyQuery(req, query);
         const target = (0, _url).format(parsedUrl);
+        // Validate the proxy target URL before handing it to http-proxy, which
+        // does not validate it (open redirect / SSRF, CVE-2026-64645).
+        new URL(target);
         const proxy = new _httpProxy.default({
             target,
             changeOrigin: true,
--- a/dist/shared/lib/router/utils/prepare-destination.js
+++ b/dist/shared/lib/router/utils/prepare-destination.js
@@ -116,7 +116,11 @@
         validate: false
     });
     const destHostnameCompiler = (0, _pathToRegexp).compile(destHostname, {
-        validate: false
+        validate: false,
+        // Percent-encode exotic rewrite/redirect param values interpolated into
+        // the destination hostname so they cannot alter the resolved host
+        // (open redirect / SSRF, CVE-2026-64645).
+        encode: encodeURIComponent
     });
     // update any params in query values
     for (const [key1, strOrArray] of Object.entries(destQuery)){

--- a/dist/server/base-server.js
+++ b/dist/server/base-server.js
@@ -422,6 +422,17 @@ class Server {
                                 finished: true
                             };
                         }
+                        // When middleware is present an unprefixed default-locale data
+                        // route must be matched against the same locale-prefixed
+                        // internal pathname used for locale-prefixed data routes.
+                        // Without inferring the default locale here the i18n middleware
+                        // matcher never runs for the default locale, letting an
+                        // unauthenticated request retrieve server-rendered JSON for
+                        // protected pages, bypassing middleware-based authorization
+                        // (CVE-2026-44573).
+                        if (!detectedLocale && this.router.catchAllMiddleware[0]) {
+                            _parsedUrl.query.__nextLocale = _parsedUrl.query.__nextDefaultLocale;
+                        }
                     }
                     return {
                         pathname,
