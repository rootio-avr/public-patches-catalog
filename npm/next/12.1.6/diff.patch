--- a/dist/build/webpack/loaders/next-image-loader.js
+++ b/dist/build/webpack/loaders/next-image-loader.js
@@ -15,8 +15,7 @@
 const VALID_BLUR_EXT = [
     'jpeg',
     'png',
-    'webp',
-    'avif'
+    'webp'
 ] // should match next/client/image.tsx
 ;
 function nextImageLoader(content) {
--- a/dist/client/image.js
+++ b/dist/client/image.js
@@ -73,6 +73,7 @@
         delete rest.loader;
     }
     let staticSrc = '';
+    let isStaticAvif = false;
     if (isStaticImport(src)) {
         const staticImageData = isStaticRequire(src) ? src.default : src;
         if (!staticImageData.src) {
@@ -80,6 +81,7 @@
         }
         blurDataURL = blurDataURL || staticImageData.blurDataURL;
         staticSrc = staticImageData.src;
+        isStaticAvif = /\.avif(?:\?|$)/i.test(staticSrc);
         if (!layout || layout !== 'fill') {
             height = height || staticImageData.height;
             width = width || staticImageData.width;
@@ -89,6 +91,11 @@
         }
     }
     src = typeof src === 'string' ? src : staticSrc;
+    // Generating a blurDataURL requires decoding the source image, so static AVIF
+    // imports do not receive one while AVIF input optimization is disabled.
+    if (isStaticAvif && placeholder === 'blur' && !blurDataURL) {
+        placeholder = 'empty';
+    }
     const widthInt = getInt(width);
     const heightInt = getInt(height);
     const qualityInt = getInt(quality);
@@ -194,8 +201,7 @@
                 const VALID_BLUR_EXT = [
                     'jpeg',
                     'png',
-                    'webp',
-                    'avif'
+                    'webp'
                 ] // should match next-image-loader
                 ;
                 throw new Error(`Image with src "${src}" has "placeholder='blur'" property but is missing the "blurDataURL" property.
--- a/dist/server/image-optimizer.js
+++ b/dist/server/image-optimizer.js
@@ -259,7 +259,12 @@
     if (upstreamType) {
         const vector = VECTOR_TYPES.includes(upstreamType);
         const animate = ANIMATABLE_TYPES.includes(upstreamType) && (0, _isAnimated).default(upstreamBuffer);
-        if (vector || animate) {
+        // AVIF input optimization is disabled. Decoding untrusted AVIF images in
+        // the underlying image processing libraries (sharp/libvips, squoosh) can
+        // trigger memory-safety issues that lead to remote code execution, so the
+        // original AVIF buffer is passed through unchanged instead of decoded.
+        const bypass = upstreamType === AVIF;
+        if (vector || animate || bypass) {
             return {
                 buffer: upstreamBuffer,
                 contentType: upstreamType,

--- a/dist/shared/lib/router/utils/prepare-destination.js
+++ b/dist/shared/lib/router/utils/prepare-destination.js
@@ -118,9 +118,10 @@
     {
         validate: false
     });
-    const destHostnameCompiler = (0, _pathToRegexp).compile(destHostname, {
-        validate: false
-    });
+    const destHostnameCompiler = (0, _pathToRegexp).compile(destHostname, {
+        validate: false,
+        encode: encodeURIComponent
+    });
     // update any params in query values
     for (const [key1, strOrArray] of Object.entries(destQuery)){
         // the value needs to start with a forward-slash to be compiled

--- a/dist/server/next-server.js
+++ b/dist/server/next-server.js
@@ -819,9 +819,16 @@
         if (!url.startsWith('http')) {
             throw new Error('To use middleware you must provide a `hostname` and `port` to the Next.js Server');
         }
+        // Normalize the trailing slash of the pathname on the URL handed to
+        // Middleware so the pathname Middleware observes (request.nextUrl.pathname)
+        // matches the trailing-slash normalized pathname used for Middleware
+        // matching above (CVE-2024-51479 trailing-slash authorization bypass).
+        const __cveNormalizedUrl = new URL(url);
+        __cveNormalizedUrl.pathname = (0, _normalizeTrailingSlash).removePathTrailingSlash(__cveNormalizedUrl.pathname);
+        const normalizedInitUrl = __cveNormalizedUrl.toString();
         const page = {};
         if (await this.hasPage(normalizedPathname)) {
-            page.name = params.parsedUrl.pathname;
+            page.name = normalizedPathname;
         } else if (this.dynamicRoutes) {
             for (const dynamicRoute of this.dynamicRoutes){
                 const matchParams = dynamicRoute.match(normalizedPathname);
@@ -857,7 +864,7 @@
                             i18n: this.nextConfig.i18n,
                             trailingSlash: this.nextConfig.trailingSlash
                         },
-                        url: url,
+                        url: normalizedInitUrl,
                         page: page,
                         body: originalBody === null || originalBody === void 0 ? void 0 : originalBody.cloneBodyStream()
                     },

--- a/dist/server/next-server.js
+++ b/dist/server/next-server.js
@@ -6,6 +6,7 @@ exports.default = void 0;
 require("./node-polyfill-fetch");
 require("./node-polyfill-web-streams");
 var _fs = _interopRequireDefault(require("fs"));
+var _crypto = _interopRequireDefault(require("crypto"));
 var _path = require("path");
 var _http = require("http");
 var _utils = require("../shared/lib/utils");
@@ -108,6 +109,10 @@ class NextNodeServer extends _baseServer.default {
                 nextConfig: this.nextConfig
             }), this.minimalMode);
         }
+        const randomBytes = _crypto.default.randomBytes(8);
+        const middlewareSubrequestId = randomBytes.toString('hex');
+        globalThis[Symbol.for('@next/middleware-subrequest-id')] = middlewareSubrequestId;
+
         if (!this.renderOpts.dev) {
             // pre-warm _document and _app as these will be
             // needed for most requests
--- a/dist/server/web/sandbox/context.js
+++ b/dist/server/web/sandbox/context.js
@@ -143,6 +143,7 @@
         const prevs = ((ref = init.headers.get(`x-middleware-subrequest`)) === null || ref === void 0 ? void 0 : ref.split(':')) || [];
         const value = prevs.concat(options.module).join(':');
         init.headers.set('x-middleware-subrequest', value);
+        init.headers.set('x-middleware-subrequest-id', globalThis[Symbol.for('@next/middleware-subrequest-id')]);
         if (!init.headers.has('user-agent')) {
             init.headers.set(`user-agent`, `Next.js Middleware`);
         }
--- a/dist/server/web/sandbox/sandbox.js
+++ b/dist/server/web/sandbox/sandbox.js
@@ -17,7 +17,7 @@ async function run(params) {
     }
     const subreq = params.request.headers[`x-middleware-subrequest`];
     const subrequests = typeof subreq === 'string' ? subreq.split(':') : [];
-    if (subrequests.includes(params.name)) {
+    if (params.request.headers['x-middleware-subrequest-id'] === globalThis[Symbol.for('@next/middleware-subrequest-id')] && subrequests.includes(params.name)) {
         return {
             waitUntil: Promise.resolve(),
             response: new context.Response(null, {
