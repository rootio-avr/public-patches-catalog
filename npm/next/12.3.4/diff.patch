diff -Naur /tmp/next-fix-gen/12.3.4-clean/dist/server/base-server.js /tmp/next-fix-gen/12.3.4-51479/dist/server/base-server.js
--- a/dist/server/base-server.js	1985-10-26 13:45:00.000000000 +0530
+++ b/dist/server/base-server.js	2026-09-11 18:03:00.429892869 +0530
@@ -148,6 +148,22 @@
             if (typeof parsedUrl.query === "string") {
                 parsedUrl.query = (0, _querystring).parse(parsedUrl.query);
             }
+            // Validate that if i18n isn't configured or the passed locale-related
+            // query parameters are not valid configured locales, they are stripped
+            // from the query. These params are otherwise spoofable by the client and
+            // can be used to alter the effective pathname seen by middleware,
+            // bypassing pathname-based authorization (CVE-2024-51479).
+            const i18nConfig = this.nextConfig.i18n;
+            const isValidLocaleQuery = (locale)=>{
+                if (!locale || typeof locale !== "string") return true;
+                if (!i18nConfig) return false;
+                return i18nConfig.locales.some((configuredLocale)=>configuredLocale.toLowerCase() === locale.toLowerCase());
+            };
+            if (!i18nConfig || !isValidLocaleQuery(parsedUrl.query.__nextLocale) || !isValidLocaleQuery(parsedUrl.query.__nextDefaultLocale)) {
+                delete parsedUrl.query.__nextLocale;
+                delete parsedUrl.query.__nextDefaultLocale;
+                delete parsedUrl.query.__nextInferredLocaleFromDefault;
+            }
             this.attachRequestMeta(req, parsedUrl);
             const domainLocale = (0, _detectDomainLocale).detectDomainLocale((ref = this.nextConfig.i18n) == null ? void 0 : ref.domains, (0, _getHostname).getHostname(parsedUrl, req.headers));
             const defaultLocale = (domainLocale == null ? void 0 : domainLocale.defaultLocale) || ((ref2 = this.nextConfig.i18n) == null ? void 0 : ref2.defaultLocale);

diff --git a/dist/server/next-server.js b/dist/server/next-server.js
index 2b28258..88593da 100644
--- a/dist/server/next-server.js
+++ b/dist/server/next-server.js
@@ -64,10 +64,14 @@ var _incrementalCache = require("./lib/incremental-cache");
 var _utils4 = require("../build/webpack/loaders/next-serverless-loader/utils");
 var _routeRegex = require("../shared/lib/router/utils/route-regex");
 var _appPaths = require("../shared/lib/router/utils/app-paths");
+var _crypto = require("crypto");
+const MIDDLEWARE_SUBREQUEST_ID_SYMBOL = Symbol.for("@next/middleware-subrequest-id");
 class NextNodeServer extends _baseServer.default {
     constructor(options){
         // Initialize super class
         super(options);
+        const middlewareSubrequestId = _crypto.randomBytes(8).toString("hex");
+        globalThis[MIDDLEWARE_SUBREQUEST_ID_SYMBOL] = middlewareSubrequestId;
         /**
      * This sets environment variable to be used at the time of SSR by head.tsx.
      * Using this from process.env allows targeting both serverless and SSR by calling
@@ -1125,6 +1129,7 @@ class NextNodeServer extends _baseServer.default {
             throw new _utils.MiddlewareNotFoundError();
         }
         const method = (params.request.method || "GET").toUpperCase();
+        const filteredHeaders = (0, _utils2).filterMiddlewareSubrequestHeader(params.request.headers);
         const result = await (0, _sandbox).run({
             distDir: this.distDir,
             name: middlewareInfo.name,
@@ -1132,7 +1137,7 @@ class NextNodeServer extends _baseServer.default {
             env: middlewareInfo.env,
             edgeFunctionEntry: middlewareInfo,
             request: {
-                headers: params.request.headers,
+                headers: filteredHeaders,
                 method,
                 nextConfig: {
                     basePath: this.nextConfig.basePath,
@@ -1367,6 +1372,7 @@ class NextNodeServer extends _baseServer.default {
         if (!url.startsWith("http")) {
             throw new Error("To use middleware you must provide a `hostname` and `port` to the Next.js Server");
         }
+        const edgeFilteredHeaders = (0, _utils2).filterMiddlewareSubrequestHeader(params.req.headers);
         const result = await (0, _sandbox).run({
             distDir: this.distDir,
             name: middlewareInfo.name,
@@ -1374,7 +1380,7 @@ class NextNodeServer extends _baseServer.default {
             env: middlewareInfo.env,
             edgeFunctionEntry: middlewareInfo,
             request: {
-                headers: params.req.headers,
+                headers: edgeFilteredHeaders,
                 method: params.req.method,
                 nextConfig: {
                     basePath: this.nextConfig.basePath,
diff --git a/dist/server/web/sandbox/context.js b/dist/server/web/sandbox/context.js
index a49a289..d6a1061 100644
--- a/dist/server/web/sandbox/context.js
+++ b/dist/server/web/sandbox/context.js
@@ -169,6 +169,10 @@ Learn More: https://nextjs.org/docs/messages/edge-dynamic-code-evaluation`), _co
                 const prevs = ((ref = init.headers.get(`x-middleware-subrequest`)) == null ? void 0 : ref.split(":")) || [];
                 const value = prevs.concat(options.moduleName).join(":");
                 init.headers.set("x-middleware-subrequest", value);
+                const middlewareSubrequestId = globalThis[Symbol.for("@next/middleware-subrequest-id")];
+                if (middlewareSubrequestId) {
+                    init.headers.set("x-middleware-subrequest-id", middlewareSubrequestId);
+                }
                 if (!init.headers.has("user-agent")) {
                     init.headers.set(`user-agent`, `Next.js Middleware`);
                 }
diff --git a/dist/server/web/utils.js b/dist/server/web/utils.js
index 4971fa5..5a20d0e 100644
--- a/dist/server/web/utils.js
+++ b/dist/server/web/utils.js
@@ -6,6 +6,20 @@ exports.fromNodeHeaders = fromNodeHeaders;
 exports.splitCookiesString = splitCookiesString;
 exports.toNodeHeaders = toNodeHeaders;
 exports.validateURL = validateURL;
+exports.filterMiddlewareSubrequestHeader = filterMiddlewareSubrequestHeader;
+const MIDDLEWARE_SUBREQUEST_ID_SYMBOL = Symbol.for("@next/middleware-subrequest-id");
+function filterMiddlewareSubrequestHeader(headers) {
+    if (typeof headers !== "object" || headers === null) {
+        return headers;
+    }
+    const result = {
+        ...headers
+    };
+    if (result["x-middleware-subrequest"] && result["x-middleware-subrequest-id"] !== globalThis[MIDDLEWARE_SUBREQUEST_ID_SYMBOL]) {
+        delete result["x-middleware-subrequest"];
+    }
+    return result;
+}
 function fromNodeHeaders(object) {
     const headers = new Headers();
     for (let [key, value] of Object.entries(object)){

--- a/dist/server/next-server.js
+++ b/dist/server/next-server.js
@@ -400,6 +400,10 @@
         delete parsedUrl.query;
         parsedUrl.search = (0, _serverRouteUtils).stringifyQuery(req, query);
         const target = (0, _url).format(parsedUrl);
+        // Keep in mind that a WHATWG URL's hostname and the parsedUrl's hostname
+        // are not strictly equal due to lowercasing, IDN translation, etc.
+        // We just make sure this is a valid URL since http-proxy doesn't validate.
+        new URL(target);
         const proxy = new _httpProxy.default({
             target,
             changeOrigin: true,
--- a/dist/shared/lib/router/utils/prepare-destination.js
+++ b/dist/shared/lib/router/utils/prepare-destination.js
@@ -137,7 +137,8 @@
         validate: false
     });
     const destHostnameCompiler = (0, _pathToRegexp).compile(destHostname, {
-        validate: false
+        validate: false,
+        encode: encodeURIComponent
     });
     // update any params in query values
     for (const [key1, strOrArray] of Object.entries(destQuery)){

--- a/dist/server/app-render.js
+++ b/dist/server/app-render.js
@@ -279,6 +279,14 @@
     return nonce;
 }
 async function renderToHTMLOrFlight(req, res, pathname, query, renderOpts, isPagesDir, isStaticGeneration = false) {
+    // CVE-2026-64641 / GHSA-m99w-x7hq-7vfj (Excessive Iteration / DoS while
+    // validating MPA Server Action form submissions) does NOT apply to
+    // next@12.3.4. The vulnerable multipart / progressive-enhancement form
+    // decode path (areAllActionIdsValid / $ACTION_REF_ / decodeAction in
+    // server/app-render/action-handler) was introduced in v13.4.0 (PR
+    // #49187) and is absent here: this release has no Server Actions
+    // action-handler at all, so there is no unbounded $ACTION_REF_ loop
+    // to bound. Documentation-only marker; no runtime change. Fix: PR #96013.
     patchFetch();
     const { CONTEXT_NAMES  } = require("../client/components/hooks-server-context");
     // @ts-expect-error createServerContext exists in react@experimental + react-dom@experimental

--- a/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack.development.js
+++ b/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack.development.js
@@ -518,7 +518,9 @@
   }
 
   return function (value) {
-    parentObject[key] = value;
+    if (key !== '__proto__') {
+      parentObject[key] = value;
+    }
     blocked.deps--;
 
     if (blocked.deps === 0) {
@@ -782,6 +784,11 @@
 
 function createFromJSONCallback(response) {
   return function (key, value) {
+    if (key === '__proto__') {
+      // Guard against prototype-pollution during Flight deserialization (GHSA-h25m-26qc-wcjf).
+      return undefined;
+    }
+
     if (typeof value === 'string') {
       // We can't use .bind here because we need the "this" value.
       return parseModelString(response, this, key, value);
--- a/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack.production.min.js
+++ b/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack.production.min.js
@@ -15,11 +15,11 @@
 function G(a,b){if("pending"===a.status||"blocked"===a.status){var c=a.reason;a.status="rejected";a.reason=b;null!==c&&E(c,b)}}function H(a,b){if("pending"===a.status||"blocked"===a.status){var c=a.value,d=a.reason;a.status="resolved_module";a.value=b;null!==c&&(B(a),F(a,c,d))}}var I=null,J=null;
 function z(a){var b=I,c=J;I=a;J=null;try{var d=JSON.parse(a.value,a._response._fromJSON);null!==J&&0<J.deps?(J.value=d,a.status="blocked",a.value=null,a.reason=null):(a.status="fulfilled",a.value=d)}catch(e){a.status="rejected",a.reason=e}finally{I=b,J=c}}
 function B(a){try{var b=a.value;if(b.async){var c=p.get(b.id);if("fulfilled"===c.status)var d=c.value;else throw c.reason;}else d=globalThis.__next_require__(b.id);var e="*"===b.name?d:""===b.name?d.__esModule?d.default:d:d[b.name];a.status="fulfilled";a.value=e}catch(f){a.status="rejected",a.reason=f}}function K(a,b){a._chunks.forEach(function(a){"pending"===a.status&&G(a,b)})}function L(a,b){var c=a._chunks,d=c.get(b);d||(d=new y("pending",null,null,a),c.set(b,d));return d}
-function N(a,b,c){if(J){var d=J;d.deps++}else d=J={deps:1,value:null};return function(e){b[c]=e;d.deps--;0===d.deps&&"blocked"===a.status&&(e=a.value,a.status="fulfilled",a.value=d.value,null!==e&&E(e,d.value))}}function O(a){return function(b){return G(a,b)}}
+function N(a,b,c){if(J){var d=J;d.deps++}else d=J={deps:1,value:null};return function(e){"__proto__"!==c&&(b[c]=e);d.deps--;0===d.deps&&"blocked"===a.status&&(e=a.value,a.status="fulfilled",a.value=d.value,null!==e&&E(e,d.value))}}function O(a){return function(b){return G(a,b)}}
 function P(a,b,c,d){switch(d[0]){case "$":if("$"===d)return t;if("$"===d[1]||"@"===d[1])return d.substring(1);d=parseInt(d.substring(1),16);a=L(a,d);switch(a.status){case "resolved_model":z(a);break;case "resolved_module":B(a)}switch(a.status){case "fulfilled":return a.value;case "pending":case "blocked":return d=I,a.then(N(d,b,c),O(d)),null;default:throw a.reason;}case "@":return b=parseInt(d.substring(1),16),b=L(a,b),{$$typeof:u,_payload:b,_init:C}}return d}
 function Q(a,b,c){var d=a._chunks,e=d.get(b);c=JSON.parse(c,a._fromJSON);var f=m(a._bundlerConfig,c);if(c=r(f)){if(e){var l=e;l.status="blocked"}else l=new y("blocked",null,null,a),d.set(b,l);c.then(function(){return H(l,f)},function(a){return G(l,a)})}else e?H(e,f):d.set(b,new y("resolved_module",f,null,a))}function R(a){K(a,Error("Connection closed."))}
 function S(a,b){if(""!==b){var c=b[0],d=b.indexOf(":",1),e=parseInt(b.substring(1,d),16);b=b.substring(d+1);switch(c){case "J":d=a._chunks;(c=d.get(e))?"pending"===c.status&&(a=c.value,e=c.reason,c.status="resolved_model",c.value=b,null!==a&&(z(c),F(c,a,e))):d.set(e,new y("resolved_model",b,null,a));break;case "M":Q(a,e,b);break;case "P":a._chunks.set(e,D(a,x(b).Provider));break;case "S":b=JSON.parse(b);a._chunks.set(e,D(a,Symbol.for(b)));break;case "E":c=JSON.parse(b);b=Error(c.message);b.stack=
-c.stack;c=a._chunks;(d=c.get(e))?G(d,b):c.set(e,new y("rejected",null,b,a));break;default:throw Error("Error parsing the data. It's probably an error code or network corruption.");}}}function T(a){return function(b,c){return"string"===typeof c?P(a,this,b,c):"object"===typeof c&&null!==c?(b=c[0]===t?{$$typeof:t,type:c[1],key:c[2],ref:null,props:c[3],_owner:null}:c,b):c}}
+c.stack;c=a._chunks;(d=c.get(e))?G(d,b):c.set(e,new y("rejected",null,b,a));break;default:throw Error("Error parsing the data. It's probably an error code or network corruption.");}}}function T(a){return function(b,c){if("__proto__"===b)return;return"string"===typeof c?P(a,this,b,c):"object"===typeof c&&null!==c?(b=c[0]===t?{$$typeof:t,type:c[1],key:c[2],ref:null,props:c[3],_owner:null}:c,b):c}}
 function U(a){var b=new TextDecoder,c=new Map;a={_bundlerConfig:a,_chunks:c,_partialRow:"",_stringDecoder:b};a._fromJSON=T(a);return a}function V(a,b){function c(b){var f=b.value;if(b.done)R(a);else{b=f;f=a._stringDecoder;for(var g=b.indexOf(10);-1<g;){var M=a._partialRow;var A=b.subarray(0,g);A=f.decode(A);S(a,M+A);a._partialRow="";b=b.subarray(g+1);g=b.indexOf(10)}a._partialRow+=f.decode(b,k);return e.read().then(c).catch(d)}}function d(b){K(a,b)}var e=b.getReader();e.read().then(c).catch(d)}
 exports.createFromFetch=function(a,b){var c=U(b&&b.moduleMap?b.moduleMap:null);a.then(function(a){V(c,a.body)},function(a){K(c,a)});return L(c,0)};exports.createFromReadableStream=function(a,b){b=U(b&&b.moduleMap?b.moduleMap:null);V(b,a);return L(b,0)};
 exports.createFromXHR=function(a,b){function c(){for(var b=a.responseText,c=f,d=b.indexOf("\n",c);-1<d;)c=e._partialRow+b.substring(c,d),S(e,c),e._partialRow="",c=d+1,d=b.indexOf("\n",c);e._partialRow+=b.substring(c);f=b.length}function d(){K(e,new TypeError("Network error"))}var e=U(b&&b.moduleMap?b.moduleMap:null),f=0;a.addEventListener("progress",c);a.addEventListener("load",function(){c();R(e)});a.addEventListener("error",d);a.addEventListener("abort",d);a.addEventListener("timeout",d);return L(e,
