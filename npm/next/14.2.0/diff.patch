--- a/dist/server/app-render/action-handler.js
+++ b/dist/server/app-render/action-handler.js
@@ -28,6 +28,22 @@
         default: obj
     };
 }
+function hasTooManyActionRefs(formData) {
+    // Guards against a denial-of-service (CVE-2026-64641) where a crafted MPA
+    // (non-fetch) Server Action form submission includes a large number of
+    // `$ACTION_REF_` fields. A legitimate submission presents at most 2 such
+    // fields: one from `<form action=...>` and one from an
+    // `<input formAction=... type="submit">`. Decoding each of them triggers a
+    // full scan of the FormData, so a payload with many of them lets an attacker
+    // burn CPU / block the event loop. Reject such payloads before decoding.
+    let seenActionRefs = 0;
+    for (const key of formData.keys()){
+        if (key.startsWith('$ACTION_REF_') && ++seenActionRefs > 2) {
+            return true;
+        }
+    }
+    return false;
+}
 function formDataFromSearchQueryString(query) {
     const searchParams = new URLSearchParams(query);
     const formData = new FormData();
@@ -336,6 +352,12 @@
                     const formData = await webRequest.request.formData();
                     if (isFetchAction) {
                         bound = await decodeReply(formData, serverModuleMap);
+                    } else if (hasTooManyActionRefs(formData)) {
+                        // Disregard payloads with an unexpected number of action
+                        // refs instead of spending CPU decoding each of them.
+                        warnBadServerActionRequest();
+                        // Skip the fetch path
+                        return;
                     } else {
                         const action = await decodeAction(formData, serverModuleMap);
                         if (typeof action === "function") {
@@ -415,6 +437,13 @@
                             duplex: "half"
                         });
                         const formData = await fakeRequest.formData();
+                        if (hasTooManyActionRefs(formData)) {
+                            // Disregard payloads with an unexpected number of action
+                            // refs instead of spending CPU decoding each of them.
+                            warnBadServerActionRequest();
+                            // Skip the fetch path
+                            return;
+                        }
                         const action = await decodeAction(formData, serverModuleMap);
                         if (typeof action === "function") {
                             // Only warn if it's a server action, otherwise skip for other post requests
--- a/dist/esm/server/app-render/action-handler.js
+++ b/dist/esm/server/app-render/action-handler.js
@@ -13,6 +13,22 @@
 import { HeadersAdapter } from "../web/spec-extension/adapters/headers";
 import { fromNodeOutgoingHttpHeaders } from "../web/utils";
 import { selectWorkerForForwarding } from "./action-utils";
+function hasTooManyActionRefs(formData) {
+    // Guards against a denial-of-service (CVE-2026-64641) where a crafted MPA
+    // (non-fetch) Server Action form submission includes a large number of
+    // `$ACTION_REF_` fields. A legitimate submission presents at most 2 such
+    // fields: one from `<form action=...>` and one from an
+    // `<input formAction=... type="submit">`. Decoding each of them triggers a
+    // full scan of the FormData, so a payload with many of them lets an attacker
+    // burn CPU / block the event loop. Reject such payloads before decoding.
+    let seenActionRefs = 0;
+    for (const key of formData.keys()){
+        if (key.startsWith('$ACTION_REF_') && ++seenActionRefs > 2) {
+            return true;
+        }
+    }
+    return false;
+}
 function formDataFromSearchQueryString(query) {
     const searchParams = new URLSearchParams(query);
     const formData = new FormData();
@@ -321,6 +337,12 @@
                     const formData = await webRequest.request.formData();
                     if (isFetchAction) {
                         bound = await decodeReply(formData, serverModuleMap);
+                    } else if (hasTooManyActionRefs(formData)) {
+                        // Disregard payloads with an unexpected number of action
+                        // refs instead of spending CPU decoding each of them.
+                        warnBadServerActionRequest();
+                        // Skip the fetch path
+                        return;
                     } else {
                         const action = await decodeAction(formData, serverModuleMap);
                         if (typeof action === "function") {
@@ -400,6 +422,13 @@
                             duplex: "half"
                         });
                         const formData = await fakeRequest.formData();
+                        if (hasTooManyActionRefs(formData)) {
+                            // Disregard payloads with an unexpected number of action
+                            // refs instead of spending CPU decoding each of them.
+                            warnBadServerActionRequest();
+                            // Skip the fetch path
+                            return;
+                        }
                         const action = await decodeAction(formData, serverModuleMap);
                         if (typeof action === "function") {
                             // Only warn if it's a server action, otherwise skip for other post requests

--- a/dist/server/app-render/action-handler.js
+++ b/dist/server/app-render/action-handler.js
@@ -23,6 +23,7 @@
 const _headers = require("../web/spec-extension/adapters/headers");
 const _utils1 = require("../web/utils");
 const _actionutils = require("./action-utils");
+const _requestmeta = require("../request-meta");
 function _interop_require_default(obj) {
     return obj && obj.__esModule ? obj : {
         default: obj
@@ -121,10 +122,26 @@
     // we use this to skip rendering the flight tree so that we don't update the UI
     // with the response from the forwarded worker
     forwardedHeaders.set("x-action-forwarded", "1");
-    const proto = ((_staticGenerationStore_incrementalCache = staticGenerationStore.incrementalCache) == null ? void 0 : _staticGenerationStore_incrementalCache.requestProtocol) || "https";
-    // For standalone or the serverful mode, use the internal origin directly
-    // other than the host headers from the request.
-    const origin = process.env.__NEXT_PRIVATE_ORIGIN || `${proto}://${host.value}`;
+    // Derive the internal origin from server-controlled request metadata (initURL)
+    // rather than the client-controllable Host header, which would otherwise allow
+    // an attacker to redirect the internal request to an arbitrary host (SSRF,
+    // CVE-2026-64649).
+    let origin = process.env.__NEXT_PRIVATE_ORIGIN;
+    if (origin === undefined) {
+        const initUrl = (0, _requestmeta.getRequestMeta)(req, "initURL");
+        if (initUrl !== undefined) {
+            try {
+                const parsedUrl = new URL(initUrl);
+                origin = parsedUrl.origin;
+            } catch (error) {
+                throw new Error("Could not determine origin for forwarded Server Actions request. This can happen if port or hostname are not configured for this server.", {
+                    cause: error
+                });
+            }
+        } else {
+            throw new Error("Invariant: Missing initURL");
+        }
+    }
     const fetchUrl = new URL(`${origin}${basePath}${workerPathname}`);
     try {
         let readableStream;
@@ -195,10 +212,26 @@
         }
         const forwardedHeaders = getForwardedHeaders(req, res);
         forwardedHeaders.set(_approuterheaders.RSC_HEADER, "1");
-        const proto = ((_staticGenerationStore_incrementalCache = staticGenerationStore.incrementalCache) == null ? void 0 : _staticGenerationStore_incrementalCache.requestProtocol) || "https";
-        // For standalone or the serverful mode, use the internal origin directly
-        // other than the host headers from the request.
-        const origin = process.env.__NEXT_PRIVATE_ORIGIN || `${proto}://${originalHost.value}`;
+        // Derive the internal origin from server-controlled request metadata (initURL)
+        // rather than the client-controllable Host header, which would otherwise allow
+        // an attacker to redirect the internal request to an arbitrary host (SSRF,
+        // CVE-2026-64649).
+        let origin = process.env.__NEXT_PRIVATE_ORIGIN;
+        if (origin === undefined) {
+            const initUrl = (0, _requestmeta.getRequestMeta)(req, "initURL");
+            if (initUrl !== undefined) {
+                try {
+                    const parsedUrl = new URL(initUrl);
+                    origin = parsedUrl.origin;
+                } catch (error) {
+                    throw new Error("Could not determine origin for forwarded Server Actions request. This can happen if port or hostname are not configured for this server.", {
+                        cause: error
+                    });
+                }
+            } else {
+                throw new Error("Invariant: Missing initURL");
+            }
+        }
         const fetchUrl = new URL(`${origin}${basePath}${parsedRedirectUrl.pathname}${parsedRedirectUrl.search}`);
         if (staticGenerationStore.revalidatedTags) {
             var _staticGenerationStore_incrementalCache_prerenderManifest_preview, _staticGenerationStore_incrementalCache_prerenderManifest, _staticGenerationStore_incrementalCache1;
--- a/dist/esm/server/app-render/action-handler.js
+++ b/dist/esm/server/app-render/action-handler.js
@@ -13,6 +13,7 @@
 import { HeadersAdapter } from "../web/spec-extension/adapters/headers";
 import { fromNodeOutgoingHttpHeaders } from "../web/utils";
 import { selectWorkerForForwarding } from "./action-utils";
+import { getRequestMeta } from "../request-meta";
 function hasTooManyActionRefs(formData) {
     // Guards against a denial-of-service (CVE-2026-64641) where a crafted MPA
     // (non-fetch) Server Action form submission includes a large number of
@@ -106,10 +107,26 @@
     // we use this to skip rendering the flight tree so that we don't update the UI
     // with the response from the forwarded worker
     forwardedHeaders.set("x-action-forwarded", "1");
-    const proto = ((_staticGenerationStore_incrementalCache = staticGenerationStore.incrementalCache) == null ? void 0 : _staticGenerationStore_incrementalCache.requestProtocol) || "https";
-    // For standalone or the serverful mode, use the internal origin directly
-    // other than the host headers from the request.
-    const origin = process.env.__NEXT_PRIVATE_ORIGIN || `${proto}://${host.value}`;
+    // Derive the internal origin from server-controlled request metadata (initURL)
+    // rather than the client-controllable Host header, which would otherwise allow
+    // an attacker to redirect the internal request to an arbitrary host (SSRF,
+    // CVE-2026-64649).
+    let origin = process.env.__NEXT_PRIVATE_ORIGIN;
+    if (origin === undefined) {
+        const initUrl = getRequestMeta(req, "initURL");
+        if (initUrl !== undefined) {
+            try {
+                const parsedUrl = new URL(initUrl);
+                origin = parsedUrl.origin;
+            } catch (error) {
+                throw new Error("Could not determine origin for forwarded Server Actions request. This can happen if port or hostname are not configured for this server.", {
+                    cause: error
+                });
+            }
+        } else {
+            throw new Error("Invariant: Missing initURL");
+        }
+    }
     const fetchUrl = new URL(`${origin}${basePath}${workerPathname}`);
     try {
         let readableStream;
@@ -180,10 +197,26 @@
         }
         const forwardedHeaders = getForwardedHeaders(req, res);
         forwardedHeaders.set(RSC_HEADER, "1");
-        const proto = ((_staticGenerationStore_incrementalCache = staticGenerationStore.incrementalCache) == null ? void 0 : _staticGenerationStore_incrementalCache.requestProtocol) || "https";
-        // For standalone or the serverful mode, use the internal origin directly
-        // other than the host headers from the request.
-        const origin = process.env.__NEXT_PRIVATE_ORIGIN || `${proto}://${originalHost.value}`;
+        // Derive the internal origin from server-controlled request metadata (initURL)
+        // rather than the client-controllable Host header, which would otherwise allow
+        // an attacker to redirect the internal request to an arbitrary host (SSRF,
+        // CVE-2026-64649).
+        let origin = process.env.__NEXT_PRIVATE_ORIGIN;
+        if (origin === undefined) {
+            const initUrl = getRequestMeta(req, "initURL");
+            if (initUrl !== undefined) {
+                try {
+                    const parsedUrl = new URL(initUrl);
+                    origin = parsedUrl.origin;
+                } catch (error) {
+                    throw new Error("Could not determine origin for forwarded Server Actions request. This can happen if port or hostname are not configured for this server.", {
+                        cause: error
+                    });
+                }
+            } else {
+                throw new Error("Invariant: Missing initURL");
+            }
+        }
         const fetchUrl = new URL(`${origin}${basePath}${parsedRedirectUrl.pathname}${parsedRedirectUrl.search}`);
         if (staticGenerationStore.revalidatedTags) {
             var _staticGenerationStore_incrementalCache_prerenderManifest_preview, _staticGenerationStore_incrementalCache_prerenderManifest, _staticGenerationStore_incrementalCache1;

--- a/dist/server/lib/server-ipc/utils.js
+++ b/dist/server/lib/server-ipc/utils.js
@@ -4,6 +4,7 @@
 });
 0 && (module.exports = {
     actionsForbiddenHeaders: null,
+    filterInternalHeaders: null,
     filterReqHeaders: null,
     ipcForbiddenHeaders: null
 });
@@ -17,6 +18,9 @@
     actionsForbiddenHeaders: function() {
         return actionsForbiddenHeaders;
     },
+    filterInternalHeaders: function() {
+        return filterInternalHeaders;
+    },
     filterReqHeaders: function() {
         return filterReqHeaders;
     },
@@ -53,5 +57,30 @@
     }
     return headers;
 };
+// These are headers that are only used internally and should
+// not be honored from the external request
+const INTERNAL_HEADERS = [
+    "x-middleware-rewrite",
+    "x-middleware-redirect",
+    "x-middleware-set-cookie",
+    "x-middleware-skip",
+    "x-middleware-override-headers",
+    "x-middleware-next",
+    "x-now-route-matches",
+    "x-matched-path"
+];
+const filterInternalHeaders = (headers)=>{
+    for(const header in headers){
+        if (INTERNAL_HEADERS.includes(header)) {
+            delete headers[header];
+        }
+        // If this request didn't origin from this session we filter
+        // out the "x-middleware-subrequest" header so we don't skip
+        // middleware incorrectly
+        if (header === "x-middleware-subrequest" && headers["x-middleware-subrequest-id"] !== globalThis[Symbol.for("@next/middleware-subrequest-id")]) {
+            delete headers["x-middleware-subrequest"];
+        }
+    }
+};
 
 //# sourceMappingURL=utils.js.map
\ No newline at end of file
--- a/dist/server/web/sandbox/context.js
+++ b/dist/server/web/sandbox/context.js
@@ -305,6 +305,7 @@
                 if ((store == null ? void 0 : store.headers.has("x-middleware-subrequest")) && !init.headers.has("x-middleware-subrequest")) {
                     init.headers.set("x-middleware-subrequest", store.headers.get("x-middleware-subrequest") ?? "");
                 }
+                init.headers.set("x-middleware-subrequest-id", globalThis[Symbol.for("@next/middleware-subrequest-id")]);
                 const prevs = ((_init_headers_get = init.headers.get(`x-middleware-subrequest`)) == null ? void 0 : _init_headers_get.split(":")) || [];
                 const value = prevs.concat(options.moduleName).join(":");
                 init.headers.set("x-middleware-subrequest", value);
--- a/dist/server/lib/router-server.js
+++ b/dist/server/lib/router-server.js
@@ -38,6 +38,7 @@
 const _getnextpathnameinfo = require("../../shared/lib/router/utils/get-next-pathname-info");
 const _gethostname = require("../../shared/lib/get-hostname");
 const _detectdomainlocale = require("../../shared/lib/i18n/detect-domain-locale");
+const _utils1 = require("./server-ipc/utils");
 function _interop_require_default(obj) {
     return obj && obj.__esModule ? obj : {
         default: obj
@@ -95,7 +96,15 @@
         });
     }
     renderServer.instance = require("./render-server");
+    const randomBytes = new Uint8Array(8);
+    crypto.getRandomValues(randomBytes);
+    const middlewareSubrequestId = Buffer.from(randomBytes).toString("hex");
+    globalThis[Symbol.for("@next/middleware-subrequest-id")] = middlewareSubrequestId;
     const requestHandlerImpl = async (req, res)=>{
+        // internal headers should not be honored by the request handler
+        if (!process.env.NEXT_PRIVATE_TEST_HEADERS) {
+            (0, _utils1.filterInternalHeaders)(req.headers);
+        }
         if (!opts.minimalMode && config.i18n && config.i18n.localeDetection !== false) {
             var _this;
             const urlParts = (req.url || "").split("?", 1);
--- a/dist/esm/server/lib/router-server.js
+++ b/dist/esm/server/lib/router-server.js
@@ -29,6 +29,7 @@
 import { getNextPathnameInfo } from "../../shared/lib/router/utils/get-next-pathname-info";
 import { getHostname } from "../../shared/lib/get-hostname";
 import { detectDomainLocale } from "../../shared/lib/i18n/detect-domain-locale";
+import { filterInternalHeaders } from "./server-ipc/utils";
 const debug = setupDebug("next:router-server:main");
 const isNextFont = (pathname)=>pathname && /\/media\/[^/]+\.(woff|woff2|eot|ttf|otf)$/.test(pathname);
 const requestHandlers = {};
@@ -81,7 +82,15 @@
         });
     }
     renderServer.instance = require("./render-server");
+    const randomBytes = new Uint8Array(8);
+    crypto.getRandomValues(randomBytes);
+    const middlewareSubrequestId = Buffer.from(randomBytes).toString("hex");
+    globalThis[Symbol.for("@next/middleware-subrequest-id")] = middlewareSubrequestId;
     const requestHandlerImpl = async (req, res)=>{
+        // internal headers should not be honored by the request handler
+        if (!process.env.NEXT_PRIVATE_TEST_HEADERS) {
+            filterInternalHeaders(req.headers);
+        }
         if (!opts.minimalMode && config.i18n && config.i18n.localeDetection !== false) {
             var _this;
             const urlParts = (req.url || "").split("?", 1);
--- a/dist/esm/server/lib/server-ipc/utils.js
+++ b/dist/esm/server/lib/server-ipc/utils.js
@@ -28,4 +28,29 @@
     return headers;
 };
 
+const INTERNAL_HEADERS = [
+    "x-middleware-rewrite",
+    "x-middleware-redirect",
+    "x-middleware-set-cookie",
+    "x-middleware-skip",
+    "x-middleware-override-headers",
+    "x-middleware-next",
+    "x-now-route-matches",
+    "x-matched-path"
+];
+export const filterInternalHeaders = (headers)=>{
+    for(const header in headers){
+        if (INTERNAL_HEADERS.includes(header)) {
+            delete headers[header];
+        }
+        // If this request didn't origin from this session we filter
+        // out the "x-middleware-subrequest" header so we don't skip
+        // middleware incorrectly
+        if (header === "x-middleware-subrequest" && headers["x-middleware-subrequest-id"] !== globalThis[Symbol.for("@next/middleware-subrequest-id")]) {
+            delete headers["x-middleware-subrequest"];
+        }
+    }
+};
+
+
 //# sourceMappingURL=utils.js.map
\ No newline at end of file
--- a/dist/esm/server/web/sandbox/context.js
+++ b/dist/esm/server/web/sandbox/context.js
@@ -279,6 +279,7 @@
                 if ((store == null ? void 0 : store.headers.has("x-middleware-subrequest")) && !init.headers.has("x-middleware-subrequest")) {
                     init.headers.set("x-middleware-subrequest", store.headers.get("x-middleware-subrequest") ?? "");
                 }
+                init.headers.set("x-middleware-subrequest-id", globalThis[Symbol.for("@next/middleware-subrequest-id")]);
                 const prevs = ((_init_headers_get = init.headers.get(`x-middleware-subrequest`)) == null ? void 0 : _init_headers_get.split(":")) || [];
                 const value = prevs.concat(options.moduleName).join(":");
                 init.headers.set("x-middleware-subrequest", value);
--- /dev/null
+++ b/test/cve-2025-29927.test.js
@@ -0,0 +1,64 @@
+// Regression test for CVE-2025-29927 (GHSA-f82v-jwr5-mffw)
+// Middleware authorization bypass via a forged "x-middleware-subrequest" header.
+//
+// The upstream fix (Next.js 14.2.25, PR #77202/#77201) added
+// `filterInternalHeaders` in server-ipc/utils, which strips an incoming
+// "x-middleware-subrequest" header unless it is accompanied by a matching
+// per-server-instance "x-middleware-subrequest-id" (a random id stored on
+// globalThis[Symbol.for('@next/middleware-subrequest-id')]). This node:assert
+// test exercises that compiled behavior directly against the installed package
+// (the npm tarball ships only compiled dist/ JS, so the upstream e2e test in
+// test/e2e/middleware-general is not runnable here).
+const assert = require('node:assert')
+const {
+  filterInternalHeaders,
+} = require('next/dist/server/lib/server-ipc/utils')
+
+const SUBREQUEST_ID_SYMBOL = Symbol.for('@next/middleware-subrequest-id')
+const sessionId = 'deadbeefcafebabe'
+globalThis[SUBREQUEST_ID_SYMBOL] = sessionId
+
+// 1) A forged subrequest header from an external request (no matching id) must
+//    be stripped so middleware is NOT skipped -> mitigates the bypass.
+const forged = {
+  'x-middleware-subrequest':
+    'middleware:middleware:middleware:middleware:middleware',
+}
+filterInternalHeaders(forged)
+assert.strictEqual(
+  forged['x-middleware-subrequest'],
+  undefined,
+  'forged x-middleware-subrequest header must be removed'
+)
+
+// 2) A forged header carrying a wrong id must also be stripped.
+const wrongId = {
+  'x-middleware-subrequest': 'middleware',
+  'x-middleware-subrequest-id': 'not-the-session-id',
+}
+filterInternalHeaders(wrongId)
+assert.strictEqual(
+  wrongId['x-middleware-subrequest'],
+  undefined,
+  'x-middleware-subrequest with a mismatched id must be removed'
+)
+
+// 3) A legitimate in-session subrequest (matching id) must be preserved.
+const legit = {
+  'x-middleware-subrequest': 'middleware',
+  'x-middleware-subrequest-id': sessionId,
+}
+filterInternalHeaders(legit)
+assert.strictEqual(
+  legit['x-middleware-subrequest'],
+  'middleware',
+  'legitimate in-session x-middleware-subrequest header must be preserved'
+)
+
+// 4) Other internal headers must be filtered out regardless.
+const internal = { 'x-middleware-rewrite': '/somewhere', 'x-matched-path': '/' }
+filterInternalHeaders(internal)
+assert.strictEqual(internal['x-middleware-rewrite'], undefined)
+assert.strictEqual(internal['x-matched-path'], undefined)
+
+console.log('CVE-2025-29927 regression test passed')

--- a/dist/server/base-server.js
+++ b/dist/server/base-server.js
@@ -1601,7 +1601,7 @@
             }
             return {
                 ...result,
-                revalidate: result.revalidate !== undefined ? result.revalidate : /* default to minimum revalidate (this should be an invariant) */ 1
+                revalidate: result.revalidate
             };
         }, {
             routeKind: routeModule == null ? void 0 : routeModule.definition.kind,
--- a/dist/esm/server/base-server.js
+++ b/dist/esm/server/base-server.js
@@ -1528,7 +1528,7 @@
             }
             return {
                 ...result,
-                revalidate: result.revalidate !== undefined ? result.revalidate : /* default to minimum revalidate (this should be an invariant) */ 1
+                revalidate: result.revalidate
             };
         }, {
             routeKind: routeModule == null ? void 0 : routeModule.definition.kind,
--- a/dist/server/render.js
+++ b/dist/server/render.js
@@ -636,6 +636,7 @@
                     defaultLocale: renderOpts.defaultLocale
                 }));
             canAccessRes = false;
+            metadata.revalidate = 0;
         } catch (serverSidePropsError) {
             // remove not found error code to prevent triggering legacy
             // 404 rendering
--- a/dist/esm/server/render.js
+++ b/dist/esm/server/render.js
@@ -605,6 +605,7 @@
                     defaultLocale: renderOpts.defaultLocale
                 }));
             canAccessRes = false;
+            metadata.revalidate = 0;
         } catch (serverSidePropsError) {
             // remove not found error code to prevent triggering legacy
             // 404 rendering
--- /dev/null
+++ b/test/cve-2024-46982.test.js
@@ -0,0 +1,66 @@
+/*
+ * Regression test for CVE-2024-46982 (GHSA-gp8f-8m3g-qvj9).
+ *
+ * Upstream fix: vercel/next.js PR #69990
+ *   - commit 7ed7f125e07ef0517a331009ed7e32691ba403d3
+ *   - commit bd164d53af259c05f1ab434004bcfdd3837d7cda
+ *
+ * The upstream fix touches full standalone-mode integration tests that are not
+ * shipped in the published npm package, so this is a minimal replacement test
+ * that asserts the two source-level fixes landed in the compiled dist output:
+ *
+ *   1. base-server: the invalid "default to minimum revalidate" fallback of `1`
+ *      is removed; the render result's revalidate value is used verbatim. The
+ *      bogus fallback let a non-cacheable (SSR) response be treated as
+ *      cacheable and poisoned the response cache.
+ *   2. render: when server-side props access fails (canAccessRes = false),
+ *      metadata.revalidate is forced to 0 so the response is never cached.
+ */
+const assert = require('node:assert');
+const fs = require('node:fs');
+const path = require('node:path');
+
+const pkgRoot = path.join(__dirname, '..');
+
+function read(rel) {
+  return fs.readFileSync(path.join(pkgRoot, rel), 'utf8');
+}
+
+const distTargets = [
+  'dist/server/base-server.js',
+  'dist/esm/server/base-server.js',
+];
+
+for (const rel of distTargets) {
+  const src = read(rel);
+  // The vulnerable fallback must be gone.
+  assert.ok(
+    !/default to minimum revalidate/.test(src),
+    `${rel}: invalid revalidate fallback comment must be removed`
+  );
+  assert.ok(
+    !/revalidate:\s*result\.revalidate\s*!==\s*undefined\s*\?\s*result\.revalidate\s*:/.test(src),
+    `${rel}: revalidate must not default to 1 when undefined`
+  );
+  // The fixed form must be present.
+  assert.ok(
+    /revalidate:\s*result\.revalidate\b/.test(src),
+    `${rel}: revalidate must pass through result.revalidate verbatim`
+  );
+}
+
+const renderTargets = [
+  'dist/server/render.js',
+  'dist/esm/server/render.js',
+];
+
+for (const rel of renderTargets) {
+  const src = read(rel);
+  // metadata.revalidate must be forced to 0 right after canAccessRes = false.
+  assert.ok(
+    /canAccessRes\s*=\s*false;\s*metadata\.revalidate\s*=\s*0;/.test(src),
+    `${rel}: metadata.revalidate must be set to 0 after canAccessRes = false`
+  );
+}
+
+console.log('CVE-2024-46982 regression test passed');

--- a/dist/server/lib/router-utils/resolve-routes.js
+++ b/dist/server/lib/router-utils/resolve-routes.js
@@ -246,6 +246,17 @@
                             updated = true;
                             normalized = normalizers.postponed.normalize(normalized, true);
                         }
+                        if (config.i18n) {
+                            const curLocaleResult = (0, _normalizelocalepath.normalizeLocalePath)(normalized, config.i18n.locales);
+                            if (curLocaleResult.detectedLocale) {
+                                parsedUrl.query.__nextLocale = curLocaleResult.detectedLocale;
+                            } else if (defaultLocale && !curLocaleResult.pathname.startsWith("/_next/")) {
+                                // Match normalized _next/data requests against the same
+                                // locale-prefixed internal pathname shape used by direct page
+                                // requests when the default locale was inferred.
+                                normalized = (0, _addpathprefix.addPathPrefix)(curLocaleResult.pathname === "/" ? `/${defaultLocale}` : (0, _addpathprefix.addPathPrefix)(curLocaleResult.pathname || "", `/${defaultLocale}`));
+                            }
+                        }
                         // If we updated the pathname, and it had a base path, re-add the
                         // base path.
                         if (updated) {
--- a/dist/esm/server/lib/router-utils/resolve-routes.js
+++ b/dist/esm/server/lib/router-utils/resolve-routes.js
@@ -231,6 +231,17 @@
                             updated = true;
                             normalized = normalizers.postponed.normalize(normalized, true);
                         }
+                        if (config.i18n) {
+                            const curLocaleResult = normalizeLocalePath(normalized, config.i18n.locales);
+                            if (curLocaleResult.detectedLocale) {
+                                parsedUrl.query.__nextLocale = curLocaleResult.detectedLocale;
+                            } else if (defaultLocale && !curLocaleResult.pathname.startsWith("/_next/")) {
+                                // Match normalized _next/data requests against the same
+                                // locale-prefixed internal pathname shape used by direct page
+                                // requests when the default locale was inferred.
+                                normalized = addPathPrefix(curLocaleResult.pathname === "/" ? `/${defaultLocale}` : addPathPrefix(curLocaleResult.pathname || "", `/${defaultLocale}`));
+                            }
+                        }
                         // If we updated the pathname, and it had a base path, re-add the
                         // base path.
                         if (updated) {

--- a/dist/server/app-render/action-handler.js
+++ b/dist/server/app-render/action-handler.js
@@ -53,6 +53,9 @@
     }
     return formData;
 }
+function hasServerActions(manifest) {
+    return Object.keys(manifest.node).length > 0 || Object.keys(manifest.edge).length > 0;
+}
 function nodeHeadersToRecord(headers) {
     const record = {};
     for (const [key, value] of Object.entries(headers)){
@@ -283,6 +286,27 @@
     if (!(0, _serveractionrequestmeta.getIsServerAction)(req)) {
         return;
     }
+
+    // We don't currently support URL encoded actions, so we bail out early.
+    // Depending on if it's a fetch action or an MPA, we return a different response.
+    if (isURLEncodedAction) {
+        if (isFetchAction) {
+            return {
+                type: "not-found"
+            };
+        } else {
+            // This is an MPA action, so we return
+            return;
+        }
+    }
+    // If the app has no server actions at all, we can 404 early.
+    if (!hasServerActions(serverActionsManifest)) {
+        const error = getActionNotFoundError(actionId);
+        console.warn(error);
+        return {
+            type: "not-found"
+        };
+    }
     if (staticGenerationStore.isStaticGeneration) {
         throw new Error("Invariant: server actions can't be handled during static rendering");
     }
@@ -384,6 +408,14 @@
                     // TODO-APP: Add streaming support
                     const formData = await webRequest.request.formData();
                     if (isFetchAction) {
+                        try {
+                            actionModId = getActionModIdOrError(actionId, serverModuleMap);
+                        } catch (err) {
+                            console.warn(err);
+                            return {
+                                type: "not-found"
+                            };
+                        }
                         bound = await decodeReply(formData, serverModuleMap);
                     } else if (hasTooManyActionRefs(formData)) {
                         // Disregard payloads with an unexpected number of action
@@ -392,6 +424,11 @@
                         // Skip the fetch path
                         return;
                     } else {
+                        if (areAllActionIdsValid(formData, serverModuleMap) === false) {
+                            // TODO: This can be from skew or manipulated input. We should handle this case
+                            // more gracefully but this preserves the prior behavior where decodeAction would throw instead.
+                            throw new Error("Failed to find Server Action. This request might be from an older or newer deployment.\nRead more: https://nextjs.org/docs/messages/failed-to-find-server-action");
+                        }
                         const action = await decodeAction(formData, serverModuleMap);
                         if (typeof action === "function") {
                             // Only warn if it's a server action, otherwise skip for other post requests
@@ -420,18 +457,21 @@
                         }
                         actionData += new TextDecoder().decode(value);
                     }
-                    if (isURLEncodedAction) {
-                        const formData = formDataFromSearchQueryString(actionData);
-                        bound = await decodeReply(formData, serverModuleMap);
-                    } else {
-                        bound = await decodeReply(actionData, serverModuleMap);
-                    }
+                    bound = await decodeReply(actionData, serverModuleMap);
                 }
             } else {
                 // Use react-server-dom-webpack/server.node which supports streaming
                 const { decodeReply, decodeReplyFromBusboy, decodeAction, decodeFormState } = require(`./react-server.node`);
                 if (isMultipartAction) {
                     if (isFetchAction) {
+                        try {
+                            actionModId = getActionModIdOrError(actionId, serverModuleMap);
+                        } catch (err) {
+                            console.warn(err);
+                            return {
+                                type: "not-found"
+                            };
+                        }
                         const readableLimit = (serverActions == null ? void 0 : serverActions.bodySizeLimit) ?? "1 MB";
                         const limit = require("next/dist/compiled/bytes").parse(readableLimit);
                         const busboy = require("busboy");
@@ -477,6 +517,11 @@
                             // Skip the fetch path
                             return;
                         }
+                        if (areAllActionIdsValid(formData, serverModuleMap) === false) {
+                            // TODO: This can be from skew or manipulated input. We should handle this case
+                            // more gracefully but this preserves the prior behavior where decodeAction would throw instead.
+                            throw new Error("Failed to find Server Action. This request might be from an older or newer deployment.\nRead more: https://nextjs.org/docs/messages/failed-to-find-server-action");
+                        }
                         const action = await decodeAction(formData, serverModuleMap);
                         if (typeof action === "function") {
                             // Only warn if it's a server action, otherwise skip for other post requests
@@ -508,12 +553,7 @@
                         throw new ApiError(413, `Body exceeded ${readableLimit} limit.
 To configure the body size limit for Server Actions, see: https://nextjs.org/docs/app/api-reference/next-config-js/serverActions#bodysizelimit`);
                     }
-                    if (isURLEncodedAction) {
-                        const formData = formDataFromSearchQueryString(actionData);
-                        bound = await decodeReply(formData, serverModuleMap);
-                    } else {
-                        bound = await decodeReply(actionData, serverModuleMap);
-                    }
+                    bound = await decodeReply(actionData, serverModuleMap);
                 }
             }
             // actions.js
@@ -656,8 +696,89 @@
         }
         return actionModId;
     } catch (err) {
-        throw new Error(`Failed to find Server Action "${actionId}". This request might be from an older or newer deployment. ${err instanceof Error ? `Original error: ${err.message}` : ""}`);
+        throw getActionNotFoundError(actionId, err);
     }
 }
 
+function getActionNotFoundError(actionId, err) {
+    return new Error(`Failed to find Server Action${actionId ? ` "${actionId}"` : ""}. This request might be from an older or newer deployment. ${err instanceof Error ? `Original error: ${err.message}` : ""}`);
+}
+const $ACTION_ = "$ACTION_";
+const $ACTION_REF_ID = "$ACTION_REF_";
+const $ACTION_ID_ = "$ACTION_ID_";
+const ACTION_ID_EXPECTED_LENGTH = 40;
+/**
+ * This function mirrors logic inside React's decodeAction and should be kept in sync with that.
+ * It pre-parses the FormData to ensure that any action IDs referred to are actual action IDs for
+ * this Next.js application.
+ */
+function areAllActionIdsValid(mpaFormData, serverModuleMap) {
+    let hasAtLeastOneAction = false;
+    // Before we attempt to decode the payload for a possible MPA action, assert that all
+    // action IDs are valid IDs. If not we should disregard the payload
+    for (let key of mpaFormData.keys()){
+        if (!key.startsWith($ACTION_)) {
+            // not a relevant field
+            continue;
+        }
+        if (key.startsWith($ACTION_ID_)) {
+            // No Bound args case
+            if (isInvalidActionIdFieldName(key, serverModuleMap)) {
+                return false;
+            }
+            hasAtLeastOneAction = true;
+        } else if (key.startsWith($ACTION_REF_ID)) {
+            // Bound args case
+            const actionDescriptorField = $ACTION_ + key.slice($ACTION_REF_ID.length) + ":0";
+            const actionFields = mpaFormData.getAll(actionDescriptorField);
+            if (actionFields.length !== 1) {
+                return false;
+            }
+            const actionField = actionFields[0];
+            if (typeof actionField !== "string") {
+                return false;
+            }
+            if (isInvalidStringActionDescriptor(actionField, serverModuleMap)) {
+                return false;
+            }
+            hasAtLeastOneAction = true;
+        }
+    }
+    return hasAtLeastOneAction;
+}
+const ACTION_DESCRIPTOR_ID_PREFIX = '{"id":"';
+function isInvalidStringActionDescriptor(actionDescriptor, serverModuleMap) {
+    if (actionDescriptor.startsWith(ACTION_DESCRIPTOR_ID_PREFIX) === false) {
+        return true;
+    }
+    const from = ACTION_DESCRIPTOR_ID_PREFIX.length;
+    const to = from + ACTION_ID_EXPECTED_LENGTH;
+    // We expect actionDescriptor to be '{"id":"<actionId>",...}'
+    const actionId = actionDescriptor.slice(from, to);
+    if (actionId.length !== ACTION_ID_EXPECTED_LENGTH || actionDescriptor[to] !== '"') {
+        return true;
+    }
+    const entry = serverModuleMap[actionId];
+    if (entry == null) {
+        return true;
+    }
+    return false;
+}
+function isInvalidActionIdFieldName(actionIdFieldName, serverModuleMap) {
+    // The field name must always start with $ACTION_ID_ but since it is
+    // the id is extracted from the key of the field we have already validated
+    // this before entering this function
+    if (actionIdFieldName.length !== $ACTION_ID_.length + ACTION_ID_EXPECTED_LENGTH) {
+        // this field name has too few or too many characters
+        return true;
+    }
+    const actionId = actionIdFieldName.slice($ACTION_ID_.length);
+    const entry = serverModuleMap[actionId];
+    if (entry == null) {
+        return true;
+    }
+    return false;
+}
+
+
 //# sourceMappingURL=action-handler.js.map
\ No newline at end of file
--- a/dist/esm/server/app-render/action-handler.js
+++ b/dist/esm/server/app-render/action-handler.js
@@ -38,6 +38,9 @@
     }
     return formData;
 }
+function hasServerActions(manifest) {
+    return Object.keys(manifest.node).length > 0 || Object.keys(manifest.edge).length > 0;
+}
 function nodeHeadersToRecord(headers) {
     const record = {};
     for (const [key, value] of Object.entries(headers)){
@@ -268,6 +271,27 @@
     if (!getIsServerAction(req)) {
         return;
     }
+
+    // We don't currently support URL encoded actions, so we bail out early.
+    // Depending on if it's a fetch action or an MPA, we return a different response.
+    if (isURLEncodedAction) {
+        if (isFetchAction) {
+            return {
+                type: "not-found"
+            };
+        } else {
+            // This is an MPA action, so we return
+            return;
+        }
+    }
+    // If the app has no server actions at all, we can 404 early.
+    if (!hasServerActions(serverActionsManifest)) {
+        const error = getActionNotFoundError(actionId);
+        console.warn(error);
+        return {
+            type: "not-found"
+        };
+    }
     if (staticGenerationStore.isStaticGeneration) {
         throw new Error("Invariant: server actions can't be handled during static rendering");
     }
@@ -369,6 +393,14 @@
                     // TODO-APP: Add streaming support
                     const formData = await webRequest.request.formData();
                     if (isFetchAction) {
+                        try {
+                            actionModId = getActionModIdOrError(actionId, serverModuleMap);
+                        } catch (err) {
+                            console.warn(err);
+                            return {
+                                type: "not-found"
+                            };
+                        }
                         bound = await decodeReply(formData, serverModuleMap);
                     } else if (hasTooManyActionRefs(formData)) {
                         // Disregard payloads with an unexpected number of action
@@ -377,6 +409,11 @@
                         // Skip the fetch path
                         return;
                     } else {
+                        if (areAllActionIdsValid(formData, serverModuleMap) === false) {
+                            // TODO: This can be from skew or manipulated input. We should handle this case
+                            // more gracefully but this preserves the prior behavior where decodeAction would throw instead.
+                            throw new Error("Failed to find Server Action. This request might be from an older or newer deployment.\nRead more: https://nextjs.org/docs/messages/failed-to-find-server-action");
+                        }
                         const action = await decodeAction(formData, serverModuleMap);
                         if (typeof action === "function") {
                             // Only warn if it's a server action, otherwise skip for other post requests
@@ -405,18 +442,21 @@
                         }
                         actionData += new TextDecoder().decode(value);
                     }
-                    if (isURLEncodedAction) {
-                        const formData = formDataFromSearchQueryString(actionData);
-                        bound = await decodeReply(formData, serverModuleMap);
-                    } else {
-                        bound = await decodeReply(actionData, serverModuleMap);
-                    }
+                    bound = await decodeReply(actionData, serverModuleMap);
                 }
             } else {
                 // Use react-server-dom-webpack/server.node which supports streaming
                 const { decodeReply, decodeReplyFromBusboy, decodeAction, decodeFormState } = require(`./react-server.node`);
                 if (isMultipartAction) {
                     if (isFetchAction) {
+                        try {
+                            actionModId = getActionModIdOrError(actionId, serverModuleMap);
+                        } catch (err) {
+                            console.warn(err);
+                            return {
+                                type: "not-found"
+                            };
+                        }
                         const readableLimit = (serverActions == null ? void 0 : serverActions.bodySizeLimit) ?? "1 MB";
                         const limit = require("next/dist/compiled/bytes").parse(readableLimit);
                         const busboy = require("busboy");
@@ -462,6 +502,11 @@
                             // Skip the fetch path
                             return;
                         }
+                        if (areAllActionIdsValid(formData, serverModuleMap) === false) {
+                            // TODO: This can be from skew or manipulated input. We should handle this case
+                            // more gracefully but this preserves the prior behavior where decodeAction would throw instead.
+                            throw new Error("Failed to find Server Action. This request might be from an older or newer deployment.\nRead more: https://nextjs.org/docs/messages/failed-to-find-server-action");
+                        }
                         const action = await decodeAction(formData, serverModuleMap);
                         if (typeof action === "function") {
                             // Only warn if it's a server action, otherwise skip for other post requests
@@ -493,12 +538,7 @@
                         throw new ApiError(413, `Body exceeded ${readableLimit} limit.
 To configure the body size limit for Server Actions, see: https://nextjs.org/docs/app/api-reference/next-config-js/serverActions#bodysizelimit`);
                     }
-                    if (isURLEncodedAction) {
-                        const formData = formDataFromSearchQueryString(actionData);
-                        bound = await decodeReply(formData, serverModuleMap);
-                    } else {
-                        bound = await decodeReply(actionData, serverModuleMap);
-                    }
+                    bound = await decodeReply(actionData, serverModuleMap);
                 }
             }
             // actions.js
@@ -641,8 +681,89 @@
         }
         return actionModId;
     } catch (err) {
-        throw new Error(`Failed to find Server Action "${actionId}". This request might be from an older or newer deployment. ${err instanceof Error ? `Original error: ${err.message}` : ""}`);
+        throw getActionNotFoundError(actionId, err);
     }
 }
 
+function getActionNotFoundError(actionId, err) {
+    return new Error(`Failed to find Server Action${actionId ? ` "${actionId}"` : ""}. This request might be from an older or newer deployment. ${err instanceof Error ? `Original error: ${err.message}` : ""}`);
+}
+const $ACTION_ = "$ACTION_";
+const $ACTION_REF_ID = "$ACTION_REF_";
+const $ACTION_ID_ = "$ACTION_ID_";
+const ACTION_ID_EXPECTED_LENGTH = 40;
+/**
+ * This function mirrors logic inside React's decodeAction and should be kept in sync with that.
+ * It pre-parses the FormData to ensure that any action IDs referred to are actual action IDs for
+ * this Next.js application.
+ */
+function areAllActionIdsValid(mpaFormData, serverModuleMap) {
+    let hasAtLeastOneAction = false;
+    // Before we attempt to decode the payload for a possible MPA action, assert that all
+    // action IDs are valid IDs. If not we should disregard the payload
+    for (let key of mpaFormData.keys()){
+        if (!key.startsWith($ACTION_)) {
+            // not a relevant field
+            continue;
+        }
+        if (key.startsWith($ACTION_ID_)) {
+            // No Bound args case
+            if (isInvalidActionIdFieldName(key, serverModuleMap)) {
+                return false;
+            }
+            hasAtLeastOneAction = true;
+        } else if (key.startsWith($ACTION_REF_ID)) {
+            // Bound args case
+            const actionDescriptorField = $ACTION_ + key.slice($ACTION_REF_ID.length) + ":0";
+            const actionFields = mpaFormData.getAll(actionDescriptorField);
+            if (actionFields.length !== 1) {
+                return false;
+            }
+            const actionField = actionFields[0];
+            if (typeof actionField !== "string") {
+                return false;
+            }
+            if (isInvalidStringActionDescriptor(actionField, serverModuleMap)) {
+                return false;
+            }
+            hasAtLeastOneAction = true;
+        }
+    }
+    return hasAtLeastOneAction;
+}
+const ACTION_DESCRIPTOR_ID_PREFIX = '{"id":"';
+function isInvalidStringActionDescriptor(actionDescriptor, serverModuleMap) {
+    if (actionDescriptor.startsWith(ACTION_DESCRIPTOR_ID_PREFIX) === false) {
+        return true;
+    }
+    const from = ACTION_DESCRIPTOR_ID_PREFIX.length;
+    const to = from + ACTION_ID_EXPECTED_LENGTH;
+    // We expect actionDescriptor to be '{"id":"<actionId>",...}'
+    const actionId = actionDescriptor.slice(from, to);
+    if (actionId.length !== ACTION_ID_EXPECTED_LENGTH || actionDescriptor[to] !== '"') {
+        return true;
+    }
+    const entry = serverModuleMap[actionId];
+    if (entry == null) {
+        return true;
+    }
+    return false;
+}
+function isInvalidActionIdFieldName(actionIdFieldName, serverModuleMap) {
+    // The field name must always start with $ACTION_ID_ but since it is
+    // the id is extracted from the key of the field we have already validated
+    // this before entering this function
+    if (actionIdFieldName.length !== $ACTION_ID_.length + ACTION_ID_EXPECTED_LENGTH) {
+        // this field name has too few or too many characters
+        return true;
+    }
+    const actionId = actionIdFieldName.slice($ACTION_ID_.length);
+    const entry = serverModuleMap[actionId];
+    if (entry == null) {
+        return true;
+    }
+    return false;
+}
+
+
 //# sourceMappingURL=action-handler.js.map
\ No newline at end of file
