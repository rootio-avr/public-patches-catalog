diff --git a/dist/esm/server/base-server.js b/dist/esm/server/base-server.js
index 1fcb31c..185bd6d 100644
--- a/dist/esm/server/base-server.js
+++ b/dist/esm/server/base-server.js
@@ -1533,7 +1533,7 @@ export default class Server {
             }
             return {
                 ...result,
-                revalidate: result.revalidate !== undefined ? result.revalidate : /* default to minimum revalidate (this should be an invariant) */ 1
+                revalidate: result.revalidate
             };
         }, {
             routeKind: routeModule == null ? void 0 : routeModule.definition.kind,
diff --git a/dist/esm/server/render.js b/dist/esm/server/render.js
index 9f828b6..1cea94c 100644
--- a/dist/esm/server/render.js
+++ b/dist/esm/server/render.js
@@ -605,6 +605,7 @@ export async function renderToHTMLImpl(req, res, pathname, query, renderOpts, ex
                     defaultLocale: renderOpts.defaultLocale
                 }));
             canAccessRes = false;
+            metadata.revalidate = 0;
         } catch (serverSidePropsError) {
             // remove not found error code to prevent triggering legacy
             // 404 rendering
diff --git a/dist/server/base-server.js b/dist/server/base-server.js
index 2aed864..47d414e 100644
--- a/dist/server/base-server.js
+++ b/dist/server/base-server.js
@@ -1606,7 +1606,7 @@ class Server {
             }
             return {
                 ...result,
-                revalidate: result.revalidate !== undefined ? result.revalidate : /* default to minimum revalidate (this should be an invariant) */ 1
+                revalidate: result.revalidate
             };
         }, {
             routeKind: routeModule == null ? void 0 : routeModule.definition.kind,
diff --git a/dist/server/render.js b/dist/server/render.js
index b808abf..f92a7c8 100644
--- a/dist/server/render.js
+++ b/dist/server/render.js
@@ -636,6 +636,7 @@ async function renderToHTMLImpl(req, res, pathname, query, renderOpts, extra) {
                     defaultLocale: renderOpts.defaultLocale
                 }));
             canAccessRes = false;
+            metadata.revalidate = 0;
         } catch (serverSidePropsError) {
             // remove not found error code to prevent triggering legacy
             // 404 rendering

--- a/dist/server/app-render/action-handler.js
+++ b/dist/server/app-render/action-handler.js
@@ -36,6 +36,18 @@
     }
     return formData;
 }
+const MAX_ACTION_REF_FIELDS = 2;
+function isMpaActionFormDataWithinBounds(formData) {
+    let seenActionRefs = 0;
+    for (const key of formData.keys()){
+        if (key.startsWith("$ACTION_REF_")) {
+            if (++seenActionRefs > MAX_ACTION_REF_FIELDS) {
+                return false;
+            }
+        }
+    }
+    return true;
+}
 function nodeHeadersToRecord(headers) {
     const record = {};
     for (const [key, value] of Object.entries(headers)){
@@ -337,6 +349,11 @@
                     if (isFetchAction) {
                         bound = await decodeReply(formData, serverModuleMap);
                     } else {
+                        if (!isMpaActionFormDataWithinBounds(formData)) {
+                            // CVE-2026-64641: reject implausible number of
+                            // $ACTION_REF_ fields before decodeAction.
+                            return;
+                        }
                         const action = await decodeAction(formData, serverModuleMap);
                         if (typeof action === "function") {
                             // Only warn if it's a server action, otherwise skip for other post requests
@@ -417,6 +434,11 @@
                             duplex: "half"
                         });
                         const formData = await fakeRequest.formData();
+                        if (!isMpaActionFormDataWithinBounds(formData)) {
+                            // CVE-2026-64641: reject implausible number of
+                            // $ACTION_REF_ fields before decodeAction.
+                            return;
+                        }
                         const action = await decodeAction(formData, serverModuleMap);
                         if (typeof action === "function") {
                             // Only warn if it's a server action, otherwise skip for other post requests
--- a/dist/esm/server/app-render/action-handler.js
+++ b/dist/esm/server/app-render/action-handler.js
@@ -21,6 +21,18 @@
     }
     return formData;
 }
+const MAX_ACTION_REF_FIELDS = 2;
+function isMpaActionFormDataWithinBounds(formData) {
+    let seenActionRefs = 0;
+    for (const key of formData.keys()){
+        if (key.startsWith("$ACTION_REF_")) {
+            if (++seenActionRefs > MAX_ACTION_REF_FIELDS) {
+                return false;
+            }
+        }
+    }
+    return true;
+}
 function nodeHeadersToRecord(headers) {
     const record = {};
     for (const [key, value] of Object.entries(headers)){
@@ -322,6 +334,11 @@
                     if (isFetchAction) {
                         bound = await decodeReply(formData, serverModuleMap);
                     } else {
+                        if (!isMpaActionFormDataWithinBounds(formData)) {
+                            // CVE-2026-64641: reject implausible number of
+                            // $ACTION_REF_ fields before decodeAction.
+                            return;
+                        }
                         const action = await decodeAction(formData, serverModuleMap);
                         if (typeof action === "function") {
                             // Only warn if it's a server action, otherwise skip for other post requests
@@ -402,6 +419,11 @@
                             duplex: "half"
                         });
                         const formData = await fakeRequest.formData();
+                        if (!isMpaActionFormDataWithinBounds(formData)) {
+                            // CVE-2026-64641: reject implausible number of
+                            // $ACTION_REF_ fields before decodeAction.
+                            return;
+                        }
                         const action = await decodeAction(formData, serverModuleMap);
                         if (typeof action === "function") {
                             // Only warn if it's a server action, otherwise skip for other post requests

diff --git a/dist/esm/server/base-server.js b/dist/esm/server/base-server.js
index 1fcb31c..4ab9ab2 100644
--- a/dist/esm/server/base-server.js
+++ b/dist/esm/server/base-server.js
@@ -457,6 +457,13 @@ export default class Server {
             req.headers["x-forwarded-port"] ??= this.port ? this.port.toString() : isHttps ? "443" : "80";
             req.headers["x-forwarded-proto"] ??= isHttps ? "https" : "http";
             req.headers["x-forwarded-for"] ??= (_originalRequest_socket1 = originalRequest.socket) == null ? void 0 : _originalRequest_socket1.remoteAddress;
+            // Validate that if i18n isn't configured or the passed parameters are not
+            // valid it should be removed from the query.
+            if (!(this.i18nProvider && this.i18nProvider.validateQuery(parsedUrl.query))) {
+                delete parsedUrl.query.__nextLocale;
+                delete parsedUrl.query.__nextDefaultLocale;
+                delete parsedUrl.query.__nextInferredLocaleFromDefault;
+            }
             // This should be done before any normalization of the pathname happens as
             // it captures the initial URL.
             this.attachRequestMeta(req, parsedUrl);
diff --git a/dist/esm/server/future/helpers/i18n-provider.js b/dist/esm/server/future/helpers/i18n-provider.js
index 632894a..e4f6b76 100644
--- a/dist/esm/server/future/helpers/i18n-provider.js
+++ b/dist/esm/server/future/helpers/i18n-provider.js
@@ -73,6 +73,28 @@
         };
     }
     /**
+   * Validates that the locale is valid.
+   *
+   * @param locale The locale to validate.
+   * @returns `true` if the locale is valid, `false` otherwise.
+   */ validate(locale) {
+        return this.lowerCaseLocales.includes(locale.toLowerCase());
+    }
+    /**
+   * Validates that the locales in the query object are valid.
+   *
+   * @param query The query object to validate.
+   * @returns `true` if the locale is valid, `false` otherwise.
+   */ validateQuery(query) {
+        if (query.__nextLocale && !this.validate(query.__nextLocale)) {
+            return false;
+        }
+        if (query.__nextDefaultLocale && !this.validate(query.__nextDefaultLocale)) {
+            return false;
+        }
+        return true;
+    }
+    /**
    * Analyzes the pathname for a locale and returns the pathname without it.
    *
    * @param pathname The pathname that could contain a locale prefix.
diff --git a/dist/esm/server/lib/router-utils/resolve-routes.js b/dist/esm/server/lib/router-utils/resolve-routes.js
index ad68736..cddd4fe 100644
--- a/dist/esm/server/lib/router-utils/resolve-routes.js
+++ b/dist/esm/server/lib/router-utils/resolve-routes.js
@@ -117,6 +117,11 @@ export function getResolveRoutes(fsChecker, config, opts, renderServer, renderSe
                     parsedUrl.pathname = maybeAddTrailingSlash(parsedUrl.pathname);
                 }
             }
+        } else {
+            // As i18n isn't configured we remove the locale related query params.
+            delete parsedUrl.query.__nextLocale;
+            delete parsedUrl.query.__nextDefaultLocale;
+            delete parsedUrl.query.__nextInferredLocaleFromDefault;
         }
         const checkLocaleApi = (pathname)=>{
             if (config.i18n && pathname === urlNoQuery && (initialLocaleResult == null ? void 0 : initialLocaleResult.detectedLocale) && pathHasPrefix(initialLocaleResult.pathname, "/api")) {
diff --git a/dist/server/base-server.js b/dist/server/base-server.js
index 2aed864..40ff073 100644
--- a/dist/server/base-server.js
+++ b/dist/server/base-server.js
@@ -530,6 +530,13 @@ class Server {
             req.headers["x-forwarded-port"] ??= this.port ? this.port.toString() : isHttps ? "443" : "80";
             req.headers["x-forwarded-proto"] ??= isHttps ? "https" : "http";
             req.headers["x-forwarded-for"] ??= (_originalRequest_socket1 = originalRequest.socket) == null ? void 0 : _originalRequest_socket1.remoteAddress;
+            // Validate that if i18n isn't configured or the passed parameters are not
+            // valid it should be removed from the query.
+            if (!(this.i18nProvider && this.i18nProvider.validateQuery(parsedUrl.query))) {
+                delete parsedUrl.query.__nextLocale;
+                delete parsedUrl.query.__nextDefaultLocale;
+                delete parsedUrl.query.__nextInferredLocaleFromDefault;
+            }
             // This should be done before any normalization of the pathname happens as
             // it captures the initial URL.
             this.attachRequestMeta(req, parsedUrl);
diff --git a/dist/server/future/helpers/i18n-provider.js b/dist/server/future/helpers/i18n-provider.js
index 8473490..4c4db05 100644
--- a/dist/server/future/helpers/i18n-provider.js
+++ b/dist/server/future/helpers/i18n-provider.js
@@ -79,6 +79,28 @@ class I18NProvider {
         };
     }
     /**
+   * Validates that the locale is valid.
+   *
+   * @param locale The locale to validate.
+   * @returns `true` if the locale is valid, `false` otherwise.
+   */ validate(locale) {
+        return this.lowerCaseLocales.includes(locale.toLowerCase());
+    }
+    /**
+   * Validates that the locales in the query object are valid.
+   *
+   * @param query The query object to validate.
+   * @returns `true` if the locale is valid, `false` otherwise.
+   */ validateQuery(query) {
+        if (query.__nextLocale && !this.validate(query.__nextLocale)) {
+            return false;
+        }
+        if (query.__nextDefaultLocale && !this.validate(query.__nextDefaultLocale)) {
+            return false;
+        }
+        return true;
+    }
+    /**
    * Analyzes the pathname for a locale and returns the pathname without it.
    *
    * @param pathname The pathname that could contain a locale prefix.
diff --git a/dist/server/lib/router-utils/resolve-routes.js b/dist/server/lib/router-utils/resolve-routes.js
index 64dd8bc..2c38e42 100644
--- a/dist/server/lib/router-utils/resolve-routes.js
+++ b/dist/server/lib/router-utils/resolve-routes.js
@@ -132,6 +132,11 @@ function getResolveRoutes(fsChecker, config, opts, renderServer, renderServerOpt
                     parsedUrl.pathname = maybeAddTrailingSlash(parsedUrl.pathname);
                 }
             }
+        } else {
+            // As i18n isn't configured we remove the locale related query params.
+            delete parsedUrl.query.__nextLocale;
+            delete parsedUrl.query.__nextDefaultLocale;
+            delete parsedUrl.query.__nextInferredLocaleFromDefault;
         }
         const checkLocaleApi = (pathname)=>{
             if (config.i18n && pathname === urlNoQuery && (initialLocaleResult == null ? void 0 : initialLocaleResult.detectedLocale) && (0, _pathhasprefix.pathHasPrefix)(initialLocaleResult.pathname, "/api")) {
