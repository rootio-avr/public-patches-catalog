diff --git a/dist/server/next-server.js b/dist/server/next-server.js
index 0fa61dc..e969247 100644
--- a/dist/server/next-server.js
+++ b/dist/server/next-server.js
@@ -9,6 +9,7 @@ require("./node-polyfill-fetch");
 var _utils = require("../shared/lib/utils");
 var _requestMeta = require("./request-meta");
 var _fs = _interopRequireDefault(require("fs"));
+var _crypto = _interopRequireDefault(require("crypto"));
 var _path = require("path");
 var _http = require("http");
 var _constants = require("../shared/lib/constants");
@@ -94,6 +95,9 @@ class NextNodeServer extends _baseServer.default {
                 nextConfig: this.nextConfig
             }));
         }
+        const randomBytes = _crypto.default.randomBytes(8);
+        const middlewareSubrequestId = randomBytes.toString('hex');
+        global[Symbol.for('@next/middleware-subrequest-id')] = middlewareSubrequestId;
         if (!this.renderOpts.dev) {
             // pre-warm _document and _app as these will be
             // needed for most requests
diff --git a/dist/server/web/sandbox/context.js b/dist/server/web/sandbox/context.js
index 587c802..ea0a115 100644
--- a/dist/server/web/sandbox/context.js
+++ b/dist/server/web/sandbox/context.js
@@ -147,6 +147,7 @@ function getModuleContext(options) {
         const prevs = ((ref = init.headers.get(`x-middleware-subrequest`)) === null || ref === void 0 ? void 0 : ref.split(':')) || [];
         const value = prevs.concat(options.module).join(':');
         init.headers.set('x-middleware-subrequest', value);
+        init.headers.set('x-middleware-subrequest-id', global[Symbol.for('@next/middleware-subrequest-id')]);
         init.headers.set(`user-agent`, `Next.js Middleware`);
         if (typeof input === 'object' && 'url' in input) {
             return fetch(input.url, {
diff --git a/dist/server/web/sandbox/sandbox.js b/dist/server/web/sandbox/sandbox.js
index b5fd270..bdbb5a0 100644
--- a/dist/server/web/sandbox/sandbox.js
+++ b/dist/server/web/sandbox/sandbox.js
@@ -16,7 +16,7 @@ async function run(params) {
     }
     const subreq = params.request.headers[`x-middleware-subrequest`];
     const subrequests = typeof subreq === 'string' ? subreq.split(':') : [];
-    if (subrequests.includes(params.name)) {
+    if (params.request.headers['x-middleware-subrequest-id'] === global[Symbol.for('@next/middleware-subrequest-id')] && subrequests.includes(params.name)) {
         return {
             waitUntil: Promise.resolve(),
             response: new context.Response(null, {

diff --git a/dist/server/next-server.js b/dist/server/next-server.js
index 0fa61dc..456c71f 100644
--- a/dist/server/next-server.js
+++ b/dist/server/next-server.js
@@ -339,6 +339,10 @@ class NextNodeServer extends _baseServer.default {
         delete parsedUrl.query;
         parsedUrl.search = (0, _baseServer).stringifyQuery(req, query);
         const target = (0, _url).format(parsedUrl);
+        // Keep in mind that a WHATWG URL's hostname and the parsedUrl's hostname
+        // are not strictly equal due to lowercasing, IDN translation, IPv4 and IPv6 normalization, etc.
+        // We just make sure this is a valid URL since http-proxy doesn't validate.
+        new URL(target);
         const proxy = new _httpProxy.default({
             target,
             changeOrigin: true,
diff --git a/dist/shared/lib/router/utils/prepare-destination.js b/dist/shared/lib/router/utils/prepare-destination.js
index 97742d2..250a41d 100644
--- a/dist/shared/lib/router/utils/prepare-destination.js
+++ b/dist/shared/lib/router/utils/prepare-destination.js
@@ -122,7 +122,8 @@ function prepareDestination(args) {
         validate: false
     });
     const destHostnameCompiler = (0, _pathToRegexp).compile(destHostname, {
-        validate: false
+        validate: false,
+        encode: encodeURIComponent
     });
     // update any params in query values
     for (const [key, strOrArray] of Object.entries(destQuery)){

--- a/dist/server/base-server.js
+++ b/dist/server/base-server.js
@@ -180,6 +180,21 @@
             if (typeof parsedUrl.query === 'string') {
                 parsedUrl.query = (0, _querystring).parse(parsedUrl.query);
             }
+            // Validate that if i18n isn't configured, or the incoming locale query
+            // params aren't valid, they are stripped from the query.
+            // `__nextLocale` and `__nextDefaultLocale` are internal params that
+            // Next.js derives from the request path itself, so a client must never
+            // be able to inject them. Trusting attacker-supplied values here allows
+            // bypassing middleware pathname-based authorization (CVE-2024-51479).
+            const __i18nLocales = this.nextConfig.i18n == null ? undefined : this.nextConfig.i18n.locales;
+            const __isValidLocale = (locale)=>{
+                return !!__i18nLocales && typeof locale === 'string' && __i18nLocales.some((l)=>l.toLowerCase() === locale.toLowerCase());
+            };
+            const __localeQueryIsValid = !!__i18nLocales && (!parsedUrl.query.__nextLocale || __isValidLocale(parsedUrl.query.__nextLocale)) && (!parsedUrl.query.__nextDefaultLocale || __isValidLocale(parsedUrl.query.__nextDefaultLocale));
+            if (!__localeQueryIsValid) {
+                delete parsedUrl.query.__nextLocale;
+                delete parsedUrl.query.__nextDefaultLocale;
+            }
             // When there are hostname and port we build an absolute URL
             const initUrl = this.hostname && this.port ? `http://${this.hostname}:${this.port}${req.url}` : req.url;
             (0, _requestMeta).addRequestMeta(req, '__NEXT_INIT_URL', initUrl);

--- a/dist/server/image-optimizer.js
+++ b/dist/server/image-optimizer.js
@@ -43,6 +43,13 @@ const CACHE_VERSION = 3;
 const VECTOR_TYPES = [
     SVG
 ];
+// AVIF image optimization is disabled to avoid decoding untrusted AVIF input
+// through the native image decoder (sharp/libvips), which is affected by an
+// out-of-bounds write vulnerability. AVIF upstream images bypass optimization
+// entirely and are returned unmodified.
+const BYPASS_TYPES = [
+    AVIF
+];
 const BLUR_IMG_SIZE = 8 // should match `next-image-loader`
 ;
 let sharp;
@@ -304,7 +311,9 @@ async function imageOptimizer(_req, _res, paramsResult, nextConfig, handleReques
     if (upstreamType) {
         const vector = VECTOR_TYPES.includes(upstreamType);
         const animate = ANIMATABLE_TYPES.includes(upstreamType) && (0, _isAnimated).default(upstreamBuffer);
-        if (vector || animate) {
+        // AVIF optimization is disabled: never decode untrusted AVIF input through
+        // the native image decoder. Return the original buffer untouched instead.
+        if (vector || animate || BYPASS_TYPES.includes(upstreamType)) {
             return {
                 buffer: upstreamBuffer,
                 contentType: upstreamType,
