--- a/lib/core/core_utils.js
+++ b/lib/core/core_utils.js
@@ -36,6 +36,7 @@
 exports.log2 = log2;
 exports.numberToString = numberToString;
 exports.parseXFAPath = parseXFAPath;
+exports.normalizeCSSFontFamily = normalizeCSSFontFamily;
 exports.readInt8 = readInt8;
 exports.readUint16 = readUint16;
 exports.readUint32 = readUint32;
@@ -250,7 +251,7 @@
 }
 
 function parseXFAPath(path) {
-  const positionPattern = /(.+)\[(\d+)\]$/;
+  const positionPattern = /^(.+)\[(\d+)\]$/;
   return path.split(".").map(component => {
     const m = component.match(positionPattern);
 
@@ -266,6 +267,10 @@
       pos: 0
     };
   });
+}
+
+function normalizeCSSFontFamily(fontFamily) {
+  return fontFamily.replace(/( +)(\d)?/g, (_, spaces, digit) => digit === undefined ? " " : digit);
 }
 
 function escapePDFName(str) {
--- a/lib/core/document.js
+++ b/lib/core/document.js
@@ -1061,7 +1061,7 @@
       }
 
       let fontFamily = descriptor.get("FontFamily");
-      fontFamily = fontFamily.replace(/[ ]+(\d)/g, "$1");
+      fontFamily = (0, _core_utils.normalizeCSSFontFamily)(fontFamily);
       const fontWeight = descriptor.get("FontWeight");
       const italicAngle = -descriptor.get("ItalicAngle");
       const cssFontInfo = {
--- a/lib/core/xml_parser.js
+++ b/lib/core/xml_parser.js
@@ -60,11 +60,13 @@
 
 class XMLParserBase {
   _resolveEntities(s) {
-    return s.replace(/&([^;]+);/g, (all, entity) => {
+    return s.replace(/&([^;&]+);/g, (all, entity) => {
       if (entity.substring(0, 2) === "#x") {
-        return String.fromCodePoint(parseInt(entity.substring(2), 16));
+        const code = parseInt(entity.substring(2), 16);
+        return code >= 0 && code <= 0x10ffff ? String.fromCodePoint(code) : all;
       } else if (entity.substring(0, 1) === "#") {
-        return String.fromCodePoint(parseInt(entity.substring(1), 10));
+        const code = parseInt(entity.substring(1), 10);
+        return code >= 0 && code <= 0x10ffff ? String.fromCodePoint(code) : all;
       }
 
       switch (entity) {
