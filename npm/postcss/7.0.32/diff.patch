--- a/lib/previous-map.js
+++ b/lib/previous-map.js
@@ -38,6 +38,7 @@
    * @param {processOptions} [opts] {@link Processor#process} options.
    */
   function PreviousMap(css, opts) {
+    if (opts.unsafeMap) this.unsafeMap = true;
     this.loadAnnotation(css);
     /**
      * Was source map inlined by data-uri to input CSS.
@@ -150,6 +151,23 @@
     } else if (this.annotation) {
       var map = this.annotation;
       if (file) map = _path.default.join(_path.default.dirname(file), map);
+
+      if (!this.unsafeMap) {
+        // Only load map files with a `.map` extension.
+        if (!/\.map$/i.test(map)) return false;
+        // Refuse to load a map when we do not know the input file location.
+        if (!file) return false;
+        // Refuse to load a map outside of the input file's directory.
+        var relativePath = _path.default.relative(_path.default.dirname(file), map);
+        if (
+          relativePath === '..' ||
+          relativePath.indexOf('..' + _path.default.sep) === 0 ||
+          _path.default.isAbsolute(relativePath)
+        ) {
+          return false;
+        }
+      }
+
       this.root = _path.default.dirname(map);
 
       if (_fs.default.existsSync && _fs.default.existsSync(map)) {
--- /dev/null
+++ b/test/cve-2026-45623.test.js
@@ -0,0 +1,64 @@
+let path = require('path')
+let fs = require('fs-extra')
+
+let parse = require('../lib/parse')
+
+let dir = path.join(__dirname, 'cve-2026-45623-fixtures')
+let mapObj = {
+  version: 3,
+  file: null,
+  sources: [],
+  names: [],
+  mappings: ''
+}
+let map = JSON.stringify(mapObj)
+
+afterEach(() => {
+  if (fs.existsSync(dir)) fs.removeSync(dir)
+})
+
+it('does not load map from non-.map file', () => {
+  let from = path.join(dir, 'a.css')
+  fs.outputFileSync(path.join(dir, 'a.txt'), map)
+  let input = parse('a{}\n/*# sourceMappingURL=a.txt */', { from }).source.input
+  expect(input.map).not.toBeDefined()
+})
+
+it('does not load map from outside the from folder', () => {
+  let from = path.join(dir, 'subdir', 'a.css')
+  fs.outputFileSync(path.join(dir, 'outside.map'), map)
+  let input = parse('a{}\n/*# sourceMappingURL=../outside.map */', { from })
+    .source.input
+  expect(input.map).not.toBeDefined()
+})
+
+it('does not load relative map without from', () => {
+  let cwd = path.join(dir, 'subdir')
+  fs.outputFileSync(path.join(cwd, 'previous.map'), map)
+  let previousCwd = process.cwd()
+  try {
+    process.chdir(cwd)
+    let input = parse('a{}\n/*# sourceMappingURL=previous.map */').source.input
+    expect(input.map).not.toBeDefined()
+  } finally {
+    process.chdir(previousCwd)
+  }
+})
+
+it('loads map from outside the from folder with unsafeMap', () => {
+  let from = path.join(dir, 'subdir', 'a.css')
+  fs.outputFileSync(path.join(dir, 'outside.map'), map)
+  let input = parse('a{}\n/*# sourceMappingURL=../outside.map */', {
+    from,
+    unsafeMap: true
+  }).source.input
+  expect(input.map.text).toEqual(map)
+})
+
+it('still loads a co-located .map file with from', () => {
+  let from = path.join(dir, 'a.css')
+  fs.outputFileSync(path.join(dir, 'a.css.map'), map)
+  let input = parse('a{}\n/*# sourceMappingURL=a.css.map */', { from }).source
+    .input
+  expect(input.map.text).toEqual(map)
+})

--- a/lib/previous-map.js
+++ b/lib/previous-map.js
@@ -11,6 +11,16 @@
 
 function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
 
+function realPath(file) {
+  try {
+    return _fs.default.realpathSync(file);
+  } catch (e) {
+    // Missing or dangling: keep the literal path. The existsSync() check below
+    // still gates the read, and a path that does not exist cannot escape.
+    return file;
+  }
+}
+
 function fromBase64(str) {
   if (Buffer) {
     return Buffer.from(str, 'base64').toString();
@@ -151,31 +161,38 @@
     } else if (this.annotation) {
       var map = this.annotation;
       if (file) map = _path.default.join(_path.default.dirname(file), map);
+      return this.loadFile(map, file, false);
+    }
+  };
 
-      if (!this.unsafeMap) {
-        // Only load map files with a `.map` extension.
-        if (!/\.map$/i.test(map)) return false;
-        // Refuse to load a map when we do not know the input file location.
-        if (!file) return false;
-        // Refuse to load a map outside of the input file's directory.
-        var relativePath = _path.default.relative(_path.default.dirname(file), map);
-        if (
-          relativePath === '..' ||
-          relativePath.indexOf('..' + _path.default.sep) === 0 ||
-          _path.default.isAbsolute(relativePath)
-        ) {
-          return false;
-        }
-      }
-
-      this.root = _path.default.dirname(map);
+  _proto.loadFile = function loadFile(file, cssFile, trusted) {
+    // For security reasons only load a source map that the annotation points
+    // at when: it is an actual `.map` file, we know the referencing CSS file
+    // (`opts.from`), and the resolved map path stays inside the CSS file's
+    // directory. `unsafeMap` opts out of the containment check. `trusted`
+    // marks callers (an explicit `map.prev` function) that supply the path
+    // themselves rather than deriving it from untrusted CSS content.
+    if (!trusted && !this.unsafeMap) {
+      if (!/\.map$/i.test(file)) return false;
+      if (!cssFile) return false;
+
+      // Compare *resolved* paths: relative() is textual, so without resolving
+      // symlinks first a link at or below the CSS file's directory could point
+      // the map outside it.
+      var rel = _path.default.relative(realPath(_path.default.dirname(cssFile)), realPath(file));
 
-      if (_fs.default.existsSync && _fs.default.existsSync(map)) {
-        return _fs.default.readFileSync(map, 'utf-8').toString().trim();
-      } else {
+      if (rel === '..' || rel.indexOf('..' + _path.default.sep) === 0 || _path.default.isAbsolute(rel)) {
         return false;
       }
     }
+
+    this.root = _path.default.dirname(file);
+
+    if (_fs.default.existsSync && _fs.default.existsSync(file)) {
+      return _fs.default.readFileSync(file, 'utf-8').toString().trim();
+    } else {
+      return false;
+    }
   };
 
   _proto.isMap = function isMap(map) {
