--- a/index.js
+++ b/index.js
@@ -98,21 +98,26 @@
 function expand(str, max, isTop) {
   var expansions = [];
 
-  var m = balanced('{', '}', str);
-  if (!m) return [str];
+  // The `{a},b}` rewrite below restarts expansion on a rewritten string with
+  // the same `max` and `isTop = true`. Loop instead of recursing so a long run
+  // of non-expanding `{}` groups can't exhaust the call stack.
+  for (;;) {
+    const m = balanced('{', '}', str)
+    if (!m) return [str]
 
-  // no need to expand pre, since it is guaranteed to be free of brace-sets
-  var pre = m.pre;
-  var post = m.post.length
-    ? expand(m.post, max, false)
-    : [''];
+    // no need to expand pre, since it is guaranteed to be free of brace-sets
+    const pre = m.pre
 
-  if (/\$$/.test(m.pre)) {    
-    for (var k = 0; k < post.length && k < max; k++) {
-      var expansion = pre+ '{' + m.body + '}' + post[k];
-      expansions.push(expansion);
+    if (/\$$/.test(m.pre)) {
+      const post =
+        m.post.length ? expand(m.post, max, false) : ['']
+      for (let k = 0; k < post.length && k < max; k++) {
+        const expansion = pre + '{' + m.body + '}' + post[k]
+        expansions.push(expansion)
+      }
+      return expansions
     }
-  } else {
+
     var isNumericSequence = /^-?\d+\.\.-?\d+(?:\.\.-?\d+)?$/.test(m.body);
     var isAlphaSequence = /^[a-zA-Z]\.\.[a-zA-Z](?:\.\.-?\d+)?$/.test(m.body);
     var isSequence = isNumericSequence || isAlphaSequence;
@@ -121,11 +126,18 @@
       // {a},b}
       if (m.post.match(/,(?!,).*\}/)) {
         str = m.pre + '{' + m.body + escClose + m.post;
-        return expand(str, max, true);
+        isTop = true;
+        continue;
       }
       return [str];
     }
 
+    // Only expand post once we know this brace set actually expands. Computing
+    // it before the early returns above expanded post a second time on every
+    // non-expanding `{}`, which is what made inputs like `a{},{},{}...` blow up
+    // exponentially.
+    const post =
+      m.post.length ? expand(m.post, max, false) : ['']
     var n;
     if (isSequence) {
       n = m.body.split(/\.\./);
@@ -199,7 +211,7 @@
           expansions.push(expansion);
       }
     }
-  }
 
-  return expansions;
+    return expansions;
+  }
 }
--- /dev/null
+++ b/test/unbound-recursion.js
@@ -0,0 +1,22 @@
+var test = require('tape');
+var expand = require('..');
+
+
+// https://github.com/juliangruber/brace-expansion/security/advisories/GHSA-3jxr-9vmj-r5cp
+test('unbound recursion', async t => {
+  // A run of non-expanding `{}` groups used to expand `post` once per group,
+  // doubling the work on every group. This 30-group, 90 byte input blocked
+  // for minutes.
+  const str =
+    'a{},{},{},{},{},{},{},{},{},{},{},{},{},{},{},{},{},{},{},{},{},{},{},{},{},{},{},{},{},{}'
+  const startTime = performance.now()
+  const expanded = expand(str)
+  const endTime = performance.now()
+  const timeTaken = endTime - startTime
+  t.deepEqual(expanded, [str], 'does not expand')
+  t.ok(
+    timeTaken < 1000,
+    `Expected time (${timeTaken}ms) to be less than 1000ms`,
+  )
+  t.end()
+})

--- a/index.js
+++ b/index.js
@@ -8,6 +8,17 @@
 var escComma = '\0COMMA'+Math.random()+'\0';
 var escPeriod = '\0PERIOD'+Math.random()+'\0';
 
+// `max` caps the *number* of expansions, but not their length. An input like
+// `'{a,b}'.repeat(1500)` stays under that count while making every result grow
+// with the number of chained brace groups. The result set, and the intermediate
+// arrays built while combining brace sets, then grow large enough to exhaust
+// memory and crash the process with an uncatchable OOM error (CVE-2026-14257).
+// `EXPANSION_MAX_LENGTH` bounds the total number of characters accumulated at
+// each combination step, so output is truncated instead of growing without
+// bound. The default sits well above any realistic expansion, so legitimate
+// input is unaffected; it can be overridden via the `maxLength` option.
+var EXPANSION_MAX_LENGTH = 4000000;
+
 function numeric(str) {
   return parseInt(str, 10) == str
     ? parseInt(str, 10)
@@ -67,6 +78,9 @@
 
   options = options || {};
   var max = options.max == null ? Infinity : options.max;
+  var maxLength = options.maxLength == null
+    ? EXPANSION_MAX_LENGTH
+    : options.maxLength;
 
   // I don't know why Bash 4.3 does this, but it does.
   // Anything starting with {} will have the first two bytes preserved
@@ -78,9 +92,20 @@
     str = '\\{\\}' + str.substr(2);
   }
 
-  return expand(escapeBraces(str), max, true).map(unescapeBraces);
+  return expand(escapeBraces(str), max, maxLength, true).map(unescapeBraces);
 }
 
+// Push `expansion` onto `expansions` unless doing so would exceed `maxLength`
+// total characters across the accumulated results. Returns false once the
+// length bound is reached so callers can stop building output (CVE-2026-14257).
+function pushBounded(expansions, expansion, state, maxLength) {
+  if (state.length + expansion.length > maxLength)
+    return false;
+  expansions.push(expansion);
+  state.length += expansion.length;
+  return true;
+}
+
 function embrace(str) {
   return '{' + str + '}';
 }
@@ -95,8 +120,12 @@
   return i >= y;
 }
 
-function expand(str, max, isTop) {
+function expand(str, max, maxLength, isTop) {
   var expansions = [];
+  // Running total of characters accumulated in `expansions`. Threaded through
+  // every push so the total output length stays bounded by `maxLength`,
+  // truncating instead of growing without bound (CVE-2026-14257).
+  var state = { length: 0 };
 
   // The `{a},b}` rewrite below restarts expansion on a rewritten string with
   // the same `max` and `isTop = true`. Loop instead of recursing so a long run
@@ -110,10 +139,11 @@
 
     if (/\$$/.test(m.pre)) {
       const post =
-        m.post.length ? expand(m.post, max, false) : ['']
+        m.post.length ? expand(m.post, max, maxLength, false) : ['']
       for (let k = 0; k < post.length && k < max; k++) {
         const expansion = pre + '{' + m.body + '}' + post[k]
-        expansions.push(expansion)
+        if (!pushBounded(expansions, expansion, state, maxLength))
+          break
       }
       return expansions
     }
@@ -137,7 +167,7 @@
     // non-expanding `{}`, which is what made inputs like `a{},{},{}...` blow up
     // exponentially.
     const post =
-      m.post.length ? expand(m.post, max, false) : ['']
+      m.post.length ? expand(m.post, max, maxLength, false) : ['']
     var n;
     if (isSequence) {
       n = m.body.split(/\.\./);
@@ -145,11 +175,16 @@
       n = parseCommaParts(m.body);
       if (n.length === 1) {
         // x{{a,b}}y ==> x{a}y x{b}y
-        n = expand(n[0], max, false).map(embrace);
+        n = expand(n[0], max, maxLength, false).map(embrace);
         if (n.length === 1) {
-          return post.map(function(p) {
-            return m.pre + n[0] + p;
-          });
+          var single = [];
+          var singleState = { length: 0 };
+          for (var si = 0; si < post.length; si++) {
+            var singleExpansion = m.pre + n[0] + post[si];
+            if (!pushBounded(single, singleExpansion, singleState, maxLength))
+              break;
+          }
+          return single;
         }
       }
     }
@@ -200,15 +235,17 @@
       N = [];
 
       for (var j = 0; j < n.length; j++) {
-        N.push.apply(N, expand(n[j], max, false));
+        N.push.apply(N, expand(n[j], max, maxLength, false));
       }
     }
 
     for (var j = 0; j < N.length; j++) {
       for (var k = 0; k < post.length && expansions.length < max; k++) {
         var expansion = pre + N[j] + post[k];
-        if (!isTop || isSequence || expansion)
-          expansions.push(expansion);
+        if (!isTop || isSequence || expansion) {
+          if (!pushBounded(expansions, expansion, state, maxLength))
+            break;
+        }
       }
     }
 
--- /dev/null
+++ b/test/max-length.js
@@ -0,0 +1,53 @@
+var test = require('tape');
+var expand = require('..');
+
+// CVE-2026-14257: the `max` option caps the *number* of expansions but not
+// their length. Chaining many brace groups (e.g. `'{a,b}'.repeat(1500)`) keeps
+// the count under `max` while each result grows with the number of groups.
+// Building the long results, plus the intermediate arrays combined along the
+// way, exhausted memory and crashed the process with an uncatchable
+// out-of-memory error. The fix bounds the total accumulated output length.
+test('total expansion length is bounded (CVE-2026-14257)', function(t) {
+  var str = new Array(1500 + 1).join('{a,b}');
+  var startTime = Date.now();
+  var expanded = expand(str);
+  var timeTaken = Date.now() - startTime;
+
+  var totalLength = expanded.reduce(function(sum, s) {
+    return sum + s.length;
+  }, 0);
+
+  t.ok(
+    totalLength <= 4000000,
+    'total expansion length (' + totalLength + ') is bounded to 4,000,000'
+  );
+  t.ok(expanded.length > 0, 'still returns a (truncated) result');
+  t.ok(
+    expanded.every(function(s) { return /^[ab]*$/.test(s); }),
+    'results are valid expansions'
+  );
+  t.ok(
+    timeTaken < 10000,
+    'completes quickly (' + timeTaken + 'ms) instead of exhausting memory'
+  );
+  t.end();
+});
+
+test('maxLength option bounds output (CVE-2026-14257)', function(t) {
+  var str = new Array(1500 + 1).join('{a,b}');
+  var expanded = expand(str, { maxLength: 100 });
+  var totalLength = expanded.reduce(function(sum, s) {
+    return sum + s.length;
+  }, 0);
+  t.ok(
+    totalLength <= 100,
+    'custom maxLength caps total output length (' + totalLength + ' <= 100)'
+  );
+  t.end();
+});
+
+test('short input is unaffected by the length bound', function(t) {
+  t.deepEqual(expand('a{b,c}d'), ['abd', 'acd'], 'basic expansion preserved');
+  t.deepEqual(expand('{a,b{1..3},c}'), ['a', 'b1', 'b2', 'b3', 'c'], 'nested preserved');
+  t.end();
+});
