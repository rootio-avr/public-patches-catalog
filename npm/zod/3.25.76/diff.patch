--- a/v4/core/schemas.js
+++ b/v4/core/schemas.js
@@ -316,6 +316,9 @@
 export function isValidBase64(data) {
     if (data === "")
         return true;
+    // atob ignores whitespace, so reject it up front. (AIKIDO-2026-10706)
+    if (/\s/.test(data))
+        return false;
     if (data.length % 4 !== 0)
         return false;
     try {
--- a/v4/core/schemas.cjs
+++ b/v4/core/schemas.cjs
@@ -347,6 +347,9 @@
 function isValidBase64(data) {
     if (data === "")
         return true;
+    // atob ignores whitespace, so reject it up front. (AIKIDO-2026-10706)
+    if (/\s/.test(data))
+        return false;
     if (data.length % 4 !== 0)
         return false;
     try {

--- a/v4/core/schemas.js
+++ b/v4/core/schemas.js
@@ -842,6 +842,10 @@
         const _catchall = catchall._zod;
         const t = _catchall.def.type;
         for (const key of Object.keys(input)) {
+            // skip __proto__ so it can't replace the result prototype via the
+            // assignment setter on the plain {} we build into (AIKIDO-2026-10707)
+            if (key === "__proto__")
+                continue;
             if (keySet.has(key))
                 continue;
             if (t === "never") {
--- a/v4/core/schemas.cjs
+++ b/v4/core/schemas.cjs
@@ -873,6 +873,10 @@
         const _catchall = catchall._zod;
         const t = _catchall.def.type;
         for (const key of Object.keys(input)) {
+            // skip __proto__ so it can't replace the result prototype via the
+            // assignment setter on the plain {} we build into (AIKIDO-2026-10707)
+            if (key === "__proto__")
+                continue;
             if (keySet.has(key))
                 continue;
             if (t === "never") {
