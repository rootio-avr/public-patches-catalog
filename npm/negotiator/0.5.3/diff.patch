--- a/lib/language.js
+++ b/lib/language.js
@@ -19,7 +19,7 @@
 }
 
 function parseLanguage(s, i) {
-  var match = s.match(/^\s*(\S+?)(?:-(\S+?))?\s*(?:;(.*))?$/);
+  var match = s.match(/^\s*([^\s\-;]+)(?:-([^\s;]+))?\s*(?:;(.*))?$/);
   if (!match) return null;
 
   var prefix = match[1],

--- a/lib/charset.js
+++ b/lib/charset.js
@@ -19,7 +19,7 @@ function parseAcceptCharset(accept) {
 }
 
 function parseCharset(s, i) {
-  var match = s.match(/^\s*(\S+?)\s*(?:;(.*))?$/);
+  var match = s.match(/^\s*([^\s;]+)\s*(?:;(.*))?$/);
   if (!match) return null;
 
   var charset = match[1];
--- a/lib/encoding.js
+++ b/lib/encoding.js
@@ -35,7 +35,7 @@ function parseAcceptEncoding(accept) {
 }
 
 function parseEncoding(s, i) {
-  var match = s.match(/^\s*(\S+?)\s*(?:;(.*))?$/);
+  var match = s.match(/^\s*([^\s;]+)\s*(?:;(.*))?$/);
 
   if (!match) return null;
 
--- a/lib/mediaType.js
+++ b/lib/mediaType.js
@@ -27,7 +27,7 @@ function parseAccept(accept) {
 };
 
 function parseMediaType(s, i) {
-  var match = s.match(/\s*(\S+?)\/([^;\s]+)\s*(?:;(.*))?/);
+  var match = s.match(/\s*([^\s\/;]+)\/([^;\s]+)\s*(?:;(.*))?/);
   if (!match) return null;
 
   var type = match[1],
