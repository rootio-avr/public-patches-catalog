--- a/dist/cjs/index.js
+++ b/dist/cjs/index.js
@@ -934,6 +934,9 @@
                 case '@':
                 case '!':
                     this.debug('%s\t%s %s %j <-- stateChar', pattern, i, re, c);
+                    // coalesce consecutive non-globstar * characters (CVE-2026-26996 ReDoS)
+                    if (c === '*' && stateChar === '*')
+                        continue;
                     // if we already have a stateChar, then it means
                     // that there was something like ** or +? in there.
                     // Handle the stateChar, then proceed with this one.
--- a/dist/mjs/index.js
+++ b/dist/mjs/index.js
@@ -922,6 +922,9 @@
                 case '@':
                 case '!':
                     this.debug('%s\t%s %s %j <-- stateChar', pattern, i, re, c);
+                    // coalesce consecutive non-globstar * characters (CVE-2026-26996 ReDoS)
+                    if (c === '*' && stateChar === '*')
+                        continue;
                     // if we already have a stateChar, then it means
                     // that there was something like ** or +? in there.
                     // Handle the stateChar, then proceed with this one.

--- a/dist/cjs/index.js
+++ b/dist/cjs/index.js
@@ -816,6 +816,16 @@
     parse(pattern) {
         assertValidPattern(pattern);
         const options = this.options;
+        // Cap on how deeply nested extglob pattern lists may be parsed.
+        // Nested extglobs compile into nested quantified regexp groups
+        // (e.g. `*(*(a))` -> `(?:(?:a)*)*?`), which are prone to
+        // catastrophic backtracking (ReDoS, GHSA-23c5-xmqv-rm74).  Any
+        // extglob opened beyond this depth is treated as literal
+        // parentheses (noext-style degradation).  Default 2.
+        const maxExtglobRecursion = (typeof options.maxExtglobRecursion === 'number' &&
+            options.maxExtglobRecursion >= 0)
+            ? options.maxExtglobRecursion
+            : 2;
         // shortcuts
         if (pattern === '**')
             return exports.GLOBSTAR;
@@ -951,6 +961,26 @@
                         re += '\\(';
                         continue;
                     }
+                    // Limit nested extglob recursion (ReDoS,
+                    // GHSA-23c5-xmqv-rm74).  Once the number of *active*
+                    // extglob groups reaches maxExtglobRecursion, degrade
+                    // this one (and everything inside it) to literal
+                    // parentheses, exactly as `noext: true` would.  A marker
+                    // entry keeps the matching `|` and `)` literal too.
+                    const activeExtglobDepth = patternListStack.reduce((n, e) => (e.noext ? n : n + 1), 0);
+                    if (activeExtglobDepth >= maxExtglobRecursion) {
+                        clearStateChar();
+                        patternListStack.push({
+                            type: false,
+                            start: i - 1,
+                            reStart: re.length,
+                            open: '',
+                            close: '',
+                            noext: true,
+                        });
+                        re += '\\(';
+                        continue;
+                    }
                     const plEntry = {
                         type: stateChar,
                         start: i - 1,
@@ -977,6 +1007,14 @@
                         re += '\\)';
                         continue;
                     }
+                    // A `)` closing a pattern list degraded past the
+                    // nested-extglob recursion limit is emitted literally.
+                    if (plEntry.noext) {
+                        patternListStack.pop();
+                        clearStateChar();
+                        re += '\\)';
+                        continue;
+                    }
                     patternListStack.pop();
                     // closing an extglob
                     clearStateChar();
@@ -996,6 +1034,13 @@
                         re += '\\|';
                         continue;
                     }
+                    // A `|` inside a pattern list degraded past the
+                    // nested-extglob recursion limit is a literal `|`.
+                    if (plEntry.noext) {
+                        clearStateChar();
+                        re += '\\|';
+                        continue;
+                    }
                     clearStateChar();
                     re += '|';
                     // next subpattern can start with a dot?
@@ -1037,6 +1082,12 @@
         // Go through and escape them, taking care not to double-escape any
         // | chars that were already escaped.
         for (pl = patternListStack.pop(); pl; pl = patternListStack.pop()) {
+            // Pattern lists degraded past the nested-extglob recursion
+            // limit already emitted their `\\(` literally and never opened
+            // a regexp group, so there is nothing to rewrite here.
+            if (pl.noext) {
+                continue;
+            }
             let tail;
             tail = re.slice(pl.reStart + pl.open.length);
             this.debug(this.pattern, 'setting tail', re, pl);
--- a/dist/mjs/index.js
+++ b/dist/mjs/index.js
@@ -804,6 +804,16 @@
     parse(pattern) {
         assertValidPattern(pattern);
         const options = this.options;
+        // Cap on how deeply nested extglob pattern lists may be parsed.
+        // Nested extglobs compile into nested quantified regexp groups
+        // (e.g. `*(*(a))` -> `(?:(?:a)*)*?`), which are prone to
+        // catastrophic backtracking (ReDoS, GHSA-23c5-xmqv-rm74).  Any
+        // extglob opened beyond this depth is treated as literal
+        // parentheses (noext-style degradation).  Default 2.
+        const maxExtglobRecursion = (typeof options.maxExtglobRecursion === 'number' &&
+            options.maxExtglobRecursion >= 0)
+            ? options.maxExtglobRecursion
+            : 2;
         // shortcuts
         if (pattern === '**')
             return GLOBSTAR;
@@ -939,6 +949,26 @@
                         re += '\\(';
                         continue;
                     }
+                    // Limit nested extglob recursion (ReDoS,
+                    // GHSA-23c5-xmqv-rm74).  Once the number of *active*
+                    // extglob groups reaches maxExtglobRecursion, degrade
+                    // this one (and everything inside it) to literal
+                    // parentheses, exactly as `noext: true` would.  A marker
+                    // entry keeps the matching `|` and `)` literal too.
+                    const activeExtglobDepth = patternListStack.reduce((n, e) => (e.noext ? n : n + 1), 0);
+                    if (activeExtglobDepth >= maxExtglobRecursion) {
+                        clearStateChar();
+                        patternListStack.push({
+                            type: false,
+                            start: i - 1,
+                            reStart: re.length,
+                            open: '',
+                            close: '',
+                            noext: true,
+                        });
+                        re += '\\(';
+                        continue;
+                    }
                     const plEntry = {
                         type: stateChar,
                         start: i - 1,
@@ -965,6 +995,14 @@
                         re += '\\)';
                         continue;
                     }
+                    // A `)` closing a pattern list degraded past the
+                    // nested-extglob recursion limit is emitted literally.
+                    if (plEntry.noext) {
+                        patternListStack.pop();
+                        clearStateChar();
+                        re += '\\)';
+                        continue;
+                    }
                     patternListStack.pop();
                     // closing an extglob
                     clearStateChar();
@@ -984,6 +1022,13 @@
                         re += '\\|';
                         continue;
                     }
+                    // A `|` inside a pattern list degraded past the
+                    // nested-extglob recursion limit is a literal `|`.
+                    if (plEntry.noext) {
+                        clearStateChar();
+                        re += '\\|';
+                        continue;
+                    }
                     clearStateChar();
                     re += '|';
                     // next subpattern can start with a dot?
@@ -1025,6 +1070,12 @@
         // Go through and escape them, taking care not to double-escape any
         // | chars that were already escaped.
         for (pl = patternListStack.pop(); pl; pl = patternListStack.pop()) {
+            // Pattern lists degraded past the nested-extglob recursion
+            // limit already emitted their `\\(` literally and never opened
+            // a regexp group, so there is nothing to rewrite here.
+            if (pl.noext) {
+                continue;
+            }
             let tail;
             tail = re.slice(pl.reStart + pl.open.length);
             this.debug(this.pattern, 'setting tail', re, pl);
