diff --git a/build/esm/JaegerPropagator.js b/build/esm/JaegerPropagator.js
index 996f741..d836fe9 100644
--- a/build/esm/JaegerPropagator.js
+++ b/build/esm/JaegerPropagator.js
@@ -90,8 +90,15 @@ export class JaegerPropagator {
         for (const baggageEntry of baggageValues) {
             if (baggageEntry.value === undefined)
                 continue;
+            let decodedValue;
+            try {
+                decodedValue = decodeURIComponent(baggageEntry.value);
+            }
+            catch {
+                continue;
+            }
             currentBaggage = currentBaggage.setEntry(baggageEntry.key, {
-                value: decodeURIComponent(baggageEntry.value),
+                value: decodedValue,
             });
         }
         newContext = propagation.setBaggage(newContext, currentBaggage);
@@ -107,7 +114,14 @@ const VALID_HEX_RE = /^[0-9a-f]{1,2}$/i;
  * @return {SpanContext} - returns a span context represented by the serializedString.
  **/
 function deserializeSpanContext(serializedString) {
-    const headers = decodeURIComponent(serializedString).split(':');
+    let decoded;
+    try {
+        decoded = decodeURIComponent(serializedString);
+    }
+    catch {
+        return null;
+    }
+    const headers = decoded.split(':');
     if (headers.length !== 4) {
         return null;
     }
diff --git a/build/esnext/JaegerPropagator.js b/build/esnext/JaegerPropagator.js
index 996f741..d836fe9 100644
--- a/build/esnext/JaegerPropagator.js
+++ b/build/esnext/JaegerPropagator.js
@@ -90,8 +90,15 @@ export class JaegerPropagator {
         for (const baggageEntry of baggageValues) {
             if (baggageEntry.value === undefined)
                 continue;
+            let decodedValue;
+            try {
+                decodedValue = decodeURIComponent(baggageEntry.value);
+            }
+            catch {
+                continue;
+            }
             currentBaggage = currentBaggage.setEntry(baggageEntry.key, {
-                value: decodeURIComponent(baggageEntry.value),
+                value: decodedValue,
             });
         }
         newContext = propagation.setBaggage(newContext, currentBaggage);
@@ -107,7 +114,14 @@ const VALID_HEX_RE = /^[0-9a-f]{1,2}$/i;
  * @return {SpanContext} - returns a span context represented by the serializedString.
  **/
 function deserializeSpanContext(serializedString) {
-    const headers = decodeURIComponent(serializedString).split(':');
+    let decoded;
+    try {
+        decoded = decodeURIComponent(serializedString);
+    }
+    catch {
+        return null;
+    }
+    const headers = decoded.split(':');
     if (headers.length !== 4) {
         return null;
     }
diff --git a/build/src/JaegerPropagator.js b/build/src/JaegerPropagator.js
index 099e566..2ee43a4 100644
--- a/build/src/JaegerPropagator.js
+++ b/build/src/JaegerPropagator.js
@@ -93,8 +93,15 @@ class JaegerPropagator {
         for (const baggageEntry of baggageValues) {
             if (baggageEntry.value === undefined)
                 continue;
+            let decodedValue;
+            try {
+                decodedValue = decodeURIComponent(baggageEntry.value);
+            }
+            catch {
+                continue;
+            }
             currentBaggage = currentBaggage.setEntry(baggageEntry.key, {
-                value: decodeURIComponent(baggageEntry.value),
+                value: decodedValue,
             });
         }
         newContext = api_1.propagation.setBaggage(newContext, currentBaggage);
@@ -111,7 +118,14 @@ const VALID_HEX_RE = /^[0-9a-f]{1,2}$/i;
  * @return {SpanContext} - returns a span context represented by the serializedString.
  **/
 function deserializeSpanContext(serializedString) {
-    const headers = decodeURIComponent(serializedString).split(':');
+    let decoded;
+    try {
+        decoded = decodeURIComponent(serializedString);
+    }
+    catch {
+        return null;
+    }
+    const headers = decoded.split(':');
     if (headers.length !== 4) {
         return null;
     }
