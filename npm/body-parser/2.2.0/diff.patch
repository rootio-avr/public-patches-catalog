--- a/lib/types/urlencoded.js
+++ b/lib/types/urlencoded.js
@@ -167,11 +167,16 @@
  *
  * @param {string} body
  * @param {number} limit
+ * @return {number|undefined} Returns undefined if limit exceeded
  * @api private
  */
-
 function parameterCount (body, limit) {
-  var len = body.split('&').length
-
-  return len > limit ? undefined : len - 1
+  let count = 0
+  let index = -1
+  do {
+    count++
+    if (count > limit) return undefined // Early exit if limit exceeded
+    index = body.indexOf('&', index + 1)
+  } while (index !== -1)
+  return count
 }

--- a/lib/utils.js
+++ b/lib/utils.js
@@ -59,11 +59,15 @@
   }

   var inflate = options?.inflate !== false
-  var limit = typeof options?.limit !== 'number'
-    ? bytes.parse(options?.limit || '100kb')
-    : options?.limit
+  var limit = typeof options?.limit === 'undefined' || options?.limit === null
+    ? 102400 // 100kb default
+    : bytes.parse(options.limit)
   var type = options?.type || defaultType
   var verify = options?.verify || false
+
+  if (limit === null) {
+    throw new TypeError(`option limit "${String(options.limit)}" is invalid`)
+  }

   if (verify !== false && typeof verify !== 'function') {
     throw new TypeError('option verify must be function')
--- /dev/null
+++ b/test/cve-2026-12590.test.js
@@ -0,0 +1,54 @@
+'use strict'
+
+const assert = require('node:assert')
+const { normalizeOptions } = require('../lib/utils.js')
+
+// Regression test for CVE-2026-12590: improve limit option validation.
+// Invalid limit values must be rejected with a TypeError instead of being
+// silently coerced (which previously allowed unbounded/undefined limits).
+describe('CVE-2026-12590: normalizeOptions limit validation', () => {
+  it('returns the default limit when limit is undefined', () => {
+    const result = normalizeOptions({ limit: undefined }, 'application/json')
+    assert.strictEqual(result.limit, 100 * 1024) // 100kb in bytes
+  })
+
+  it('returns the default limit when limit is null', () => {
+    const result = normalizeOptions({ limit: null }, 'application/json')
+    assert.strictEqual(result.limit, 100 * 1024) // 100kb in bytes
+  })
+
+  it('accepts zero as a valid limit', () => {
+    const result = normalizeOptions({ limit: 0 }, 'application/json')
+    assert.strictEqual(result.limit, 0)
+  })
+
+  it('accepts a numeric limit', () => {
+    const result = normalizeOptions({ limit: 1234 }, 'application/json')
+    assert.strictEqual(result.limit, 1234)
+  })
+
+  it('parses a string limit with a unit', () => {
+    const result = normalizeOptions({ limit: '200kb' }, 'application/json')
+    assert.strictEqual(result.limit, 200 * 1024) // 200kb in bytes
+  })
+
+  it('throws a TypeError for an invalid string limit', () => {
+    assert.throws(() => {
+      normalizeOptions({ limit: 'invalid' }, 'application/json')
+    }, /option limit "invalid" is invalid/)
+    assert.throws(() => {
+      normalizeOptions({ limit: '' }, 'application/json')
+    }, /option limit "" is invalid/)
+  })
+
+  it('throws a TypeError for a NaN limit', () => {
+    assert.throws(() => {
+      normalizeOptions({ limit: NaN }, 'application/json')
+    }, /option limit "NaN" is invalid/)
+  })
+
+  it('throws a TypeError for a boolean limit', () => {
+    assert.throws(() => {
+      normalizeOptions({ limit: true }, 'application/json')
+    }, /option limit "true" is invalid/)
+  })
+})
