--- a/internal/iterator.js
+++ b/internal/iterator.js
@@ -43,6 +43,9 @@
     var len = okeys.length;
     return function next() {
         var key = okeys[++i];
+        if (key === '__proto__') {
+            return next();
+        }
         return i < len ? { value: obj[key], key: key } : null;
     };
 }
--- a/dist/async.js
+++ b/dist/async.js
@@ -948,6 +948,9 @@
     var len = okeys.length;
     return function next() {
         var key = okeys[++i];
+        if (key === '__proto__') {
+            return next();
+        }
         return i < len ? {value: obj[key], key: key} : null;
     };
 }
