--- a/classes/comparator.js
+++ b/classes/comparator.js
@@ -20,6 +20,7 @@
       }
     }
 
+    comp = comp.trim().split(/\s+/).join(' ')
     debug('comparator', comp, options)
     this.options = options
     this.loose = !!options.loose
@@ -132,7 +133,7 @@
 
 module.exports = Comparator
 
-const {re, t} = require('../internal/re')
+const { safeRe: re, t } = require('../internal/re')
 const cmp = require('../functions/cmp')
 const debug = require('../internal/debug')
 const SemVer = require('./semver')
--- a/classes/range.js
+++ b/classes/range.js
@@ -31,19 +31,26 @@
     this.loose = !!options.loose
     this.includePrerelease = !!options.includePrerelease
 
-    // First, split based on boolean or ||
+    // First reduce all whitespace as much as possible so we do not have to rely
+    // on potentially slow regexes like \s*. This is then stored and used for
+    // future error messages as well.
     this.raw = range
-    this.set = range
-      .split(/\s*\|\|\s*/)
+      .trim()
+      .split(/\s+/)
+      .join(' ')
+
+    // First, split on ||
+    this.set = this.raw
+      .split('||')
       // map the range to a 2d array of comparators
-      .map(range => this.parseRange(range.trim()))
+      .map(r => this.parseRange(r))
       // throw out any comparator lists that are empty
       // this generally means that it was not a valid range, which is allowed
       // in loose mode, but will still throw if the WHOLE range is invalid.
       .filter(c => c.length)
 
     if (!this.set.length) {
-      throw new TypeError(`Invalid SemVer Range: ${range}`)
+      throw new TypeError(`Invalid SemVer Range: ${this.raw}`)
     }
 
     this.format()
@@ -51,9 +58,7 @@
 
   format () {
     this.range = this.set
-      .map((comps) => {
-        return comps.join(' ').trim()
-      })
+      .map((comps) => comps.join(' ').trim())
       .join('||')
       .trim()
     return this.range
@@ -65,7 +70,6 @@
 
   parseRange (range) {
     const loose = this.options.loose
-    range = range.trim()
     // `1.2.3 - 1.2.4` => `>=1.2.3 <=1.2.4`
     const hr = loose ? re[t.HYPHENRANGELOOSE] : re[t.HYPHENRANGE]
     range = range.replace(hr, hyphenReplace(this.options.includePrerelease))
@@ -80,8 +84,6 @@
     // `^ 1.2.3` => `^1.2.3`
     range = range.replace(re[t.CARETTRIM], caretTrimReplace)
 
-    // normalize spaces
-    range = range.split(/\s+/).join(' ')
 
     // At this point, the range is completely trimmed and
     // ready to be split into comparators.
@@ -148,7 +150,7 @@
 const debug = require('../internal/debug')
 const SemVer = require('./semver')
 const {
-  re,
+  safeRe: re,
   t,
   comparatorTrimReplace,
   tildeTrimReplace,
@@ -380,12 +382,15 @@
 const replaceStars = (comp, options) => {
   debug('replaceStars', comp, options)
   // Looseness is ignored here.  star is always as loose as it gets!
-  return comp.trim().replace(re[t.STAR], '')
+  return comp
+    .trim()
+    .replace(re[t.STAR], '')
 }
 
 const replaceGTE0 = (comp, options) => {
   debug('replaceGTE0', comp, options)
-  return comp.trim()
+  return comp
+    .trim()
     .replace(re[options.includePrerelease ? t.GTE0PRE : t.GTE0], '')
 }
 
@@ -423,7 +428,7 @@
     to = `<=${to}`
   }
 
-  return (`${from} ${to}`).trim()
+  return `${from} ${to}`.trim()
 }
 
 const testSet = (set, version, options) => {
--- a/classes/semver.js
+++ b/classes/semver.js
@@ -1,6 +1,6 @@
 const debug = require('../internal/debug')
 const { MAX_LENGTH, MAX_SAFE_INTEGER } = require('../internal/constants')
-const { re, t } = require('../internal/re')
+const { safeRe: re, t } = require('../internal/re')
 
 const { compareIdentifiers } = require('../internal/identifiers')
 class SemVer {
--- a/functions/coerce.js
+++ b/functions/coerce.js
@@ -1,6 +1,6 @@
 const SemVer = require('../classes/semver')
 const parse = require('./parse')
-const {re, t} = require('../internal/re')
+const { safeRe: re, t } = require('../internal/re')
 
 const coerce = (version, options) => {
   if (version instanceof SemVer) {
--- a/internal/re.js
+++ b/internal/re.js
@@ -4,16 +4,27 @@
 
 // The actual regexps go on exports.re
 const re = exports.re = []
+const safeRe = exports.safeRe = []
 const src = exports.src = []
 const t = exports.t = {}
 let R = 0
 
 const createToken = (name, value, isGlobal) => {
+  // Replace all greedy whitespace to prevent regex dos issues. These regex are
+  // used internally via the safeRe object since all inputs in this library get
+  // normalized first to trim and collapse all extra whitespace. The original
+  // regexes are exported for userland consumption and lower level usage. A
+  // future breaking change could export the safer regex only with a note that
+  // all input should have extra whitespace removed.
+  const safe = value
+    .split('\\s*').join('\\s{0,1}')
+    .split('\\s+').join('\\s')
   const index = R++
   debug(index, value)
   t[name] = index
   src[index] = value
   re[index] = new RegExp(value, isGlobal ? 'g' : undefined)
+  safeRe[index] = new RegExp(safe, isGlobal ? 'g' : undefined)
 }
 
 // The following Regular Expressions can be used for tokenizing,
@@ -178,5 +189,5 @@
 // Star ranges basically just allow anything at all.
 createToken('STAR', '(<|>)?=?\\s*\\*')
 // >=0.0.0 is like a star
-createToken('GTE0', '^\\s*>=\\s*0\.0\.0\\s*$')
-createToken('GTE0PRE', '^\\s*>=\\s*0\.0\.0-0\\s*$')
+createToken('GTE0', '^\\s*>=\\s*0\\.0\.0\\s*$')
+createToken('GTE0PRE', '^\\s*>=\\s*0\\.0\.0-0\\s*$')
--- a/test/cve-2022-25883.test.js
+++ b/test/cve-2022-25883.test.js
@@ -0,0 +1,41 @@
+@@ -0,0 +1,40 @@
++const { test } = require('tap')
++const Range = require('../classes/range')
++const SemVer = require('../classes/semver')
++const Comparator = require('../classes/comparator')
++const validRange = require('../ranges/valid')
++const minVersion = require('../ranges/min-version')
++const minSatisfying = require('../ranges/min-satisfying')
++const maxSatisfying = require('../ranges/max-satisfying')
++
++// Regression test for CVE-2022-25883 (ReDoS via untrimmed whitespace).
++// A range/version/comparator padded with a large amount of whitespace would
++// take a very long time to process if any ReDoS-susceptible regexes were used.
++// The tap global timeout will error if this test takes too long.
++const s = (n = 500000) => ' '.repeat(n)
++
++test('regex dos via range whitespace', (t) => {
++  const r = `1.2.3 ${s()} <1.3.0`
++
++  t.equal(new Range(r).range, '1.2.3 <1.3.0')
++  t.equal(validRange(r), '1.2.3 <1.3.0')
++  t.equal(minVersion(r).version, '1.2.3')
++  t.equal(minSatisfying(['1.2.3'], r), '1.2.3')
++  t.equal(maxSatisfying(['1.2.3'], r), '1.2.3')
++
++  t.end()
++})
++
++test('semver version whitespace', (t) => {
++  const v = `${s(125)}1.2.3${s(125)}`
++  const tooLong = `${s()}1.2.3${s()}`
++  t.equal(new SemVer(v).version, '1.2.3')
++  t.throws(() => new SemVer(tooLong))
++  t.end()
++})
++
++test('comparator whitespace', (t) => {
++  const c = `${s()}<${s()}1.2.3${s()}`
++  t.equal(new Comparator(c).value, '<1.2.3')
++  t.end()
++})
