diff --git a/semver.js b/semver.js
index d315d5d..f8599af 100644
--- a/semver.js
+++ b/semver.js
@@ -28,9 +28,17 @@ var MAX_SAFE_COMPONENT_LENGTH = 16
 
 // The actual regexps go on exports.re
 var re = exports.re = []
+var safeRe = exports.safeRe = []
 var src = exports.src = []
 var R = 0
 
+// Helper to create safe regex that replaces greedy whitespace patterns
+var createSafeRe = function (value) {
+  return value
+    .split('\\s*').join('\\s{0,1}')
+    .split('\\s+').join('\\s')
+}
+
 // The following Regular Expressions can be used for tokenizing,
 // validating, and parsing SemVer version strings.
 
@@ -239,6 +247,9 @@ for (var i = 0; i < R; i++) {
   if (!re[i]) {
     re[i] = new RegExp(src[i])
   }
+  // Create safe regex with bounded whitespace
+  var safe = createSafeRe(src[i])
+  safeRe[i] = new RegExp(safe)
 }
 
 exports.parse = parse
@@ -731,6 +742,7 @@ function Comparator (comp, options) {
     return new Comparator(comp, options)
   }
 
+  comp = comp.trim().split(/\s+/).join(' ')
   debug('comparator', comp, options)
   this.options = options
   this.loose = !!options.loose
@@ -861,15 +873,21 @@ function Range (range, options) {
   this.loose = !!options.loose
   this.includePrerelease = !!options.includePrerelease
 
-  // First, split based on boolean or ||
+  // First reduce all whitespace as much as possible so we do not have to rely
+  // on potentially slow regexes like \s*. This is then stored and used for
+  // future error messages as well.
   this.raw = range
-  this.set = range.split(/\s*\|\|\s*/).map(function (range) {
-    return this.parseRange(range.trim())
+    .trim()
+    .split(/\s+/)
+    .join(' ')
+
+  // First, split on ||
+  this.set = this.raw.split(/\|\|/).map(function (range) {
+    return this.parseRange(range)
   }, this).filter(function (c) {
     // throw out any that are not relevant for whatever reason
     return c.length
   })
-
   if (!this.set.length) {
     throw new TypeError('Invalid SemVer Range: ' + range)
   }
@@ -890,7 +908,7 @@ Range.prototype.toString = function () {
 
 Range.prototype.parseRange = function (range) {
   var loose = this.options.loose
-  range = range.trim()
+
   // `1.2.3 - 1.2.4` => `>=1.2.3 <=1.2.4`
   var hr = loose ? re[HYPHENRANGELOOSE] : re[HYPHENRANGE]
   range = range.replace(hr, hyphenReplace)
@@ -905,8 +923,8 @@ Range.prototype.parseRange = function (range) {
   // `^ 1.2.3` => `^1.2.3`
   range = range.replace(re[CARETTRIM], caretTrimReplace)
 
-  // normalize spaces
-  range = range.split(/\s+/).join(' ')
+
+
 
   // At this point, the range is completely trimmed and
   // ready to be split into comparators.

