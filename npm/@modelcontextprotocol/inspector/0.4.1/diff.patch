diff --git a/client/src/App.tsx b/client/src/App.tsx
index 246e035..71f4d27 100644
--- a/client/src/App.tsx
+++ b/client/src/App.tsx
@@ -20,6 +20,7 @@ import React, { Suspense, useEffect, useRef, useState } from "react";
 import { useConnection } from "./lib/hooks/useConnection";
 import { useDraggablePane } from "./lib/hooks/useDraggablePane";
 
+import { SESSION_KEYS } from "./lib/constants";
 import { StdErrNotification } from "./lib/notificationTypes";
 
 import { Tabs, TabsList, TabsTrigger } from "@/components/ui/tabs";
@@ -97,6 +98,16 @@ const App = () => {
   >([]);
   const [roots, setRoots] = useState<Root[]>([]);
   const [env, setEnv] = useState<Record<string, string>>({});
+  const [proxyAuthToken] = useState<string>(() => {
+    // Check URL params first, then session storage
+    const urlParams = new URLSearchParams(window.location.search);
+    const urlToken = urlParams.get('MCP_PROXY_AUTH_TOKEN');
+    if (urlToken) {
+      sessionStorage.setItem(SESSION_KEYS.PROXY_AUTH_TOKEN, urlToken);
+      return urlToken;
+    }
+    return sessionStorage.getItem(SESSION_KEYS.PROXY_AUTH_TOKEN) || '';
+  });
 
   const [pendingSampleRequests, setPendingSampleRequests] = useState<
     Array<
@@ -159,6 +170,7 @@ const App = () => {
     sseUrl,
     env,
     proxyServerUrl: PROXY_SERVER_URL,
+    proxyAuthToken,
     onNotification: (notification) => {
       setNotifications((prev) => [...prev, notification as ServerNotification]);
     },
diff --git a/client/src/lib/constants.ts b/client/src/lib/constants.ts
index 13a2370..901080b 100644
--- a/client/src/lib/constants.ts
+++ b/client/src/lib/constants.ts
@@ -4,4 +4,5 @@ export const SESSION_KEYS = {
   SERVER_URL: "mcp_server_url",
   ACCESS_TOKEN: "mcp_access_token",
   REFRESH_TOKEN: "mcp_refresh_token",
+  PROXY_AUTH_TOKEN: "mcp_proxy_auth_token",
 } as const;
diff --git a/client/src/lib/hooks/useConnection.ts b/client/src/lib/hooks/useConnection.ts
index 6c42c3f..d4cfe82 100644
--- a/client/src/lib/hooks/useConnection.ts
+++ b/client/src/lib/hooks/useConnection.ts
@@ -30,6 +30,7 @@ interface UseConnectionOptions {
   env: Record<string, string>;
   proxyServerUrl: string;
   requestTimeout?: number;
+  proxyAuthToken?: string;
   onNotification?: (notification: Notification) => void;
   onStdErrNotification?: (notification: Notification) => void;
   onPendingRequest?: (request: any, resolve: any, reject: any) => void;
@@ -44,6 +45,7 @@ export function useConnection({
   env,
   proxyServerUrl,
   requestTimeout = DEFAULT_REQUEST_TIMEOUT_MSEC,
+  proxyAuthToken,
   onNotification,
   onStdErrNotification,
   onPendingRequest,
@@ -196,6 +198,10 @@ export function useConnection({
       if (accessToken) {
         headers["Authorization"] = `Bearer ${accessToken}`;
       }
+      // Add proxy authentication if token is provided
+      if (proxyAuthToken) {
+        headers["Authorization"] = `Bearer ${proxyAuthToken}`;
+      }
 
       const clientTransport = new SSEClientTransport(backendUrl, {
         eventSourceInit: {
diff --git a/server/src/index.ts b/server/src/index.ts
index 2874b45..9a4bd7f 100644
--- a/server/src/index.ts
+++ b/server/src/index.ts
@@ -1,6 +1,7 @@
 #!/usr/bin/env node
 
 import cors from "cors";
+import { randomBytes } from "node:crypto";
 import { parseArgs } from "node:util";
 import { parse as shellParseArgs } from "shell-quote";
 
@@ -35,6 +36,50 @@ const { values } = parseArgs({
 const app = express();
 app.use(cors());
 
+// Security: Generate session token for authentication
+const sessionToken = randomBytes(32).toString('hex');
+const authDisabled = !!process.env.DANGEROUSLY_OMIT_AUTH;
+
+// Origin validation middleware to prevent DNS rebinding attacks
+const originValidationMiddleware = (req: express.Request, res: express.Response, next: express.NextFunction) => {
+  const origin = req.headers.origin;
+
+  // Default origins based on CLIENT_PORT or use environment variable
+  const clientPort = process.env.CLIENT_PORT || '6274';
+  const defaultOrigins = [
+    `http://localhost:${clientPort}`,
+    `http://127.0.0.1:${clientPort}`
+  ];
+  const allowedOrigins = process.env.ALLOWED_ORIGINS?.split(',') || defaultOrigins;
+
+  if (origin && !allowedOrigins.includes(origin)) {
+    console.error(`Invalid origin: ${origin}`);
+    res.status(403).json({
+      error: 'Forbidden - invalid origin',
+      message: 'Request blocked to prevent DNS rebinding attacks. Configure allowed origins via environment variable.'
+    });
+    return;
+  }
+  next();
+};
+
+// Authentication middleware
+const authMiddleware = (req: express.Request, res: express.Response, next: express.NextFunction) => {
+  if (authDisabled) {
+    return next();
+  }
+
+  const authHeader = req.headers.authorization;
+  if (!authHeader || authHeader !== `Bearer ${sessionToken}`) {
+    res.status(401).json({
+      error: "Unauthorized",
+      message: "Authentication required. Use the session token shown in the console when starting the server."
+    });
+    return;
+  }
+  next();
+};
+
 let webAppTransports: SSEServerTransport[] = [];
 
 const createTransport = async (req: express.Request) => {
@@ -96,7 +141,7 @@ const createTransport = async (req: express.Request) => {
   }
 };
 
-app.get("/sse", async (req, res) => {
+app.get("/sse", originValidationMiddleware, authMiddleware, async (req, res) => {
   try {
     console.log("New SSE connection");
 
@@ -150,7 +195,7 @@ app.get("/sse", async (req, res) => {
   }
 });
 
-app.post("/message", async (req, res) => {
+app.post("/message", originValidationMiddleware, authMiddleware, async (req, res) => {
   try {
     const sessionId = req.query.sessionId;
     console.log(`Received message for sessionId ${sessionId}`);
@@ -167,7 +212,7 @@ app.post("/message", async (req, res) => {
   }
 });
 
-app.get("/config", (req, res) => {
+app.get("/config", originValidationMiddleware, authMiddleware, (req, res) => {
   try {
     res.json({
       defaultEnvironment,
@@ -180,15 +225,27 @@ app.get("/config", (req, res) => {
   }
 });
 
-const PORT = process.env.PORT || 3000;
+const PORT = parseInt(process.env.PORT || '3000', 10);
+const HOST = process.env.HOST || '127.0.0.1';
 
 try {
-  const server = app.listen(PORT);
+  const server = app.listen(PORT, HOST);
 
   server.on("listening", () => {
     const addr = server.address();
     const port = typeof addr === "string" ? addr : addr?.port;
-    console.log(`Proxy server listening on port ${port}`);
+    console.log(`Proxy server listening on ${HOST}:${port}`);
+    if (!authDisabled) {
+      console.log(`🔑 Session token: ${sessionToken}`);
+      console.log(`Use this token to authenticate requests or set DANGEROUSLY_OMIT_AUTH=true to disable auth`);
+
+      // Display clickable URL with pre-filled token
+      const clientPort = process.env.CLIENT_PORT || '6274';
+      const clientUrl = `http://localhost:${clientPort}/?MCP_PROXY_AUTH_TOKEN=${sessionToken}`;
+      console.log(`\n🔗 Open inspector with token pre-filled:\n   ${clientUrl}`);
+    } else {
+      console.log(`⚠️  WARNING: Authentication is disabled. This is not recommended.`);
+    }
   });
 } catch (error) {
   console.error("Failed to start server:", error);
