--- a/node.js
+++ b/node.js
@@ -69,7 +69,9 @@
 );
 
 exports.formatters.o = function(v) {
   return inspect(v, this.useColors)
-    .replace(/\s*\n\s*/g, ' ');
+    .split('\n').map(function(str) {
+      return str.trim();
+    }).join(' ');
 };
 
 /**
