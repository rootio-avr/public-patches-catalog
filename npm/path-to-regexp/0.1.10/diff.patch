--- a/index.js
+++ b/index.js
@@ -84,6 +84,7 @@
       backtrack = slash || format ? '' : path.slice(pos, offset);

       if (match === '*') {
+        backtrack = '';
         extraOffset += 3;
         return '(.*)';
       }
@@ -113,6 +114,7 @@
         + ')'
         + optional;

+      backtrack = '';
       extraOffset += result.length - match.length;

       return result;
