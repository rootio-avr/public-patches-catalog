--- a/deps/dicer/lib/HeaderParser.js
+++ b/deps/dicer/lib/HeaderParser.js
@@ -73,5 +73,7 @@ HeaderParser.prototype._parseHeader = function () {
   for (var i = 0; i < len; ++i) { // eslint-disable-line no-var
     if (lines[i].length === 0) { continue }
+    // Reject bare line breaks instead of exposing them in parsed values
+    if (lines[i].includes('\r') || lines[i].includes('\n')) { return }
     if (lines[i][0] === '\t' || lines[i][0] === ' ') {
       // folded header content
       // RFC2822 says to just remove the CRLF and not the whitespace following
--- a/lib/types/multipart.js
+++ b/lib/types/multipart.js
@@ -139,8 +139,10 @@ function Multipart (boy, cfg) {
         for (i = 0, len = parsed.length; i < len; ++i) {
           if (RE_NAME.test(parsed[i][0])) {
             fieldname = parsed[i][1]
+            if (hasBareLineBreak(fieldname)) { return skipPart(part) }
           } else if (RE_FILENAME.test(parsed[i][0])) {
             filename = parsed[i][1]
+            if (hasBareLineBreak(filename)) { return skipPart(part) }
             if (!preservePath) { filename = basename(filename) }
           }
         }
@@ -289,6 +291,10 @@

 function skipPart (part) {
   part.resume()
+}
+
+function hasBareLineBreak (value) {
+  return typeof value === 'string' && (value.includes('\r') || value.includes('\n'))
 }

 function FileStream (opts) {
