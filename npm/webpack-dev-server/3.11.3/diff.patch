--- a/lib/Server.js
+++ b/lib/Server.js
@@ -115,6 +115,7 @@
     this.setupHooks();
     this.setupApp();
     this.setupCheckHostRoute();
+    this.setupCrossOriginCheck();
     this.setupDevMiddleware();
 
     // set express routes
@@ -202,6 +203,20 @@
     });
   }
 
+  setupCrossOriginCheck() {
+    this.app.all('*', (req, res, next) => {
+      if (
+        req.headers['sec-fetch-mode'] === 'no-cors' &&
+        req.headers['sec-fetch-site'] === 'cross-site'
+      ) {
+        res.statusCode = 403;
+        res.end('Cross-Origin request blocked');
+        return;
+      }
+      next();
+    });
+  }
+
   setupDevMiddleware() {
     // middleware for serving webpack bundle
     this.middleware = webpackDevMiddleware(
@@ -849,14 +864,14 @@
   }
 
   checkHost(headers) {
-    return this.checkHeaders(headers, 'host');
+    return this.checkHeaders(headers, 'host', true);
   }
 
   checkOrigin(headers) {
-    return this.checkHeaders(headers, 'origin');
+    return this.checkHeaders(headers, 'origin', false);
   }
 
-  checkHeaders(headers, headerToCheck) {
+  checkHeaders(headers, headerToCheck, allowIP) {
     // allow user to opt-out this security check, at own risk
     if (this.disableHostCheck) {
       return true;
@@ -890,8 +905,7 @@
     // always allow localhost host, for convenience (hostname === 'localhost')
     // allow hostname of listening address  (hostname === this.hostname)
     const isValidHostname =
-      ip.isV4Format(hostname) ||
-      ip.isV6Format(hostname) ||
+      (allowIP && (ip.isV4Format(hostname) || ip.isV6Format(hostname))) ||
       hostname === 'localhost' ||
       hostname === this.hostname;
 

--- a/lib/Server.js
+++ b/lib/Server.js
@@ -899,10 +899,11 @@
   ).hostname;
-    // always allow requests with explicit IPv4 or IPv6-address.
+    // allow requests with explicit IPv4 or IPv6-address if allowIP is true.
+    // Note that IP should not be automatically allowed for Origin headers,
+    // otherwise an untrusted remote IP host can send requests.
+    //
     // A note on IPv6 addresses:
     // hostHeader will always contain the brackets denoting
     // an IPv6-address in URLs,
     // these are removed from the hostname in url.parse(),
     // so we have the pure IPv6-address in hostname.
-    // always allow localhost host, for convenience (hostname === 'localhost')
-    // allow hostname of listening address  (hostname === this.hostname)
     const isValidHostname =

--- a/lib/Server.js
+++ b/lib/Server.js
@@ -195,6 +195,11 @@
   setupCheckHostRoute() {
     this.app.all('*', (req, res, next) => {
       if (this.checkHost(req.headers)) {
+        // Block cross-origin resource loading when Sec-Fetch-* headers are absent
+        if (!this.disableHostCheck && !this.isUserCORSWildcardEnabled()) {
+          res.setHeader('Cross-Origin-Resource-Policy', 'same-origin');
+        }
+
         return next();
       }
 
@@ -846,6 +851,19 @@
     }
 
     next();
+  }
+
+  isUserCORSWildcardEnabled() {
+    if (!this.headers) {
+      return false;
+    }
+
+    return Object.entries(this.headers).some(
+      ([key, value]) =>
+        key.toLowerCase() === 'access-control-allow-origin' &&
+        typeof value === 'string' &&
+        value.trim() === '*'
+    );
   }
 
   checkHost(headers) {

--- a/lib/Server.js
+++ b/lib/Server.js
@@ -131,8 +131,18 @@
 
     // Proxy websockets without the initial http request
     // https://github.com/chimurai/http-proxy-middleware#external-websocket-upgrade
+    const hmrPath = this.sockPath;
     this.websocketProxies.forEach(function(wsProxy) {
-      this.listeningApp.on('upgrade', wsProxy.upgrade);
+      const proxyUpgrade = wsProxy.upgrade;
+      this.listeningApp.on('upgrade', (req, socket, head) => {
+        if (hmrPath && req.url) {
+          const { pathname } = new URL(req.url, 'http://0.0.0.0');
+          if (pathname === hmrPath) {
+            return;
+          }
+        }
+        proxyUpgrade(req, socket, head);
+      });
     }, this);
   }
 
