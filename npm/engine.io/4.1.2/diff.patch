--- a/lib/server.js
+++ b/lib/server.js
@@ -432,6 +432,9 @@ class Server extends EventEmitter {
           // then the socket needs to die!
           setTimeout(function() {
             if (socket.writable && socket.bytesWritten <= 0) {
+              socket.on("error", function() {
+                debug("error while destroying upgrade");
+              });
               return socket.end();
             }
           }, destroyUpgradeTimeout);
