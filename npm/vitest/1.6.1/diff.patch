diff --git a/packages/vitest/src/api/setup.ts b/packages/vitest/src/api/setup.ts
index 3a01b7a..92ecd1e 100644
--- a/packages/vitest/src/api/setup.ts
+++ b/packages/vitest/src/api/setup.ts
@@ -8,10 +8,11 @@ import type { WebSocket } from 'ws'
 import { WebSocketServer } from 'ws'
 import { isFileServingAllowed } from 'vite'
 import type { ViteDevServer } from 'vite'
+import { cleanUrl } from 'vite-node/utils'
 import type { StackTraceParserOptions } from '@vitest/utils/source-map'
 import { API_PATH } from '../constants'
 import type { Vitest } from '../node'
-import type { File, ModuleGraphData, Reporter, TaskResultPack, UserConsoleLog } from '../types'
+import type { ApiConfig, File, ModuleGraphData, Reporter, TaskResultPack, UserConsoleLog } from '../types'
 import { getModuleGraph, isPrimitive, noop, stringifyReplace } from '../utils'
 import type { WorkspaceProject } from '../node/workspace'
 import { parseErrorStacktrace } from '../utils/source-map'
@@ -46,8 +47,15 @@ export function setup(vitestOrWorkspace: Vitest | WorkspaceProject, _server?: Vi
     })
   })
 
+  function resolveActiveApiConfig(): ApiConfig | undefined {
+    if (_server && _server !== ctx.server && 'config' in vitestOrWorkspace)
+      return vitestOrWorkspace.config.browser?.api ?? ctx.config.api
+
+    return ctx.config.api
+  }
+
   function checkFileAccess(path: string) {
-    if (!isFileServingAllowed(path, server))
+    if (!isFileServingAllowed(cleanUrl(path), server))
       throw new Error(`Access denied to "${path}". See Vite config documentation for "server.fs": https://vitejs.dev/config/server-options.html#server-fs-strict.`)
   }
 
@@ -97,15 +105,21 @@ export function setup(vitestOrWorkspace: Vitest | WorkspaceProject, _server?: Vi
         async saveTestFile(id, content) {
           if (!ctx.state.filesMap.has(id) || !existsSync(id))
             throw new Error(`Test file "${id}" was not registered, so it cannot be updated using the API.`)
+          if (!resolveActiveApiConfig()?.allowWrite)
+            return
           return fs.writeFile(id, content, 'utf-8')
         },
         async saveSnapshotFile(id, content) {
           checkFileAccess(id)
+          if (!resolveActiveApiConfig()?.allowWrite)
+            return
           await fs.mkdir(dirname(id), { recursive: true })
           return fs.writeFile(id, content, 'utf-8')
         },
         async removeSnapshotFile(id) {
           checkFileAccess(id)
+          if (!resolveActiveApiConfig()?.allowWrite)
+            return
           if (!existsSync(id))
             throw new Error(`Snapshot file "${id}" does not exist.`)
           return fs.unlink(id)
@@ -114,6 +128,8 @@ export function setup(vitestOrWorkspace: Vitest | WorkspaceProject, _server?: Vi
           ctx.snapshot.add(snapshot)
         },
         async rerun(files) {
+          if (!resolveActiveApiConfig()?.allowExec)
+            return
           await ctx.rerunFiles(files)
         },
         getConfig() {
@@ -139,6 +155,9 @@ export function setup(vitestOrWorkspace: Vitest | WorkspaceProject, _server?: Vi
           return getModuleGraph(ctx, id)
         },
         updateSnapshot(file?: File) {
+          const apiConfig = resolveActiveApiConfig()
+          if (!apiConfig?.allowExec || !apiConfig?.allowWrite)
+            return Promise.resolve()
           if (!file)
             return ctx.updateSnapshot()
           return ctx.updateSnapshot([file.filepath])
diff --git a/packages/vitest/src/integrations/browser/server.ts b/packages/vitest/src/integrations/browser/server.ts
index 3c6d8db..4b3adaa 100644
--- a/packages/vitest/src/integrations/browser/server.ts
+++ b/packages/vitest/src/integrations/browser/server.ts
@@ -33,7 +33,7 @@ export async function createBrowserServer(project: WorkspaceProject, configFile:
         enforce: 'post',
         name: 'vitest:browser:config',
         async config(config) {
-          const server = resolveApiServerConfig(config.test?.browser || {}) || {
+          const server = resolveApiServerConfig(config.test?.browser || {}, project.ctx.config.api) || {
             port: defaultBrowserPort,
           }
 
diff --git a/packages/vitest/src/node/config.ts b/packages/vitest/src/node/config.ts
index f859823..d1076ef 100644
--- a/packages/vitest/src/node/config.ts
+++ b/packages/vitest/src/node/config.ts
@@ -39,6 +39,7 @@ function parseInspector(inspect: string | undefined | boolean | number) {
 
 export function resolveApiServerConfig<Options extends ApiConfig & UserConfig>(
   options: Options,
+  parentApi?: ApiConfig,
 ): ApiConfig | undefined {
   let api: ApiConfig | undefined
 
@@ -57,6 +58,10 @@ export function resolveApiServerConfig<Options extends ApiConfig & UserConfig>(
         api.strictPort = options.api.strictPort
       if (options.api.host)
         api.host = options.api.host
+      if (options.api.allowWrite !== undefined)
+        api.allowWrite = options.api.allowWrite
+      if (options.api.allowExec !== undefined)
+        api.allowExec = options.api.allowExec
     }
     else {
       api = { ...options.api }
@@ -71,6 +76,15 @@ export function resolveApiServerConfig<Options extends ApiConfig & UserConfig>(
     api = { middlewareMode: true }
   }
 
+  if (api && !api.middlewareMode && api.host && api.host !== 'localhost' && api.host !== '127.0.0.1') {
+    api.allowWrite ??= parentApi?.allowWrite ?? false
+    api.allowExec ??= parentApi?.allowExec ?? false
+  }
+  else if (api) {
+    api.allowWrite ??= parentApi?.allowWrite ?? true
+    api.allowExec ??= parentApi?.allowExec ?? true
+  }
+
   return api
 }
 
@@ -541,7 +555,7 @@ export function resolveConfig(
   if (resolved.browser.enabled && stdProvider === 'stackblitz')
     resolved.browser.provider = 'none'
 
-  resolved.browser.api = resolveApiServerConfig(resolved.browser) || {
+  resolved.browser.api = resolveApiServerConfig(resolved.browser, resolved.api) || {
     port: defaultBrowserPort,
   }
 
diff --git a/packages/vitest/src/types/config.ts b/packages/vitest/src/types/config.ts
index 341eda4..71d2146 100644
--- a/packages/vitest/src/types/config.ts
+++ b/packages/vitest/src/types/config.ts
@@ -25,7 +25,20 @@ export type VitestEnvironment = BuiltinEnvironment | (string & Record<never, nev
 export type { Pool, PoolOptions }
 export type CSSModuleScopeStrategy = 'stable' | 'scoped' | 'non-scoped'
 
-export type ApiConfig = Pick<ServerOptions, 'port' | 'strictPort' | 'host' | 'middlewareMode'>
+export type ApiConfig = Pick<ServerOptions, 'port' | 'strictPort' | 'host' | 'middlewareMode'> & {
+  /**
+   * Allow any write operations from the API server.
+   *
+   * @default true if API is not exposed to the network, false otherwise
+   */
+  allowWrite?: boolean
+  /**
+   * Allow running test files via the API.
+   *
+   * @default true if API is not exposed to the network, false otherwise
+   */
+  allowExec?: boolean
+}
 
 export type { JSDOMOptions, HappyDOMOptions }
 

diff --git a/pnpm-lock.yaml b/pnpm-lock.yaml
index 21d963a..4b0a070 100644
--- a/pnpm-lock.yaml
+++ b/pnpm-lock.yaml
@@ -1168,8 +1168,8 @@ importers:
         specifier: ^7.1.0
         version: 7.1.0
       sweetalert2:
-        specifier: ^11.6.16
-        version: 11.6.16
+        specifier: ^11.22.4
+        version: 11.22.4
       tinyspy:
         specifier: ^1.0.2
         version: 1.0.2
@@ -16347,8 +16347,8 @@ packages:
     resolution: {integrity: sha512-ovssysQTa+luh7A5Weu3Rta6FJlFBBbInjOh722LIt6klpU2/HtdUbszju/G4devcvk8PGt7FCLv5wftu3THUA==}
     dev: true
 
-  /sweetalert2@11.6.16:
-    resolution: {integrity: sha512-T2FO8LptErsjE4r0WMfiSk4YbeUvPadNaUZ/cADMEOnws000znrf8zFX9S5e/spvzJDyRI5En73WQyDZhGypxQ==}
+  /sweetalert2@11.22.4:
+    resolution: {integrity: sha512-JwcRODfozxiKmspFp+xctZ2izAmLAKbRPcoLMEW7LdugN/YmNrX1LT7hdBW87qsgupEO1ukBBuB17KzKFKW0tg==}
     dev: true
 
   /symbol-tree@3.2.4:
diff --git a/test/core/package.json b/test/core/package.json
index 53e5fce..c499b5c 100644
--- a/test/core/package.json
+++ b/test/core/package.json
@@ -22,7 +22,7 @@
     "axios": "^0.26.1",
     "debug": "^4.3.4",
     "strip-ansi": "^7.1.0",
-    "sweetalert2": "^11.6.16",
+    "sweetalert2": "^11.22.4",
     "tinyspy": "^1.0.2",
     "url": "^0.11.0",
     "vitest": "workspace:*",
