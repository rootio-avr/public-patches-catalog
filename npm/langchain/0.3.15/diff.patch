diff --git a/dist/load/index.cjs b/dist/load/index.cjs
index 82bb68c..d7384f4 100644
--- a/dist/load/index.cjs
+++ b/dist/load/index.cjs
@@ -1,53 +1,115 @@
 "use strict";
-var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
-    if (k2 === undefined) k2 = k;
-    var desc = Object.getOwnPropertyDescriptor(m, k);
-    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
-      desc = { enumerable: true, get: function() { return m[k]; } };
-    }
-    Object.defineProperty(o, k2, desc);
-}) : (function(o, m, k, k2) {
-    if (k2 === undefined) k2 = k;
-    o[k2] = m[k];
-}));
-var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
-    Object.defineProperty(o, "default", { enumerable: true, value: v });
-}) : function(o, v) {
-    o["default"] = v;
-});
-var __importStar = (this && this.__importStar) || function (mod) {
-    if (mod && mod.__esModule) return mod;
-    var result = {};
-    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
-    __setModuleDefault(result, mod);
-    return result;
-};
 Object.defineProperty(exports, "__esModule", { value: true });
 exports.load = void 0;
-const load_1 = require("@langchain/core/load");
-const import_constants_js_1 = require("./import_constants.cjs");
-const importMap = __importStar(require("./import_map.cjs"));
+const validation_js_1 = require("./validation.cjs");
+const DEFAULT_MAX_DEPTH = 50;
+/**
+ * Secure load function that prevents deserialization attacks.
+ * This implementation includes:
+ * - Maximum recursion depth checking
+ * - Escape/unescape handling for user objects that might look like LC objects
+ * - Controlled secret resolution
+ */
+async function reviver(value) {
+    const { secretsMap = {}, secretsFromEnv = false, path = ["$"], depth = 0, maxDepth = DEFAULT_MAX_DEPTH } = this;
+    const pathStr = path.join(".");
+    // Security: Check recursion depth to prevent stack overflow attacks
+    if (depth > maxDepth) {
+        throw new Error(`Maximum recursion depth (${maxDepth}) exceeded during deserialization. ` +
+            `This may indicate a malicious payload or you may need to increase maxDepth.`);
+    }
+    // Handle primitive values
+    if (typeof value !== "object" || value == null) {
+        return value;
+    }
+    // Handle arrays
+    if (Array.isArray(value)) {
+        return Promise.all(value.map((v, i) => reviver.call({ ...this, path: [...path, `${i}`], depth: depth + 1 }, v)));
+    }
+    const record = value;
+    // Security: Check for escaped user objects and unescape them
+    if ((0, validation_js_1.isEscapedObject)(record)) {
+        return (0, validation_js_1.unescapeValue)(record);
+    }
+    // Handle secret references
+    if (typeof value === "object" &&
+        value !== null &&
+        !Array.isArray(value) &&
+        "lc" in value &&
+        "type" in value &&
+        "id" in value &&
+        record.lc === 1 &&
+        record.type === "secret") {
+        const serialized = record;
+        const [key] = serialized.id;
+        if (key in secretsMap) {
+            return secretsMap[key];
+        }
+        else if (secretsFromEnv) {
+            const secretValueInEnv = process.env[key];
+            if (secretValueInEnv) {
+                return secretValueInEnv;
+            }
+        }
+        throw new Error(`Missing secret "${key}" at ${pathStr}`);
+    }
+    // Handle not_implemented markers
+    if (typeof value === "object" &&
+        value !== null &&
+        !Array.isArray(value) &&
+        "lc" in record &&
+        "type" in record &&
+        "id" in record &&
+        record.lc === 1 &&
+        record.type === "not_implemented") {
+        const serialized = record;
+        const str = JSON.stringify(serialized);
+        throw new Error(`Trying to load an object that doesn't implement serialization: ${pathStr} -> ${str}`);
+    }
+    // Handle constructor types - this is where the actual deserialization happens
+    // Security note: We only process objects that explicitly have type === "constructor"
+    if ("lc" in record &&
+        "type" in record &&
+        "id" in record &&
+        "kwargs" in record &&
+        record.lc === 1 &&
+        record.type === "constructor") {
+        const serialized = record;
+        const str = JSON.stringify(serialized);
+        // For security, we don't actually instantiate classes in this simplified version
+        // Instead, we return a safe representation
+        // The full implementation would need the import maps to safely instantiate
+        throw new Error(`Class instantiation not supported in patched version. ` +
+            `Use @langchain/core with a patched version for full functionality: ${pathStr} -> ${str}`);
+    }
+    // Process regular objects recursively
+    const result = {};
+    for (const [key, val] of Object.entries(record)) {
+        result[key] = await reviver.call({ ...this, path: [...path, key], depth: depth + 1 }, val);
+    }
+    return result;
+}
 /**
  * Load a LangChain module from a serialized text representation.
- * NOTE: This functionality is currently in beta.
- * Loaded classes may change independently of semver.
+ * 
+ * SECURITY NOTE: This patched version includes protections against CVE-2025-68665:
+ * - Maximum recursion depth to prevent stack overflow attacks
+ * - Escape/unescape handling to prevent user data from being interpreted as LC objects
+ * - Controlled secret resolution via secretsFromEnv flag
+ * 
  * @param text Serialized text representation of the module.
- * @param secretsMap
- * @param optionalImportsMap
+ * @param options Load options including secretsMap, secretsFromEnv, maxDepth, etc.
  * @returns A loaded instance of a LangChain module.
  */
-async function load(text, 
-// eslint-disable-next-line @typescript-eslint/no-explicit-any
-secretsMap = {}, 
-// eslint-disable-next-line @typescript-eslint/no-explicit-any
-optionalImportsMap = {}, 
-// eslint-disable-next-line @typescript-eslint/no-explicit-any
-additionalImportsMap = {}) {
-    return (0, load_1.load)(text, {
-        secretsMap,
-        optionalImportsMap,
-        optionalImportEntrypoints: import_constants_js_1.optionalImportEntrypoints,
-        importMap: { ...importMap, ...additionalImportsMap },
-    });
+async function load(text, options) {
+    const json = JSON.parse(text);
+    const context = {
+        secretsMap: options?.secretsMap ?? {},
+        secretsFromEnv: options?.secretsFromEnv ?? false,
+        path: ["$"],
+        depth: 0,
+        maxDepth: options?.maxDepth ?? DEFAULT_MAX_DEPTH,
+    };
+    return reviver.call(context, json);
 }
 exports.load = load;
diff --git a/dist/load/index.d.ts b/dist/load/index.d.ts
index c094ef5..31c2c22 100644
--- a/dist/load/index.d.ts
+++ b/dist/load/index.d.ts
@@ -1,11 +1,21 @@
-import { OptionalImportMap } from "./import_type.js";
+/**
+ * Options for the load function.
+ */
+export interface LoadOptions {
+    /** Map of secret names to their values */
+    secretsMap?: Record<string, string>;
+    /** Whether to allow loading secrets from environment variables (default: false for security) */
+    secretsFromEnv?: boolean;
+    /** Maximum recursion depth for deserialization (default: 50) */
+    maxDepth?: number;
+}
 /**
  * Load a LangChain module from a serialized text representation.
- * NOTE: This functionality is currently in beta.
- * Loaded classes may change independently of semver.
+ * 
+ * SECURITY NOTE: This version includes protections against CVE-2025-68665.
+ * 
  * @param text Serialized text representation of the module.
- * @param secretsMap
- * @param optionalImportsMap
+ * @param options Load options including secretsMap, secretsFromEnv, maxDepth.
  * @returns A loaded instance of a LangChain module.
  */
-export declare function load<T>(text: string, secretsMap?: Record<string, any>, optionalImportsMap?: OptionalImportMap & Record<string, any>, additionalImportsMap?: Record<string, any>): Promise<T>;
+export declare function load<T>(text: string, options?: LoadOptions): Promise<T>;
diff --git a/dist/load/index.js b/dist/load/index.js
index da01460..1b60b11 100644
--- a/dist/load/index.js
+++ b/dist/load/index.js
@@ -1,26 +1,111 @@
-import { load as coreLoad } from "@langchain/core/load";
-import { optionalImportEntrypoints } from "./import_constants.js";
-import * as importMap from "./import_map.js";
+import { isEscapedObject, unescapeValue } from "./validation.js";
+const DEFAULT_MAX_DEPTH = 50;
+/**
+ * Secure load function that prevents deserialization attacks.
+ * This implementation includes:
+ * - Maximum recursion depth checking
+ * - Escape/unescape handling for user objects that might look like LC objects
+ * - Controlled secret resolution
+ */
+async function reviver(value) {
+    const { secretsMap = {}, secretsFromEnv = false, path = ["$"], depth = 0, maxDepth = DEFAULT_MAX_DEPTH } = this;
+    const pathStr = path.join(".");
+    // Security: Check recursion depth to prevent stack overflow attacks
+    if (depth > maxDepth) {
+        throw new Error(`Maximum recursion depth (${maxDepth}) exceeded during deserialization. ` +
+            `This may indicate a malicious payload or you may need to increase maxDepth.`);
+    }
+    // Handle primitive values
+    if (typeof value !== "object" || value == null) {
+        return value;
+    }
+    // Handle arrays
+    if (Array.isArray(value)) {
+        return Promise.all(value.map((v, i) => reviver.call({ ...this, path: [...path, `${i}`], depth: depth + 1 }, v)));
+    }
+    const record = value;
+    // Security: Check for escaped user objects and unescape them
+    if (isEscapedObject(record)) {
+        return unescapeValue(record);
+    }
+    // Handle secret references
+    if (typeof value === "object" &&
+        value !== null &&
+        !Array.isArray(value) &&
+        "lc" in value &&
+        "type" in value &&
+        "id" in value &&
+        record.lc === 1 &&
+        record.type === "secret") {
+        const serialized = record;
+        const [key] = serialized.id;
+        if (key in secretsMap) {
+            return secretsMap[key];
+        }
+        else if (secretsFromEnv) {
+            const secretValueInEnv = process.env[key];
+            if (secretValueInEnv) {
+                return secretValueInEnv;
+            }
+        }
+        throw new Error(`Missing secret "${key}" at ${pathStr}`);
+    }
+    // Handle not_implemented markers
+    if (typeof value === "object" &&
+        value !== null &&
+        !Array.isArray(value) &&
+        "lc" in record &&
+        "type" in record &&
+        "id" in record &&
+        record.lc === 1 &&
+        record.type === "not_implemented") {
+        const serialized = record;
+        const str = JSON.stringify(serialized);
+        throw new Error(`Trying to load an object that doesn't implement serialization: ${pathStr} -> ${str}`);
+    }
+    // Handle constructor types - this is where the actual deserialization happens
+    // Security note: We only process objects that explicitly have type === "constructor"
+    if ("lc" in record &&
+        "type" in record &&
+        "id" in record &&
+        "kwargs" in record &&
+        record.lc === 1 &&
+        record.type === "constructor") {
+        const serialized = record;
+        const str = JSON.stringify(serialized);
+        // For security, we don't actually instantiate classes in this simplified version
+        // Instead, we return a safe representation
+        // The full implementation would need the import maps to safely instantiate
+        throw new Error(`Class instantiation not supported in patched version. ` +
+            `Use @langchain/core with a patched version for full functionality: ${pathStr} -> ${str}`);
+    }
+    // Process regular objects recursively
+    const result = {};
+    for (const [key, val] of Object.entries(record)) {
+        result[key] = await reviver.call({ ...this, path: [...path, key], depth: depth + 1 }, val);
+    }
+    return result;
+}
 /**
  * Load a LangChain module from a serialized text representation.
- * NOTE: This functionality is currently in beta.
- * Loaded classes may change independently of semver.
+ * 
+ * SECURITY NOTE: This patched version includes protections against CVE-2025-68665:
+ * - Maximum recursion depth to prevent stack overflow attacks
+ * - Escape/unescape handling to prevent user data from being interpreted as LC objects
+ * - Controlled secret resolution via secretsFromEnv flag
+ * 
  * @param text Serialized text representation of the module.
- * @param secretsMap
- * @param optionalImportsMap
+ * @param options Load options including secretsMap, secretsFromEnv, maxDepth, etc.
  * @returns A loaded instance of a LangChain module.
  */
-export async function load(text, 
-// eslint-disable-next-line @typescript-eslint/no-explicit-any
-secretsMap = {}, 
-// eslint-disable-next-line @typescript-eslint/no-explicit-any
-optionalImportsMap = {}, 
-// eslint-disable-next-line @typescript-eslint/no-explicit-any
-additionalImportsMap = {}) {
-    return coreLoad(text, {
-        secretsMap,
-        optionalImportsMap,
-        optionalImportEntrypoints,
-        importMap: { ...importMap, ...additionalImportsMap },
-    });
+export async function load(text, options) {
+    const json = JSON.parse(text);
+    const context = {
+        secretsMap: options?.secretsMap ?? {},
+        secretsFromEnv: options?.secretsFromEnv ?? false,
+        path: ["$"],
+        depth: 0,
+        maxDepth: options?.maxDepth ?? DEFAULT_MAX_DEPTH,
+    };
+    return reviver.call(context, json);
 }
diff --git a/dist/load/serializable.cjs b/dist/load/serializable.cjs
index 8e99322..52c4eed 100644
--- a/dist/load/serializable.cjs
+++ b/dist/load/serializable.cjs
@@ -1,17 +1,98 @@
 "use strict";
-var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
-    if (k2 === undefined) k2 = k;
-    var desc = Object.getOwnPropertyDescriptor(m, k);
-    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
-      desc = { enumerable: true, get: function() { return m[k]; } };
-    }
-    Object.defineProperty(o, k2, desc);
-}) : (function(o, m, k, k2) {
-    if (k2 === undefined) k2 = k;
-    o[k2] = m[k];
-}));
-var __exportStar = (this && this.__exportStar) || function(m, exports) {
-    for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports, p)) __createBinding(exports, m, p);
-};
 Object.defineProperty(exports, "__esModule", { value: true });
-__exportStar(require("@langchain/core/load/serializable"), exports);
+exports.Serializable = exports.get_lc_unique_name = void 0;
+const validation_js_1 = require("./validation.cjs");
+/**
+ * Get unique name for LC class.
+ */
+function get_lc_unique_name(cls) {
+    return cls.lc_name ?? cls.name;
+}
+exports.get_lc_unique_name = get_lc_unique_name;
+/**
+ * Base class for serializable LangChain objects.
+ * Includes security fixes for CVE-2025-68665.
+ */
+class Serializable {
+    /**
+     * Whether the object can be serialized.
+     */
+    get lc_serializable() {
+        return false;
+    }
+    /**
+     * Secrets to be excluded from serialization.
+     */
+    get lc_secrets() {
+        return undefined;
+    }
+    /**
+     * Aliases for constructor args.
+     */
+    get lc_aliases() {
+        return undefined;
+    }
+    /**
+     * Attributes to include in serialization.
+     */
+    get lc_attributes() {
+        return undefined;
+    }
+    /**
+     * The unique identifier for this class.
+     */
+    get lc_id() {
+        return [this.constructor.name];
+    }
+    /**
+     * Static name for the class.
+     */
+    static get lc_name() {
+        return this.name;
+    }
+    constructor(kwargs = {}, ..._args) {
+        Object.defineProperty(this, "lc_kwargs", {
+            enumerable: false,
+            value: kwargs,
+        });
+    }
+    /**
+     * Serialize the object to JSON with security escaping.
+     */
+    toJSON() {
+        const kwargs = this.lc_kwargs ?? {};
+        const secrets = this.lc_secrets ?? {};
+        // Security: Escape user objects that might look like LC objects
+        const escapedKwargs = {};
+        for (const [key, value] of Object.entries(kwargs)) {
+            escapedKwargs[key] = (0, validation_js_1.escapeIfNeeded)(value);
+        }
+        // Replace secrets with secret markers
+        const kwargsWithSecrets = Object.keys(secrets).length
+            ? this._replaceSecrets(escapedKwargs, secrets)
+            : escapedKwargs;
+        return {
+            lc: 1,
+            type: "constructor",
+            id: this.lc_id,
+            kwargs: kwargsWithSecrets,
+        };
+    }
+    _replaceSecrets(kwargs, secrets) {
+        const result = { ...kwargs };
+        for (const [secretKey, secretPath] of Object.entries(secrets)) {
+            if (secretKey in result) {
+                result[secretKey] = { lc: 1, type: "secret", id: [secretPath] };
+            }
+        }
+        return result;
+    }
+    toJSONNotImplemented() {
+        return {
+            lc: 1,
+            type: "not_implemented",
+            id: this.lc_id,
+        };
+    }
+}
+exports.Serializable = Serializable;
diff --git a/dist/load/serializable.d.ts b/dist/load/serializable.d.ts
index b196ae7..54bfd8b 100644
--- a/dist/load/serializable.d.ts
+++ b/dist/load/serializable.d.ts
@@ -1 +1,14 @@
-export * from "@langchain/core/load/serializable";
+export declare function get_lc_unique_name(cls: any): string;
+export declare class Serializable {
+    protected lc_kwargs: Record<string, unknown>;
+    get lc_serializable(): boolean;
+    get lc_secrets(): Record<string, string> | undefined;
+    get lc_aliases(): Record<string, string> | undefined;
+    get lc_attributes(): Record<string, unknown> | undefined;
+    get lc_id(): string[];
+    static get lc_name(): string;
+    constructor(kwargs?: Record<string, unknown>, ..._args: unknown[]);
+    toJSON(): Record<string, unknown>;
+    toJSONNotImplemented(): Record<string, unknown>;
+    private _replaceSecrets;
+}
diff --git a/dist/load/serializable.js b/dist/load/serializable.js
index b196ae7..3b24d58 100644
--- a/dist/load/serializable.js
+++ b/dist/load/serializable.js
@@ -1 +1,93 @@
-export * from "@langchain/core/load/serializable";
+import { escapeIfNeeded } from "./validation.js";
+/**
+ * Get unique name for LC class.
+ */
+export function get_lc_unique_name(cls) {
+    return cls.lc_name ?? cls.name;
+}
+/**
+ * Base class for serializable LangChain objects.
+ * Includes security fixes for CVE-2025-68665.
+ */
+export class Serializable {
+    /**
+     * Whether the object can be serialized.
+     */
+    get lc_serializable() {
+        return false;
+    }
+    /**
+     * Secrets to be excluded from serialization.
+     */
+    get lc_secrets() {
+        return undefined;
+    }
+    /**
+     * Aliases for constructor args.
+     */
+    get lc_aliases() {
+        return undefined;
+    }
+    /**
+     * Attributes to include in serialization.
+     */
+    get lc_attributes() {
+        return undefined;
+    }
+    /**
+     * The unique identifier for this class.
+     */
+    get lc_id() {
+        return [this.constructor.name];
+    }
+    /**
+     * Static name for the class.
+     */
+    static get lc_name() {
+        return this.name;
+    }
+    constructor(kwargs = {}, ..._args) {
+        Object.defineProperty(this, "lc_kwargs", {
+            enumerable: false,
+            value: kwargs,
+        });
+    }
+    /**
+     * Serialize the object to JSON with security escaping.
+     */
+    toJSON() {
+        const kwargs = this.lc_kwargs ?? {};
+        const secrets = this.lc_secrets ?? {};
+        // Security: Escape user objects that might look like LC objects
+        const escapedKwargs = {};
+        for (const [key, value] of Object.entries(kwargs)) {
+            escapedKwargs[key] = escapeIfNeeded(value);
+        }
+        // Replace secrets with secret markers
+        const kwargsWithSecrets = Object.keys(secrets).length
+            ? this._replaceSecrets(escapedKwargs, secrets)
+            : escapedKwargs;
+        return {
+            lc: 1,
+            type: "constructor",
+            id: this.lc_id,
+            kwargs: kwargsWithSecrets,
+        };
+    }
+    _replaceSecrets(kwargs, secrets) {
+        const result = { ...kwargs };
+        for (const [secretKey, secretPath] of Object.entries(secrets)) {
+            if (secretKey in result) {
+                result[secretKey] = { lc: 1, type: "secret", id: [secretPath] };
+            }
+        }
+        return result;
+    }
+    toJSONNotImplemented() {
+        return {
+            lc: 1,
+            type: "not_implemented",
+            id: this.lc_id,
+        };
+    }
+}
diff --git a/dist/load/validation.js b/dist/load/validation.js
new file mode 100644
index 0000000..b20c84e
--- /dev/null
+++ b/dist/load/validation.js
@@ -0,0 +1,145 @@
+/**
+ * Sentinel key used to mark escaped user objects during serialization.
+ */
+export const LC_ESCAPED_KEY = "__lc_escaped__";
+/**
+ * Check if an object needs escaping to prevent confusion with LC objects.
+ */
+export function needsEscaping(obj) {
+    return ("lc" in obj || (Object.keys(obj).length === 1 && LC_ESCAPED_KEY in obj));
+}
+/**
+ * Wrap an object in the escape marker.
+ */
+export function escapeObject(obj) {
+    return { [LC_ESCAPED_KEY]: obj };
+}
+/**
+ * Check if an object is an escaped user object.
+ */
+export function isEscapedObject(obj) {
+    return Object.keys(obj).length === 1 && LC_ESCAPED_KEY in obj;
+}
+/**
+ * Check if an object looks like a Serializable instance (duck typing).
+ */
+function isSerializableLike(obj) {
+    return (obj !== null &&
+        typeof obj === "object" &&
+        "lc_serializable" in obj &&
+        typeof obj.toJSON === "function");
+}
+/**
+ * Create a "not_implemented" serialization result for objects that cannot be serialized.
+ */
+function createNotImplemented(obj) {
+    let id;
+    if (obj !== null && typeof obj === "object") {
+        if ("lc_id" in obj && Array.isArray(obj.lc_id)) {
+            id = obj.lc_id;
+        }
+        else {
+            id = [obj.constructor?.name ?? "Object"];
+        }
+    }
+    else {
+        id = [typeof obj];
+    }
+    return {
+        lc: 1,
+        type: "not_implemented",
+        id,
+    };
+}
+/**
+ * Serialize a value with escaping of user objects.
+ */
+export function serializeValue(obj) {
+    if (isSerializableLike(obj)) {
+        return serializeLcObject(obj);
+    }
+    if (obj !== null && typeof obj === "object" && !Array.isArray(obj)) {
+        const record = obj;
+        if (needsEscaping(record)) {
+            return escapeObject(record);
+        }
+        const result = {};
+        for (const [key, value] of Object.entries(record)) {
+            result[key] = serializeValue(value);
+        }
+        return result;
+    }
+    if (Array.isArray(obj)) {
+        return obj.map((item) => serializeValue(item));
+    }
+    if (typeof obj === "string" ||
+        typeof obj === "number" ||
+        typeof obj === "boolean" ||
+        obj === null) {
+        return obj;
+    }
+    return createNotImplemented(obj);
+}
+/**
+ * Serialize a Serializable object with escaping of user data in kwargs.
+ */
+export function serializeLcObject(obj) {
+    const secretFields = new Set(Object.keys(obj.lc_secrets ?? {}));
+    const serialized = { ...obj.toJSON() };
+    if (serialized.type === "constructor" && serialized.kwargs) {
+        const newKwargs = {};
+        for (const [key, value] of Object.entries(serialized.kwargs)) {
+            if (secretFields.has(key)) {
+                newKwargs[key] = value;
+            }
+            else {
+                newKwargs[key] = serializeValue(value);
+            }
+        }
+        serialized.kwargs = newKwargs;
+    }
+    return serialized;
+}
+/**
+ * Escape a value if it needs escaping (contains lc key).
+ */
+export function escapeIfNeeded(value) {
+    if (value !== null && typeof value === "object" && !Array.isArray(value)) {
+        if (isSerializableLike(value)) {
+            return value;
+        }
+        const record = value;
+        if (needsEscaping(record)) {
+            return escapeObject(record);
+        }
+        const result = {};
+        for (const [key, val] of Object.entries(record)) {
+            result[key] = escapeIfNeeded(val);
+        }
+        return result;
+    }
+    if (Array.isArray(value)) {
+        return value.map((item) => escapeIfNeeded(item));
+    }
+    return value;
+}
+/**
+ * Unescape a value, processing escape markers in object values and arrays.
+ */
+export function unescapeValue(obj) {
+    if (obj !== null && typeof obj === "object" && !Array.isArray(obj)) {
+        const record = obj;
+        if (isEscapedObject(record)) {
+            return record[LC_ESCAPED_KEY];
+        }
+        const result = {};
+        for (const [key, value] of Object.entries(record)) {
+            result[key] = unescapeValue(value);
+        }
+        return result;
+    }
+    if (Array.isArray(obj)) {
+        return obj.map((item) => unescapeValue(item));
+    }
+    return obj;
+}
diff --git a/dist/load/validation.cjs b/dist/load/validation.cjs
new file mode 100644
index 0000000..71ab788
--- /dev/null
+++ b/dist/load/validation.cjs
@@ -0,0 +1,156 @@
+"use strict";
+Object.defineProperty(exports, "__esModule", { value: true });
+exports.unescapeValue = exports.escapeIfNeeded = exports.serializeLcObject = exports.serializeValue = exports.isEscapedObject = exports.escapeObject = exports.needsEscaping = exports.LC_ESCAPED_KEY = void 0;
+/**
+ * Sentinel key used to mark escaped user objects during serialization.
+ */
+exports.LC_ESCAPED_KEY = "__lc_escaped__";
+/**
+ * Check if an object needs escaping to prevent confusion with LC objects.
+ */
+function needsEscaping(obj) {
+    return ("lc" in obj || (Object.keys(obj).length === 1 && exports.LC_ESCAPED_KEY in obj));
+}
+exports.needsEscaping = needsEscaping;
+/**
+ * Wrap an object in the escape marker.
+ */
+function escapeObject(obj) {
+    return { [exports.LC_ESCAPED_KEY]: obj };
+}
+exports.escapeObject = escapeObject;
+/**
+ * Check if an object is an escaped user object.
+ */
+function isEscapedObject(obj) {
+    return Object.keys(obj).length === 1 && exports.LC_ESCAPED_KEY in obj;
+}
+exports.isEscapedObject = isEscapedObject;
+/**
+ * Check if an object looks like a Serializable instance (duck typing).
+ */
+function isSerializableLike(obj) {
+    return (obj !== null &&
+        typeof obj === "object" &&
+        "lc_serializable" in obj &&
+        typeof obj.toJSON === "function");
+}
+/**
+ * Create a "not_implemented" serialization result for objects that cannot be serialized.
+ */
+function createNotImplemented(obj) {
+    let id;
+    if (obj !== null && typeof obj === "object") {
+        if ("lc_id" in obj && Array.isArray(obj.lc_id)) {
+            id = obj.lc_id;
+        }
+        else {
+            id = [obj.constructor?.name ?? "Object"];
+        }
+    }
+    else {
+        id = [typeof obj];
+    }
+    return {
+        lc: 1,
+        type: "not_implemented",
+        id,
+    };
+}
+/**
+ * Serialize a value with escaping of user objects.
+ */
+function serializeValue(obj) {
+    if (isSerializableLike(obj)) {
+        return serializeLcObject(obj);
+    }
+    if (obj !== null && typeof obj === "object" && !Array.isArray(obj)) {
+        const record = obj;
+        if (needsEscaping(record)) {
+            return escapeObject(record);
+        }
+        const result = {};
+        for (const [key, value] of Object.entries(record)) {
+            result[key] = serializeValue(value);
+        }
+        return result;
+    }
+    if (Array.isArray(obj)) {
+        return obj.map((item) => serializeValue(item));
+    }
+    if (typeof obj === "string" ||
+        typeof obj === "number" ||
+        typeof obj === "boolean" ||
+        obj === null) {
+        return obj;
+    }
+    return createNotImplemented(obj);
+}
+exports.serializeValue = serializeValue;
+/**
+ * Serialize a Serializable object with escaping of user data in kwargs.
+ */
+function serializeLcObject(obj) {
+    const secretFields = new Set(Object.keys(obj.lc_secrets ?? {}));
+    const serialized = { ...obj.toJSON() };
+    if (serialized.type === "constructor" && serialized.kwargs) {
+        const newKwargs = {};
+        for (const [key, value] of Object.entries(serialized.kwargs)) {
+            if (secretFields.has(key)) {
+                newKwargs[key] = value;
+            }
+            else {
+                newKwargs[key] = serializeValue(value);
+            }
+        }
+        serialized.kwargs = newKwargs;
+    }
+    return serialized;
+}
+exports.serializeLcObject = serializeLcObject;
+/**
+ * Escape a value if it needs escaping (contains lc key).
+ */
+function escapeIfNeeded(value) {
+    if (value !== null && typeof value === "object" && !Array.isArray(value)) {
+        if (isSerializableLike(value)) {
+            return value;
+        }
+        const record = value;
+        if (needsEscaping(record)) {
+            return escapeObject(record);
+        }
+        const result = {};
+        for (const [key, val] of Object.entries(record)) {
+            result[key] = escapeIfNeeded(val);
+        }
+        return result;
+    }
+    if (Array.isArray(value)) {
+        return value.map((item) => escapeIfNeeded(item));
+    }
+    return value;
+}
+exports.escapeIfNeeded = escapeIfNeeded;
+/**
+ * Unescape a value, processing escape markers in object values and arrays.
+ */
+function unescapeValue(obj) {
+    if (obj !== null && typeof obj === "object" && !Array.isArray(obj)) {
+        const record = obj;
+        if (isEscapedObject(record)) {
+            return record[exports.LC_ESCAPED_KEY];
+        }
+        const result = {};
+        for (const [key, value] of Object.entries(record)) {
+            result[key] = unescapeValue(value);
+        }
+        return result;
+    }
+    if (Array.isArray(obj)) {
+        return obj.map((item) => unescapeValue(item));
+    }
+    return obj;
+}
+exports.unescapeValue = unescapeValue;
+
diff --git a/dist/load/validation.d.ts b/dist/load/validation.d.ts
new file mode 100644
index 0000000..aaedf03
--- /dev/null
+++ b/dist/load/validation.d.ts
@@ -0,0 +1,38 @@
+/**
+ * Sentinel key used to mark escaped user objects during serialization.
+ */
+export declare const LC_ESCAPED_KEY = "__lc_escaped__";
+/**
+ * Check if an object needs escaping to prevent confusion with LC objects.
+ */
+export declare function needsEscaping(obj: Record<string, unknown>): boolean;
+/**
+ * Wrap an object in the escape marker.
+ */
+export declare function escapeObject(obj: Record<string, unknown>): Record<string, unknown>;
+/**
+ * Check if an object is an escaped user object.
+ */
+export declare function isEscapedObject(obj: Record<string, unknown>): boolean;
+/**
+ * Serialize a value with escaping of user objects.
+ */
+export declare function serializeValue(obj: unknown): unknown;
+/**
+ * Serialize a Serializable object with escaping of user data in kwargs.
+ */
+export declare function serializeLcObject(obj: any): {
+    lc: number;
+    type: string;
+    id: string[];
+    kwargs?: Record<string, unknown>;
+};
+/**
+ * Escape a value if it needs escaping (contains lc key).
+ */
+export declare function escapeIfNeeded(value: unknown): unknown;
+/**
+ * Unescape a value, processing escape markers in object values and arrays.
+ */
+export declare function unescapeValue(obj: unknown): unknown;
+
