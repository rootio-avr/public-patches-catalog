--- a/lib/util.js
+++ b/lib/util.js
@@ -8,6 +8,10 @@ const parseRange = require('range-parser');
 
 const HASH_REGEXP = /[0-9a-f]{10,}/;
 
+// Matches a `..` path segment (at the start, end, or between separators),
+// used to detect path traversal attempts. See CVE-2024-29180.
+const UP_PATH_REGEXP = /(?:^|[\\/])\.\.(?:[\\/]|$)/;
+
 // support for multi-compiler configuration
 // see: https://github.com/webpack/webpack-dev-server/issues/641
 function getPaths(publicPath, compiler, url) {
@@ -94,13 +98,32 @@ module.exports = {
       return false;
     }
 
+    // Decode the requested path *before* joining it to the output directory.
+    // Otherwise encoded traversal sequences (e.g. `%2e%2e%2f`) survive
+    // `path.join` unchanged and only get decoded afterwards, allowing reads
+    // outside the output directory. Reject null bytes and `..` traversal
+    // outright. See CVE-2024-29180.
+    if (filename) {
+      filename = querystring.unescape(filename);
+
+      // Null byte(s) - reject
+      if (filename.indexOf('\0') !== -1) {
+        return false;
+      }
+
+      // ".." is malicious - reject path traversal
+      if (UP_PATH_REGEXP.test(path.normalize(`./${filename}`))) {
+        return false;
+      }
+    }
+
     let uri = outputPath;
 
     /* istanbul ignore if */
     if (process.platform === 'win32') {
       // Path Handling for Microsoft Windows
       if (filename) {
-        uri = path.posix.join(outputPath || '', querystring.unescape(filename));
+        uri = path.posix.join(outputPath || '', filename);
 
         if (!path.win32.isAbsolute(uri)) {
           uri = `/${uri}`;
@@ -119,8 +142,9 @@ module.exports = {
       }
     }
 
-    // if no matches, use outputPath as filename
-    return querystring.unescape(uri);
+    // `filename` has already been decoded above; `outputPath` is trusted and
+    // is used verbatim, so no further unescaping is required here.
+    return uri;
   },
 
   handleRangeHeaders(content, req, res) {
--- /dev/null
+++ b/test/cve-2024-29180.test.js
@@ -0,0 +1,49 @@
+import { getFilenameFromUrl } from '../lib/util';
+
+// Regression test for CVE-2024-29180 (path traversal in getFilenameFromUrl).
+// A request path is decoded before it is joined with the output directory, and
+// any decoded path that escapes the output directory ("..") or contains a null
+// byte must be rejected (getFilenameFromUrl returns `false`, which the
+// middleware turns into a pass-through / not-found) instead of resolving to a
+// file above the served root.
+describe('CVE-2024-29180: getFilenameFromUrl path traversal', () => {
+  function resolve(url, outputPath = '/dist', publicPath = '/') {
+    return getFilenameFromUrl(publicPath, { outputPath }, url);
+  }
+
+  const maliciousUrls = [
+    // Percent-encoded "../" traversal (the advisory PoC shape).
+    '/..%2f..%2f..%2fetc%2fpasswd',
+    // Encoded dots + encoded slash.
+    '/%2e%2e/%2e%2e/etc/passwd',
+    // Plain unencoded traversal.
+    '/../../../etc/passwd',
+    // Fully encoded dots and slashes.
+    '/%2e%2e%2f%2e%2e%2fetc%2fpasswd',
+  ];
+
+  for (const url of maliciousUrls) {
+    it(`rejects traversal for ${url}`, () => {
+      expect(resolve(url)).toBe(false);
+    });
+  }
+
+  it('rejects a null byte in the path', () => {
+    expect(resolve('/foo%00.js')).toBe(false);
+  });
+
+  // Legitimate requests must keep working after the fix.
+  it('still resolves a normal file', () => {
+    expect(resolve('/a.js')).toBe('/dist/a.js');
+  });
+
+  it('still resolves percent-encoded (non-traversal) characters', () => {
+    expect(resolve('/f%C3%B6%C3%B6.js', '/')).toBe('/föö.js');
+  });
+
+  it('still allows dot segments that are not traversal', () => {
+    expect(resolve('/folder-name-with-dots/mono-v6.x.x', '/')).toBe(
+      '/folder-name-with-dots/mono-v6.x.x'
+    );
+  });
+});
