--- a/lib/nghttp2_session.c
+++ b/lib/nghttp2_session.c
@@ -5584,6 +5584,10 @@ ssize_t nghttp2_session_mem_recv(nghttp2
           return rv;
         }
 
+        if (iframe->state == NGHTTP2_IB_IGN_ALL) {
+          return (ssize_t)inlen;
+        }
+
         on_begin_frame_called = 1;
 
         rv = session_process_headers_frame(session);
@@ -5907,6 +5911,10 @@ ssize_t nghttp2_session_mem_recv(nghttp2
           if (nghttp2_is_fatal(rv)) {
             return rv;
           }
+
+          if (iframe->state == NGHTTP2_IB_IGN_ALL) {
+            return (ssize_t)inlen;
+          }
         }
       }
 
@@ -6276,6 +6284,10 @@ ssize_t nghttp2_session_mem_recv(nghttp2
           if (nghttp2_is_fatal(rv)) {
             return rv;
           }
+
+          if (iframe->state == NGHTTP2_IB_IGN_ALL) {
+            return (ssize_t)inlen;
+          }
         }
         session_inbound_frame_reset(session);
       }
@@ -6445,6 +6457,10 @@ ssize_t nghttp2_session_mem_recv(nghttp2
         if (nghttp2_is_fatal(rv)) {
           return rv;
         }
+
+        if (iframe->state == NGHTTP2_IB_IGN_ALL) {
+          return (ssize_t)inlen;
+        }
       } else {
         iframe->state = NGHTTP2_IB_IGN_HEADER_BLOCK;
       }
@@ -6495,6 +6511,10 @@ ssize_t nghttp2_session_mem_recv(nghttp2
         if (nghttp2_is_fatal(rv)) {
           return rv;
         }
+
+        if (iframe->state == NGHTTP2_IB_IGN_ALL) {
+          return (ssize_t)inlen;
+        }
       }
 
       busy = 1;
@@ -6553,6 +6573,10 @@ ssize_t nghttp2_session_mem_recv(nghttp2
           return rv;
         }
 
+        if (iframe->state == NGHTTP2_IB_IGN_ALL) {
+          return (ssize_t)inlen;
+        }
+
         data_readlen = inbound_frame_effective_readlen(
             iframe, iframe->payloadleft, readlen);
 
@@ -6617,6 +6641,10 @@ ssize_t nghttp2_session_mem_recv(nghttp2
             if (nghttp2_is_fatal(rv)) {
               return NGHTTP2_ERR_CALLBACK_FAILURE;
             }
+
+            if (iframe->state == NGHTTP2_IB_IGN_ALL) {
+              return (ssize_t)inlen;
+            }
           }
         }
       }
@@ -6630,6 +6658,10 @@ ssize_t nghttp2_session_mem_recv(nghttp2
         return rv;
       }
 
+      if (iframe->state == NGHTTP2_IB_IGN_ALL) {
+        return (ssize_t)inlen;
+      }
+
       session_inbound_frame_reset(session);
 
       break;
@@ -6697,6 +6729,10 @@ ssize_t nghttp2_session_mem_recv(nghttp2
           return rv;
         }
 
+        if (iframe->state == NGHTTP2_IB_IGN_ALL) {
+          return (ssize_t)inlen;
+        }
+
         if (rv != 0) {
           busy = 1;
 
@@ -6715,6 +6751,10 @@ ssize_t nghttp2_session_mem_recv(nghttp2
         return rv;
       }
 
+      if (iframe->state == NGHTTP2_IB_IGN_ALL) {
+        return (ssize_t)inlen;
+      }
+
       session_inbound_frame_reset(session);
 
       break;
@@ -6743,6 +6783,10 @@ ssize_t nghttp2_session_mem_recv(nghttp2
         return rv;
       }
 
+      if (iframe->state == NGHTTP2_IB_IGN_ALL) {
+        return (ssize_t)inlen;
+      }
+
       session_inbound_frame_reset(session);
 
       break;
