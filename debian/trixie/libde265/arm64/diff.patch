Description: CVE-2025-61147 - Replace unsafe atoi() calls with validated parse_param()
 Segmentation fault vulnerability in the component
 decoder_context::compute_framedrop_table(). Caused by unsafe atoi()
 calls on command-line parameters without validation, allowing denial
 of service. This patch replaces atoi() with a new parse_param()
 function using std::optional (C++17) for bounds-checked integer
 parsing, and bumps the C++ standard from 11 to 17.
Origin: upstream, https://github.com/strukturag/libde265/commit/8b17e0930f77db07f55e0b89399a8f054ddbecf7
Author: Dirk Farin <dirk.farin@gmail.com>
Last-Update: 2026-03-31

--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -5,7 +5,7 @@ project (libde265
     VERSION 1.0.15
 )

-set(CMAKE_CXX_STANDARD 11)
+set(CMAKE_CXX_STANDARD 17)
 set(CMAKE_CXX_STANDARD_REQUIRED ON)
 set(CMAKE_CXX_EXTENSIONS OFF)
 set(CMAKE_POSITION_INDEPENDENT_CODE ON)
--- a/dec265/dec265.cc
+++ b/dec265/dec265.cc
@@ -27,6 +27,10 @@
 #define DO_MEMORY_LOGGING 0

 #include "de265.h"
+#include <stdexcept>
+#include <iostream>
+#include <optional>
+
 #ifdef HAVE_CONFIG_H
 #include "config.h"
 #endif
@@ -563,6 +567,40 @@
 #endif


+int parse_param(const char* arg, std::optional<int> lower_bound, std::optional<int> upper_bound, const char* arg_name)
+{
+  int value;
+
+  try {
+    size_t len;
+    value = std::stoi(optarg, &len);
+    if (arg[len] != 0) {
+      std::cerr << "invalid argument to " << arg_name << "\n";
+      exit(5);
+    }
+  } catch (std::invalid_argument const& ex) {
+    std::cerr << "invalid argument to " << arg_name << "\n";
+    exit(5);
+  }
+  catch (std::out_of_range const& ex) {
+    std::cerr << "argument to -T is out of range\n";
+    exit(5);
+  }
+
+  if (lower_bound && value < *lower_bound) {
+    std::cerr << "argument to " << arg_name << " may not be smaller than " << *lower_bound << "\n";
+    exit(5);
+  }
+
+  if (upper_bound && value > *upper_bound) {
+    std::cerr << "argument to " << arg_name << " may not be larger than " << *upper_bound << "\n";
+    exit(5);
+  }
+
+  return value;
+}
+
+
 int main(int argc, char** argv)
 {
   while (1) {
@@ -578,9 +616,9 @@

     switch (c) {
     case 'q': quiet++; break;
-    case 't': nThreads=atoi(optarg); break;
+    case 't': nThreads=parse_param(optarg, 0, std::nullopt, "-t"); break;
     case 'c': check_hash=true; break;
-    case 'f': max_frames=atoi(optarg); break;
+    case 'f': max_frames=parse_param(optarg, 1, std::nullopt, "-f"); break;
     case 'o': write_yuv=true; output_filename=optarg; break;
     case 'h': show_help=true; break;
     case 'd': dump_headers=true; break;
@@ -592,7 +630,7 @@
     case 'm': measure_quality=true; reference_filename=optarg; break;
     case 's': show_ssim_map=true; break;
     case 'e': show_psnr_map=true; break;
-    case 'T': highestTID=atoi(optarg); break;
+    case 'T': highestTID = parse_param(optarg, 0, std::nullopt, "-T"); break;
     case 'v': verbosity++; break;
     }
   }
Description: CVE-2026-33164 - Fix reallocation of metadata array when ctb size changes
 A malformed H.265 PPS NAL unit causes a segmentation fault in
 pic_parameter_set::set_derived_values() in libde265 versions prior to
 1.0.17. The fix adds a check for ctb size changes during metadata array
 reallocation and narrows the log2unitSize type from int to uint8_t.
Origin: upstream, https://github.com/strukturag/libde265/commit/c7891e412106130b83f8e8ea8b7f907e9449b658
Author: Dirk Farin <dirk.farin@gmail.com>
Last-Update: 2026-03-31

--- a/libde265/image.cc
+++ b/libde265/image.cc
@@ -452,8 +452,9 @@

     // CTB info

-    if (ctb_info.width_in_units != sps->PicWidthInCtbsY ||
-        ctb_info.height_in_units != sps->PicHeightInCtbsY)
+    if (ctb_info.width_in_units  != sps->PicWidthInCtbsY  ||
+        ctb_info.height_in_units != sps->PicHeightInCtbsY ||
+        ctb_info.log2unitSize    != sps->Log2CtbSizeY)
       {
         delete[] ctb_progress;

--- a/libde265/image.h
+++ b/libde265/image.h
@@ -149,7 +149,7 @@
   // private:
   DataUnit* data;
   int data_size;
-  int log2unitSize;
+  uint8_t log2unitSize;
   int width_in_units;
   int height_in_units;
 };
