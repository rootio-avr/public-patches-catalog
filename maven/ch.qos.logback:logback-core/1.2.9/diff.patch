--- a/logback-core/src/main/java/ch/qos/logback/core/util/OptionHelper.java
+++ b/logback-core/src/main/java/ch/qos/logback/core/util/OptionHelper.java
@@ -43,7 +43,30 @@
     public static Object instantiateByClassName(String className, Class<?> superClass, ClassLoader classLoader) throws IncompatibleClassException,
                     DynamicClassLoadingException {
         return instantiateByClassNameAndParameter(className, superClass, classLoader, null, null);
     }
+
+    /**
+     * Instantiate an object of the given class, but only if it is assignable to the
+     * expected superclass. This restricts object creation to the expected type,
+     * preventing instantiation of arbitrary classes during configuration parsing.
+     *
+     * @param classObj the class to instantiate
+     * @param superClass the expected supertype that classObj must be assignable to
+     * @return a new instance of classObj
+     * @throws IncompatibleClassException if classObj is not assignable to superClass
+     * @throws DynamicClassLoadingException if instantiation fails
+     */
+    public static Object instantiateClassWithSuperclassRestriction(Class<?> classObj, Class<?> superClass)
+                    throws IncompatibleClassException, DynamicClassLoadingException {
+        if (!superClass.isAssignableFrom(classObj)) {
+            throw new IncompatibleClassException(superClass, classObj);
+        }
+        try {
+            return classObj.newInstance();
+        } catch (Throwable t) {
+            throw new DynamicClassLoadingException("Failed to instantiate type " + classObj.getName(), t);
+        }
+    }

     public static Object instantiateByClassNameAndParameter(String className, Class<?> superClass, ClassLoader classLoader, Class<?> type, Object parameter)
                     throws IncompatibleClassException, DynamicClassLoadingException {
--- a/logback-core/src/main/java/ch/qos/logback/core/joran/util/PropertySetter.java
+++ b/logback-core/src/main/java/ch/qos/logback/core/joran/util/PropertySetter.java
@@ -395,4 +395,41 @@
         return getByConcreteType(name, relevantMethod);
     }

+    /**
+     * Determine the expected property type for a complex property, based on the
+     * setter or adder method's parameter type. This is used to validate that an
+     * object being created during configuration parsing is of the expected type.
+     *
+     * @param nestedElementTagName the tag name of the nested element
+     * @param aggregationType the aggregation type (AS_COMPLEX_PROPERTY or AS_COMPLEX_PROPERTY_COLLECTION)
+     * @return the expected Class type for the property
+     */
+    public Class<?> getTypeForComplexProperty(String nestedElementTagName, AggregationType aggregationType) {
+        Method aMethod = null;
+        String cName = capitalizeFirstLetter(nestedElementTagName);
+        switch (aggregationType) {
+        case AS_COMPLEX_PROPERTY:
+            aMethod = findSetterMethod(nestedElementTagName);
+            break;
+        case AS_COMPLEX_PROPERTY_COLLECTION:
+            aMethod = findAdderMethod(cName);
+            break;
+        default:
+            break;
+        }
+
+        if (aMethod == null) {
+            addError("Could not find method for property [" + nestedElementTagName + "].");
+            return null;
+        }
+
+        Class<?> paramType = getParameterClassForMethod(aMethod);
+        if (paramType == null) {
+            addError("Could not determine parameter type for property [" + nestedElementTagName + "].");
+            return null;
+        }
+
+        return paramType;
+    }
+
 }
--- a/logback-core/src/main/java/ch/qos/logback/core/joran/action/NestedComplexPropertyIA.java
+++ b/logback-core/src/main/java/ch/qos/logback/core/joran/action/NestedComplexPropertyIA.java
@@ -118,7 +118,17 @@
                 addInfo("Assuming default type [" + componentClass.getName() + "] for [" + localName + "] property");
             }

-            actionData.setNestedComplexProperty(componentClass.newInstance());
+            // Restrict object creation to expected supertype (CVE-2026-1225)
+            Class<?> expectedType = actionData.parentBean.getTypeForComplexProperty(
+                            actionData.getComplexPropertyName(), actionData.getAggregationType());
+            if (expectedType != null) {
+                actionData.setNestedComplexProperty(
+                                OptionHelper.instantiateClassWithSuperclassRestriction(componentClass, expectedType));
+            } else {
+                actionData.inError = true;
+                addError("Could not determine expected type for property [" + localName + "]");
+                return;
+            }

             // pass along the repository
             if (actionData.getNestedComplexProperty() instanceof ContextAware) {
