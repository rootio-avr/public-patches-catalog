diff --git a/core/pom.xml b/core/pom.xml
--- a/core/pom.xml
+++ b/core/pom.xml
@@ -3,6 +3,7 @@
     <groupId>org.apache.mina</groupId>
     <artifactId>mina-parent</artifactId>
     <version>2.0.0-RC1</version>
+    <relativePath>../parent/pom.xml</relativePath>
   </parent>
 
   <artifactId>mina-core</artifactId>
diff --git a/transport-apr/pom.xml b/transport-apr/pom.xml
--- a/transport-apr/pom.xml
+++ b/transport-apr/pom.xml
@@ -3,6 +3,7 @@
     <groupId>org.apache.mina</groupId>
     <artifactId>mina-parent</artifactId>
     <version>2.0.0-RC1</version>
+    <relativePath>../parent/pom.xml</relativePath>
   </parent>
 
   <artifactId>mina-transport-apr</artifactId>
diff --git a/filter-compression/pom.xml b/filter-compression/pom.xml
--- a/filter-compression/pom.xml
+++ b/filter-compression/pom.xml
@@ -3,6 +3,7 @@
     <groupId>org.apache.mina</groupId>
     <artifactId>mina-parent</artifactId>
     <version>2.0.0-RC1</version>
+    <relativePath>../parent/pom.xml</relativePath>
   </parent>
 
   <artifactId>mina-filter-compression</artifactId>
diff --git a/statemachine/pom.xml b/statemachine/pom.xml
--- a/statemachine/pom.xml
+++ b/statemachine/pom.xml
@@ -3,6 +3,7 @@
     <groupId>org.apache.mina</groupId>
     <artifactId>mina-parent</artifactId>
     <version>2.0.0-RC1</version>
+    <relativePath>../parent/pom.xml</relativePath>
   </parent>
 
   <artifactId>mina-statemachine</artifactId>
diff --git a/integration-beans/pom.xml b/integration-beans/pom.xml
--- a/integration-beans/pom.xml
+++ b/integration-beans/pom.xml
@@ -3,6 +3,7 @@
     <groupId>org.apache.mina</groupId>
     <artifactId>mina-parent</artifactId>
     <version>2.0.0-RC1</version>
+    <relativePath>../parent/pom.xml</relativePath>
   </parent>
 
   <artifactId>mina-integration-beans</artifactId>
diff --git a/integration-xbean/pom.xml b/integration-xbean/pom.xml
--- a/integration-xbean/pom.xml
+++ b/integration-xbean/pom.xml
@@ -3,6 +3,7 @@
     <groupId>org.apache.mina</groupId>
     <artifactId>mina-parent</artifactId>
     <version>2.0.0-RC1</version>
+    <relativePath>../parent/pom.xml</relativePath>
   </parent>
 
   <artifactId>mina-integration-xbean</artifactId>
diff --git a/integration-ognl/pom.xml b/integration-ognl/pom.xml
--- a/integration-ognl/pom.xml
+++ b/integration-ognl/pom.xml
@@ -3,6 +3,7 @@
     <groupId>org.apache.mina</groupId>
     <artifactId>mina-parent</artifactId>
     <version>2.0.0-RC1</version>
+    <relativePath>../parent/pom.xml</relativePath>
   </parent>
 
   <artifactId>mina-integration-ognl</artifactId>
diff --git a/integration-jmx/pom.xml b/integration-jmx/pom.xml
--- a/integration-jmx/pom.xml
+++ b/integration-jmx/pom.xml
@@ -3,6 +3,7 @@
     <groupId>org.apache.mina</groupId>
     <artifactId>mina-parent</artifactId>
     <version>2.0.0-RC1</version>
+    <relativePath>../parent/pom.xml</relativePath>
   </parent>
 
   <artifactId>mina-integration-jmx</artifactId>
diff --git a/example/pom.xml b/example/pom.xml
--- a/example/pom.xml
+++ b/example/pom.xml
@@ -3,6 +3,7 @@
     <groupId>org.apache.mina</groupId>
     <artifactId>mina-parent</artifactId>
     <version>2.0.0-RC1</version>
+    <relativePath>../parent/pom.xml</relativePath>
   </parent>
 
   <artifactId>mina-example</artifactId>

--- a/core/src/main/java/org/apache/mina/filter/ssl/SslFilter.java
+++ b/core/src/main/java/org/apache/mina/filter/ssl/SslFilter.java
@@ -687,7 +687,7 @@ public class SslFilter extends IoFilterAdapter {
         }
     }

-    private static class EncryptedWriteRequest extends WriteRequestWrapper {
+    /* package protected */ static class EncryptedWriteRequest extends WriteRequestWrapper {
         private final IoBuffer encryptedMessage;

         private EncryptedWriteRequest(WriteRequest writeRequest,
--- a/core/src/main/java/org/apache/mina/filter/ssl/SslHandler.java
+++ b/core/src/main/java/org/apache/mina/filter/ssl/SslHandler.java
@@ -30,6 +30,7 @@ import javax.net.ssl.SSLException;
 import javax.net.ssl.SSLHandshakeException;

 import org.apache.mina.core.buffer.IoBuffer;
+import org.apache.mina.core.RuntimeIoException;
 import org.apache.mina.core.filterchain.IoFilterEvent;
 import org.apache.mina.core.filterchain.IoFilter.NextFilter;
 import org.apache.mina.core.future.DefaultWriteFuture;
@@ -38,6 +39,7 @@ import org.apache.mina.core.session.IoEventType;
 import org.apache.mina.core.session.IoSession;
 import org.apache.mina.core.write.DefaultWriteRequest;
 import org.apache.mina.core.write.WriteRequest;
+import org.apache.mina.filter.ssl.SslFilter.EncryptedWriteRequest;
 import org.apache.mina.util.CircularQueue;
 import org.slf4j.Logger;
 import org.slf4j.LoggerFactory;
@@ -440,9 +442,31 @@ class SslHandler {
         * UNDERFLOW - Need to read more data from the socket. It's normal.
         * CLOSED - The other peer closed the socket. Also normal.
         */
-        if (status != SSLEngineResult.Status.OK
-                && status != SSLEngineResult.Status.CLOSED
-                && status != SSLEngineResult.Status.BUFFER_UNDERFLOW) {
+        if (status == SSLEngineResult.Status.BUFFER_OVERFLOW) {
+            throw new SSLException("SSLEngine error during decrypt: " + status
+                    + " inNetBuffer: " + inNetBuffer + "appBuffer: "
+                    + appBuffer);
+        } else if (status == SSLEngineResult.Status.CLOSED) {
+            Exception exception = new RuntimeIoException("SSL/TLS close_notify received");
+
+            // Empty the Ssl queue
+            for (IoFilterEvent event : filterWriteEventQueue) {
+                EncryptedWriteRequest writeRequest = (EncryptedWriteRequest) event.getParameter();
+                WriteFuture writeFuture = writeRequest.getParentRequest().getFuture();
+                writeFuture.setException(exception);
+            }
+
+            // Empty the session queue
+            while (!session.getWriteRequestQueue().isEmpty(session)) {
+                WriteRequest writeRequest = session.getWriteRequestQueue().poll(session);
+                WriteFuture writeFuture = writeRequest.getFuture();
+                writeFuture.setException(exception);
+            }
+
+            // We *must* shutdown session
+            session.close(true);
+        } else if (status != SSLEngineResult.Status.OK
+                   && status != SSLEngineResult.Status.BUFFER_UNDERFLOW) {
             throw new SSLException("SSLEngine error during decrypt: " + status
                     + " inNetBuffer: " + inNetBuffer + "appBuffer: "
                     + appBuffer);
         }
