Description: Fix CVE-2026-5588 - PKIX draft CompositeVerifier accepts empty signature sequence
 The generic composite signature verifier in JcaContentVerifierProviderBuilder
 could return a positive verification result without actually checking any
 signature (e.g., when given an empty signature sequence). The fix introduces
 an `atLeastOneChecked` flag and ANDs it into the return value so that
 verify() returns false when no signature was actually verified.
 Backported from upstream commit 656bae0 (released in BouncyCastle 1.84).
 The target uses DERBitString.getBytes() (1.70 API) which is preserved
 unchanged; only the security guard is added. A regression test
 (testForgedComposite) is added; testComposite from upstream is omitted
 because it depends on ML-DSA APIs not present in 1.70.
Origin: upstream, https://github.com/bcgit/bc-java/commit/656bae0dbd9b1521f840521ff786e78749fe3057
Author: David Hook <dgh@cryptoworkshop.com>
Last-Update: 2026-06-24

diff --git a/pkix/src/main/java/org/bouncycastle/operator/jcajce/JcaContentVerifierProviderBuilder.java b/pkix/src/main/java/org/bouncycastle/operator/jcajce/JcaContentVerifierProviderBuilder.java
index 8f2102b..6ae7756 100644
--- a/pkix/src/main/java/org/bouncycastle/operator/jcajce/JcaContentVerifierProviderBuilder.java
+++ b/pkix/src/main/java/org/bouncycastle/operator/jcajce/JcaContentVerifierProviderBuilder.java
@@ -429,17 +429,20 @@ public boolean verify(byte[] expected)
             {
                 ASN1Sequence sigSeq = ASN1Sequence.getInstance(expected);
                 boolean failed = false;
+                boolean atLeastOneChecked = false;
+
                 for (int i = 0; i != sigSeq.size(); i++)
                 {
                     if (sigs[i] != null)
                     {
+                        atLeastOneChecked = true;
                         if (!sigs[i].verify(DERBitString.getInstance(sigSeq.getObjectAt(i)).getBytes()))
                         {
                             failed = true;
                         }
                     }
                 }
-                return !failed;
+                return !failed & atLeastOneChecked;
             }
             catch (SignatureException e)
             {
diff --git a/pkix/src/test/java/org/bouncycastle/cert/cmp/test/AllTests.java b/pkix/src/test/java/org/bouncycastle/cert/cmp/test/AllTests.java
index d7bf2b8..1e55afb 100644
--- a/pkix/src/test/java/org/bouncycastle/cert/cmp/test/AllTests.java
+++ b/pkix/src/test/java/org/bouncycastle/cert/cmp/test/AllTests.java
@@ -17,6 +17,8 @@
 import junit.framework.TestSuite;
 import org.bouncycastle.asn1.ASN1Integer;
 import org.bouncycastle.asn1.ASN1Primitive;
+import org.bouncycastle.asn1.DERBitString;
+import org.bouncycastle.asn1.DERNull;
 import org.bouncycastle.asn1.DERSequence;
 import org.bouncycastle.asn1.cmp.CMPCertificate;
 import org.bouncycastle.asn1.cmp.CertConfirmContent;
@@ -25,6 +27,7 @@
 import org.bouncycastle.asn1.cmp.CertResponse;
 import org.bouncycastle.asn1.cmp.CertifiedKeyPair;
 import org.bouncycastle.asn1.cmp.PKIBody;
+import org.bouncycastle.asn1.cmp.PKIHeaderBuilder;
 import org.bouncycastle.asn1.cmp.PKIMessage;
 import org.bouncycastle.asn1.cmp.PKIStatus;
 import org.bouncycastle.asn1.cmp.PKIStatusInfo;
@@ -34,9 +37,12 @@
 import org.bouncycastle.asn1.crmf.EncryptedValue;
 import org.bouncycastle.asn1.crmf.ProofOfPossession;
 import org.bouncycastle.asn1.crmf.SubsequentMessage;
+import org.bouncycastle.asn1.misc.MiscObjectIdentifiers;
 import org.bouncycastle.asn1.pkcs.EncryptedPrivateKeyInfo;
+import org.bouncycastle.asn1.pkcs.PKCSObjectIdentifiers;
 import org.bouncycastle.asn1.pkcs.PrivateKeyInfo;
 import org.bouncycastle.asn1.x500.X500Name;
+import org.bouncycastle.asn1.x509.AlgorithmIdentifier;
 import org.bouncycastle.asn1.x509.GeneralName;
 import org.bouncycastle.cert.CertException;
 import org.bouncycastle.cert.X509CertificateHolder;
@@ -405,4 +411,47 @@ private static PKIMessage loadMessage(String name)
             throw new RuntimeException(e.toString());
         }
     }
-}
\ No newline at end of file
+
+    public void testForgedComposite()
+        throws Exception
+    {
+        // setup: a trusted RSA keypair (stand-in for a CA / server key)
+        KeyPairGenerator kpg = KeyPairGenerator.getInstance("RSA", BC);
+        kpg.initialize(2048);
+        KeyPair kp = kpg.generateKeyPair();
+        PublicKey trustedKey = kp.getPublic();
+
+        ContentVerifierProvider verifier =
+            new JcaContentVerifierProviderBuilder().build(trustedKey);
+
+        GeneralName sender = new GeneralName(new X500Name("CN=attacker"));
+        GeneralName recip = new GeneralName(new X500Name("CN=victim"));
+        PKIBody body = new PKIBody(PKIBody.TYPE_CONFIRM, DERNull.INSTANCE);
+
+        // NORMAL: legitimately signed with the private key
+        ContentSigner signer =
+            new JcaContentSignerBuilder("SHA256withRSA").build(kp.getPrivate());
+        ProtectedPKIMessage legit = new ProtectedPKIMessageBuilder(sender, recip)
+            .setMessageTime(new Date())
+            .setBody(body)
+            .build(signer);
+
+        // protectionAlg.algorithm  = 1.3.6.1.4.1.18227.2.1 (id_alg_composite)
+        // protectionAlg.parameters = SEQUENCE { AlgId(sha256WithRSA, NULL) }
+        // protection               = BIT STRING wrapping 30 00 (empty SEQ)
+        AlgorithmIdentifier innerAlg = new AlgorithmIdentifier(
+            PKCSObjectIdentifiers.sha256WithRSAEncryption, DERNull.INSTANCE);
+        AlgorithmIdentifier compositeAlg = new AlgorithmIdentifier(
+            MiscObjectIdentifiers.id_alg_composite,
+            new DERSequence(innerAlg));
+
+        PKIHeaderBuilder fh = new PKIHeaderBuilder(2, sender, recip);
+        fh.setProtectionAlg(compositeAlg);
+        DERBitString emptySigSeq = new DERBitString(
+            new DERSequence().getEncoded());
+
+        PKIMessage forged = new PKIMessage(fh.build(), body, emptySigSeq);
+        ProtectedPKIMessage forgedPM = new ProtectedPKIMessage(new GeneralPKIMessage(forged));
+        assertFalse(forgedPM.verify(verifier));
+    }
+}

Description: Build fixes for bc-java 1.70 in the patch orchestrator image
 (1) Drop io.spring.nohttp 0.0.8 (NoSuchMethodError on Gradle 8.x fallback).
 (2) Apply maven-publish in allprojects alongside java; configure MavenPublication
     in afterEvaluate. Hook-only fix — set inject_maven_publish: false in meta.json.
Last-Update: 2026-06-25

diff --git a/build.gradle b/build.gradle
index 844d4d2..c2ded17 100644
--- a/build.gradle
+++ b/build.gradle
@@ -8,7 +8,6 @@ buildscript {
 }
 
 plugins {
-   id "io.spring.nohttp" version "0.0.8"
    id "checkstyle"
 }
 
@@ -21,11 +20,9 @@ if (JavaVersion.current().isJava8Compatible())
   }
 }
 
-// this needs to go here, otherwise it can't find config
-apply plugin: 'io.spring.nohttp'
-
 allprojects {
   apply plugin: 'java'
+  apply plugin: 'maven-publish'
   apply plugin: 'idea'
   apply plugin: 'checkstyle'
 
@@ -78,3 +75,34 @@ subprojects {
 }
 
 test.dependsOn([':core:test', ':prov:test', ':pkix:test', ':mail:test', 'pg:test', ':tls:test'])
+subprojects { sub ->
+    sub.afterEvaluate {
+        if (sub == sub.rootProject) {
+            return
+        }
+        def jt = sub.tasks.findByName('jar')
+        if (jt == null) {
+            return
+        }
+        if (sub.publishing.publications.findByName('mavenJava') != null) {
+            return
+        }
+        sub.publishing {
+            publications {
+                mavenJava(MavenPublication) {
+                    groupId = 'org.bouncycastle'
+                    artifactId = jt.archiveBaseName.get()
+                    version = sub.version.toString()
+                    artifact jt
+                    if (sub.tasks.names.contains('sourcesJar')) {
+                        artifact sub.tasks.sourcesJar
+                    }
+                    if (sub.tasks.names.contains('javadocJar')) {
+                        artifact sub.tasks.javadocJar
+                    }
+                }
+            }
+        }
+    }
+}
+
