diff --git a/src/main/java/META-INF/versions/9/module-info.java b/src/main/java/META-INF/versions/9/module-info.java
deleted file mode 100644
index 0fd13f8..0000000
--- a/src/main/java/META-INF/versions/9/module-info.java
+++ /dev/null
@@ -1,76 +0,0 @@
-module org.bouncycastle.fips.core
-{
-    provides java.security.Provider with org.bouncycastle.jcajce.provider.BouncyCastleFipsProvider;
-
-    exports org.bouncycastle;
-    exports org.bouncycastle.asn1;
-    exports org.bouncycastle.asn1.anssi;
-    exports org.bouncycastle.asn1.bc;
-    exports org.bouncycastle.asn1.bsi;
-    exports org.bouncycastle.asn1.cmp;
-    exports org.bouncycastle.asn1.cms;
-    exports org.bouncycastle.asn1.cms.ecc;
-    exports org.bouncycastle.asn1.crmf;
-    exports org.bouncycastle.asn1.cryptopro;
-    exports org.bouncycastle.asn1.dvcs;
-    exports org.bouncycastle.asn1.eac;
-    exports org.bouncycastle.asn1.edec;
-    exports org.bouncycastle.asn1.esf;
-    exports org.bouncycastle.asn1.ess;
-    exports org.bouncycastle.asn1.gm;
-    exports org.bouncycastle.asn1.gnu;
-    exports org.bouncycastle.asn1.iana;
-    exports org.bouncycastle.asn1.icao;
-    exports org.bouncycastle.asn1.isismtt;
-    exports org.bouncycastle.asn1.isismtt.ocsp;
-    exports org.bouncycastle.asn1.isismtt.x509;
-    exports org.bouncycastle.asn1.iso;
-    exports org.bouncycastle.asn1.kisa;
-    exports org.bouncycastle.asn1.microsoft;
-    exports org.bouncycastle.asn1.misc;
-    exports org.bouncycastle.asn1.mozilla;
-    exports org.bouncycastle.asn1.nist;
-    exports org.bouncycastle.asn1.nsri;
-    exports org.bouncycastle.asn1.ntt;
-    exports org.bouncycastle.asn1.ocsp;
-    exports org.bouncycastle.asn1.oiw;
-    exports org.bouncycastle.asn1.pkcs;
-    exports org.bouncycastle.asn1.rosstandart;
-    exports org.bouncycastle.asn1.sec;
-    exports org.bouncycastle.asn1.smime;
-    exports org.bouncycastle.asn1.teletrust;
-    exports org.bouncycastle.asn1.tsp;
-    exports org.bouncycastle.asn1.ua;
-    exports org.bouncycastle.asn1.util;
-    exports org.bouncycastle.asn1.x500;
-    exports org.bouncycastle.asn1.x500.style;
-    exports org.bouncycastle.asn1.x509;
-    exports org.bouncycastle.asn1.x509.qualified;
-    exports org.bouncycastle.asn1.x509.sigi;
-    exports org.bouncycastle.asn1.x9;
-    exports org.bouncycastle.crypto;
-    exports org.bouncycastle.crypto.asymmetric;
-    exports org.bouncycastle.crypto.fips;
-    exports org.bouncycastle.crypto.general;
-    exports org.bouncycastle.crypto.util;
-    exports org.bouncycastle.jcajce;
-    exports org.bouncycastle.jcajce.io;
-    exports org.bouncycastle.jcajce.interfaces;
-    exports org.bouncycastle.jcajce.provider;
-    exports org.bouncycastle.jcajce.spec;
-    exports org.bouncycastle.jcajce.util;
-    exports org.bouncycastle.math.ec;
-    exports org.bouncycastle.math.ec.custom.sec;
-    exports org.bouncycastle.math.ec.endo;
-    exports org.bouncycastle.math.ec.rfc7748;
-    exports org.bouncycastle.math.ec.rfc8032;
-    exports org.bouncycastle.math.field;
-    exports org.bouncycastle.util;
-    exports org.bouncycastle.util.encoders;
-    exports org.bouncycastle.util.io;
-    exports org.bouncycastle.util.io.pem;
-    exports org.bouncycastle.util.test;
-
-    requires java.logging;
-    requires java.naming;
-}

--- a/src/main/java/org/bouncycastle/crypto/fips/AESEngine.java
+++ b/src/main/java/org/bouncycastle/crypto/fips/AESEngine.java
@@ -443,7 +443,16 @@
     {
         if (params instanceof KeyParameter)
         {
+            int[][] oldKey = WorkingKey;
             WorkingKey = generateWorkingKey(((KeyParameter)params).getKey(), forEncryption);
+            // deterministically zeroize any previous working key on re-key
+            if (oldKey != null)
+            {
+                for (int i = 0; i != oldKey.length; i++)
+                {
+                    Arrays.fill(oldKey[i], 0);
+                }
+            }
             this.forEncryption = forEncryption;
             if (forEncryption)
             {
@@ -636,20 +645,5 @@
              (s[r2&255]&255) ^ ((Si[(r1>>8)&255]&255)<<8) ^ ((Si[(r0>>16)&255]&255)<<16) ^ (s[(r3>>24)&255]<<24) ^ KW[0][2],
              (Si[r3&255]&255) ^ ((s[(r2>>8)&255]&255)<<8) ^ ((s[(r1>>16)&255]&255)<<16) ^ (s[(r0>>24)&255]<<24) ^ KW[0][3],
         out, outOff);
-    }
-
-    @Override
-    protected void finalize()
-        throws Throwable
-    {
-        super.finalize();
-
-        if (WorkingKey != null)
-        {
-            for (int i = 0; i != WorkingKey.length; i++)
-            {
-                Arrays.fill(WorkingKey[i], 0);
-            }
-        }
     }
 }
--- a/src/main/java/org/bouncycastle/crypto/fips/DesEdeEngine.java
+++ b/src/main/java/org/bouncycastle/crypto/fips/DesEdeEngine.java
@@ -65,6 +65,10 @@
 
         this.forEncryption = encrypting;
 
+        int[] oldKey1 = workingKey1;
+        int[] oldKey2 = workingKey2;
+        int[] oldKey3 = workingKey3;
+
         byte[] key1 = new byte[8];
         System.arraycopy(keyMaster, 0, key1, 0, key1.length);
         workingKey1 = generateWorkingKey(encrypting, key1);
@@ -84,6 +88,21 @@
             workingKey3 = workingKey1;
         }
 
+        // deterministically zeroize any previous working keys on re-key
+        // (workingKey3 aliases workingKey1 for 16-byte keys, so guard against a double wipe)
+        if (oldKey1 != null)
+        {
+            Arrays.fill(oldKey1, 0);
+        }
+        if (oldKey2 != null)
+        {
+            Arrays.fill(oldKey2, 0);
+        }
+        if (oldKey3 != null && oldKey3 != oldKey1)
+        {
+            Arrays.fill(oldKey3, 0);
+        }
+
         // these shouldn't be changed by reset as they are properties of the key.
         blockCount = 0;
         firstBlock = true;
@@ -158,25 +177,5 @@
 
     public void reset()
     {
-    }
-
-    @Override
-    protected void finalize()
-        throws Throwable
-    {
-        super.finalize();
-
-        if (workingKey1 != null)
-        {
-            Arrays.fill(workingKey1, 0);
-        }
-        if (workingKey2 != null)
-        {
-            Arrays.fill(workingKey2, 0);
-        }
-        if (workingKey1 != workingKey3 && workingKey3 != null)
-        {
-            Arrays.fill(workingKey3, 0);
-        }
     }
 }
--- a/src/main/java/org/bouncycastle/crypto/fips/CTRSP800DRBG.java
+++ b/src/main/java/org/bouncycastle/crypto/fips/CTRSP800DRBG.java
@@ -539,15 +539,6 @@
         return entropy;
     }
 
-    @Override
-    protected void finalize() throws Throwable
-    {
-        super.finalize();
-
-        Arrays.fill(_Key, (byte)0);
-        Arrays.fill(_V, (byte)0);
-    }
-
     public VariantInternalKatTest createSelfTest(FipsAlgorithm algorithm)
     {
         return new VariantInternalKatTest(algorithm)
--- a/src/main/java/org/bouncycastle/crypto/fips/HMacSP800DRBG.java
+++ b/src/main/java/org/bouncycastle/crypto/fips/HMacSP800DRBG.java
@@ -314,15 +314,6 @@
         return entropy;
     }
 
-    @Override
-    protected void finalize() throws Throwable
-    {
-        super.finalize();
-
-        Arrays.fill(_K, (byte)0);
-        Arrays.fill(_V, (byte)0);
-    }
-
     public VariantInternalKatTest createSelfTest(FipsAlgorithm algorithm)
     {
         return new VariantInternalKatTest(algorithm)
--- a/src/main/java/org/bouncycastle/crypto/fips/HashSP800DRBG.java
+++ b/src/main/java/org/bouncycastle/crypto/fips/HashSP800DRBG.java
@@ -318,9 +318,22 @@
 
     private void reseedFromSeedMaterial(byte[] seedMaterial)
     {
+        byte[] oldV = _V;
+        byte[] oldC = _C;
+
         _V = hashSeedMaterial(seedMaterial);
         _C = hashSeedMaterial(Arrays.concatenate(ZERO, _V));
         _reseedCounter = 1;
+
+        // erase the previous working state now that the new V/C are in place.
+        if (oldV != null)
+        {
+            Arrays.fill(oldV, (byte)0);
+        }
+        if (oldC != null)
+        {
+            Arrays.fill(oldC, (byte)0);
+        }
     }
 
     private byte[] hashSeedMaterial(byte[] seedMaterial)
@@ -345,15 +358,6 @@
         return entropy;
     }
 
-    @Override
-    protected void finalize() throws Throwable
-    {
-        super.finalize();
-
-        Arrays.fill(_V, (byte)0);
-        Arrays.fill(_C, (byte)0);
-    }
-
     public VariantInternalKatTest createSelfTest(FipsAlgorithm algorithm)
     {
         return new VariantInternalKatTest(algorithm)
@@ -362,8 +366,11 @@
             void evaluate()
                 throws Exception
             {
-                byte[] origV = _V;
-                byte[] origC = _C;
+                // clone (not alias) the live state: reseedFromSeedMaterial now erases the old V/C
+                // arrays, so the restore below must hold independent copies, not references to the
+                // very arrays the KATs will wipe.
+                byte[] origV = Arrays.clone(_V);
+                byte[] origC = Arrays.clone(_C);
                 long origReseedCounter = _reseedCounter;
                 EntropySource origEntropySource = _entropySource;
                 int origSeedLength = _seedLength;
@@ -475,8 +482,11 @@
             void evaluate()
                 throws Exception
             {
-                byte[] origV = _V;
-                byte[] origC = _C;
+                // clone (not alias) the live state: reseedFromSeedMaterial now erases the old V/C
+                // arrays, so the restore below must hold independent copies, not references to the
+                // very arrays the KATs will wipe.
+                byte[] origV = Arrays.clone(_V);
+                byte[] origC = Arrays.clone(_C);
                 long origReseedCounter = _reseedCounter;
                 EntropySource origEntropySource = _entropySource;
                 int origSeedLength = _seedLength;
@@ -490,7 +500,12 @@
 
                     byte[][] expected = reseedKats.get(algorithm.getName());
 
+                    byte[] displacedV = _V;
                     _V = Arrays.clone(reseedVs.get(algorithm.getName()));
+                    if (displacedV != null)
+                    {
+                        Arrays.fill(displacedV, (byte)0);
+                    }
 
                     _entropySource = new DRBGUtils.KATEntropyProvider().get(entropyStrength);
 
--- a/src/main/java/org/bouncycastle/crypto/fips/FipsPBKD.java
+++ b/src/main/java/org/bouncycastle/crypto/fips/FipsPBKD.java
@@ -1,5 +1,9 @@
 package org.bouncycastle.crypto.fips;
 
+import java.util.concurrent.atomic.AtomicBoolean;
+
+import javax.security.auth.Destroyable;
+
 import org.bouncycastle.crypto.CryptoServicesRegistrar;
 import org.bouncycastle.crypto.PasswordBasedDeriver;
 import org.bouncycastle.crypto.PasswordConverter;
@@ -62,6 +66,7 @@
      */
     public static final class Parameters
         extends FipsParameters
+        implements Destroyable
     {
         private final FipsDigestAlgorithm digestAlgorithm;
         private final PasswordConverter converter;
@@ -70,6 +75,8 @@
 
         private byte[] password;
 
+        private final AtomicBoolean hasBeenDestroyed = new AtomicBoolean(false);
+
         private Parameters(FipsDigestAlgorithm digestAlgorithm, PasswordConverter converter, byte[] password, int iterationCount, byte[] salt)
         {
             super(ALGORITHM_PBKDF2);
@@ -132,14 +139,25 @@
             return converter;
         }
 
-        protected void finalize()
+        /**
+         * Explicitly zeroize the retained password. After this call the parameters
+         * can no longer produce a correct derived key.
+         */
+        public void destroy()
         {
-            synchronized (this)
+            if (!hasBeenDestroyed.getAndSet(true))
             {
-                // explicitly zeroize password on deallocation
-                Arrays.fill(password, (byte)0);
-                password = null;
+                synchronized (this)
+                {
+                    // explicitly zeroize password
+                    Arrays.fill(password, (byte)0);
+                }
             }
+        }
+
+        public boolean isDestroyed()
+        {
+            return hasBeenDestroyed.get();
         }
     }
 
--- a/src/main/java/org/bouncycastle/crypto/general/PBKD.java
+++ b/src/main/java/org/bouncycastle/crypto/general/PBKD.java
@@ -2,7 +2,10 @@
 
 import java.util.HashMap;
 import java.util.Map;
+import java.util.concurrent.atomic.AtomicBoolean;
 
+import javax.security.auth.Destroyable;
+
 import org.bouncycastle.crypto.Algorithm;
 import org.bouncycastle.crypto.DigestAlgorithm;
 import org.bouncycastle.crypto.PasswordBasedDeriver;
@@ -120,6 +123,7 @@
      */
     public static final class Parameters
         extends GeneralParameters<Algorithm>
+        implements Destroyable
     {
         private final DigestAlgorithm digestAlgorithm;
         private final PasswordConverter converter;
@@ -128,6 +132,8 @@
         private final byte[] salt;
         private final int iterationCount;
 
+        private final AtomicBoolean hasBeenDestroyed = new AtomicBoolean(false);
+
         private Parameters(GeneralAlgorithm algorithm, DigestAlgorithm digestAlgorithm, PasswordConverter converter, byte[] password, int iterationCount, byte[] salt)
         {
             super(algorithm);
@@ -178,14 +184,21 @@
             return converter;
         }
 
-        @Override
-        protected void finalize()
-            throws Throwable
+        /**
+         * Explicitly zeroize the retained password. After this call the parameters
+         * can no longer produce a correct derived key.
+         */
+        public void destroy()
         {
-            super.finalize();
+            if (!hasBeenDestroyed.getAndSet(true))
+            {
+                Arrays.fill(password, (byte)0);
+            }
+        }
 
-            // explicitly zeroize password on deallocation
-            Arrays.fill(password, (byte)0);
+        public boolean isDestroyed()
+        {
+            return hasBeenDestroyed.get();
         }
     }
 

--- a/src/main/java/org/bouncycastle/asn1/ASN1InputStream.java
+++ b/src/main/java/org/bouncycastle/asn1/ASN1InputStream.java
@@ -21,6 +21,7 @@
     extends FilterInputStream
     implements BERTags
 {
+    private final int depth;
     private final int limit;
     private final boolean lazyEvaluate;
 
@@ -98,12 +99,43 @@
         int         limit,
         boolean     lazyEvaluate)
     {
+        this(input, StreamUtil.findDepth(), limit, lazyEvaluate);
+    }
+
+    /**
+     * Continue a lazily-deferred parse at a specific remaining nesting depth, so that forcing a
+     * LazyEncodedSequence keeps decrementing the nested-construction guard instead of resetting it
+     * to the configured maximum. Without this a deeply nested SEQUENCE captured lazily would parse
+     * one level per force() with the depth reset each time, defeating the depth limit and allowing
+     * a StackOverflowError on a whole-tree walk of crafted input.
+     */
+    ASN1InputStream(
+        byte[]  input,
+        boolean lazyEvaluate,
+        int     depth)
+    {
+        this(new ByteArrayInputStream(input), depth, input.length, lazyEvaluate);
+    }
+
+    private ASN1InputStream(
+        InputStream input,
+        int         depth,
+        int         limit,
+        boolean     lazyEvaluate)
+    {
         super(input);
+        this.depth = depth;
         this.limit = limit;
         this.lazyEvaluate = lazyEvaluate;
         this.tmpBuffers = new byte[11][];
     }
 
+    private ASN1InputStream createSubStream(InputStream sub)
+        throws IOException
+    {
+        return new ASN1InputStream(sub, StreamUtil.decrementDepth(depth), StreamUtil.findLimit(sub), lazyEvaluate);
+    }
+
     int getLimit()
     {
         return limit;
@@ -151,7 +183,7 @@
 
         if ((tag & TAGGED) != 0)
         {
-            return new ASN1StreamParser(defIn).readTaggedObject(isConstructed, tagNo);
+            return ASN1StreamParser.createSubParser(defIn, depth, limit).readTaggedObject(isConstructed, tagNo);
         }
 
         if (isConstructed)
@@ -175,7 +207,7 @@
                 case SEQUENCE:
                     if (lazyEvaluate)
                     {
-                        return new LazyEncodedSequence(defIn.toByteArray());
+                        return new LazyEncodedSequence(defIn.toByteArray(), StreamUtil.decrementDepth(depth));
                     }
                     else
                     {
@@ -210,7 +242,7 @@
     ASN1EncodableVector buildDEREncodableVector(
         DefiniteLengthInputStream dIn) throws IOException
     {
-        return new ASN1InputStream(dIn).buildEncodableVector();
+        return createSubStream(dIn).buildEncodableVector();
     }
 
     public ASN1Primitive readObject()
@@ -247,7 +279,7 @@
             }
 
             IndefiniteLengthInputStream indIn = new IndefiniteLengthInputStream(this, limit);
-            ASN1StreamParser sp = new ASN1StreamParser(indIn, limit);
+            ASN1StreamParser sp = ASN1StreamParser.createSubParser(indIn, depth, limit);
 
             if ((tag & APPLICATION) != 0)
             {
--- a/src/main/java/org/bouncycastle/asn1/ASN1StreamParser.java
+++ b/src/main/java/org/bouncycastle/asn1/ASN1StreamParser.java
@@ -12,7 +12,18 @@
  */
 public class ASN1StreamParser
 {
+    /**
+     * Create a parser for the contents of a constructed object one level deeper than
+     * {@code parentDepth}, decrementing (and bounds-checking) the nested-construction budget.
+     */
+    static ASN1StreamParser createSubParser(InputStream in, int parentDepth, int limit)
+        throws IOException
+    {
+        return new ASN1StreamParser(in, StreamUtil.decrementDepth(parentDepth), limit);
+    }
+
     private final InputStream _in;
+    private final int         _depth;
     private final int         _limit;
     private final byte[][] tmpBuffers;
 
@@ -26,7 +37,16 @@
         InputStream in,
         int         limit)
     {
+        this(in, StreamUtil.findDepth(), limit);
+    }
+
+    private ASN1StreamParser(
+        InputStream in,
+        int         depth,
+        int         limit)
+    {
         this._in = in;
+        this._depth = depth;
         this._limit = limit;
 
         this.tmpBuffers = new byte[11][];
@@ -38,6 +58,12 @@
         this(new ByteArrayInputStream(encoding), encoding.length);
     }
 
+    private ASN1StreamParser createSubParser(InputStream in)
+        throws IOException
+    {
+        return new ASN1StreamParser(in, StreamUtil.decrementDepth(_depth), StreamUtil.findLimit(in));
+    }
+
     ASN1Encodable readIndef(int tagValue) throws IOException
     {
         // Note: INDEF => CONSTRUCTED
@@ -155,7 +181,7 @@
             }
 
             IndefiniteLengthInputStream indIn = new IndefiniteLengthInputStream(_in, _limit);
-            ASN1StreamParser sp = new ASN1StreamParser(indIn, _limit);
+            ASN1StreamParser sp = createSubParser(indIn, _depth, _limit);
 
             if ((tag & BERTags.APPLICATION) != 0)
             {
@@ -180,7 +206,7 @@
 
             if ((tag & BERTags.TAGGED) != 0)
             {
-                return new BERTaggedObjectParser(isConstructed, tagNo, new ASN1StreamParser(defIn));
+                return new BERTaggedObjectParser(isConstructed, tagNo, createSubParser(defIn));
             }
 
             if (isConstructed)
@@ -192,13 +218,13 @@
                         //
                         // yes, people actually do this...
                         //
-                        return new BEROctetStringParser(new ASN1StreamParser(defIn));
+                        return new BEROctetStringParser(createSubParser(defIn));
                     case BERTags.SEQUENCE:
-                        return new DERSequenceParser(new ASN1StreamParser(defIn));
+                        return new DERSequenceParser(createSubParser(defIn));
                     case BERTags.SET:
-                        return new DERSetParser(new ASN1StreamParser(defIn));
+                        return new DERSetParser(createSubParser(defIn));
                     case BERTags.EXTERNAL:
-                        return new DERExternalParser(new ASN1StreamParser(defIn));
+                        return new DERExternalParser(createSubParser(defIn));
                     default:
                         throw new IOException("unknown tag " + tagNo + " encountered");
                 }
--- a/src/main/java/org/bouncycastle/asn1/LazyConstructionEnumeration.java
+++ b/src/main/java/org/bouncycastle/asn1/LazyConstructionEnumeration.java
@@ -12,9 +12,9 @@
     private ASN1InputStream aIn;
     private Object          nextObj;
 
-    public LazyConstructionEnumeration(byte[] encoded)
+    public LazyConstructionEnumeration(byte[] encoded, int depth)
     {
-        aIn = new ASN1InputStream(encoded, true);
+        aIn = new ASN1InputStream(encoded, true, depth);
         nextObj = readObject();
     }
 
--- a/src/main/java/org/bouncycastle/asn1/LazyEncodedSequence.java
+++ b/src/main/java/org/bouncycastle/asn1/LazyEncodedSequence.java
@@ -13,17 +13,20 @@
     extends ASN1Sequence
 {
     private byte[] encoded;
+    private final int contentsDepth;
 
     LazyEncodedSequence(
-        byte[] encoded)
+        byte[] encoded,
+        int contentsDepth)
         throws IOException
     {
         this.encoded = encoded;
+        this.contentsDepth = contentsDepth;
     }
 
     private void parse()
     {
-        Enumeration en = new LazyConstructionEnumeration(encoded);
+        Enumeration en = new LazyConstructionEnumeration(encoded, contentsDepth);
 
         while (en.hasMoreElements())
         {
@@ -50,7 +53,7 @@
             return super.getObjects();
         }
 
-        return new LazyConstructionEnumeration(encoded);
+        return new LazyConstructionEnumeration(encoded, contentsDepth);
     }
 
     public synchronized int size()
--- a/src/main/java/org/bouncycastle/asn1/StreamUtil.java
+++ b/src/main/java/org/bouncycastle/asn1/StreamUtil.java
@@ -9,11 +9,39 @@
 import java.io.InputStream;
 import java.nio.channels.FileChannel;
 
+import org.bouncycastle.util.Properties;
+
 class StreamUtil
 {
     private static final long  MAX_MEMORY = Runtime.getRuntime().maxMemory();
 
+    private static final String MAX_CONS_DEPTH = "org.bouncycastle.asn1.max_cons_depth";
+    private static final String MAX_LIMIT = "org.bouncycastle.asn1.max_limit";
+
     /**
+     * Decrement the remaining nested-construction budget, throwing if it would go
+     * below zero. Guards against a StackOverflowError on deeply nested constructed
+     * encodings of crafted input.
+     */
+    static int decrementDepth(int parentDepth) throws IOException
+    {
+        if (parentDepth <= 0)
+        {
+            throw new ASN1Exception("maximum nested construction level reached");
+        }
+        return parentDepth - 1;
+    }
+
+    /**
+     * Find the maximum permitted nested-construction depth. Defaults to 64 and can be
+     * overridden via the {@code org.bouncycastle.asn1.max_cons_depth} property.
+     */
+    static int findDepth()
+    {
+        return Math.max(0, Properties.asInteger(MAX_CONS_DEPTH, 64));
+    }
+
+    /**
      * Find out possible longest length...
      *
      * @param in input stream of interest
@@ -48,6 +76,22 @@
             catch (IOException e)
             {
                 // ignore - they'll find out soon enough!
+            }
+        }
+
+        String limit = Properties.getPropertyValue(MAX_LIMIT);
+        if (limit != null)
+        {
+            switch (limit.charAt(limit.length() - 1))
+            {
+            case 'k':
+                return Integer.parseInt(limit.substring(0, limit.length() - 1)) * 1024;
+            case 'm':
+                return Integer.parseInt(limit.substring(0, limit.length() - 1)) * 1024 * 1024;
+            case 'g':
+                return Integer.parseInt(limit.substring(0, limit.length() - 1)) * 1024 * 1024 * 1024;
+            default:
+                return Integer.parseInt(limit);
             }
         }
 
--- a/src/main/java/org/bouncycastle/util/Properties.java
+++ b/src/main/java/org/bouncycastle/util/Properties.java
@@ -49,4 +49,16 @@
             }
         });
     }
+
+    public static int asInteger(String propertyName, int defaultValue)
+    {
+        String p = getPropertyValue(propertyName);
+
+        if (p != null)
+        {
+            return Integer.parseInt(p);
+        }
+
+        return defaultValue;
+    }
 }

--- a/src/main/java/org/bouncycastle/asn1/x509/PKIXNameConstraintValidator.java
+++ b/src/main/java/org/bouncycastle/asn1/x509/PKIXNameConstraintValidator.java
@@ -1031,11 +1031,15 @@
 
     private boolean emailIsConstrained(String email, String constraint)
     {
-        String sub = email.substring(email.indexOf('@') + 1);
+        // RFC 1034 root-label trailing dot: the dNSName path already ignores a trailing dot
+        // (see withinDomain), so apply the same canonicalisation to the rfc822Name host. Otherwise
+        // a trailing dot (e.g. "ceo@bank.com.") slips a leaf past an excluded/permitted "bank.com"
+        // rfc822Name constraint.
+        String sub = stripTrailingDot(email.substring(email.indexOf('@') + 1));
         // a particular mailbox
         if (constraint.indexOf('@') != -1)
         {
-            if (email.equalsIgnoreCase(constraint))
+            if (stripTrailingDot(email).equalsIgnoreCase(stripTrailingDot(constraint)))
             {
                 return true;
             }
@@ -1043,7 +1047,7 @@
         // on particular host
         else if (!(constraint.charAt(0) == '.'))
         {
-            if (sub.equalsIgnoreCase(constraint))
+            if (sub.equalsIgnoreCase(stripTrailingDot(constraint)))
             {
                 return true;
             }
@@ -1056,6 +1060,17 @@
         return false;
     }
 
+    private static String stripTrailingDot(String s)
+    {
+        // length > 1 so a single bare "." (theoretically the empty-label root) is preserved
+        // rather than reduced to "".
+        if (s != null && s.length() > 1 && s.charAt(s.length() - 1) == '.')
+        {
+            return s.substring(0, s.length() - 1);
+        }
+        return s;
+    }
+
     private boolean withinDomain(String testDomain, String domain)
     {
         String tempDomain = domain;
@@ -1800,11 +1815,13 @@
 
     private boolean isUriConstrained(String uri, String constraint)
     {
-        String host = extractHostFromURL(uri);
+        // Strip an RFC 1034 root-label trailing dot from the host, matching the dNSName path, so a
+        // URI host such as "competitor.example." cannot slip past a "competitor.example" constraint.
+        String host = stripTrailingDot(extractHostFromURL(uri));
         // a host
         if (!constraint.startsWith("."))
         {
-            if (host.equalsIgnoreCase(constraint))
+            if (host.equalsIgnoreCase(stripTrailingDot(constraint)))
             {
                 return true;
             }
