--- a/src/java/org/apache/commons/collections/CollectionUtils.java
+++ b/src/java/org/apache/commons/collections/CollectionUtils.java
@@ -478,13 +478,13 @@
             return ((Object[])obj)[idx];
         } 
         else if(obj instanceof Enumeration) {
-            Enumeration enum = (Enumeration)obj;
-            while(enum.hasMoreElements()) {
+            Enumeration enumVar = (Enumeration)obj;
+            while(enumVar.hasMoreElements()) {
                 idx--;
                 if(idx == -1) {
-                    return enum.nextElement();
+                    return enumVar.nextElement();
                 } else {
-                    enum.nextElement();
+                    enumVar.nextElement();
                 }
             }
         } 
--- a/src/java/org/apache/commons/collections/EnumerationIterator.java
+++ b/src/java/org/apache/commons/collections/EnumerationIterator.java
@@ -86,8 +86,8 @@
         this(enumeration, null);
     }
 
-    public EnumerationIterator( Enumeration enum, Collection collection ) {
-        this.enumeration = enum;
+    public EnumerationIterator( Enumeration enumVar, Collection collection ) {
+        this.enumeration = enumVar;
         this.collection = collection;
         this.last = null;
     }
--- a/src/java/org/apache/commons/collections/FastHashMap.java
+++ b/src/java/org/apache/commons/collections/FastHashMap.java
@@ -63,6 +63,8 @@
 package org.apache.commons.collections;
 
 
+import java.io.IOException;
+import java.io.ObjectInputStream;
 import java.io.Serializable;
 import java.util.Collection;
 import java.util.HashMap;
@@ -521,5 +523,49 @@
 
     }
 
+
+
+    // --------------------------------------------------------- Serialization
+
+
+    /**
+     * List of class names that are known to be unsafe for deserialization
+     * due to potential remote code execution (CVE-2015-6420, COLLECTIONS-580).
+     */
+    private static final String[] UNSAFE_CLASSES = {
+        "org.apache.commons.collections.functors.InvokerTransformer",
+        "org.apache.commons.collections.functors.InstantiateTransformer",
+        "org.apache.commons.collections.functors.CloneTransformer",
+        "org.apache.commons.collections.functors.PrototypeFactory",
+        "org.apache.commons.collections.functors.PrototypeFactory$PrototypeCloneFactory",
+        "org.apache.commons.collections.functors.PrototypeFactory$PrototypeSerializationFactory"
+    };
+
+
+    /**
+     * Deserialization override that validates no unsafe transformer classes
+     * are present in the deserialized map (CVE-2015-6420).
+     */
+    private void readObject(ObjectInputStream in)
+            throws IOException, ClassNotFoundException {
+        in.defaultReadObject();
+        if (map != null) {
+            Iterator it = map.values().iterator();
+            while (it.hasNext()) {
+                Object value = it.next();
+                if (value != null) {
+                    String className = value.getClass().getName();
+                    for (int i = 0; i < UNSAFE_CLASSES.length; i++) {
+                        if (UNSAFE_CLASSES[i].equals(className)) {
+                            throw new UnsupportedOperationException(
+                                "Deserialization of " + className
+                                + " is disabled for security reasons. "
+                                + "See CVE-2015-6420 / COLLECTIONS-580.");
+                        }
+                    }
+                }
+            }
+        }
+    }
 
 }
--- a/src/java/org/apache/commons/collections/FastTreeMap.java
+++ b/src/java/org/apache/commons/collections/FastTreeMap.java
@@ -63,6 +63,8 @@
 package org.apache.commons.collections;
 
 
+import java.io.IOException;
+import java.io.ObjectInputStream;
 import java.io.Serializable;
 import java.util.Collection;
 import java.util.Comparator;
@@ -631,5 +633,49 @@
 
     }
 
+
+
+    // --------------------------------------------------------- Serialization
+
+
+    /**
+     * List of class names that are known to be unsafe for deserialization
+     * due to potential remote code execution (CVE-2015-6420, COLLECTIONS-580).
+     */
+    private static final String[] UNSAFE_CLASSES = {
+        "org.apache.commons.collections.functors.InvokerTransformer",
+        "org.apache.commons.collections.functors.InstantiateTransformer",
+        "org.apache.commons.collections.functors.CloneTransformer",
+        "org.apache.commons.collections.functors.PrototypeFactory",
+        "org.apache.commons.collections.functors.PrototypeFactory$PrototypeCloneFactory",
+        "org.apache.commons.collections.functors.PrototypeFactory$PrototypeSerializationFactory"
+    };
+
+
+    /**
+     * Deserialization override that validates no unsafe transformer classes
+     * are present in the deserialized map (CVE-2015-6420).
+     */
+    private void readObject(ObjectInputStream in)
+            throws IOException, ClassNotFoundException {
+        in.defaultReadObject();
+        if (map != null) {
+            Iterator it = map.values().iterator();
+            while (it.hasNext()) {
+                Object value = it.next();
+                if (value != null) {
+                    String className = value.getClass().getName();
+                    for (int i = 0; i < UNSAFE_CLASSES.length; i++) {
+                        if (UNSAFE_CLASSES[i].equals(className)) {
+                            throw new UnsupportedOperationException(
+                                "Deserialization of " + className
+                                + " is disabled for security reasons. "
+                                + "See CVE-2015-6420 / COLLECTIONS-580.");
+                        }
+                    }
+                }
+            }
+        }
+    }
 
 }

diff --git a/src/java/org/apache/commons/collections/LRUMap.java b/src/java/org/apache/commons/collections/LRUMap.java
index efe2486..0b011c8 100644
--- a/src/java/org/apache/commons/collections/LRUMap.java
+++ b/src/java/org/apache/commons/collections/LRUMap.java
@@ -179,6 +179,21 @@ public class LRUMap extends SequencedHashMap implements Externalizable {
     // Externalizable interface
     //-------------------------------------------------------------------------        
     public void readExternal( ObjectInput in )  throws IOException, ClassNotFoundException {
+        // CVE-2015-7501 hardening: disable deserialization by default to prevent
+        // this class from being used as part of a deserialization gadget chain.
+        String deserializeEnabled;
+        try {
+            deserializeEnabled = System.getProperty(
+                SequencedHashMap.DESERIALIZE_PROPERTY);
+        } catch (SecurityException ex) {
+            deserializeEnabled = null;
+        }
+        if (deserializeEnabled == null || !deserializeEnabled.equalsIgnoreCase("true")) {
+            throw new UnsupportedOperationException(
+                "Deserialization of LRUMap is disabled for security reasons " +
+                "(CVE-2015-7501). Set system property '" +
+                SequencedHashMap.DESERIALIZE_PROPERTY + "' to 'true' to override.");
+        }
         maximumSize = in.readInt();
         int size = in.readInt();
         
diff --git a/src/java/org/apache/commons/collections/SequencedHashMap.java b/src/java/org/apache/commons/collections/SequencedHashMap.java
index b515a4f..597e79a 100644
--- a/src/java/org/apache/commons/collections/SequencedHashMap.java
+++ b/src/java/org/apache/commons/collections/SequencedHashMap.java
@@ -98,6 +98,12 @@ import java.util.NoSuchElementException;
  */
 public class SequencedHashMap implements Map, Cloneable, Externalizable {
 
+
+  /**
+   *  System property key to enable deserialization (CVE-2015-7501 hardening).
+   */
+  public static final String DESERIALIZE_PROPERTY =
+      "org.apache.commons.collections.enableDeserialization";
   /**
    *  {@link java.util.Map.Entry} that doubles as a node in the linked list
    *  of sequenced mappings.  
@@ -888,6 +894,20 @@ public class SequencedHashMap implements Map, Cloneable, Externalizable {
   public void readExternal( ObjectInput in ) 
     throws IOException, ClassNotFoundException 
   {
+    // CVE-2015-7501 hardening: disable deserialization by default to prevent
+    // this class from being used as part of a deserialization gadget chain.
+    String deserializeEnabled;
+    try {
+      deserializeEnabled = System.getProperty(DESERIALIZE_PROPERTY);
+    } catch (SecurityException ex) {
+      deserializeEnabled = null;
+    }
+    if (deserializeEnabled == null || !deserializeEnabled.equalsIgnoreCase("true")) {
+      throw new UnsupportedOperationException(
+          "Deserialization of SequencedHashMap is disabled for security reasons " +
+          "(CVE-2015-7501). Set system property '" + DESERIALIZE_PROPERTY +
+          "' to 'true' to override.");
+    }
     int size = in.readInt();    
     for(int i = 0; i < size; i++)  {
       Object key = in.readObject();
diff --git a/pom.xml b/pom.xml
new file mode 100644
index 0000000..c4fc64f
--- /dev/null
+++ b/pom.xml
@@ -0,0 +1,40 @@
+<?xml version="1.0" encoding="UTF-8"?>
+<project xmlns="http://maven.apache.org/POM/4.0.0"
+         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
+         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
+    <modelVersion>4.0.0</modelVersion>
+
+    <groupId>commons-collections</groupId>
+    <artifactId>commons-collections</artifactId>
+    <version>2.0-root.io.1</version>
+    <packaging>jar</packaging>
+
+    <name>Commons Collections</name>
+    <description>Jakarta Commons Collections Package</description>
+
+    <properties>
+        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
+        <maven.compiler.source>1.4</maven.compiler.source>
+        <maven.compiler.target>1.4</maven.compiler.target>
+    </properties>
+
+    <build>
+        <sourceDirectory>src/java</sourceDirectory>
+        <testSourceDirectory>src/test</testSourceDirectory>
+        <plugins>
+            <plugin>
+                <groupId>org.apache.maven.plugins</groupId>
+                <artifactId>maven-compiler-plugin</artifactId>
+                <version>3.11.0</version>
+                <configuration>
+                    <source>1.4</source>
+                    <target>1.4</target>
+                    <excludes>
+                        <exclude>**/MultiMap.java</exclude>
+                        <exclude>**/MultiHashMap.java</exclude>
+                    </excludes>
+                </configuration>
+            </plugin>
+        </plugins>
+    </build>
+</project>

diff --git a/build.xml b/build.xml
index 9f56d4f..db7859f 100644
--- a/build.xml
+++ b/build.xml
@@ -49,6 +49,7 @@
       <property name="Name-Long" value="Jakarta Commons Collections Package"/>
 
       <!-- The current version number of this component -->
+      <property name="version"                 value="2.0"/>
       <property name="component.version"       value="2.0"/>
       <property name="doc.release.notes" value="RELEASE-NOTES-${component.version}.html"/>
       <property name="doc.status" value="STATUS.html"/>
@@ -258,6 +259,9 @@
       <copy todir="${workdir}/META-INF" file="./LICENSE"/>
       
       <jar jarfile="${dest.jardir.jar}" manifest="${source.src}/conf/MANIFEST.MF">
+         <manifest>
+            <attribute name="Implementation-Version" value="${version}"/>
+         </manifest>
          <fileset dir="${workdir}"/>
       </jar>
       <delete dir="${workdir}"/>
diff --git a/src/conf/MANIFEST.MF b/src/conf/MANIFEST.MF
index 79e7995..02fae68 100644
--- a/src/conf/MANIFEST.MF
+++ b/src/conf/MANIFEST.MF
@@ -2,5 +2,4 @@ Extension-Name: org.apache.commons.collections
 Specification-Vendor: Apache Software Foundation
 Specification-Version: 1.0
 Implementation-Vendor: Apache Software Foundation
-Implementation-Version: 1.1-dev
 
diff --git a/src/java/org/apache/commons/collections/MultiHashMap.java b/src/java/org/apache/commons/collections/MultiHashMap.java
index 4a9bf15..11b39ae 100644
--- a/src/java/org/apache/commons/collections/MultiHashMap.java
+++ b/src/java/org/apache/commons/collections/MultiHashMap.java
@@ -146,7 +146,7 @@ public class MultiHashMap extends HashMap implements MultiMap
         return false;
     }
     
-    public Object remove( Object key, Object item )
+    public Object removeItem( Object key, Object item )
     {
         ArrayList valuesForKey = (ArrayList) super.get( key );
         
diff --git a/src/java/org/apache/commons/collections/MultiMap.java b/src/java/org/apache/commons/collections/MultiMap.java
index f2891fc..a08bbf4 100644
--- a/src/java/org/apache/commons/collections/MultiMap.java
+++ b/src/java/org/apache/commons/collections/MultiMap.java
@@ -75,6 +75,6 @@ import java.util.Map;
  */
 public interface MultiMap extends Map {
     
-    public Object remove( Object key, Object item );
+    public Object removeItem( Object key, Object item );
    
 }
