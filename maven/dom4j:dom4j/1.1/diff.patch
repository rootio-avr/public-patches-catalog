--- a/src/java/org/dom4j/Namespace.java
+++ b/src/java/org/dom4j/Namespace.java
@@ -66,6 +66,10 @@ public class Namespace extends AbstractNode {
     public Namespace(String prefix, String uri) {
         this.prefix = (prefix != null) ? prefix : "";
         this.uri = (uri != null) ? uri : "";;
+
+        if (this.prefix.length() > 0) {
+            QName.validateNCName(this.prefix);
+        }
     }
 
     
--- a/src/java/org/dom4j/QName.java
+++ b/src/java/org/dom4j/QName.java
@@ -12,6 +12,7 @@ package org.dom4j;
 import java.io.IOException;
 import java.io.Serializable;
 import java.io.ObjectInputStream;
+import java.util.regex.Pattern;
 import java.io.ObjectOutputStream;
 
 import org.dom4j.tree.QNameCache;
@@ -26,6 +27,42 @@ import org.dom4j.tree.QNameCache;
 public class QName implements Serializable {
 
     protected transient static QNameCache cache = new QNameCache();
+
+    /**
+     * {@code NameStartChar} without colon.
+     * @see <a href="https://www.w3.org/TR/xml/#sec-common-syn">XML 1.0</a>
+     */
+    private static final String NAME_START_CHAR = "_A-Za-z\u00C0-\u00D6\u00D8-\u00F6\u00F8-\u02FF\u0370-\u037D\u037F-\u1FFF\u200C-\u200D\u2070-\u218F\u2C00-\u2FEF\u3001-\uD7FF\uF900-\uFDCF\uFDF0-\uFFFD";
+
+    /**
+     * {@code NameChar} without colon.
+     * @see <a href="https://www.w3.org/TR/xml/#sec-common-syn">XML 1.0</a>
+     */
+    private static final String NAME_CHAR = NAME_START_CHAR + "-.0-9\u00B7\u0300-\u036F\u203F-\u2040";
+
+    /**
+     * {@code NCName}
+     * @see <a href="https://www.w3.org/TR/xml-names/#ns-qualnames">Namespaces in XML 1.0</a>
+     */
+    private static final String NCNAME = "["+NAME_START_CHAR+"]["+NAME_CHAR+"]*";
+
+    /**
+     * Regular expression for {@code Name} (with colon).
+     * @see <a href="https://www.w3.org/TR/xml/#sec-common-syn">XML 1.0</a>
+     */
+    private static final Pattern RE_NAME = Pattern.compile("[:"+NAME_START_CHAR+"][:"+NAME_CHAR+"]*");
+
+    /**
+     * Regular expression for {@code NCName}.
+     * @see <a href="https://www.w3.org/TR/xml-names/#ns-qualnames">Namespaces in XML 1.0</a>
+     */
+    private static final Pattern RE_NCNAME = Pattern.compile(NCNAME);
+
+    /**
+     * Regular expression for {@code QName}.
+     * @see <a href="https://www.w3.org/TR/xml-names/#ns-qualnames">Namespaces in XML 1.0</a>
+     */
+    private static final Pattern RE_QNAME = Pattern.compile("(?:"+NCNAME+":)?"+NCNAME);
     
     
     /** The local name of the element or attribute */
@@ -71,12 +108,19 @@ public class QName implements Serializable {
     public QName(String name, Namespace namespace) {
         this.name = (name == null) ? "" : name;
         this.namespace = (namespace == null) ? Namespace.NO_NAMESPACE : namespace;
+        if (this.namespace.equals(Namespace.NO_NAMESPACE)) {
+            validateName(this.name);
+        } else {
+            validateNCName(this.name);
+        }
     }
 
     public QName(String name, Namespace namespace, String qualifiedName) {
         this.name = (name == null) ? "" : name;
         this.qualifiedName = qualifiedName;
         this.namespace = (namespace == null) ? Namespace.NO_NAMESPACE : namespace;
+        validateNCName(this.name);
+        validateQName(this.qualifiedName);
     }
 
     
@@ -189,6 +233,27 @@ public class QName implements Serializable {
         namespace = Namespace.get( prefix, uri );
     }
 
+    private static void validateName(String name) {
+        if (!RE_NAME.matcher(name).matches()) {
+            throw new IllegalArgumentException(
+                "Illegal character in name: '" + name + "'.");
+        }
+    }
+
+    protected static void validateNCName(String ncname) {
+        if (!RE_NCNAME.matcher(ncname).matches()) {
+            throw new IllegalArgumentException(
+                "Illegal character in local name: '" + ncname + "'.");
+        }
+    }
+
+    private static void validateQName(String qname) {
+        if (!RE_QNAME.matcher(qname).matches()) {
+            throw new IllegalArgumentException(
+                "Illegal character in qualified name: '" + qname + "'.");
+        }
+    }
+
 
 }
 
--- a/src/java/org/dom4j/tree/QNameCache.java
+++ b/src/java/org/dom4j/tree/QNameCache.java
@@ -105,6 +105,9 @@ public class QNameCache {
         if ( index < 0 ) {
             return get( qualifiedName, Namespace.get( uri ) );
         }
+        else if ( index == 0 ) {
+            throw new IllegalArgumentException("Qualified name cannot start with ':'.");
+        }
         else {
             String name = qualifiedName.substring( index + 1 );
             String prefix = qualifiedName.substring( 0, index );

diff --git a/src/java/org/dom4j/DocumentHelper.java b/src/java/org/dom4j/DocumentHelper.java
--- a/src/java/org/dom4j/DocumentHelper.java
+++ b/src/java/org/dom4j/DocumentHelper.java
@@ -211,7 +211,7 @@ public class DocumentHelper {
       * @throws DocumentException if the document could not be parsed
       */
     public static Document parseText(String text) throws DocumentException {
-        SAXReader reader = new SAXReader();
+        SAXReader reader = SAXReader.createDefault();
         return reader.read( new StringReader( text ) );
     }
 
diff --git a/src/java/org/dom4j/io/SAXReader.java b/src/java/org/dom4j/io/SAXReader.java
--- a/src/java/org/dom4j/io/SAXReader.java
+++ b/src/java/org/dom4j/io/SAXReader.java
@@ -113,7 +113,25 @@ public class SAXReader {
     /** The SAX filter used to filter SAX events */
     private XMLFilter xmlFilter;
     
-    
+
+    /** Creates a SAXReader with secure default settings.
+      * External DTD loading, external general entities, and external parameter
+      * entities are disabled to prevent XXE attacks.
+      *
+      * @return a new SAXReader with secure defaults
+      */
+    public static SAXReader createDefault() {
+        SAXReader reader = new SAXReader();
+        try {
+            reader.setFeature("http://apache.org/xml/features/nonvalidating/load-external-dtd", false);
+            reader.setFeature("http://xml.org/sax/features/external-general-entities", false);
+            reader.setFeature("http://xml.org/sax/features/external-parameter-entities", false);
+        }
+        catch (SAXException e) {
+            // nothing to do, incompatible reader
+        }
+        return reader;
+    }
     public SAXReader() {
     }
 

--- a/build.xml
+++ b/build.xml
@@ -159,7 +159,13 @@
            destdir="${build.dest}"
            debug="${debug}"
            optimize="${optimize}"
-           deprecation="${deprecation}"/>
+           deprecation="${deprecation}"
+           source="1.6"
+           target="1.6">
+      <classpath>
+        <fileset dir="${lib.dir}" includes="*.jar"/>
+      </classpath>
+    </javac>
   </target>
 
   <!-- =================================================================== -->
@@ -202,18 +208,28 @@
 
     <jar jarfile="${build.dir}/${name}-core.jar"
          basedir="${build.dest}"
-         includes="org/dom4j/**"/>
+         includes="org/dom4j/**">
+      <manifest>
+        <attribute name="Implementation-Version" value="${version}"/>
+      </manifest>
+    </jar>
 
     <jar jarfile="${build.dir}/${name}.jar"
          basedir="${build.dest}"
          includes="org/dom4j/**,org/saxpath/**,org/jaxen/**,com/werken/**"
-         excludes="org/jaxen/dom/**,org/jaxen/exml/**,org/jaxen/jdom/**"
-         manifest="${conf.dir}/MANIFEST.MF"/>
+         excludes="org/jaxen/dom/**,org/jaxen/exml/**,org/jaxen/jdom/**">
+      <manifest>
+        <attribute name="Implementation-Version" value="${version}"/>
+      </manifest>
+    </jar>
 
     <jar jarfile="${build.dir}/${name}-full.jar"
          basedir="${build.dest}"
-         includes="**"
-         manifest="${conf.dir}/MANIFEST.MF"/>
+         includes="**">
+      <manifest>
+        <attribute name="Implementation-Version" value="${version}"/>
+      </manifest>
+    </jar>
   </target>
 
   <!-- =================================================================== -->

--- a/src/java/org/dom4j/DocumentHelper.java
+++ b/src/java/org/dom4j/DocumentHelper.java
@@ -231,7 +231,7 @@ public class DocumentHelper {
       * existed on the path or were created by this method.
       */
     public static Element makeElement(Branch source, String path) {
-        StringTokenizer enum = new StringTokenizer( path, "/" );
+        StringTokenizer tokens = new StringTokenizer( path, "/" );
         Element parent;
         if ( source instanceof Document ) {
             Document document = (Document) source;
@@ -239,7 +239,7 @@ public class DocumentHelper {
             
             // lets throw a NoSuchElementException 
             // if we are given an empty path
-            String name = enum.nextToken();
+            String name = tokens.nextToken();
             if ( parent == null ) {
                 parent = document.addElement( name );
             }
@@ -248,8 +248,8 @@ public class DocumentHelper {
             parent = (Element) source;
         }
         Element element = null;
-        while ( enum.hasMoreTokens() ) {
-            String name = enum.nextToken();
+        while ( tokens.hasMoreTokens() ) {
+            String name = tokens.nextToken();
             if ( name.indexOf( ':' ) > 0 ) {
                 element = parent.element( parent.getQName( name ) );
             }
--- a/src/java/org/dom4j/io/aelfred/XmlParser.java
+++ b/src/java/org/dom4j/io/aelfred/XmlParser.java
@@ -1450,7 +1450,7 @@ loop:
     {
 	String name;
 	int type;
-	String enum = null;
+	String enumVal = null;
 
 	// Read the attribute name.
 	name = readNmtoken (true);
@@ -1462,12 +1462,12 @@ loop:
 	// Get the string of enumerated values
 	// if necessary.
 	if (type == ATTRIBUTE_ENUMERATED || type == ATTRIBUTE_NOTATION) {
-	    enum = dataBufferToString ();
+	    enumVal = dataBufferToString ();
 	}
 
 	// Read the default value.
 	requireWhitespace ();
-	parseDefault (elementName, name, type, enum);
+	parseDefault (elementName, name, type, enumVal);
     }
 
 
@@ -1565,7 +1565,7 @@ loop:
 	String elementName,
 	String name,
 	int type,
-	String enum
+	String enumVal
     ) throws Exception
     {
 	int	valueType = ATTRIBUTE_DEFAULT_SPECIFIED;
@@ -1595,7 +1595,7 @@ loop:
 	    }
 	} else
 	    value = readLiteral (flags);
-	setAttribute (elementName, name, type, enum, value, valueType);
+	setAttribute (elementName, name, type, enumVal, value, valueType);
     }
 
 
