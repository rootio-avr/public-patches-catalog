--- a/controllers/eventsource/resource.go
+++ b/controllers/eventsource/resource.go
@@ -306,7 +306,21 @@
 	}
 	if args.EventSource.Spec.Template != nil && args.EventSource.Spec.Template.Container != nil {
-		if err := mergo.Merge(&eventSourceContainer, args.EventSource.Spec.Template.Container, mergo.WithOverride); err != nil {
-			return nil, err
-		}
+		c := args.EventSource.Spec.Template.Container
+		_ = mergo.Merge(&eventSourceContainer.Resources, c.Resources, mergo.WithOverride)
+		if eventSourceContainer.SecurityContext == nil {
+			eventSourceContainer.SecurityContext = c.SecurityContext
+		}
+		if eventSourceContainer.ImagePullPolicy == "" {
+			eventSourceContainer.ImagePullPolicy = c.ImagePullPolicy
+		}
+		if len(c.Env) > 0 {
+			eventSourceContainer.Env = append(eventSourceContainer.Env, c.Env...)
+		}
+		if len(c.EnvFrom) > 0 {
+			eventSourceContainer.EnvFrom = append(eventSourceContainer.EnvFrom, c.EnvFrom...)
+		}
+		if len(c.VolumeMounts) > 0 {
+			eventSourceContainer.VolumeMounts = append(eventSourceContainer.VolumeMounts, c.VolumeMounts...)
+		}
 	}
 	eventSourceContainer.Name = "main"
--- a/controllers/sensor/resource.go
+++ b/controllers/sensor/resource.go
@@ -267,7 +267,21 @@
 	}
 	if args.Sensor.Spec.Template != nil && args.Sensor.Spec.Template.Container != nil {
-		if err := mergo.Merge(&sensorContainer, args.Sensor.Spec.Template.Container, mergo.WithOverride); err != nil {
-			return nil, err
-		}
+		c := args.Sensor.Spec.Template.Container
+		_ = mergo.Merge(&sensorContainer.Resources, c.Resources, mergo.WithOverride)
+		if sensorContainer.SecurityContext == nil {
+			sensorContainer.SecurityContext = c.SecurityContext
+		}
+		if sensorContainer.ImagePullPolicy == "" {
+			sensorContainer.ImagePullPolicy = c.ImagePullPolicy
+		}
+		if len(c.Env) > 0 {
+			sensorContainer.Env = append(sensorContainer.Env, c.Env...)
+		}
+		if len(c.EnvFrom) > 0 {
+			sensorContainer.EnvFrom = append(sensorContainer.EnvFrom, c.EnvFrom...)
+		}
+		if len(c.VolumeMounts) > 0 {
+			sensorContainer.VolumeMounts = append(sensorContainer.VolumeMounts, c.VolumeMounts...)
+		}
 	}
 	sensorContainer.Name = "main"
